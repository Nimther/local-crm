# Fixture SPECIFICATION.md — every name from env.example present

| Variable | Notes |
|---|---|
| `GHCR_IMAGE_BASE` | image registry base |
| `IMAGE_TAG` | full git SHA tag |
| `FIXTURE_SECRET_ONE` | fixture secret |
| `FIXTURE_SECRET_TWO` | fixture secret |
| `FIXTURE_PREFIX` | fixture — shares a prefix with the next row |
| `FIXTURE_PREFIX_LONGER` | fixture — shares a prefix with the previous row |
