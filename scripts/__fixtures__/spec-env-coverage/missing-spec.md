# Fixture SPECIFICATION.md — the second fixture secret is deliberately
# absent from this file, and only the LONGER prefix-sharing name is present
# (proving the shorter one is not credited merely because the longer one
# appears). Deliberately not naming either omitted variable literally in
# this comment -- doing so would make it "present" for the coverage check.

| Variable | Notes |
|---|---|
| `GHCR_IMAGE_BASE` | image registry base |
| `IMAGE_TAG` | full git SHA tag |
| `FIXTURE_SECRET_ONE` | fixture secret |
| `FIXTURE_PREFIX_LONGER` | fixture — shares a prefix with a shorter name not listed here |
