# Fixture for scripts/__tests__/check-lockfile-npm10.test.mjs --
# "tag-mismatch" case. Deliberately pins a DIFFERENT node major than
# Dockerfile.api/.web -- this is the disagreement the guard must catch
# before ever invoking npm.
FROM node:20-slim AS deps
FROM deps AS build
FROM node:20-slim AS runtime
