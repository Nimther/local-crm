# Fixture for scripts/validate-prod-compose.mjs's test suite: no
# `pgbackrest` service declared at all. Deliberately minimal -- only `db`;
# the "missing-service" checks for the other five will also fire, and the
# test only asserts the specific rule this fixture targets is present.
services:
  db:
    image: postgres:17
