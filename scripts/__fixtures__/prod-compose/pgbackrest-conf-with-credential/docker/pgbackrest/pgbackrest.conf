# Fixture for scripts/validate-prod-compose.mjs's test suite:
# checkPgbackrestConfigHasNoCredential(baseDir) reads
# `<baseDir>/docker/pgbackrest/pgbackrest.conf` directly (not through the
# compose-YAML resolvers) -- this is a small standalone directory tree
# playing the role of a repository root, with one deliberately non-compliant
# line: a literal secret value where the real file only ever has an
# environment-variable-delivered one.
[global]
repo1-type=s3
repo1-cipher-type=aes-256-cbc
repo1-cipher-pass=this-should-never-be-a-literal-value
