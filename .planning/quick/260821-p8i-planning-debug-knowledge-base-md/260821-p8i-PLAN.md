---
phase: quick-260821-p8i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/debug/knowledge-base.md
autonomous: true
requirements: [QT-260821-p8i]

must_haves:
  truths:
    - "The 10-line addition already present in the working tree of `.planning/debug/knowledge-base.md` — the resolved-debug-session entry `ci-tenant-fairness-double-run` — is committed to branch `gsd/phase-20-campaign-template-correctness` byte-for-byte unchanged."
    - "The committed blob is identical to the pre-task working-tree content: `git rev-parse HEAD:.planning/debug/knowledge-base.md` returns `69871c23883a7a0d0f4d55982451c0202cf882e8`."
    - "The new commit touches exactly one path — `git show --numstat --format= HEAD` is the single line `10\t0\t.planning/debug/knowledge-base.md`."
    - "After the commit the working tree is clean for that path (`git diff -- .planning/debug/knowledge-base.md` is empty) and the file still hashes to `47c54af6b530a887af8d825210d97ab164600d0a39f3b3e2a4fb6d6579274d11`, proving nothing rewrote, reflowed, or regenerated it along the way."
    - "The commit subject is exactly `docs(debug): add ci-tenant-fairness-double-run entry to knowledge base` — a documentation-type commit, not a fix/feat."
    - "No file other than `.planning/debug/knowledge-base.md` is staged, committed, or modified by this plan."
  artifacts:
    - "One new git commit on `gsd/phase-20-campaign-template-correctness` containing only `.planning/debug/knowledge-base.md` (the file itself is pre-existing and is NOT authored by this plan)"
  key_links:
    - "the pre-existing uncommitted diff -> the commit: the diff IS the deliverable. It cannot be re-derived. `git checkout --`, `git restore`, `git stash`, or any regeneration of the file destroys the work product with no recovery path, and the pinned sha256 gate is the only mechanism that detects it."
    - "plain `git add` -> the tracked-file exception: `.gitignore` line 16 lists `.planning/`, but this file is one of 836 already-tracked paths under `.planning/`, so plain `git add <path>` stages it. `gsd-tools commit` applies its own ignore check and returns `skipped_gitignored` for `.planning/` paths — it silently does nothing, so it must not be used here."
    - "path-scoped staging -> commit isolation: `git add -A` / `git add .` on this branch would sweep in whatever else the session touches; only the single explicit path may be staged, and the post-commit one-path gate is what proves it."
---

<objective>
Commit the single modification already sitting in the working tree —
`.planning/debug/knowledge-base.md`, +10 lines, the resolved-debug-session entry
`ci-tenant-fairness-double-run` — as a documentation commit, without altering one byte of it.

Purpose: the debug knowledge base is the input `gsd-debugger` reads to surface known-pattern
hypotheses on future sessions. The entry exists on disk but is uncommitted, so it is one stray
`git checkout` away from being lost and is invisible to every other checkout of this repo.

Output: exactly one new commit on branch `gsd/phase-20-campaign-template-correctness`
containing exactly one file. No content is authored, edited, or regenerated by this plan.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Do NOT add `.planning/debug/knowledge-base.md` to this context block and do not open it with
Read/Edit/Write. It is the payload, not source material. Everything the task needs to know
about it is pinned in `<pinned_facts>` below.
</context>

<pinned_facts>

Verified at plan time (2026-08-21) from the repository root
`/Users/primeropanther/Projects/mega-crm`:

| Fact | Value |
|------|-------|
| Branch | `gsd/phase-20-campaign-template-correctness` |
| Dirty paths in the working tree | exactly one: `.planning/debug/knowledge-base.md` |
| Diff shape | `10` insertions, `0` deletions |
| Worktree file SHA-256 | `47c54af6b530a887af8d825210d97ab164600d0a39f3b3e2a4fb6d6579274d11` |
| Worktree file git blob hash | `69871c23883a7a0d0f4d55982451c0202cf882e8` |
| Blob hash at HEAD (pre-commit) | `8aeb45ea1e0a212bf721ca666db2ab6752b36aa0` |
| File length | 35 lines |
| Tracked by git | yes (`git ls-files --error-unmatch` succeeds; 836 tracked paths under `.planning/`) |
| Matched by `.gitignore` | yes, line 16 `.planning/` — irrelevant for `git add` because the path is already tracked |
| Occurrences of `ci-tenant-fairness-double-run` in the file | 2 (the entry heading and the branch name in its "Files changed" line) |

The added block is a new `## ci-tenant-fairness-double-run — …` section inserted near the top
of the file, directly above the existing `## segm-04-live-count-race` entry, following the
file's established entry format (Date / Error patterns / Root cause(s) / Fix / Files changed /
Why not caught / Recurrence guard).

</pinned_facts>

<hard_constraints>

Absolute. A task that satisfies its own acceptance criteria while breaking one of these has
failed.

1. **Do not modify the payload.** Do not open `.planning/debug/knowledge-base.md` with
   Read, Edit, or Write. Do not reformat it, reflow it, fix its typos, normalize its
   em-dashes, sort its entries, regenerate it from a debug session file under
   `.planning/debug/resolved/`, or "improve" the entry's wording. Its current bytes are the
   deliverable.

2. **Do not run any command that can discard the payload.** Forbidden against this path:
   `git checkout --`, `git checkout <ref> -- <path>`, `git restore`, `git stash`,
   `git stash push`, `git clean`, `git reset --hard`, `git commit --amend`. Also do not switch
   branches or pull/rebase — a checkout across this file silently clobbers it (recorded
   repo pitfall).

3. **Stage exactly one path.** Use `git add .planning/debug/knowledge-base.md` and nothing
   else. Never `git add -A`, `git add .`, `git add -u`, or `git commit -a`. No `-f` flag is
   needed or wanted (the path is already tracked).

4. **Do not use `gsd-tools commit` / `gsd_run query commit` for this path.** It applies its own
   gitignore check and returns `skipped_gitignored` for anything under `.planning/`, producing
   a no-op that looks like success. Use plain `git add` + plain `git commit`.

5. **Touch nothing else.** Do not create, edit, or commit any other file — not
   `SPECIFICATION.md`, not `.planning/WINDOWS.md` (force-adding a fresh copy of that ledger is
   a known clobber vector), not `.planning/STATE.md` (the quick workflow owns its own
   bookkeeping, after this task), not anything under `apps/` or `packages/`. Only this plan's
   own SUMMARY.md, written by the workflow at the end, is exempt.

6. **No `run_in_background`.** Every command in this task is instantaneous and must run in the
   foreground. Backgrounding a command and then stopping is a recorded cause of total work
   loss in this repo.

7. **Main checkout, absolute paths.** This quick task runs sequentially in
   `/Users/primeropanther/Projects/mega-crm` — do not create or assume a worktree. Use
   `git -C /Users/primeropanther/Projects/mega-crm …` or absolute paths; do not `cd`.

8. **Verify the payload's integrity AFTER the last git operation.** The clobber failure mode
   is silent and happens during a git op, so a pre-commit-only check proves nothing. The
   sha256 and grep gates run last.

</hard_constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Stage and commit the knowledge-base entry verbatim, gated end to end</name>
  <files>.planning/debug/knowledge-base.md</files>
  <precondition>
The working tree still holds the un-committed entry exactly as pinned. Assert BOTH, and halt
without staging anything if either fails (the payload has been lost or altered — report it,
do not attempt to reconstruct it):

  - `git -C /Users/primeropanther/Projects/mega-crm diff --numstat -- .planning/debug/knowledge-base.md` is the single line `10`, `0`, `.planning/debug/knowledge-base.md`
  - `shasum -a 256 /Users/primeropanther/Projects/mega-crm/.planning/debug/knowledge-base.md` starts with `47c54af6b530a887af8d825210d97ab164600d0a39f3b3e2a4fb6d6579274d11`

Note: do NOT gate on a globally clean `git status` or on the current HEAD sha — unrelated
bookkeeping may legitimately be dirty, and the path-scoped gates above plus the post-commit
one-path gate already cover every real failure.
  </precondition>
  <action>
Three commands, in this order, in the foreground, from the repository root
`/Users/primeropanther/Projects/mega-crm`. Nothing is authored or edited.

1. Assert the precondition above (numstat + sha256). Halt on mismatch.

2. Stage the single path with plain git: `git add .planning/debug/knowledge-base.md`. No other
   pathspec, no `-A`, no `-f`, no `gsd-tools commit` (per hard constraints 3 and 4). Then
   confirm the index holds exactly that one change before committing: `git diff --cached
   --numstat` must emit one line only, with 10 insertions and 0 deletions for that path. If a
   second line appears, unstage the extras with `git restore --staged <extra-path>` — which is
   safe on other paths but must never be pointed at the payload — and re-check.

3. Commit with plain git and this exact subject:

   `docs(debug): add ci-tenant-fairness-double-run entry to knowledge base`

   Use `docs` as the type, not `fix` or `feat` — no behavior changed, only the knowledge base
   grew. A body is optional; if you write one, describe the entry as recording an already-
   resolved session (the CI double-run root cause fixed in PR #23 / `c08f84d`) and keep it
   free of any claim that this commit changed code. Do not use `--amend`. Do not add
   `Co-Authored-By` or other trailers unless the workflow injects them itself.

Then run the verify gate. Its sha256 and grep checks run after the commit on purpose: the
clobber risk lives inside the git operations, so post-op is the only placement that proves the
committed bytes and the on-disk bytes are still the pinned ones.
  </action>
  <acceptance_criteria>
    - Pre-commit: `git diff --cached --numstat` emits exactly one line, `10` / `0` / `.planning/debug/knowledge-base.md`.
    - Post-commit: `git show --numstat --format= HEAD` emits exactly one line, `10` / `0` / `.planning/debug/knowledge-base.md`.
    - `git rev-parse HEAD:.planning/debug/knowledge-base.md` equals `69871c23883a7a0d0f4d55982451c0202cf882e8` — the committed content is byte-identical to the pre-task working-tree content.
    - `git log -1 --format=%s` equals `docs(debug): add ci-tenant-fairness-double-run entry to knowledge base`.
    - `git diff -- .planning/debug/knowledge-base.md` is empty (nothing left uncommitted for that path).
    - `shasum -a 256` of the file still begins `47c54af6b530a887af8d825210d97ab164600d0a39f3b3e2a4fb6d6579274d11`, checked after the commit.
    - `grep -c 'ci-tenant-fairness-double-run' .planning/debug/knowledge-base.md` is at least 1 (observed value at plan time: 2), checked after the commit.
    - `git status --porcelain -- apps packages SPECIFICATION.md package.json` is empty — no source or spec file was touched.
  </acceptance_criteria>
  <verify>
    <automated>G=/Users/primeropanther/Projects/mega-crm; P=.planning/debug/knowledge-base.md; EXP=$(printf '10\t0\t%s' "$P"); test "$(git -C "$G" show --numstat --format= HEAD)" = "$EXP" || { echo "FAIL: HEAD commit is not exactly one file with 10 insertions/0 deletions"; exit 1; }; test "$(git -C "$G" rev-parse "HEAD:$P")" = 69871c23883a7a0d0f4d55982451c0202cf882e8 || { echo "FAIL: committed blob differs from the pinned pre-task content"; exit 1; }; test "$(git -C "$G" log -1 --format=%s)" = 'docs(debug): add ci-tenant-fairness-double-run entry to knowledge base' || { echo "FAIL: commit subject mismatch"; exit 1; }; test -z "$(git -C "$G" diff -- "$P")" || { echo "FAIL: path still has uncommitted changes"; exit 1; }; test -z "$(git -C "$G" diff --cached -- "$P")" || { echo "FAIL: path still staged"; exit 1; }; test "$(shasum -a 256 "$G/$P" | cut -d' ' -f1)" = 47c54af6b530a887af8d825210d97ab164600d0a39f3b3e2a4fb6d6579274d11 || { echo "FAIL: on-disk file no longer matches the pinned sha256 — payload altered or clobbered"; exit 1; }; test "$(grep -c 'ci-tenant-fairness-double-run' "$G/$P")" -ge 1 || { echo "FAIL: entry token absent from the file"; exit 1; }; test -z "$(git -C "$G" status --porcelain -- apps packages SPECIFICATION.md package.json)" || { echo "FAIL: source or spec files were modified"; exit 1; }; echo OK</automated>
  </verify>
  <done>One new commit exists on `gsd/phase-20-campaign-template-correctness` whose entire content is `.planning/debug/knowledge-base.md` at blob `69871c2`, with subject `docs(debug): add ci-tenant-fairness-double-run entry to knowledge base`; the file on disk still hashes to `47c54af6…`; nothing else was staged, committed, or modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uncommitted working-tree state -> git history | The deliverable exists only as an uncommitted diff. Until the commit lands, any git operation in the session can destroy it irrecoverably. |
| this quick task -> the rest of the repository | The task is authorized to stage exactly one path on a live phase branch; a broad `git add` would fold unrelated session state into a documentation commit. |
| `.planning/` gitignore rule -> the tracked-file exception | Tooling that consults `.gitignore` without consulting the index (notably `gsd-tools commit`) reports success while doing nothing to this path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-P8I-01 | Denial of Service | The uncommitted +10-line payload | high | mitigate | Hard constraint 2 forbids `git checkout --`, `git restore`, `git stash`, `git clean`, `git reset --hard`, and branch switches against the path; the post-commit sha256 gate (`47c54af6…`) detects a clobber even if it happened silently mid-operation. |
| T-P8I-02 | Tampering | Content of `.planning/debug/knowledge-base.md` | high | mitigate | Hard constraint 1 forbids opening the file with Read/Edit/Write; the pinned blob-hash gate (`git rev-parse HEAD:<path>` = `69871c2`) proves the committed bytes are the pre-task bytes, and the sha256 gate proves the on-disk bytes are too. |
| T-P8I-03 | Tampering | Commit scope on a live phase branch | medium | mitigate | Hard constraint 3 restricts staging to one explicit pathspec; the pre-commit `git diff --cached --numstat` single-line check and the post-commit `git show --numstat --format= HEAD` single-line check both fail closed if anything else rode along. |
| T-P8I-04 | Repudiation | Silent no-op commit via `gsd-tools commit` | medium | mitigate | Hard constraint 4 bans it for this path; the post-commit gates (one-path numstat + blob hash + empty `git diff`) cannot pass if no real commit was created. |
| T-P8I-05 | Denial of Service | Backgrounded command losing the session's work | medium | mitigate | Hard constraint 6 forbids `run_in_background`; all commands here are instantaneous and run in the foreground. |
| T-P8I-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing and invokes no package manager. Any install proposed during execution requires slopcheck plus a blocking human checkpoint before running. |
</threat_model>

<verification>
Run from `/Users/primeropanther/Projects/mega-crm` after the task's commit:

1. Commit isolation — `git show --stat HEAD` lists exactly one file, `.planning/debug/knowledge-base.md`, with 10 insertions and 0 deletions.
2. Content fidelity — `git rev-parse HEAD:.planning/debug/knowledge-base.md` returns `69871c23883a7a0d0f4d55982451c0202cf882e8`, and `shasum -a 256 .planning/debug/knowledge-base.md` returns `47c54af6b530a887af8d825210d97ab164600d0a39f3b3e2a4fb6d6579274d11`.
3. Nothing left behind — `git diff -- .planning/debug/knowledge-base.md` and `git diff --cached -- .planning/debug/knowledge-base.md` are both empty.
4. Entry present — `grep -c 'ci-tenant-fairness-double-run' .planning/debug/knowledge-base.md` returns at least 1.
5. Commit type — `git log -1 --format=%s` is the exact `docs(debug): …` subject; the commit is documentation-classified.
6. Blast radius — `git status --porcelain -- apps packages SPECIFICATION.md package.json` is empty.
</verification>

<success_criteria>
- The `ci-tenant-fairness-double-run` knowledge-base entry is in git history on `gsd/phase-20-campaign-template-correctness`, byte-identical to the working-tree version that existed before this plan ran.
- The commit contains exactly one file and carries a `docs(debug):` subject.
- The file's content was never opened for editing; no other file in the repository was staged, committed, or modified.
</success_criteria>

<output>
Create `.planning/quick/260821-p8i-planning-debug-knowledge-base-md/260821-p8i-SUMMARY.md` when done.

Record: the resulting commit sha; confirmation that `git rev-parse HEAD:.planning/debug/knowledge-base.md` matched `69871c2…` and the on-disk sha256 matched `47c54af6…` after the commit; and — if the precondition failed — exactly which of the two pinned values mismatched, with no attempt to reconstruct the entry.
</output>
