---
phase: quick-260722-q4t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [QUICK-README]
must_haves:
  truths:
    - "Новый разработчик, открыв README.md, понимает что за продукт Mega CRM и что вошло в v1.0"
    - "Новый разработчик может поднять локальное окружение по инструкции из README без чтения исходников"
    - "Каждая команда, версия, порт и переменная окружения в README подтверждается файлом в репозитории"
  artifacts:
    - "README.md в корне репозитория"
  key_links:
    - "npm-скрипты в README соответствуют scripts в package.json (root + apps/web)"
    - "Список обязательных переменных в README соответствует baseRequired в scripts/check-env.mjs и envSchema в apps/api/src/env.ts"
    - "Порты и образы в README соответствуют docker-compose.yml и apps/web/vite.config.ts"
---

<objective>
Создать `README.md` в корне репозитория Mega CRM: обзор продукта, возможности v1.0, структура монорепо, стек, быстрый старт, переменные окружения, npm-команды, ссылки на документацию. Язык — русский, как остальная документация проекта.

Purpose: у проекта после закрытия milestone v1.0 (~57k LOC, 3 приложения, 8 shared-пакетов) нет точки входа для человека, впервые открывающего репозиторий. Вся информация сейчас размазана по `.planning/`.
Output: один файл `README.md` в корне, все факты в котором сверены с исходниками репозитория.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@package.json
@docker-compose.yml
@scripts/check-env.mjs
@apps/api/src/env.ts
@apps/web/vite.config.ts
</context>

<facts_verified>
Факты ниже уже сверены планировщиком с репозиторием — используй их как источник истины, но при расхождении с реальным файлом побеждает файл.

**Монорепо (npm workspaces `apps/*`, `packages/*`, `engines.node >= 22`):**
- `apps/api` (`@mega-crm/api`) — Fastify HTTP API
- `apps/web` (`@mega-crm/web`) — React 19 + Vite SPA
- `apps/worker` (`@mega-crm/worker`) — BullMQ воркеры
- `packages/db` (`@mega-crm/db`) — Drizzle-схема и миграции
- `packages/tenant-context` — контекст тенанта / RLS
- `packages/contacts-core` — shared upsert контактов
- `packages/segments-core` — компилятор определений сегментов в SQL
- `packages/delivery-core` — send pipeline, unsubscribe-токены
- `packages/flows-core` — модель и валидация графа цепочек
- `packages/kms` — envelope encryption (local/aws)
- `packages/shared-schemas` — общие Zod-схемы

**Версии (из package.json соответствующих workspace):**
- Node >= 22, TypeScript ^5.9.3
- API: fastify 5.9.0, zod 4.4.3, @fastify/type-provider-zod 1.0.0, drizzle-orm 0.45.2, pg 8.22.0, bullmq 5.79.1, ioredis 5.11.0, better-auth 1.6.23, @sendgrid/mail 8.1.6, @sendgrid/eventwebhook ^8.0.0, csv-parse 7.0.1, pino 10.3.1
- Worker: bullmq 5.79.1, ioredis 5.11.0, rate-limiter-flexible 11.2.0
- Web: react 19.2.7, vite 8.1.3, @xyflow/react 12.11.2, @tanstack/react-query 5.101.2, @tanstack/react-table ^8.21.3, zustand 5.0.14, react-hook-form 7.80.0, react-router 8.1.0, recharts 3.9.2, tailwindcss 3.4.19, Radix UI primitives
- db: drizzle-kit 0.31.10
- Тесты: vitest 4.1.9, @playwright/test 1.61.1

**docker-compose.yml:** сервис `db` — образ `postgres:17`, БД `mega_crm`, пользователь/пароль `postgres`/`postgres`, порт 5432, init-скрипт `docker/init-app-role.sql`; сервис `redis` — образ `redis:7`, порт 6379. Оба с healthcheck и именованными volume.

**Порты:** API — 4000 (`API_PORT`, default 4000); Vite dev-сервер — 5173, проксирует `/api` на `http://localhost:4000`; worker HTTP-порта не слушает.

**npm-скрипты (root package.json):** `predev` (`node scripts/check-env.mjs && node scripts/migrate-dev.mjs`), `dev` (concurrently api+web+worker), `build`, `test`, `db:generate`, `db:migrate`.
**Скрипты в workspace:** `apps/api` и `apps/worker` — `dev` (tsx watch с `--env-file=../../.env`), `build`, `start`, `test`, `test:watch`; `apps/web` — `dev`, `build`, `preview`, `test`, `test:e2e` (playwright).

**Обязательные переменные окружения (baseRequired в scripts/check-env.mjs):** `DATABASE_URL`, `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `WEB_URL`, `PLATFORM_SENDGRID_API_KEY`, `PLATFORM_MAIL_FROM`, `REDIS_URL`, `UNSUBSCRIBE_TOKEN_SECRET`, `PUBLIC_APP_URL`. Плюс условно: `KMS_LOCAL_KEK` при `KMS_PROVIDER=local` (default) либо `KMS_KEK_ID` при `KMS_PROVIDER=aws`.
**Ограничения из apps/api/src/env.ts:** `BETTER_AUTH_SECRET` >= 16 символов; `UNSUBSCRIBE_TOKEN_SECRET` >= 32 символа; `BETTER_AUTH_URL`, `WEB_URL`, `PUBLIC_APP_URL` — валидные URL; `PLATFORM_MAIL_FROM` — валидный email; `API_PORT` default 4000; `NODE_ENV` из `development|test|production`, default `development`; `KMS_PROVIDER=local` запрещён при `NODE_ENV=production`; `PUBLIC_APP_URL` должен быть https при `NODE_ENV=production`.
**Предупреждения check-env.mjs (не фатальные):** `PUBLIC_APP_URL` на localhost или на схеме http делает невозможной живую доставку SendGrid webhook — отсылка к `docs/webhook-live-uat.md`.

**Существующая документация:** `.planning/PROJECT.md`, `docs/webhook-live-uat.md`. Других docs-файлов нет.
</facts_verified>

<tasks>

<task type="auto">
  <name>Task 1: Написать README.md в корне репозитория</name>
  <files>README.md</files>
  <action>
Создать новый файл `README.md` в корне репозитория (сейчас его нет). Язык — русский, технические идентификаторы (имена пакетов, переменных, команд) — как есть. Только Markdown, без HTML-разметки. Все факты берутся из блока `<facts_verified>` этого плана и из перечисленных в `<context>` файлов; ничего не выдумывать.

Структура файла (именно в этом порядке, разделы уровня `##`):

1. **Заголовок `# Mega CRM`** и подзаголовок-строка: B2C marketing automation, multi-tenant SaaS. Дальше 2–4 абзаца по `.planning/PROJECT.md` (разделы «What This Is» и «Core Value»): аналог Klaviyo по модели, но без модуля сделок и sales-пайплайна; маркетологи тенантов ведут контакты и сегменты, строят триггерные цепочки в canvas-редакторе, запускают broadcast-кампании; доставка — SendGrid v3 mail/send + Dynamic Templates по модели BYO API key у каждого тенанта. Указать текущее состояние: v1.0 MVP выпущен 2026-07-14, все 7 фаз roadmap завершены.

2. **`## Возможности`** — маркированный список по разделу «Validated» из `.planning/PROJECT.md`, по одному пункту на возможность, короткой строкой: multi-tenant воркспейсы с приглашениями и ролями Owner/Admin/Member; подключение SendGrid-ключа тенанта с валидацией и хранением через envelope encryption; управление контактами (CRUD в UI, CSV-импорт с маппингом колонок, Contacts API, автосоздание из событий); идентификация контакта по external_id с email как запасным ключом; server-side event ingestion со свободной JSON-схемой событий; динамические сегменты по свойствам профиля и по поведению; broadcast-кампании (сегмент + Dynamic Template, тестовое письмо, планирование, live-прогресс); триггерные цепочки в canvas-редакторе с версионированной публикацией, exit conditions, контролем повторного входа, quiet hours и frequency cap; очередь отправки с разделением email:triggered / email:broadcast и per-tenant RPS-троттлингом; SendGrid Event Webhook с подписью и авто-suppression; аналитика — метрики кампаний и шагов цепочек, timeline контакта, сводный дашборд, журнал отправок с фильтрами.

3. **`## Архитектура`** — короткий абзац о том, что это npm-workspaces монорепо, затем две таблицы. Таблица «Приложения» с колонками Путь / Пакет / Назначение для `apps/api`, `apps/web`, `apps/worker`. Таблица «Пакеты» с колонками Путь / Назначение для всех восьми `packages/*` из `<facts_verified>`. После таблиц — 2–3 строки о хранилищах: Postgres с RLS на tenant-таблицах и партиционированными `events`/`send_events`, Redis как backend BullMQ и per-tenant token bucket.

4. **`## Технологический стек`** — таблица Слой / Технологии с версиями строго из `<facts_verified>`. Строки: Язык и рантайм; Backend (API); Очереди и воркеры; База данных; Frontend; Тесты. Версии не округлять и не «улучшать».

5. **`## Быстрый старт`** — нумерованные шаги с fenced-блоками `bash`:
   1. Требования: Node >= 22 (поле `engines` в корневом package.json), npm, Docker.
   2. `docker compose up -d` — поднимает Postgres 17 (порт 5432, БД `mega_crm`, пользователь/пароль `postgres`/`postgres`) и Redis 7 (порт 6379).
   3. Скопировать `.env.example` в `.env` и заполнить значения (см. раздел про переменные окружения).
   4. `npm install` — установка зависимостей всех workspace.
   5. `npm run dev` — поднимает api, web и worker через concurrently. Отдельно пояснить, что `predev` перед стартом прогоняет `scripts/check-env.mjs` (падает, если обязательная переменная отсутствует или пуста) и `scripts/migrate-dev.mjs` (применяет незакаченные Drizzle-миграции).
   6. Адреса после старта: API — http://localhost:4000, веб-интерфейс — http://localhost:5173 (Vite проксирует `/api` на API), worker собственного порта не слушает.

6. **`## Переменные окружения`** — таблица Переменная / Обязательна / Описание для всех переменных из `<facts_verified>`: девять обязательных из baseRequired, условные `KMS_PROVIDER` / `KMS_LOCAL_KEK` / `KMS_KEK_ID`, необязательные `API_PORT` и `NODE_ENV` с их default-значениями. В описаниях отразить ограничения из `apps/api/src/env.ts` (минимальные длины секретов, формат URL/email, запрет `KMS_PROVIDER=local` в production, требование https для `PUBLIC_APP_URL` в production). Под таблицей — абзац-предупреждение: для живой доставки SendGrid webhook `PUBLIC_APP_URL` должен быть публично доступным https-адресом (ngrok/cloudflared), localhost и http не работают — со ссылкой на `docs/webhook-live-uat.md`. Реальные значения секретов в README не приводить: placeholder-значения записывать в угловых скобках, например `<ваш-ключ>`.

7. **`## Команды`** — таблица Команда / Что делает. Только команды, реально существующие в package.json: `npm run dev`, `npm run build`, `npm run test`, `npm run db:generate`, `npm run db:migrate` (корень); `npm run dev -w apps/api`, `npm run dev -w apps/web`, `npm run dev -w apps/worker` (запуск одного приложения); `npm run test -w apps/api`; `npm run test:e2e -w apps/web` (Playwright); `npm start -w apps/api` и `npm start -w apps/worker` (запуск собранной сборки из dist). Не придумывать lint, format, typecheck, ci и прочие отсутствующие скрипты.

8. **`## Структура проекта`** — fenced-блок с деревом верхнего уровня: `apps/` (api, web, worker), `packages/` (восемь пакетов), `docker/`, `docs/`, `scripts/`, `.planning/`, `docker-compose.yml`, `package.json`, `tsconfig.base.json`. Каждая строка с коротким комментарием.

9. **`## Документация`** — маркированный список из двух ссылок: `.planning/PROJECT.md` (контекст продукта, требования, ключевые решения) и `docs/webhook-live-uat.md` (runbook живого UAT SendGrid webhook: туннель и scope ключа).

Запреты для этого файла: не добавлять картинки-шильдики из внешних сервисов, не указывать статус CI, покрытие тестами, лицензию или инструкции по деплою — ничего из этого в репозитории не подтверждается. Не описывать команды, порты и переменные, которых нет в перечисленных источниках.
  </action>
  <verify>
    <automated>test -f README.md && grep -q '^# Mega CRM' README.md && grep -q 'docker compose up' README.md && grep -q 'npm run dev' README.md && grep -q 'localhost:5173' README.md && grep -q 'localhost:4000' README.md && grep -q 'docs/webhook-live-uat.md' README.md && grep -q '.planning/PROJECT.md' README.md && test $(grep -c '^## ' README.md) -ge 8 && echo OK</automated>
  </verify>
  <done>`README.md` существует в корне, содержит все девять разделов в указанном порядке, написан по-русски, ссылается на `.planning/PROJECT.md` и `docs/webhook-live-uat.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Сверить факты README с репозиторием и исправить расхождения</name>
  <files>README.md</files>
  <action>
Прогнать проверочные команды из блока `<verify>` и устранить каждое найденное расхождение правкой `README.md` (не правкой исходников — это документационная задача, код не меняется).

Что проверяется и как чинить:

1. **npm-скрипты.** Собрать все вхождения вида `npm run <имя>` из README и сверить с ключами `scripts` в корневом `package.json`, `apps/api/package.json`, `apps/web/package.json`, `apps/worker/package.json`, `packages/db/package.json`. Любое имя, которого нет ни в одном из них, удалить из README или заменить на существующее.

2. **Переменные окружения.** Собрать из README все токены вида ЗАГЛАВНЫЕ_С_ПОДЧЁРКИВАНИЕМ и сверить с `scripts/check-env.mjs` и `apps/api/src/env.ts`. Токен, не встречающийся ни в одном из этих двух файлов, из README удалить. Отдельно убедиться, что все девять обязательных переменных из baseRequired присутствуют в таблице README.

3. **Порты и образы.** Убедиться, что README упоминает 4000, 5173, 5432, 6379, `postgres:17` и `redis:7` в согласии с `docker-compose.yml` и `apps/web/vite.config.ts`.

4. **Отсутствие секретов.** Убедиться, что README не содержит реальных значений ключей — только placeholder в угловых скобках. Проверочный гейт ищет присвоенное значение реального SendGrid-ключа и должен давать ноль совпадений.

5. **Список пакетов.** Убедиться, что каждая директория из `apps/` и `packages/` упомянута в README ровно как существует на диске, и что README не упоминает несуществующих пакетов.

После правок перезапустить проверки — все должны пройти.
  </action>
  <verify>
    <automated>set -e; comm -23 <(grep -oE 'npm run [a-zA-Z:]+' README.md | sed 's/npm run //' | sort -u) <(node -e "const fs=require('fs');const files=['package.json','apps/api/package.json','apps/web/package.json','apps/worker/package.json','packages/db/package.json'];const s=new Set();for(const f of files){for(const k of Object.keys(JSON.parse(fs.readFileSync(f,'utf8')).scripts||{}))s.add(k)}console.log([...s].join('\n'))" | sort -u) > /tmp/bad-scripts.txt; test ! -s /tmp/bad-scripts.txt || { echo "UNKNOWN SCRIPTS:"; cat /tmp/bad-scripts.txt; exit 1; }; for v in $(grep -oE '\b[A-Z][A-Z0-9]*_[A-Z0-9_]+\b' README.md | sort -u); do grep -q "$v" scripts/check-env.mjs apps/api/src/env.ts || { echo "UNKNOWN ENV VAR: $v"; exit 1; }; done; for v in DATABASE_URL REDIS_URL BETTER_AUTH_SECRET BETTER_AUTH_URL WEB_URL PLATFORM_SENDGRID_API_KEY PLATFORM_MAIL_FROM UNSUBSCRIBE_TOKEN_SECRET PUBLIC_APP_URL; do grep -q "$v" README.md || { echo "MISSING ENV VAR: $v"; exit 1; }; done; for t in 4000 5173 5432 6379 postgres:17 redis:7; do grep -q "$t" README.md || { echo "MISSING FACT: $t"; exit 1; }; done; test "$(grep -c '=SG\.' README.md)" -eq 0; for d in $(ls apps) ; do grep -q "apps/$d" README.md || { echo "MISSING APP: $d"; exit 1; }; done; for d in $(ls packages); do grep -q "packages/$d" README.md || { echo "MISSING PACKAGE: $d"; exit 1; }; done; echo ALL FACTS OK</automated>
  </verify>
  <done>Все проверочные команды проходят: в README нет несуществующих npm-скриптов и переменных окружения, присутствуют все девять обязательных переменных, все apps/packages, корректные порты и образы, нет реальных значений секретов.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → публичный читатель README | README — документ, который читают вне команды; всё, что в него попадает, считается публичным |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q4T-01 | Information Disclosure | README.md | high | mitigate | README документирует только имена переменных и placeholder-значения в угловых скобках; реальные значения из `.env` не копируются. Автоматический гейт в Task 2 проверяет отсутствие присвоенного реального SendGrid-ключа |
| T-Q4T-02 | Tampering | README.md | low | accept | Задача только документационная: изменяется единственный файл `README.md`, исходники и конфигурация не трогаются; риск изменения поведения системы отсутствует |

Пакеты не устанавливаются — задача не содержит install-шагов, Package Legitimacy Gate неприменим.
</threat_model>

<verification>
1. `README.md` существует в корне репозитория и не пуст.
2. Проверочная команда Task 2 завершается выводом `ALL FACTS OK`.
3. `git status` показывает изменённым только `README.md` (плюс артефакты `.planning/`).
</verification>

<success_criteria>
- Новый разработчик может поднять окружение, следуя только README: docker compose → .env → npm install → npm run dev → открыть http://localhost:5173.
- Каждая команда, версия, порт, пакет и переменная окружения в README подтверждается файлом репозитория.
- В README нет badge-картинок, CI-статусов, лицензии и инструкций по деплою.
- Изменён ровно один файл исходного дерева: `README.md`.
</success_criteria>

<output>
Create `.planning/quick/260722-q4t-readme-md/260722-q4t-SUMMARY.md` when done
</output>
