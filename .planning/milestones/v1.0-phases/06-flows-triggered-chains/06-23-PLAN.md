---
phase: 06-flows-triggered-chains
plan: 23
type: execute
wave: 2
depends_on:
  - 06-22
files_modified:
  - apps/web/src/features/contacts/CsvImportWizard.tsx
autonomous: true
gap_closure: true
requirements:
  - FLOW-05

must_haves:
  truths:
    - "The CSV import column-mapping step renders a constrained IANA timezone combobox — the SAME TimezoneCombobox used on the contact form and workspace send settings — presented as the default timezone for imported rows without one, so the surface UAT Test 10 walks now visibly exposes a timezone dropdown (closing the claim-vs-implementation gap the user reported: «выпадающий список со списком часовых поясов отсутствует»)."
    - "Choosing a default timezone includes it in the dry-run request; leaving it unset sends no default (backward-compatible with imports that map their own timezone column)."
    - "The control is constrained to real IANA zones (no free-text entry); the server independently re-validates the default (06-22), so an invalid zone can never be stored."
  artifacts:
    - "apps/web/src/features/contacts/CsvImportWizard.tsx — MappingStep renders TimezoneCombobox as the per-import default-timezone control and threads defaultTimezone into the dry-run POST body"
  key_links:
    - "TimezoneCombobox (value/onChange) → MappingStep defaultTimezone state → dry-run POST { mapping, duplicatePolicy, defaultTimezone } → csvDryRunRequestSchema (06-22) → server-side default application"
---

<objective>
Close the user-facing half of UAT Test 10's gap (06-UAT.md 2026-07-13, FLOW-05; debug session .planning/debug/timezone-combobox-missing.md). The contact form and workspace send settings already render the constrained `TimezoneCombobox`, but the CSV column-mapping surface never did — so a user walking Test 10's three promised surfaces found the one with no timezone dropdown and reported it missing. This plan renders the promised combobox on that surface as the semantically-correct "default timezone for rows without one" control (a column-mapping UI maps columns to fields, so a per-import default is the right shape for a value picker there), wired to the server-side default-timezone capability shipped in 06-22.

This is UI wiring only: the existing `TimezoneCombobox` component (same directory) is reused verbatim, and the dry-run request now carries the chosen default. The web unit lane is node-only (no jsdom/@testing-library, per project convention), so the render itself is verified by the typecheck/build gate here plus the Test 10 re-run at phase UAT.

Purpose: make the promised constrained IANA timezone combobox actually appear on the CSV mapping surface, driving the real default-timezone behavior.
Output: a default-timezone `TimezoneCombobox` in the CSV mapping step whose selection flows into the dry-run request.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/timezone-combobox-missing.md

# The surface to edit (MappingStep builds the dry-run body):
@apps/web/src/features/contacts/CsvImportWizard.tsx
# The constrained combobox to reuse (same directory — value/onChange contract):
@apps/web/src/features/contacts/TimezoneCombobox.tsx
# The dry-run request contract extended in 06-22 (defaultTimezone is now accepted):
@packages/shared-schemas/src/csv-import.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the default-timezone TimezoneCombobox in the CSV mapping step</name>
  <files>apps/web/src/features/contacts/CsvImportWizard.tsx</files>
  <action>
    Add a constrained default-timezone control to `MappingStep` and thread it into the dry-run request.

    1. Import `TimezoneCombobox` from "./TimezoneCombobox" (same directory).
    2. In `MappingStep`, add local state `const [defaultTimezone, setDefaultTimezone] = useState<string | null>(null)`.
    3. Render a labelled block in the MappingStep card (place it near the duplicate-policy RadioGroup, inside the existing CardContent): a `Label` reading «Часовой пояс по умолчанию» with a short muted helper line explaining it applies to imported rows that do not have their own timezone value (mirror the existing helper-text styling used elsewhere in the wizard). Render `<TimezoneCombobox value={defaultTimezone} onChange={setDefaultTimezone} />` under it. The combobox already supports clearing back to null via its «Очистить» action.
    4. In `dryRunMutation.mutationFn`, include the default in the POST body only when set: build the request object as `{ mapping, duplicatePolicy, ...(defaultTimezone ? { defaultTimezone } : {}) }` so an unset control sends no `defaultTimezone` field (backward-compatible). The request already targets `.../imports/${upload.importId}/dry-run`; the body type now matches the 06-22 `csvDryRunRequestSchema`.

    Do NOT add a free-text timezone input anywhere — the constrained TimezoneCombobox (backed by Intl.supportedValuesOf) is the only entry path, and the server re-validates (06-22).
  </action>
  <verify>
    <automated>npm run build -w apps/web</automated>
    <human-check>Phase UAT Test 10 re-run: open /w/{slug}/contacts/import, upload a CSV, and confirm the column-mapping step renders a searchable «Часовой пояс по умолчанию» timezone dropdown that lists IANA zones (deferred to end-of-phase human_verify per project convention).</human-check>
  </verify>
  <done>apps/web build (tsc --noEmit && vite build) passes; MappingStep renders the constrained TimezoneCombobox as the default-timezone control and includes `defaultTimezone` in the dry-run body only when a zone is chosen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web client → API (dry-run body) | The chosen default timezone leaves the browser toward the CSV import pipeline (server re-validates in 06-22) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-23-01 | Tampering | default-timezone client input | low | mitigate | The control is a constrained TimezoneCombobox (Intl.supportedValuesOf, no free text) — the client-side half of the mitigation; the server independently re-validates the default via isValidIanaTimezone (06-22, T-06-22-01), so a tampered request body still cannot store an invalid zone |
</threat_model>

<verification>
- `npm run build -w apps/web` passes (tsc typecheck confirms the dry-run body matches the extended csvDryRunRequestSchema).
- Phase-UAT Test 10 re-run confirms the mapping surface now renders the timezone dropdown.
</verification>

<success_criteria>
- The CSV mapping step visibly renders a constrained IANA timezone combobox (the promised surface), presented as the default timezone for rows without one.
- The selection flows into the dry-run request and drives the 06-22 server-side default application; an unset control is backward-compatible.
</success_criteria>

<output>
Create `.planning/phases/06-flows-triggered-chains/06-23-SUMMARY.md` when done.
</output>
