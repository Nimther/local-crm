---
phase: 06-flows-triggered-chains
plan: 10
type: execute
wave: 3
depends_on: ["06-02", "06-04"]
autonomous: false
files_modified:
  - apps/web/package.json
  - apps/web/src/features/flows/api.ts
  - apps/web/src/features/flows/canvas/FlowCanvas.tsx
  - apps/web/src/features/flows/canvas/nodeTypes.tsx
  - apps/web/src/features/flows/canvas/NodePalette.tsx
  - apps/web/src/features/flows/canvas/NodeConfigPanel.tsx
  - apps/web/src/features/flows/canvas/useAutosaveDraft.ts
  - apps/web/src/components/ui/switch.tsx
requirements: [FLOW-01]
must_haves:
  truths:
    - "A marketer can drag the five node types (trigger, delay/wait, branch, send, exit) from a palette onto the canvas, connect them with edges, and configure each node in a side panel"
    - "A branch node renders exactly two labelled outgoing edges (Да / Нет) per D-13"
    - "Draft changes autosave via a debounced (1s) mutation with a Сохранено / Сохранение… status indicator (no manual save button)"
    - "Unconfigured / publish-invalid nodes render the destructive-ring state and Не настроено placeholder, computed by the SAME validateFlowDefinition the server uses"
    - "@xyflow/react resolves to the real xyflow/xyflow registry entry (verified at the legitimacy checkpoint) — the package is 12.11.x, not the stale reactflow package"
  artifacts:
    - path: apps/web/src/features/flows/canvas/FlowCanvas.tsx
      provides: "ReactFlow canvas with Background/Controls/MiniMap/Panel, custom node/edge types, drag-drop add"
      contains: "ReactFlow"
    - path: apps/web/src/features/flows/canvas/nodeTypes.tsx
      provides: "five custom node components with icon chips + config summaries + invalid-ring state"
      contains: "GitBranch"
    - path: apps/web/src/features/flows/canvas/useAutosaveDraft.ts
      provides: "debounced draft-save mutation + save-state indicator"
      contains: "debounce"
    - path: apps/web/src/features/flows/api.ts
      provides: "TanStack Query hooks for flow CRUD/draft/publish (mirrors campaigns/api.ts)"
      contains: "apiPatch"
  key_links:
    - from: apps/web/src/features/flows/canvas/NodeConfigPanel.tsx
      to: packages/flows-core/src/flow-validate.ts
      via: "canvas re-uses validateFlowDefinition for instant publish-blocker feedback (server remains authority)"
      pattern: "validateFlowDefinition"
    - from: apps/web/src/features/flows/canvas/useAutosaveDraft.ts
      to: apps/web/src/features/flows/api.ts
      via: "debounced PATCH /flows/:id draft mutation"
      pattern: "apiPatch"
  prohibitions:
    - statement: "Do NOT install the reactflow npm package — it is stale (last published June 2024). Use @xyflow/react only."
      status: resolved
      verification: "apps/web/package.json depends on @xyflow/react (not reactflow); legitimacy checkpoint confirms repository.url resolves to xyflow/xyflow"
    - statement: "The canvas client-side validation must NOT be treated as the publish authority — it is instant-feedback only; publish always re-validates server-side (06-04)"
      status: resolved
      verification: "publish action calls POST /flows/:id/publish and renders the server-returned blocker list; client validity is never sent as a trusted flag"
---

<objective>
Build the signature canvas flow builder (FLOW-01) with @xyflow/react: five draggable node types, binary-labelled branch edges, a node-config side panel, and debounced draft autosave — reusing the shared flow-definition schema and validator from 06-02 for instant publish-blocker feedback. This is the one genuinely new UI surface in the product.

Purpose: Delivers FLOW-01's literal drag-and-drop canvas requirement. Runs in parallel with the backend engine track once contracts (06-02) + API (06-04) exist.
Output: A working canvas at /w/{slug}/flows/:id backed by the flow draft API, plus the new shadcn switch primitive.
</objective>

> MVP user story (phase): **As a** marketer, **I want to** visually build a triggered chain by dragging nodes on a canvas, **so that** I can design automation without code.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-flows-triggered-chains/06-CONTEXT.md
@.planning/phases/06-flows-triggered-chains/06-RESEARCH.md
@.planning/phases/06-flows-triggered-chains/06-PATTERNS.md
@.planning/phases/06-flows-triggered-chains/06-UI-SPEC.md
@.planning/phases/06-flows-triggered-chains/06-02-SUMMARY.md
@.planning/phases/06-flows-triggered-chains/06-04-SUMMARY.md

<interfaces>
No internal canvas analog exists (RESEARCH: @xyflow/react is the project's first canvas) — build against the library's documented node/edge API.
UI contract (06-UI-SPEC Canvas & Node Visual Language — authoritative):
- Node card: fixed 240px, card-styled, header icon chip + title + overflow menu, body config summary, Не настроено placeholder for unconfigured (drives publish-invalid state). Selected: ring-2 ring-primary. Invalid: ring-2 ring-destructive + red dot + tooltip.
- Node chips (canvas-only categorical): Trigger Zap indigo-600/indigo-50; Delay Clock amber-600/amber-50; Branch GitBranch blue-600/blue-50 (the one net-new hue, chips only); Send Mail green-600/green-50; Exit LogOut neutral-500/neutral-100.
- Edges: neutral-300 2px; branch's two edges labelled Да (green-600) / Нет (neutral-500); selected edge indigo-600.
- Chrome: Background dot-grid neutral-200 on neutral-50; Controls bottom-left, MiniMap bottom-right; NodePalette in a top-left Panel (draggable rows). snapGrid [16,16]. Autosave debounced 1s; toolbar status Сохранено/Сохранение…; no manual save button.
API-client analog: apps/web/src/features/campaigns/api.ts — thin apiGet/apiPost/apiPatch/apiDelete wrappers, one fn per endpoint, response interfaces hand-mirrored (no shared response types). Reuse lib/api.ts apiPatch (added in 04-08).
Shared validator: validateFlowDefinition + flowDefinitionSchema from @mega-crm/flows-core (06-02). Flow DTOs from @mega-crm/shared-schemas.
shadcn switch install: npx shadcn@latest add switch (official registry; needed for the per-flow quiet-hours toggle used in 06-11).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package-legitimacy gate for @xyflow/react (blocking human)</name>
  <action>Do NOT install @xyflow/react until a human confirms its registry entry. This is a manual verification of a [SUS]-flagged package; there is no CLI action to automate away the human confirmation. Follow the how-to-verify steps and only proceed once approved.</action>
  <what-built>Nothing yet — this is the mandatory package-legitimacy gate BEFORE installing @xyflow/react. RESEARCH flagged the package [SUS] (automated "too-new" heuristic on the latest patch date), disposition "approved with note"; per protocol a human must confirm the registry entry before merge. This checkpoint is not auto-approvable.</what-built>
  <how-to-verify>
    1. Run `npm view @xyflow/react version` and confirm it resolves to 12.11.x.
    2. Run `npm view @xyflow/react repository.url` and confirm it points to github.com/xyflow/xyflow (the real, actively-maintained org).
    3. Run `npm view @xyflow/react dist-tags` and confirm latest is a published, non-deprecated release.
    4. Confirm you are NOT about to install the stale `reactflow` package (CLAUDE.md "What NOT to Use").
  </how-to-verify>
  <resume-signal>Type "approved" once the registry entry is confirmed as xyflow/xyflow, or describe the discrepancy.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install @xyflow/react + shadcn switch; scaffold flows feature API client</name>
  <read_first>
    - apps/web/package.json (dependency style + workspace flag)
    - apps/web/src/features/campaigns/api.ts (apiGet/apiPost/apiPatch wrappers + response interfaces)
    - apps/web/src/lib/api.ts (apiPatch added in 04-08)
    - apps/web/components.json (shadcn config)
    - packages/shared-schemas/src/flow.ts (flow DTOs)
  </read_first>
  <action>
    After the legitimacy checkpoint is approved, run `npm install @xyflow/react -w apps/web` (12.11.x) and `npx shadcn@latest add switch` (writes apps/web/src/components/ui/switch.tsx). Create apps/web/src/features/flows/api.ts mirroring campaigns/api.ts: hand-mirrored response interfaces (FlowResponse, FlowListResponse) + TanStack Query hooks — useFlows (list, keepPreviousData), useFlow(id), useCreateFlow (POST /flows), useUpdateFlowDraft (PATCH /flows/:id), usePublishFlow (POST /flows/:id/publish), usePauseFlow/useResumeFlow, useDuplicateFlow. Use the existing apiGet/apiPost/apiPatch/apiDelete from lib/api.ts. Do not invent shared response types (campaigns has none — same convention).
  </action>
  <verify>
    <automated>npm run build -w apps/web && node -e "require('@xyflow/react')" 2>/dev/null; grep -q '@xyflow/react' apps/web/package.json</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json depends on @xyflow/react 12.11.x (NOT reactflow)
    - apps/web/src/components/ui/switch.tsx exists (shadcn switch)
    - apps/web builds clean; flows/api.ts exports the TanStack Query hooks listed above
  </acceptance_criteria>
  <done>@xyflow/react + switch installed; the flows API client is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Custom node types + palette + edges + canvas chrome</name>
  <read_first>
    - .planning/phases/06-flows-triggered-chains/06-UI-SPEC.md (Canvas & Node Visual Language, Color, Typography, Spacing)
    - packages/flows-core/src/flow-definition-schema.ts (node/edge shape + delay kinds)
    - apps/web/src/features/campaigns (existing feature layout/styling conventions)
  </read_first>
  <action>
    Create nodeTypes.tsx exporting five custom node components (trigger, delay, branch, send, exit) as 240px card-styled nodes with the exact icon chips from the UI-SPEC (lucide Zap/Clock/GitBranch/Mail/LogOut with the specified Tailwind color pairs; branch blue is the only new hue, chips-only), a header title + overflow menu, a body config-summary line, and the Не настроено muted placeholder when unconfigured. Selected → ring-2 ring-primary; invalid-for-publish → ring-2 ring-destructive + red dot + tooltip (the specific hard-error text mapped from validateFlowDefinition codes). Create NodePalette.tsx as a top-left Panel listing the five node types as draggable rows (icon chip + Russian label). Create FlowCanvas.tsx wiring ReactFlow with Background (dot-grid neutral-200 on neutral-50), Controls (bottom-left), MiniMap (bottom-right), snapGrid [16,16], the custom nodeTypes, and a custom branch edge that renders labelled Да (green-600) / Нет (neutral-500) edges bound to sourceHandle yes/no (D-13). Drag-drop from the palette adds a node at the drop position. Node x/y positions are free-form (exempt from the 4px scale per UI-SPEC).
  </action>
  <verify>
    <automated>npm run build -w apps/web && grep -q "GitBranch" apps/web/src/features/flows/canvas/nodeTypes.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web builds clean
    - Five node components exist with the UI-SPEC icon/color pairs; branch blue used only in the chip
    - Branch renders two labelled edges (Да/Нет) bound to sourceHandle yes/no
    - Palette supports drag-to-add for all five node types; Background/Controls/MiniMap present
  </acceptance_criteria>
  <done>The canvas renders the five node types, a draggable palette, and labelled binary branch edges.</done>
</task>

<task type="auto">
  <name>Task 3: Node-config side panel + debounced draft autosave</name>
  <read_first>
    - apps/web/src/features/flows/api.ts (useUpdateFlowDraft, from Task 1)
    - apps/web/src/features/segments (existing combobox/live-save-state affordance to mirror)
    - packages/flows-core/src/flow-validate.ts (validateFlowDefinition for instant blocker feedback)
    - .planning/phases/06-flows-triggered-chains/06-UI-SPEC.md (side-panel section titles, delay/re-entry idioms, autosave status copy)
  </read_first>
  <action>
    Create NodeConfigPanel.tsx: a right-docked card whose contents switch by selected node type — trigger (event-name combobox from observed names + free text, or segment picker per D-01/D-03), delay (radio-group Фиксированная длительность with amount + unit select OR Дождаться времени with native time input + optional day-of-week select, timezone caption), branch (segment picker via command+popover), send (Dynamic Template select + verified-sender select mirroring the campaign send-node config, D-16), exit (no config). Section titles use the UI-SPEC Heading role. Create useAutosaveDraft.ts: a debounced (1s) hook that serializes the current nodes/edges + node configs into the flowDefinitionSchema shape and calls useUpdateFlowDraft (PATCH /flows/:id); expose a save-state ('idle'|'saving') the FlowCanvas toolbar renders as Сохранено / Сохранение… (Label/meta text, never a toast). Compute the client-side publish-blocker list by calling validateFlowDefinition(currentDefinition) for instant node-invalid ring + a blocker list — but the actual publish action (in 06-11) posts to the server which re-validates (client validity is never a trusted flag).
  </action>
  <verify>
    <automated>npm run build -w apps/web && grep -q "validateFlowDefinition" apps/web/src/features/flows/canvas/NodeConfigPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web builds clean
    - NodeConfigPanel renders type-specific config for trigger/delay/branch/send/exit (send node reuses the campaign template+sender pattern)
    - useAutosaveDraft debounces 1s and drives a Сохранено/Сохранение… indicator; no manual Save button
    - Client-side publish-blocker list computed via validateFlowDefinition (instant feedback only; server remains authority)
  </acceptance_criteria>
  <done>Node configuration + debounced draft autosave work end-to-end against the flow draft API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | A stale/typosquat canvas package would compromise the build; @xyflow/react flagged [SUS] (too-new heuristic) |
| canvas client → publish API | Client validation is convenience only; the server is the publish authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10-SC | Tampering | @xyflow/react install (supply chain) | high | mitigate | blocking-human legitimacy checkpoint verifying repository.url = xyflow/xyflow before install; version pinned 12.11.x; reactflow explicitly forbidden |
| T-06-10-01 | Tampering | client-only publish validation bypassed | high | mitigate | publish (06-11) always posts to server which re-runs validateFlowDefinition (Pitfall 3); client validity never sent as a trusted flag |
| T-06-10-02 | Input Validation | malformed node/edge data serialized to draft | medium | mitigate | autosave serializes through flowDefinitionSchema shape; server PATCH re-validates via updateFlowDraftSchema |
</threat_model>

<verification>
- Legitimacy checkpoint approved; @xyflow/react 12.11.x installed (not reactflow).
- apps/web builds clean; canvas renders five node types + labelled branch edges + palette + chrome.
- Debounced autosave persists drafts with a save-state indicator.
</verification>

<success_criteria>
- A marketer can drag-and-drop and connect the five node types and configure each; drafts autosave.
- Publish-invalid nodes surface instantly via the shared validator while the server stays the authority.
</success_criteria>

## Artifacts this phase produces (created by this plan)

New file paths: apps/web/src/features/flows/api.ts, apps/web/src/features/flows/canvas/{FlowCanvas.tsx,nodeTypes.tsx,NodePalette.tsx,NodeConfigPanel.tsx,useAutosaveDraft.ts}, apps/web/src/components/ui/switch.tsx.

New symbols: useFlows/useFlow/useCreateFlow/useUpdateFlowDraft/usePublishFlow/usePauseFlow/useResumeFlow/useDuplicateFlow hooks; FlowResponse/FlowListResponse interfaces; the five custom node components; NodePalette; FlowCanvas; useAutosaveDraft; @xyflow/react dependency.

<output>
Create `.planning/phases/06-flows-triggered-chains/06-10-SUMMARY.md` when done.
</output>
