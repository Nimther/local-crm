---
phase: 06-flows-triggered-chains
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/delivery-core/src/contact-timezone.ts
  - packages/delivery-core/src/index.ts
  - apps/worker/src/queues/flows/handlers/send-node.ts
  - apps/worker/src/queues/flows/handlers/delay-node.ts
  - apps/worker/src/queues/__tests__/flow-run-advance.test.ts
autonomous: true
gap_closure: true
requirements:
  - FLOW-05

must_haves:
  truths:
    - "A contact whose stored timezone differs from the workspace default has its custom quiet-hours window evaluated in the CONTACT's timezone: a send inside the contact's local quiet window is deferred (outcome deferred_quiet_hours), never enqueued."
    - "A wait_until delay computes next_wake_at at the configured local time-of-day in the CONTACT's timezone, not the workspace default timezone, whenever the contact has a stored timezone (D-08)."
    - "The contact-timezone lookup lives in exactly one shared function so the two handlers cannot diverge again."
  artifacts:
    - "packages/delivery-core/src/contact-timezone.ts — single shared loadContactTimezone with correct bind order"
    - "apps/worker/src/queues/flows/handlers/send-node.ts — imports the shared helper, no local copy"
    - "apps/worker/src/queues/flows/handlers/delay-node.ts — imports the shared helper, no local copy"
    - "apps/worker/src/queues/__tests__/flow-run-advance.test.ts — two contact-timezone regression tests"
  key_links:
    - "send-node.ts / delay-node.ts -> @mega-crm/delivery-core loadContactTimezone -> contacts.timezone via WHERE workspace_id = $1 AND id = $2 bound [workspaceId, contactId]"
    - "loadContactTimezone result -> resolveTimezone(contactTimezone, workspaceDefault) -> the clock isInsideQuietHours / computeNextWaitUntil evaluate against"
---

<objective>
Close the sole remaining Phase 6 verification gap (06-VERIFICATION.md 2026-07-10, roadmap success criterion 3 / FLOW-05): `loadContactTimezone` in both `send-node.ts` and `delay-node.ts` binds its SQL parameters in swapped order, so the contact's own timezone never resolves and quiet-hours / wait_until always fall back to the workspace default timezone — violating locked decision D-08 ("per-contact timezone is the source of truth; quiet hours must be silence for the RECIPIENT").

Repair the complete observable behavior as one vertical slice: first prove the defect with two failing regression tests (a contact whose stored timezone places "now" inside its custom quiet window while the workspace default places it outside; and a wait_until delay whose next_wake_at must land at the contact's local time), then fix it by consolidating the duplicated helper into one shared function in `@mega-crm/delivery-core` with the correct bind order, so the two handlers cannot diverge again.

Purpose: Make quiet hours and wait-until delays honor the RECIPIENT's clock, restoring FLOW-05 and D-08.
Output: One shared `loadContactTimezone` (delivery-core), both handlers importing it, two green regression tests that fail under the current code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-flows-triggered-chains/06-VERIFICATION.md
@.planning/phases/06-flows-triggered-chains/06-REVIEW.md
@.planning/phases/06-flows-triggered-chains/06-CONTEXT.md
@apps/worker/src/queues/flows/handlers/send-node.ts
@apps/worker/src/queues/flows/handlers/delay-node.ts
@apps/worker/src/queues/__tests__/flow-run-advance.test.ts
@packages/delivery-core/src/index.ts
@packages/delivery-core/src/send-settings.ts
@packages/delivery-core/src/quiet-hours.ts
</context>

## Artifacts this phase produces

New / changed symbols introduced by this plan (exclude from cross-plan drift verification):
- `loadContactTimezone(client, workspaceId, contactId): Promise<string | null>` — newly EXPORTED from `@mega-crm/delivery-core` (previously two private copies inside the worker handlers). New file `packages/delivery-core/src/contact-timezone.ts`; re-exported from `packages/delivery-core/src/index.ts`.
- New test case in `flow-run-advance.test.ts`: "06-15/D-08/FLOW-05: a custom quiet-hours window is evaluated in the CONTACT's timezone — a send inside the contact's local window defers even when the workspace default timezone places now outside it".
- New test case in `flow-run-advance.test.ts`: "06-15/D-08/FLOW-05: a wait_until delay computes next_wake_at at the contact's local time-of-day, not the workspace default timezone".

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add two failing contact-timezone regression tests (RED)</name>
  <files>apps/worker/src/queues/__tests__/flow-run-advance.test.ts</files>
  <read_first>
    - apps/worker/src/queues/__tests__/flow-run-advance.test.ts — reuse its existing fixtures verbatim: `freshWorkspaceId`, `createFixtureContact`, `seedFlowRun`, `seedDelayFlowRun` (accepts a delay node + optional quiet-hours opts), `getFlowRunState` (returns `nextWakeAt`), `getFlowRunSteps`, and the existing "custom quiet-hours window defers" test (the model for the send-node case). Note: `createFixtureContact` inserts a contact with NO timezone.
    - apps/worker/src/queues/flows/handlers/send-node.ts — `resolveQuietHoursWindow` uses the flow's own start/end for mode `'custom'` and resolves the clock via `loadContactTimezone` -> `resolveTimezone(contactTimezone, settings.defaultTimezone)`.
    - apps/worker/src/queues/flows/handlers/delay-node.ts — `handleDelayNode` for a `wait_until` delay resolves the clock the same way, then calls `computeNextWaitUntil(now, node.delay.timeOfDay, node.delay.dayOfWeek, timezone)`.
    - packages/delivery-core/src/send-settings.ts — `upsertWorkspaceSendSettings(client, workspaceId, patch)` sets `default_timezone`; `WorkspaceSendSettings.defaultTimezone`.
    - packages/flows-core/src/flow-definition-schema.ts (lines ~46-63) — the `wait_until` delay node shape: `{ kind: "wait_until", timeOfDay: number 0-1439, dayOfWeek?: number 0-6 }`. `timeOfDay` is MINUTES since local midnight (600 = 10:00), NOT a string.
  </read_first>
  <behavior>
    - Test A (send-node quiet hours): a contact with a stored timezone whose custom quiet window contains "now" defers, even though the workspace default timezone would place "now" outside the window. Under the current swapped-bind code this FAILS (no defer / send enqueued); after the fix it PASSES.
    - Test B (delay wait_until): next_wake_at lands at the configured local time-of-day in the CONTACT's timezone, not the workspace default. Under the current code it FAILS; after the fix it PASSES.
  </behavior>
  <action>
    Add two `it(...)` cases to the existing `describe(...)` block in `flow-run-advance.test.ts` (both require live Postgres + Redis, same as the sibling tests — `TEST_DATABASE_URL` / `TEST_REDIS_URL`).

    Choose two DST-free IANA zones with a large, stable wall-clock separation so assertions never depend on the exact wall-clock minute: CONTACT_TZ = "Asia/Kolkata" (UTC+5:30, no DST) and WORKSPACE_TZ = "Pacific/Honolulu" (UTC-10, no DST) — ~15.5h apart, ~8.5h circular distance on a 24h clock.

    Shared setup for both tests: after `createFixtureContact`, set the contact's timezone with an `UPDATE contacts SET timezone = $1 WHERE workspace_id = $2 AND id = $3` (bound `[CONTACT_TZ, workspaceId, contactId]`) inside `withTenant`/`withTenantTransaction`; and set the workspace default via `upsertWorkspaceSendSettings(client, workspaceId, { defaultTimezone: WORKSPACE_TZ })` imported from `@mega-crm/delivery-core`.

    Test A (mirror the existing "custom quiet-hours window defers" test, but with the timezones above): compute the contact's CURRENT local minute-of-day in CONTACT_TZ using `Intl.DateTimeFormat("en-US", { timeZone: CONTACT_TZ, hour: "2-digit", minute: "2-digit", hourCycle: "h23" }).formatToParts(new Date())`. Build a 120-minute window centered on that minute: start = (localMinutes - 60 + 1440) % 1440, end = (localMinutes + 60) % 1440 — 60 minutes of slack each side removes any minute-boundary fragility. Seed via `seedFlowRun`, then `UPDATE flows SET quiet_hours_mode = 'custom', quiet_hours_start = <start>, quiet_hours_end = <end>` for that run's flow. Run `processFlowRunAdvance({ workspaceId, flowRunId })`. Assert: the single step has `outcome: "deferred_quiet_hours"`, `current_node_id` is still the send node (not advanced), and `emailTriggeredQueue.getJob(\`${flowRunId}-${sendNodeId}\`)` is undefined. (Because WORKSPACE_TZ's current local minute is ~8.5h away from the window center, the buggy code — resolving to the workspace default — sees "now" OUTSIDE the window and enqueues the send, failing these assertions; the fixed code resolves to CONTACT_TZ, sees "now" INSIDE, and defers.)

    Test B (wait_until delay): seed via `seedDelayFlowRun` with a delay node `{ id: "delay-1", type: "delay", delay: { kind: "wait_until", timeOfDay: 600 }, position: { x: 0, y: 50 } }` (600 = 10:00 local; omit dayOfWeek for a daily match). Run `processFlowRunAdvance`. Read `next_wake_at` via `getFlowRunState`. Format it with `Intl.DateTimeFormat("en-US", { timeZone: CONTACT_TZ, hour: "2-digit", minute: "2-digit", hourCycle: "h23" }).formatToParts(nextWakeAt)` and assert local hour === "10" AND minute === "00". As a divergence proof, also format in WORKSPACE_TZ and assert it is NOT 10:00 (Honolulu local will read 18:30 of the prior day for a 10:00 IST instant). (The buggy code computes next_wake_at at 10:00 in WORKSPACE_TZ, which is 01:30 IST — failing the CONTACT_TZ assertion; the fixed code computes 10:00 in CONTACT_TZ.)

    Do NOT modify the shared fixtures or any production code in this task — tests only. Do NOT paste implementation source into test comments.
  </action>
  <verify>
    <automated>cd apps/worker && npx vitest run src/queues/__tests__/flow-run-advance.test.ts -t "06-15"</automated>
  </verify>
  <acceptance_criteria>
    - Both new `it("06-15/D-08/FLOW-05: ...")` cases exist in `flow-run-advance.test.ts`.
    - Running only the two new cases against the CURRENT (unfixed) code produces FAILING assertions (RED): Test A fails on the `deferred_quiet_hours` / no-send-job assertions (the send is enqueued instead); Test B fails on the CONTACT_TZ hour-is-"10" assertion (next_wake_at is at 10:00 in WORKSPACE_TZ, not CONTACT_TZ).
    - The two failures are behavioral assertion failures, NOT import/compile/setup errors (the tests must reach `processFlowRunAdvance` and read real DB state).
    - The pre-existing tests in the file are unchanged and still pass.
  </acceptance_criteria>
  <done>Two regression tests exist and fail for the correct reason (contact timezone never resolved), pinning the defect before the fix.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate loadContactTimezone into delivery-core with correct bind order (GREEN)</name>
  <files>packages/delivery-core/src/contact-timezone.ts, packages/delivery-core/src/index.ts, apps/worker/src/queues/flows/handlers/send-node.ts, apps/worker/src/queues/flows/handlers/delay-node.ts</files>
  <read_first>
    - apps/worker/src/queues/flows/handlers/send-node.ts — the local `loadContactTimezone` (lines ~46-51) and its call site inside `resolveQuietHoursWindow` (line ~93): `loadContactTimezone(client, workspaceId, contactId)`.
    - apps/worker/src/queues/flows/handlers/delay-node.ts — the identical local `loadContactTimezone` (lines ~21-26) and its call site in `handleDelayNode` (line ~64).
    - packages/delivery-core/src/send-settings.ts — the established DB-helper pattern in this package: `import type { PoolClient } from "pg"`, an async function taking `(client, workspaceId, ...)` and running a parameterized `client.query`. Match this file's style and typing exactly.
    - packages/delivery-core/src/index.ts — the barrel export style (grouped `export { ... } from "./file.js"`); note `resolveTimezone` is already exported from `./quiet-hours.js`.
    - packages/delivery-core/src/quiet-hours.ts — pure/DB-free by contract (never imports `pg`); the new DB helper must NOT live here.
  </read_first>
  <action>
    Create `packages/delivery-core/src/contact-timezone.ts` exporting one async function `loadContactTimezone(client: PoolClient, workspaceId: string, contactId: string): Promise<string | null>`. It runs a single parameterized read of the `timezone` column from the `contacts` table filtered by `workspace_id = $1 AND id = $2`, and — this is the fix — passes the parameter array in the order `[workspaceId, contactId]` so `$1` (matched against `workspace_id`) receives the workspaceId and `$2` (matched against `id`) receives the contactId. Return `rows[0]?.timezone ?? null`. Import `PoolClient` as a type from `pg`, mirroring `send-settings.ts`. Add a short doc comment noting D-08 and that it is the single source for the two flow handlers (do NOT restate the buggy pre-fix parameter ordering as a literal in the comment).

    Re-export it from `packages/delivery-core/src/index.ts` (new `export { loadContactTimezone } from "./contact-timezone.js";` grouped with the other exports).

    In `send-node.ts`: delete the local `loadContactTimezone` definition and import `loadContactTimezone` from `@mega-crm/delivery-core` (add it to the existing `@mega-crm/delivery-core` import group alongside `resolveTimezone`, `getWorkspaceSendSettings`, etc.). The call site `loadContactTimezone(client, workspaceId, contactId)` stays byte-identical — only the SQL bind order changes, now inside the shared helper.

    In `delay-node.ts`: delete the local `loadContactTimezone` definition and import it from `@mega-crm/delivery-core` (add to the existing import from that package alongside `getWorkspaceSendSettings`, `resolveTimezone`). Call site unchanged.

    Do not change any other behavior in the two handlers (the surrounding try/catch fallback to `"UTC"` in send-node and the `resolveTimezone` fallback in delay-node stay exactly as they are). Do not add fenced code or the pre-fix bind literal anywhere.
  </action>
  <verify>
    <automated>cd apps/worker && npx vitest run src/queues/__tests__/flow-run-advance.test.ts && grep -rncE "function loadContactTimezone" apps/worker/src/queues/flows/handlers/send-node.ts apps/worker/src/queues/flows/handlers/delay-node.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/delivery-core/src/contact-timezone.ts` exists and its `client.query` parameter array binds `workspaceId` first and `contactId` second (order `[workspaceId, contactId]`), matching the query's `$1 = workspace_id`, `$2 = id`.
    - `packages/delivery-core/src/index.ts` exports `loadContactTimezone` (grep: `grep -c "loadContactTimezone" packages/delivery-core/src/index.ts` >= 1).
    - Both handler files import `loadContactTimezone` from `@mega-crm/delivery-core` and contain NO local `function loadContactTimezone` definition — the final `grep -rncE "function loadContactTimezone"` over the two handler files reports 0 for each.
    - The FULL `flow-run-advance.test.ts` suite passes, including the two new "06-15" cases from Task 1 (GREEN) and every pre-existing case (no regression).
    - No other quiet-hours / delay test in the file was modified to make it pass.
  </acceptance_criteria>
  <done>Contact timezone resolves correctly from a single shared helper; the two regression tests pass and the existing suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BullMQ flow-run-advance job -> Postgres | Worker reads `contacts.timezone` / `workspace_send_settings` to decide whether an email is sent inside a recipient's local quiet window. A wrong-clock decision sends email at the wrong local time (compliance) — the untrusted input is stored contact/workspace data. |
| Worker transaction -> tenant data | Every worker query must be tenant-scoped (PITFALLS #5); the timezone lookup must keep `workspace_id` in its WHERE clause. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15-01 | Tampering (of intended send-timing behavior) / Compliance | send-node.ts quiet-hours gate + delay-node wait_until | high | mitigate | Fix the swapped bind so the contact's own timezone resolves; regression Test A proves a send inside the contact's local quiet window defers, Test B proves wait_until lands at the contact's local time (D-08 / FLOW-05). |
| T-06-15-02 | Information Disclosure (cross-tenant) | shared loadContactTimezone | medium | mitigate | The consolidated helper keeps `WHERE workspace_id = $1 AND id = $2` and is always called with the job-payload `workspaceId` inside `withTenantTransaction`; no cross-tenant read path is introduced. |
| T-06-15-03 | Denial of Service (worker crash on bad zone) | resolveTimezone / Intl construction | low | accept | Pre-existing defensive try/catch -> "UTC" in send-node and `resolveTimezone`'s own validation in delay-node are unchanged; a corrupt stored zone still cannot crash a worker. |

No new runtime dependencies are added (no npm/pip/cargo installs) — supply-chain threat T-06-15-SC is not applicable to this plan.
</threat_model>

<verification>
- `cd apps/worker && npx vitest run src/queues/__tests__/flow-run-advance.test.ts` — full file green, including the two new "06-15" cases (live Postgres + Redis via TEST_DATABASE_URL / TEST_REDIS_URL).
- `grep -rncE "function loadContactTimezone" apps/worker/src/queues/flows/handlers/send-node.ts apps/worker/src/queues/flows/handlers/delay-node.ts` — both 0 (no local copies remain).
- `grep -c "loadContactTimezone" packages/delivery-core/src/index.ts` — >= 1 (exported).
- The shared helper's `client.query` binds `[workspaceId, contactId]`.
</verification>

<success_criteria>
- FLOW-05 / roadmap success criterion 3 restored: for a contact whose stored timezone differs from the workspace default, a send inside the contact's local custom quiet window is deferred, and a wait_until delay computes next_wake_at at the contact's local time-of-day.
- The contact-timezone lookup exists in exactly one shared function (delivery-core), imported by both handlers — it can no longer diverge.
- No behavioral change for contacts without a stored timezone (they still fall back to the workspace default, then UTC).
</success_criteria>

<output>
Create `.planning/phases/06-flows-triggered-chains/06-15-SUMMARY.md` when done.
</output>
