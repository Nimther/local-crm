# Phase 7: Analytics, Dashboard & Send Log - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-14
**Phase:** 7-Analytics, Dashboard & Send Log
**Areas discussed:** Метрики кампаний и шагов, Дашборд воркспейса, Timeline контакта, Send log

---

## Метрики кампаний и шагов

| Option | Description | Selected |
|--------|-------------|----------|
| От delivered | Индустриальный стандарт: open/click rate от delivered; delivery/bounce rate от sent | ✓ |
| Всё от sent | Один знаменатель везде, но расходится с привычными цифрами | |
| Показывать оба | Два процента на метрику — перегружает UI | |

**User's choice:** От delivered (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| Все узлы + письма | Проходимость на каждом узле (flow_run_steps) + delivery-метрики на send-узлах | ✓ |
| Только send-узлы | Минимум работы, но не видно утечек на развилках | |
| Письма + воронка вход/выход | Send-метрики + общая воронка, без по-узловой детализации | |

**User's choice:** Все узлы + письма (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| На canvas + вкладка | Бейджи на узлах canvas (модель Klaviyo) + табличная вкладка «Аналитика» | ✓ |
| Только вкладка-таблица | Проще, но теряется визуальная связь с графом | |
| Только на canvas | Компактно, но сравнивать шаги неудобно | |

**User's choice:** На canvas + вкладка (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| Обогатить сводку | Расширить существующий блок: счётчики + проценты + ссылка в send log | ✓ |
| Вкладка «Аналитика» | Отдельная вкладка на странице кампании — дублирует сводку | |
| You decide | На усмотрение планировщика/UI-фазы | |

**User's choice:** Обогатить сводку (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| По nodeId сквозно | Сумма по всем версиям, где nodeId встречался; просто и честно для MVP | ✓ |
| Только live-версия | Чище семантически, но цифры «обнуляются» после каждой публикации | |
| Селектор версии | Максимум контроля, но требует version-list UI (v2 по Phase 6 D-20) | |

**User's choice:** По nodeId сквозно (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| Да, ключевые колонки | Кампании: sent/delivered%/opened%/clicked%; цепочки: активные runs + отправлено | ✓ |
| Нет, только детальные | Списки как есть — меньше запросов | |
| You decide | На усмотрение планировщика | |

**User's choice:** Да, ключевые колонки (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| Да, отдельной строкой | «Пропущено: N» с разбивкой по причинам; в проценты не входит | ✓ |
| Нет, только в send log | Сводка остаётся про доставку | |

**User's choice:** Да, отдельной строкой (рекомендованный)

---

## Дашборд воркспейса

| Option | Description | Selected |
|--------|-------------|----------|
| Тренды + KPI + топы | Графики трендов и роста базы + KPI-карточки + мини-списки кампаний/цепочек | ✓ |
| Строго ANLT-04 | Только два графика — минимальный MVP | |
| You decide | Состав на усмотрение UI-фазы | |

**User's choice:** Тренды + KPI + топы (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| Пресеты 7/30/90 | Дефолт 30 дней, гранулярность по дням | ✓ |
| Пресеты + произвольный | + date-range picker — гибче, но больше UI и краевых случаев | |

**User's choice:** Пресеты 7/30/90 (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| Да, дашборд = home | Заменяет WorkspaceHome; онбординг-чеклист остаётся блоком сверху | ✓ |
| Отдельный пункт меню | «Аналитика» в сайдбаре; home как есть | |

**User's choice:** Да, дашборд = home (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| Минуты — ок | Инкрементальная агрегация с лагом до нескольких минут | ✓ |
| Максимально live | On-the-fly из сырых таблиц — риск повторить историю statement_timeout | |
| Час и более — ок | Дешевле, но эффект свежей кампании не виден | |

**User's choice:** Минуты — ок (рекомендованный)

---

## Timeline контакта

| Option | Description | Selected |
|--------|-------------|----------|
| Новая history-таблица | История смен статуса из всех точек смены; начинается с этой фазы | ✓ |
| Реконструкция из событий | Без новой таблицы; ручные/CSV-смены невидимы | |

**User's choice:** Новая history-таблица (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| Единый timeline | ContactEventFeed эволюционирует в общую ленту с фильтром по типу | ✓ |
| Две вкладки | События отдельно, почтовая активность отдельно — хронология разорвана | |

**User's choice:** Единый timeline (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| Схлопывать | Одна запись на первое открытие со счётчиком ×N; клики по URL | ✓ |
| Каждое событие отдельно | Полная сырая лента — активный читатель заспамит timeline | |
| Только первое | Из sends.first_opened_at — повторы невидимы совсем | |

**User's choice:** Схлопывать (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| + цепочки и excluded | Входы/выходы цепочек и пропущенные письма с причиной | ✓ |
| Строго ANLT-03 | Только пять обязательных типов | |

**User's choice:** + цепочки и excluded (рекомендованный)

---

## Send log

| Option | Description | Selected |
|--------|-------------|----------|
| Отдельная страница + предфильтры | «Журнал отправок» в сайдбаре; ссылки с других страниц через URL-параметры | ✓ |
| Встроенные таблицы | Вкладки на страницах кампании/цепочки/контакта — три похожие таблицы | |

**User's choice:** Отдельная страница + предфильтры (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| Панель с историей событий | Drawer: полная хронология событий письма, причины, ссылки | ✓ |
| Только строка в таблице | Всё в колонках — причины и повторы не помещаются | |

**User's choice:** Панель с историей событий (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| Один вычисляемый статус | Итоговый статус по цепочке Phase 5 D-06 + failed/excluded; multi-select фильтр | ✓ |
| Статус + флаги-факты | + независимые иконки-факты — информативнее, но перегруженнее | |

**User's choice:** Один вычисляемый статус (рекомендованный)

| Option | Description | Selected |
|--------|-------------|----------|
| Отложить | Не в ANLT-05; экспорт больших объёмов — отдельная задача в backlog | ✓ |
| Да, в этой фазе | Кнопка «Экспорт CSV» по текущим фильтрам | |

**User's choice:** Отложить (рекомендованный)

---

## Claude's Discretion

- Схема rollup-таблиц и механика инкрементальной агрегации + периодической сверки (ROADMAP 07-01)
- Выбор chart-библиотеки (Recharts vs Tremor)
- Пагинация/индексы send log и timeline на больших объёмах
- Расчёт «роста базы» и «новых контактов»
- RLS новых таблиц, tenant context в воркерах агрегации
- Доступ по ролям к аналитике (по умолчанию — читают все члены воркспейса)
- Русские UI-тексты; детали UI — в UI-SPEC (/gsd-ui-phase)

## Deferred Ideas

- CSV-экспорт send log (фоновая генерация)
- Произвольный date-range на дашборде
- Селектор версии цепочки в per-step аналитике
- Ретроспективная реконструкция смен subscription status
- Расширенная диагностика webhook (из Phase 5 deferred) — не входит в ANLT-01…05, остаётся в бэклоге
