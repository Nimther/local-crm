---
phase: 03-segmentation-engine
plan: 08
type: execute
wave: 7
depends_on: [03-06, 03-07]
files_modified:
  - apps/web/e2e/segments-tags.spec.ts
  - apps/web/e2e/segments-behavior.spec.ts
autonomous: true
requirements: [SEGM-01, SEGM-02, SEGM-04]
gap_closure: true
must_haves:
  truths:
    - "An automated E2E proves a tags segment is buildable and savable end-to-end: build «Теги» → «есть тег» → tag value, save, reopen, and the tags condition is intact (closes the failed truth's tags half with an automated check)."
    - "An automated E2E proves the CR-01 regression: clicking «Сохранить сегмент» on the default unconfigured condition shows an inline validation error and does NOT navigate away / silently fail."
    - "An automated E2E exercises the SEGM-02 behavioral row: the count input hides when «ни разу» is selected and shows for «выполнено ≥ N раз»; the days input hides for «за всё время» and shows for «за последние N дней»; the condition round-trips through save and reopen."
    - "An automated check exercises the SEGM-04 degraded state: when preview-count responds `{ degraded: true }`, the amber «(устарело)» marker renders and the last-good count is preserved (never blanked to zero)."
  artifacts:
    - "apps/web/e2e/segments-tags.spec.ts (tags build→save→reopen + CR-01 default-empty-condition regression)"
    - "apps/web/e2e/segments-behavior.spec.ts (SEGM-02 behavioral conditional inputs + round-trip; SEGM-04 degraded state via Playwright route interception)"
  key_links:
    - "These specs exercise the 03-07 web UI (tags row, shared validateDefinition, error UI) against the running app, flipping the verification's UI-tier failed truth and the two behavior_unverified items to automated-proven."
    - "The SEGM-04 degraded-state spec uses page.route interception to return { degraded: true } from preview-count, since a real statement_timeout cannot be forced at test-data volume (the API mechanism is already proven in preview-count.test.ts)."
---

<objective>
Add automated end-to-end coverage for the UI-tier behaviors the verification could not confirm: the tags slice, the CR-01 regression, the SEGM-02 behavioral conditional inputs, and the SEGM-04 degraded state. This is what lets the re-verification pass flip the failed truth (UI half) and the two behavior_unverified items to verified without human judgment.

User-observable slices proven here: "a marketer can build and save a tags segment" and "saving an unconfigured segment tells me what's wrong" — plus regression guards for the behavioral row and the degraded live-count.

Purpose (gap closure): supplies the automated coverage the verification report flagged as missing — the tags path (no E2E built one), CR-01 (the default-state silent failure had no test), SEGM-02 behavioral row conditional UI (03-03-SUMMARY D3, human_judgment), and SEGM-04 degraded amber state (03-03-SUMMARY D4, human_judgment). Addresses SEGM-01 (tags + CR-01), SEGM-02 (behavioral row), SEGM-04 (degraded state).
Output: two new Playwright specs, kept separate from the existing green segments.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-segmentation-engine/03-UI-SPEC.md
@.planning/phases/03-segmentation-engine/03-VERIFICATION.md
@.planning/phases/03-segmentation-engine/03-CONTEXT.md
</context>

<artifacts_produced>
## Artifacts this phase produces (03-08 slice)

New symbols (excluded from source-drift verification):
- New Playwright spec `apps/web/e2e/segments-tags.spec.ts` (tags build→save→reopen; CR-01 default-empty-condition regression).
- New Playwright spec `apps/web/e2e/segments-behavior.spec.ts` (SEGM-02 behavioral conditional inputs + round-trip; SEGM-04 degraded-state via route interception).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Tags-slice E2E + CR-01 default-empty-condition regression</name>
  <files>apps/web/e2e/segments-tags.spec.ts</files>
  <read_first>
    - apps/web/e2e/segments.spec.ts (the full register→create-workspace→open Сегменты→build→save flow to reuse: selectors like getByRole button «Выберите поле», getByRole option «Страна», getByPlaceholder «Значение», «Сохранить сегмент», detail-page assertions lines 8-79)
    - apps/web/src/features/segments/SegmentBuilder.tsx (post-03-07 selectors: «Теги» option, «есть тег»/«нет тега» operators, the tag-name «Значение» input; the operator Select and field combobox roles)
    - apps/web/src/features/segments/SegmentCreatePage.tsx (post-03-07: the inline validation error text and serverError rendering; «Сохранить сегмент» button; the default builder opens with one empty attribute condition)
    - apps/web/src/features/segments/validateDefinition.ts (the exact «Выберите поле в каждом условии» / «Укажите значение условия» copy the CR-01 assertion checks)
    - .planning/phases/03-segmentation-engine/03-VERIFICATION.md (the failed truth: tags unreachable + CR-01 silent save)
  </read_first>
  <action>
    Create a new Playwright spec (do NOT edit the existing segments.spec.ts — keep the green happy-path intact). Reuse its register→create-workspace→navigate-to-Сегменты→«Создать сегмент» preamble (copy the steps; Playwright specs are independent files).

    Test A — tags build→save→reopen: in the builder's default first condition, open the field combobox, choose «Теги», choose operator «есть тег», fill the tag-name «Значение» (e.g. `vip`), wait for the «контактов подходит» live-count label to settle, name the segment, click «Сохранить сегмент», assert navigation back to the list and the segment name is visible. Reopen the segment; assert the definition builder is prefilled with «Теги» + «есть тег» + value `vip` (the tags condition round-trips).

    Test B — CR-01 regression: on a fresh «Создать сегмент» page, fill only the name field, leave the default unconfigured condition (field unselected), click «Сохранить сегмент», and assert (1) an inline validation error «Выберите поле в каждом условии» is visible, and (2) the URL is still the create page (no navigation) — i.e. the save was blocked loudly, not silently. This is the exact scenario the verification report calls the broken primary create flow.

    Follow the existing spec's timing conventions (waitForURL, the 10s live-count timeout for the count label).
  </action>
  <verify>
    <automated>npm run test:e2e -w apps/web -- segments-tags.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `segments-tags.spec.ts` builds a «Теги»/«есть тег» condition, saves, and asserts the segment appears in the list.
    - The spec reopens the segment and asserts the tags condition (field «Теги», operator «есть тег», value) is prefilled.
    - The CR-01 test fills only the name, saves the default empty condition, and asserts the inline error «Выберите поле в каждом условии» is visible AND the create page URL is unchanged.
    - `npm run test:e2e -w apps/web -- segments-tags.spec.ts` passes.
  </acceptance_criteria>
  <done>The tags slice and the CR-01 loud-failure behavior are proven by an automated E2E — the re-verification pass can flip the failed truth's UI half without human judgment.</done>
</task>

<task type="auto">
  <name>Task 2: SEGM-02 behavioral conditional inputs + SEGM-04 degraded state (route-intercepted)</name>
  <files>apps/web/e2e/segments-behavior.spec.ts</files>
  <read_first>
    - apps/web/e2e/segments.spec.ts (the register→workspace→builder preamble to reuse)
    - apps/web/src/features/segments/SegmentBuilder.tsx (BehavioralConditionRow lines 402-486: «Добавить условие»→«По событию»; count-operator select «выполнено ≥ N раз»/«ни разу» controls the count Input; timeframe select «за последние N дней»/«за всё время» controls the days Input; the EventCombobox free-text «Использовать «…»» fallback; the live-count panel + isDegraded amber «(устарело)» rendering lines 646-674)
    - .planning/phases/03-segmentation-engine/03-VERIFICATION.md (behavior_unverified_items: the exact SEGM-02 and SEGM-04 test/expected text, lines 42-50 and 136-146)
    - .planning/phases/03-segmentation-engine/03-UI-SPEC.md (degraded-state copy «(устарело)» + amber banner; behavioral row layout line 157)
  </read_first>
  <action>
    Create a second new Playwright spec (independent file). Reuse the register→workspace→«Создать сегмент» preamble.

    Test A — SEGM-02 behavioral conditional inputs + round-trip: add a behavioral condition («Добавить условие» → «По событию»). Enter an event name via the combobox free-text fallback. Assert: with «выполнено ≥ N раз» selected the count number input is visible, and selecting «ни разу» hides the count input; with «за последние N дней» selected the days input is visible, and selecting «за всё время» hides it. Configure a concrete behavioral condition (e.g. «выполнено ≥ 2 раз» «за последние 30 дней»), name and save the segment, reopen it, and assert the behavioral row round-trips (event name, count operator, count, timeframe, days all prefilled). Use role/label/placeholder selectors consistent with segments.spec.ts.

    Test B — SEGM-04 degraded state via route interception: on the «Создать сегмент» page, register a Playwright `page.route` handler for the preview-count endpoint (`**/segments/preview-count`) that fulfills with a 200 JSON body `{ degraded: true }`. First let a real (non-intercepted) count settle for a simple valid condition so a last-good count is captured, THEN enable the interception and trigger a re-evaluation (edit the condition value) so a `{ degraded: true }` response arrives. Assert the amber «(устарело)» marker is visible and the previously-shown count number is still present (not blanked to «—»/zero), per the D-08 UI-SPEC contract. Document in the spec header that this intercepts the response because a real statement_timeout cannot be forced at test-data volume (the API 57014 mechanism is separately proven in preview-count.test.ts).
  </action>
  <verify>
    <automated>npm run test:e2e -w apps/web -- segments-behavior.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The SEGM-02 test asserts the count input is hidden on «ни разу» and visible on «выполнено ≥ N раз», and the days input is hidden on «за всё время» and visible on «за последние N дней».
    - The SEGM-02 test saves and reopens the segment and asserts the behavioral condition is prefilled (event, count operator, count, timeframe, days).
    - The SEGM-04 test uses `page.route` to return `{ degraded: true }` from preview-count, and asserts the amber «(устарело)» marker is visible while the last-good count number remains displayed (not blanked).
    - `npm run test:e2e -w apps/web -- segments-behavior.spec.ts` passes.
  </acceptance_criteria>
  <done>The behavioral conditional-input UI and the degraded live-count state are covered by automated E2E, converting the two behavior_unverified items to automated-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test harness → running app | Tests drive the real UI + API (Test A paths) and, for the degraded state, intercept a single response client-side; no production trust boundary is altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-16 | Tampering (test fidelity) | route-intercepted degraded-state test | low | accept | The interception returns the exact `{ degraded: true }` shape the real route emits on 57014 (verified against segments.routes.ts's catch block); it tests the UI contract, not a fabricated shape. The API side of the mechanism is proven separately by preview-count.test.ts. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages — Playwright is already the installed E2E runner (apps/web test:e2e). |
</threat_model>

<verification>
- `npm run test:e2e -w apps/web -- segments-tags.spec.ts` and `npm run test:e2e -w apps/web -- segments-behavior.spec.ts` both pass.
- The existing `segments.spec.ts` remains green (untouched).
- The tags slice, CR-01 regression, SEGM-02 conditional inputs, and SEGM-04 degraded state are each exercised by an automated assertion.
</verification>

<success_criteria>
Every UI-tier behavior the verification could not confirm now has an automated check: the tags slice and CR-01 regression (SEGM-01), the behavioral conditional inputs (SEGM-02), and the degraded live-count (SEGM-04). Re-verification can flip the failed truth's UI half and both behavior_unverified items to verified.
</success_criteria>

<output>
Create `.planning/phases/03-segmentation-engine/03-08-SUMMARY.md` when done.
</output>
