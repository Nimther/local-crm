# Phase 3: Segmentation Engine - Context

**Gathered:** 2026-07-05
**Status:** Ready for planning

> **⚠ Auto-selected decisions.** Пользователь выбрал все 4 области для обсуждения, но был недоступен во время вопросов (нет ответа за 60с). Все решения ниже — рекомендованные варианты, выбранные Claude в auto-режиме (yolo mode, прецедент Phase 1/2 «away from keyboard»). Пользователь может пересмотреть любое решение до/во время планирования — перезапустить `/gsd-discuss-phase 3` или отредактировать этот файл.

<domain>
## Phase Boundary

Маркетолог определяет динамические аудитории по атрибутам профиля (страна, теги, кастомные свойства) и по поведению (события с count/timeframe), видит live-счётчик подходящих контактов по мере редактирования условий, сохраняет сегмент — и один общий движок оценки отдаёт идентичный состав сегмента и кампаниям (Phase 4, аудитория), и цепочкам (Phase 6, триггеры/exit conditions). Требования: SEGM-01…04. Режим фазы: MVP (user-flow first).

</domain>

<decisions>
## Implementation Decisions

### Модель определения сегмента (комбинаторы)
- **D-01 (auto):** Двухуровневая модель AND/OR по образцу Klaviyo: условия внутри группы объединяются через OR, группы между собой — через AND («(страна = RU ИЛИ страна = KZ) И (сделал заказ за 30 дней)»). Произвольная вложенность групп — НЕ в v1. Плоский AND-only отвергнут: «RU или KZ» не выразить, а миграция определений задним числом дороже, чем двухуровневая модель сразу.
- **D-02 (auto):** Отрицания — first-class: поведенческое «НЕ сделал / ни разу за период» (прямо требуется success criteria «не открывал письма за 90 дней») и негативные операторы атрибутов («не равно», «не содержит», «тег отсутствует»).
- **D-03 (auto):** Операторы типизированы по реестру свойств (Phase 2 D-10): string — равно/не равно/содержит/пусто/не пусто; number — = ≠ > ≥ < ≤; bool — истина/ложь; date — до/после/в последние N дней; теги — есть/нет. Стандартные поля (страна, город, имя, телефон) — как string-атрибуты. Реестр свойств — источник списка атрибутов и подсказок типов в билдере.
- **D-04 (auto):** Статус подписки доступен как обычное условие профиля (сегменты реактивации «unsubscribed за последний месяц» и т.п.), НО pre-send фильтр Phase 4 (SUBS-03) остаётся независимым обязательным гейтом — сегмент никогда не отменяет suppression/unsubscribe при отправке.

### Поведенческие условия
- **D-05 (auto):** Выбор события в билдере — из наблюдаемых имён событий воркспейса (distinct по таблице events) + свободный ввод как fallback (реестра типов событий нет — EVNT-V2-01 отложен).
- **D-06 (auto):** Форма поведенческого условия v1: «{событие} {выполнено ≥ N раз | ни разу} за {последние N дней | всё время}». Этого достаточно для обоих канонических примеров роадмапа («сделал заказ за 30 дней», «не открывал за 90 дней»). Варианты «не более N раз» / «ровно N» — Claude's discretion, если дёшево.
- **D-07 (auto):** Фильтры по свойствам события («заказ, где сумма > 100») — НЕ в v1, отложено в v2 (см. Deferred). События freeform (имя + JSON) без реестра схем — надёжный типизированный UI для property-фильтров построить не на чем; scope роадмапа требует только count/timeframe.

### Live-превью
- **D-08 (auto):** Счётчик пересчитывается автоматически при каждом изменении определения (debounce), с индикатором загрузки; count точный (не приблизительный). Производительность count-запроса на целевом масштабе (100k–1M контактов) — предмет бенчмарка ресёрчера (флаг из STATE.md), при необходимости планировщик закладывает деградацию (например, таймаут → подсказка «уточните условия»).
- **D-09 (auto):** В билдере — только счётчик, без inline-списка контактов. Посмотреть «кто попал» можно на странице сегмента (список участников, D-12) — это покрывает потребность доверия к определению без усложнения билдера.

### Страница сегментов и жизненный цикл
- **D-10 (auto):** «Сегменты» — самостоятельный раздел в навигации воркспейса рядом с «Контактами» (`/w/{slug}/segments`).
- **D-11 (auto):** Список сегментов: имя, число участников (последний вычисленный + метка времени пересчёта), создан/обновлён, автор. Как и когда пересчитывается счётчик списка — за движком (discretion), но UI показывает «на момент X».
- **D-12 (auto):** Страница сегмента: определение (редактируемое в том же билдере) + пагинированный список участников — переиспользовать паттерн таблицы контактов Phase 2 (поиск/пагинация, keepPreviousData из 02-13).
- **D-13 (auto):** Сегменты всегда динамические — статических снапшотов в v1 нет. Редактирование определения меняет состав «на будущее» везде, где сегмент используется (кампании/цепочки ссылаются по id).
- **D-14 (auto):** Удаление сегмента в Phase 3 разрешено свободно (на него ещё никто не ссылается), но схема/API закладываются под restrict-when-referenced: Phase 4/6 будут блокировать удаление сегмента, используемого кампанией/цепочкой. Переименование свободное.

### Claude's Discretion
- Внутренности движка: on-demand SQL vs materialized membership — решает ресёрчер по бенчмарку на целевом масштабе (STATE.md research flag; НЕ коммититься в materialized-подход без бенчмарка); генерация SQL из definition-JSON, индексы (GIN по properties/tags и т.п.), кэширование count, интервал debounce.
- Формат/версионирование definition-JSON сегмента, Zod-схемы в shared-schemas, RLS новых таблиц по паттерну Phase 1/2.
- Контракт движка для потребителей SEGM-03 (кампании Phase 4: resolve полного состава; цепочки Phase 6: проверка принадлежности контакта) — спроектировать API движка под оба сценария, не строя самих потребителей.
- Пустые состояния, русские тексты UI в стиле Phase 2, детали UX билдера (UI-SPEC придёт из /gsd-ui-phase — ui_phase включён).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Ресёрч проекта (стек, архитектура, риски)
- `.planning/research/STACK.md` — Postgres 16/17, Drizzle (raw SQL escape hatches для сегментных запросов), JSONB-операторы, партиционирование events
- `.planning/research/ARCHITECTURE.md` — границы компонентов, shared tables + workspace_id
- `.planning/research/PITFALLS.md` — Pitfall #5 (tenant context на каждую транзакцию) применим к движку сегментов и любым фоновым пересчётам
- `.planning/research/SUMMARY.md` — сводные риски; бенчмарк поведенческих запросов на 100k–1M контактов ДО коммита в materialized-membership (продублировано в `.planning/STATE.md` Blockers)

### Требования и решения
- `.planning/REQUIREMENTS.md` — SEGM-01…04 (эта фаза); CAMP-*/FLOW-* (соседние — потребители движка через SEGM-03)
- `.planning/ROADMAP.md` §Phase 3 — goal (user story), success criteria, предварительная разбивка планов
- `.planning/phases/02-contacts-event-ingestion/02-CONTEXT.md` — D-09/D-10 (стандартные поля + реестр свойств как вход сегмент-билдера), D-13 (фильтры по кастомным свойствам зарезервированы за этой фазой)

### Схемы данных (вход движка)
- `packages/db/src/schema/contacts.ts` — contacts: стандартные поля, tags array, properties JSONB, subscription_status
- `packages/db/src/schema/events.ts` — events: партиционированная, PK (workspace_id, id, occurred_at), monthly + DEFAULT партиции; читать комментарий про hand-written migrations
- `packages/db/src/schema/` (property registry) — workspace_property_registry: имена и наблюдаемые типы кастомных свойств

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `packages/tenant-context` — withTenantTransaction (SET LOCAL GUC) для всех запросов движка; воркеры/пересчёты обязаны устанавливать tenant context
- `packages/db/src/schema/contacts.ts` + `events.ts` + property registry — весь вход сегментации уже существует и проиндексирован под workspace_id
- `packages/shared-schemas` — сюда добавляются Zod-схемы segment definition (единый контракт API/фронт)
- `apps/web/src/features/contacts` — паттерн таблицы (поиск/фильтры/пагинация, keepPreviousData + dim refetch cue из 02-13) переиспользуется для списка участников сегмента; live-счётчик билдера использует тот же debounce+keepPreviousData подход
- `apps/api/src/modules/*` — Fastify-модуль `segments` по образцу contacts/events

### Established Patterns
- Zod через @fastify/type-provider-zod на всех роутах; better-auth cookie-сессии для UI-маршрутов
- RLS ENABLE+FORCE миграции по паттерну Phase 1/2 для новых таблиц (segments, membership-таблица если будет)
- Русскоязычный UI, Radix + react-hook-form + TanStack Query

### Integration Points
- Phase 4 (кампании): resolve полного состава сегмента как аудитории — контракт движка проектируется сейчас (SEGM-03)
- Phase 6 (цепочки): точечная проверка «контакт ∈ сегмент?» для триггеров/exit conditions — тот же движок, второй режим вызова
- Онбординг-чеклист воркспейса (Phase 1 D-23) может расшириться пунктом «создайте первый сегмент» — опционально, discretion
- Реестр свойств (Phase 2 D-10) — источник атрибутов билдера; событийная таблица — источник имён событий (D-05)

</code_context>

<specifics>
## Specific Ideas

- Klaviyo — референс модели сегментации: двухуровневые AND/OR группы, динамические сегменты, поведенческие условия с count/timeframe.
- Live-счётчик — та же философия «надёжность видна пользователю», что и лента событий в карточке контакта (Phase 2): маркетолог сразу видит, что определение работает.

</specifics>

<deferred>
## Deferred Ideas

- Фильтры по свойствам события в поведенческих условиях («заказ, где сумма > 100») — v2, после появления реестра наблюдаемых схем событий (EVNT-V2-01)
- Произвольная вложенность групп условий (группы в группах) — v2, если двухуровневой модели окажется мало
- Статические сегменты/снапшоты и ручные списки контактов — v2
- Приблизительный/оценочный count для очень больших воркспейсов — только если бенчмарк покажет, что точный count не тянет
- Папки/теги для организации сегментов — v2

</deferred>

---

*Phase: 3-Segmentation Engine*
*Context gathered: 2026-07-05*
