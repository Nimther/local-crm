---
phase: 01-workspace-foundation-team-access
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [TENANT-05, TENANT-04]
files_modified:
  - apps/api/src/modules/tenancy/sendgrid-key.ts
  - apps/api/src/modules/tenancy/__tests__/sendgrid-key-connect.test.ts
  - apps/api/src/modules/tenancy/invites.ts
  - apps/api/src/modules/tenancy/__tests__/invite-flow.test.ts
  - apps/api/src/modules/platform-mail/templates/invite.ts
  - apps/api/src/modules/platform-mail/__tests__/platform-mail.test.ts
  - apps/api/src/db.ts

must_haves:
  truths:
    - "GET /api/workspaces/:slug/sendgrid-key returns 404 (not 200) for an unauthenticated caller and for an authenticated non-member of that workspace; only a member of the workspace receives keyMask/status/lastCheckedAt (closes TENANT-05 / Success Criterion 5)."
    - "The GET sendgrid-key route returns the SAME 404 body for a nonexistent/soft-deleted workspace and for a real workspace the caller does not belong to, so it cannot be used as a workspace-enumeration oracle."
    - "A plain Member receives 403 from GET /api/workspaces/:slug/invites and therefore cannot read pending-invite emails, roles, or inviteUrl accept tokens (WR-02)."
    - "Invite emails neutralize attacker-controlled workspace names: an orgName containing HTML markup is HTML-entity-escaped in the rendered invite body, so no attacker HTML/anchor renders in the recipient's inbox (CR-02)."
    - "The production pg Pool has an 'error' listener, so an idle-connection termination is logged instead of crashing the API process (CR-03)."
  artifacts:
    - "apps/api/src/modules/tenancy/sendgrid-key.ts (GET route membership-gated)"
    - "apps/api/src/modules/tenancy/invites.ts (GET /invites permission-gated)"
    - "apps/api/src/modules/platform-mail/templates/invite.ts (escapeHtml + escaped orgName)"
    - "apps/api/src/db.ts (pool error listener)"
    - "apps/api/src/modules/tenancy/__tests__/sendgrid-key-connect.test.ts (GET auth/membership regression tests)"
    - "apps/api/src/modules/tenancy/__tests__/invite-flow.test.ts (Member-403 regression test on GET /invites)"
    - "apps/api/src/modules/platform-mail/__tests__/platform-mail.test.ts (orgName-escaping test)"
  key_links:
    - "sendgrid-key.ts GET handler -> member-roles.getCallerRoles(headers, slug) -> throw mapped to 404 (unauthenticated / non-member / unknown slug)"
    - "invites.ts GET /invites -> role-guard.requirePermission(\"invitation\", \"create\") preHandler"
    - "platform-mail/templates/invite.ts renderInviteHtml -> escapeHtml(orgName)"
    - "db.ts pool -> pool.on(\"error\") -> logger.error"
---

<objective>
Close the Phase 1 verification blocker and three same-surface Warning findings so the phase re-verifies as passed. The one hard gap: GET /api/workspaces/:slug/sendgrid-key currently has zero authentication and zero membership check (CR-01), letting any unauthenticated caller read another workspace's SendGrid key mask/status/lastCheckedAt by slug — a live cross-tenant information-disclosure violating Success Criterion 5 / TENANT-05. Also fixed on the same tenancy/mail surface: WR-02 (unguarded GET /invites leaks accept tokens to Members), CR-02 (unescaped orgName HTML injection in invite emails), CR-03 (missing production pg Pool error handler that crashes the API on idle-connection termination).

Purpose: Restore the phase's core value proposition — "every workspace's data fully isolated from day one" — which the unauthenticated GET route breaks today, and remove the two least-privilege / injection gaps and one crash-on-failover gap on the same surface.
Output: Membership-gated GET sendgrid-key route with regression tests (401/404 for unauth + non-member), permission-gated GET /invites with a Member-403 regression test, HTML-escaped invite email body with an escaping test, and a production pg Pool error listener.
Scope guardrail: This is gap closure only — no refactors, no new features, no touching the 16 human-verification/UAT items. Do not attempt WR-01/WR-03..WR-07/IN-01..IN-09 (out of scope).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-foundation-team-access/01-VERIFICATION.md
@.planning/phases/01-workspace-foundation-team-access/01-REVIEW.md
</context>

<artifacts_this_plan_produces>
Symbols this plan creates or modifies (source-grounding exclusion list for review convergence — these are intentionally new/changed and must NOT be flagged as drift):

**Created:**
- `escapeHtml(s: string): string` — new module-private helper in `apps/api/src/modules/platform-mail/templates/invite.ts` (entity-escapes `& < > " '`).
- New test case(s) in `apps/api/src/modules/tenancy/__tests__/sendgrid-key-connect.test.ts` covering GET `/api/workspaces/:slug/sendgrid-key`: unauthenticated → 404, authenticated non-member → 404, workspace Member → 200, Owner-with-connected-key → 200 + mask.
- New test case in `apps/api/src/modules/tenancy/__tests__/invite-flow.test.ts`: a plain Member GET `/api/workspaces/:slug/invites` → 403; Owner → 200.
- New test case in `apps/api/src/modules/platform-mail/__tests__/platform-mail.test.ts`: `renderInviteHtml` escapes an HTML-bearing orgName.

**Modified:**
- The GET `/api/workspaces/:slug/sendgrid-key` handler in `apps/api/src/modules/tenancy/sendgrid-key.ts` — adds a session+membership guard (new imports of `toFetchHeaders` from `../../middleware/role-guard.js` and `getCallerRoles` from `./member-roles.js`).
- The GET `/api/workspaces/:slug/invites` route registration in `apps/api/src/modules/tenancy/invites.ts` — adds a `requirePermission("invitation", "create")` preHandler (import already present in-file).
- `renderInviteHtml` in `apps/api/src/modules/platform-mail/templates/invite.ts` — now routes `params.orgName` through `escapeHtml` before interpolation (marker comment `MEGA_CRM_INVITE_TEMPLATE` preserved).
- `apps/api/src/db.ts` — adds `import { logger } from "./logger.js"` and a `pool.on("error", ...)` listener on the exported `pool`.
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate GET sendgrid-key on workspace membership (CR-01 blocker) + regression tests</name>
  <files>apps/api/src/modules/tenancy/sendgrid-key.ts, apps/api/src/modules/tenancy/__tests__/sendgrid-key-connect.test.ts</files>
  <read_first>
    - apps/api/src/modules/tenancy/sendgrid-key.ts (the file being modified — the GET handler at lines 36-54 has no guard; the POST connect/recheck routes below it show the established preHandler/withTenant pattern)
    - apps/api/src/modules/tenancy/member-roles.ts (getCallerRoles(headers, slug) — the tested helper that resolves the caller's roles in a workspace by slug via better-auth getActiveMemberRole)
    - apps/api/src/middleware/role-guard.ts (toFetchHeaders — converts a Fastify request into a Web Headers object for auth.api.* calls)
    - apps/api/src/modules/tenancy/workspace-lookup.ts (findActiveWorkspaceBySlug — excludes soft-deleted workspaces, D-20; keep it as the first check so soft-deleted/nonexistent both 404)
    - node_modules/better-auth/dist/plugins/organization/routes/crud-members.mjs (getActiveMemberRole at ~line 468 — throws for unauthenticated (orgSessionMiddleware), throws BAD_REQUEST for unknown slug, throws FORBIDDEN for a non-member, returns { role } for a member; this is why wrapping getCallerRoles in try/catch and returning 404 on ANY throw closes the enumeration oracle uniformly)
    - apps/api/src/modules/tenancy/__tests__/sendgrid-key-connect.test.ts (the test file to extend — reuse its signUp / markVerified / createWorkspace / verifiedOwner helpers and its db.insert(member) pattern from the Member-403 POST test at lines 144-169; it currently has zero GET coverage)
  </read_first>
  <behavior>
    - Unauthenticated GET /api/workspaces/{slug}/sendgrid-key (no session cookie) → 404, body does NOT include keyMask/status.
    - Authenticated user who is NOT a member of {slug}'s workspace → 404 (same body as a nonexistent workspace — no oracle).
    - A workspace Member (role "member") → 200 (any member may read status; the fix must NOT over-restrict to Owner/Admin) — returns { connected:false } when no key, or the mask when a key exists.
    - The workspace Owner with a connected key → 200 with keyMask + status (happy path preserved).
  </behavior>
  <action>
    In sendgrid-key.ts, add a session+membership guard to the existing GET "/api/workspaces/:slug/sendgrid-key" handler. Add two imports at the top: `toFetchHeaders` from "../../middleware/role-guard.js" and `getCallerRoles` from "./member-roles.js". In the handler, keep the existing findActiveWorkspaceBySlug(slug) call first (returns 404 for a nonexistent OR soft-deleted workspace, preserving D-20). Then, before the withTenant(workspace.id, getKey()) read, resolve the caller's membership by calling getCallerRoles(toFetchHeaders(request), slug) inside a try/catch; on ANY thrown error (unauthenticated session, unknown slug, or non-member — all three throw in better-auth's getActiveMemberRole) return reply.code(404).send({ error: "Workspace not found" }). Only proceed to read and return the key row when getCallerRoles resolves (caller is a member). Do NOT use requirePermission here: that gate 403s a Member and returns 403 (not 404) for non-members, which both over-restricts reads and re-opens the enumeration oracle — the route must be readable by any member and must return 404 (matching the nonexistent-workspace path) for non-members. Update the GET route's doc comment so it states the route is gated to workspace members (the current comment "visible to any workspace member" describes intent, not the shipped behavior). Do not change the POST connect/recheck routes.

    In sendgrid-key-connect.test.ts, add GET regression tests using the file's existing helpers. Add: (1) an unauthenticated GET (no cookie header) asserting statusCode 404 and that the JSON body has no keyMask; (2) a non-member GET — sign up a second verified user who is NOT added to the workspace, GET with that user's cookie, assert 404; (3) a Member GET — reuse the db.insert(member) pattern (role "member") from the existing Member-403 test to add a member, GET with that member's cookie, assert 200; (4) an Owner GET after a successful connect (mock scopes + verified senders, POST connect as verifiedOwner, then GET) asserting 200 with a keyMask matching the existing /^.+…\w{4}$/ shape and status "active".
  </action>
  <verify>
    <automated>npx vitest run --root apps/api src/modules/tenancy/__tests__/sendgrid-key-connect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --root apps/api src/modules/tenancy/__tests__/sendgrid-key-connect.test.ts` exits 0 with the new GET cases passing.
    - Behavior: GET /api/workspaces/:slug/sendgrid-key with no session cookie returns 404 and the response JSON contains no keyMask.
    - Behavior: GET with an authenticated non-member session returns 404 with the same `{ error: "Workspace not found" }` body a nonexistent slug returns (verified by the non-member test and the file's existing 404-on-missing-workspace behavior).
    - Behavior: GET with a workspace Member session returns 200 (not 403).
    - Behavior: GET with the Owner after connecting a key returns 200 with keyMask matching /^.+…\w{4}$/.
    - Source: sendgrid-key.ts imports getCallerRoles from "./member-roles.js" and the GET handler wraps the membership resolution in try/catch returning 404 on throw (`grep -n "getCallerRoles" apps/api/src/modules/tenancy/sendgrid-key.ts` matches inside the GET handler).
  </acceptance_criteria>
  <done>Any unauthenticated or non-member caller receives 404 (never the key mask/status) from GET sendgrid-key; workspace members still read status; the enumeration oracle is closed; regression tests lock all four behaviors. TENANT-05 / Success Criterion 5 restored for this route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Permission-gate GET /invites so Members cannot harvest accept tokens (WR-02) + regression test</name>
  <files>apps/api/src/modules/tenancy/invites.ts, apps/api/src/modules/tenancy/__tests__/invite-flow.test.ts</files>
  <read_first>
    - apps/api/src/modules/tenancy/invites.ts (the file being modified — the GET "/api/workspaces/:slug/invites" route at lines 87-111 has no preHandler; the sibling POST create/revoke/resend routes already carry requirePermission("invitation", ...); toInviteResponse at lines 22-37 embeds inviteUrl = `${WEB_URL}/invite/${inv.id}`, where the invitation id IS the accept credential)
    - apps/api/src/middleware/role-guard.ts (requirePermission — already imported in invites.ts; a plain Member has no invitation permissions per D-17, so this yields 403 for a Member, and 404 for a nonexistent/soft-deleted workspace)
    - apps/api/src/modules/tenancy/__tests__/invite-flow.test.ts (the test file to extend — has signUp / signIn / createWorkspace helpers; import `member` from "@mega-crm/db" and follow the db.insert(member) pattern used in sendgrid-key-connect.test.ts lines 144-169 to add a Member)
    - apps/api/src/modules/tenancy/__tests__/sendgrid-key-connect.test.ts (reference for the exact db.insert(member).values({ organizationId, userId, role: "member" }) shape)
  </read_first>
  <behavior>
    - A plain Member GET /api/workspaces/{slug}/invites → 403 (cannot list pending invites or read inviteUrl accept tokens).
    - The Owner GET /api/workspaces/{slug}/invites → 200 with a JSON array (existing behavior preserved).
  </behavior>
  <action>
    In invites.ts, add a preHandler to the existing GET "/api/workspaces/:slug/invites" route: change its registration to pass `{ preHandler: requirePermission("invitation", "create") }` as the second argument (matching the POST create route on the same file at line 49). requirePermission is already imported. Leave the handler body unchanged (it already filters to pending and maps through toInviteResponse). Rationale for reusing the "invitation"/"create" permission: the Team page only renders invite rows for Owner/Admin, who are exactly the roles that hold invitation:create per D-17; a Member holds no invitation permission and is correctly 403'd. Note: for a non-member/unauthenticated caller the underlying better-auth hasPermission throws (the pre-existing WR-06 behavior shared by every already-gated route on this file) — do NOT add try/catch handling for that here; WR-06 is explicitly out of scope for this gap-closure and adding it would change sibling-route behavior.

    In invite-flow.test.ts, add a regression test: an Owner signs up and creates a workspace; a second user signs up and is inserted as a member (role "member") via db.insert(member) on that workspace's organizationId; assert GET /api/workspaces/{slug}/invites with the Member's cookie returns 403; assert the same GET with the Owner's cookie returns 200 and an array body. Import `member` from "@mega-crm/db" alongside the existing invitation import.
  </action>
  <verify>
    <automated>npx vitest run --root apps/api src/modules/tenancy/__tests__/invite-flow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --root apps/api src/modules/tenancy/__tests__/invite-flow.test.ts` exits 0 with the new Member-403 case passing.
    - Behavior: GET /api/workspaces/:slug/invites returns 403 for a Member session and 200 (array) for the Owner session.
    - Source: the GET "/api/workspaces/:slug/invites" registration in invites.ts includes `preHandler: requirePermission("invitation", "create")` (`grep -n "invites\"," apps/api/src/modules/tenancy/invites.ts` context shows the preHandler on the GET route).
  </acceptance_criteria>
  <done>A Member can no longer list pending invites or read inviteUrl accept tokens; Owner/Admin listing still works; a regression test asserts the 403/200 split.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Escape orgName in invite emails (CR-02) + add production pg Pool error listener (CR-03)</name>
  <files>apps/api/src/modules/platform-mail/templates/invite.ts, apps/api/src/modules/platform-mail/__tests__/platform-mail.test.ts, apps/api/src/db.ts</files>
  <read_first>
    - apps/api/src/modules/platform-mail/templates/invite.ts (the file being modified — renderInviteHtml interpolates params.orgName raw into an h1 and a p at lines 5 and 7; the marker comment MEGA_CRM_INVITE_TEMPLATE must be preserved because tests assert it)
    - apps/api/src/modules/platform-mail/client.ts (sendInvite builds the subject `Приглашение в ${params.orgName} — Mega CRM` at line 49 and passes html from renderInviteHtml; the subject is sent as a JSON field to SendGrid, not a rendered HTML context — see rationale in action)
    - apps/api/src/modules/platform-mail/__tests__/platform-mail.test.ts (the test file to extend — the "sendInvite ... uses the in-repo invite template" test at lines 105-138 captures the outbound HTML via nock; add a focused unit test that imports renderInviteHtml directly and asserts escaping)
    - apps/api/src/db.ts (the file being modified — exports `pool = new Pool(...)` with no error listener; the chaos test attaches pool.on("error") in test only)
    - apps/api/src/logger.ts (the Pino logger to use in the pool error handler; db.ts -> logger.ts -> env.ts, no circular import)
    - apps/api/src/db/__tests__/rls-pooling-chaos.test.ts (shows the intended pool.on("error") swallow pattern its comment calls "the correct, permanent guard")
  </read_first>
  <behavior>
    - renderInviteHtml called with an orgName that contains HTML angle-bracket markup (e.g. an anchor/script tag payload) returns HTML in which those angle brackets are entity-escaped (&lt; / &gt; / &amp; / &quot; / &#39;), so the injected markup renders as inert text and does not become a live element.
    - renderInviteHtml with a plain orgName (e.g. "Acme") still contains that name and still contains the MEGA_CRM_INVITE_TEMPLATE marker.
  </behavior>
  <action>
    In invite.ts, add a module-private helper `escapeHtml(s: string): string` that replaces, in order, `&` -> `&amp;`, `<` -> `&lt;`, `>` -> `&gt;`, `"` -> `&quot;`, `'` -> `&#39;`. In renderInviteHtml, compute `const orgName = escapeHtml(params.orgName);` and use that escaped value in BOTH the h1 and the p interpolations (replace the two raw `${params.orgName}` usages). Keep the MEGA_CRM_INVITE_TEMPLATE marker comment and all surrounding markup unchanged. Optionally also route inviteUrl through escapeHtml as defense-in-depth (it is server-constructed, so this is not required). Do NOT change the subject line in client.ts: an email subject is transmitted to SendGrid as a JSON field and rendered by mail clients as plain text (not an HTML sink and not a raw SMTP header), so injected markup cannot execute there — HTML-escaping it would only surface literal entity codes in the subject. The live phishing/HTML-injection vector is the rendered body, which this task closes.

    In platform-mail.test.ts, add a unit test that imports renderInviteHtml from "../templates/invite.js" and calls it with an orgName containing angle-bracket HTML markup (construct the payload from a fragment plus a tag name so the raw executable string is assembled at runtime, not written literally). Assert the returned HTML contains the entity-escaped form (e.g. includes "&lt;" and "&gt;") and does NOT contain the un-escaped injected tag, and that a plain-orgName render still contains "MEGA_CRM_INVITE_TEMPLATE". Do not weaken the existing nock-based sendInvite test.

    In db.ts, add `import { logger } from "./logger.js";` and, immediately after the `export const pool = new Pool(...)` line, attach `pool.on("error", (err) => logger.error({ err }, "idle pg pool client error (connection dropped)"));` so an idle-connection termination (Postgres restart/failover/timeout) is logged rather than thrown as an uncaught exception that kills the process.
  </action>
  <verify>
    <automated>npx vitest run --root apps/api src/modules/platform-mail/__tests__/platform-mail.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --root apps/api src/modules/platform-mail/__tests__/platform-mail.test.ts` exits 0 with the new escaping test passing.
    - Behavior: renderInviteHtml with an HTML-markup orgName returns output containing "&lt;" and "&gt;" and not the raw injected tag; with a plain orgName it still contains "MEGA_CRM_INVITE_TEMPLATE".
    - Source: invite.ts defines an escapeHtml function and renderInviteHtml no longer interpolates a raw params.orgName (`grep -n "escapeHtml" apps/api/src/modules/platform-mail/templates/invite.ts` matches the definition and its use).
    - Source: db.ts contains a pool error listener — `grep -n "pool.on(\"error\"" apps/api/src/db.ts` matches, and db.ts imports logger from "./logger.js".
  </acceptance_criteria>
  <done>Attacker-controlled workspace names are HTML-escaped in the invite email body (phishing/HTML-injection vector closed); the production pg Pool logs idle-connection errors instead of crashing the API; the escaping test locks the behavior. Note: pool.on("error") is an operational reliability guard with no cheap behavioral harness in the existing suite — its coverage is a source assertion by design, which is acceptable for this infra-level guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → GET sendgrid-key API | Untrusted, possibly unauthenticated caller crosses here; today reaches tenant-scoped key metadata with no session/membership check |
| Member (lower-privileged) → GET /invites | An authenticated but unprivileged workspace member requests invite listings containing accept tokens |
| tenant user input (workspace name) → platform-sent email | Attacker-controllable orgName flows into HTML delivered from the platform's own SendGrid sender identity |
| Postgres → pg Pool (idle client) | Infra event (restart/failover/idle timeout) crosses into the Node process as a pool 'error' event |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Information Disclosure | GET /api/workspaces/:slug/sendgrid-key (sendgrid-key.ts) | critical | mitigate | Task 1 — require an authenticated workspace member (getCallerRoles try/catch) before reading the key row; return 404 for unauthenticated/non-member. Regression tests assert 404 for both. |
| T-01-07 | Information Disclosure | GET sendgrid-key as workspace-enumeration oracle | high | mitigate | Task 1 — return the SAME 404 body for a nonexistent/soft-deleted workspace and for a non-member of a real workspace, so status codes/bodies do not confirm workspace existence. |
| T-01-08 | Tampering / Spoofing | invite email HTML body (platform-mail/templates/invite.ts) | high | mitigate | Task 3 — escapeHtml() on orgName before interpolation, neutralizing injected anchors/markup that would phish from the platform's sender reputation. Escaping test asserts entity encoding. |
| T-01-09 | Information Disclosure / Elevation of Privilege | GET /api/workspaces/:slug/invites (invites.ts) | high | mitigate | Task 2 — requirePermission("invitation","create") preHandler so a Member (zero invitation permissions per D-17) is 403'd and cannot read inviteUrl accept tokens for pending (incl. Admin) invites. |
| T-01-10 | Denial of Service | production pg Pool (db.ts) | high | mitigate | Task 3 — pool.on("error") listener logs idle-connection termination instead of letting it become an uncaught exception that kills the API process. |
| T-01-11 | Information Disclosure (fix-introduced) | membership check error mapping in Task 1 | medium | mitigate | The new getCallerRoles call throws (better-auth APIError) for unauthenticated/non-member; the handler MUST catch and map to 404 (never let it surface as a 500 with a better-auth error body that leaks membership state). Task 1 action and acceptance criteria enforce the try/catch → 404 mapping. |

All threats in scope carry disposition `mitigate`; none accepted. Blocking threshold: high (security_block_on: high, ASVS L1) — T-01-06 (critical), T-01-07/08/09/10 (high) are all mitigated within this plan.

## Schema note
None of these changes touch packages/db schema files, drizzle-kit migrations, or drizzle/*.ts — the fixes are route/handler/template/pool-config level. No `drizzle-kit push` task is required (per the schema-gate contribution).
</threat_model>

<verification>
Phase-level re-verification readiness:
- Full API suite green including the new cases: `npx vitest run --root apps/api` (expect the prior 32 tests plus the new GET-auth, Member-403, and escaping cases all passing).
- CR-01 closed: no unauthenticated/non-member path reaches keyMask/status on GET sendgrid-key (Task 1 tests).
- WR-02 closed: Member GET /invites → 403 (Task 2 test).
- CR-02 closed: invite email body escapes orgName (Task 3 test).
- CR-03 closed: db.ts has a pool "error" listener (Task 3 source assertion).
- No debt markers introduced: `grep -rn -E "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER" apps/api/src` (excluding __tests__) yields no new matches.
</verification>

<success_criteria>
- GET /api/workspaces/:slug/sendgrid-key returns 404 for unauthenticated callers and authenticated non-members; only workspace members receive key metadata (TENANT-05 / Success Criterion 5 restored).
- The route cannot be used to enumerate workspaces (uniform 404).
- GET /api/workspaces/:slug/invites is 403 for Members (accept tokens no longer leak).
- Invite email bodies HTML-escape attacker-controlled workspace names.
- The production pg Pool has an error listener (no crash on idle-connection termination).
- `npx vitest run --root apps/api` passes in full.
</success_criteria>

<output>
Create `.planning/phases/01-workspace-foundation-team-access/01-06-SUMMARY.md` when done.
</output>
