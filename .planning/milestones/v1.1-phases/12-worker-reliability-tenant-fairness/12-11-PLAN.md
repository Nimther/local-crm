---
phase: 12-worker-reliability-tenant-fairness
plan: 11
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - apps/api/package.json
  - apps/api/src/modules/campaigns/campaign-queues.ts
  - apps/api/src/modules/contacts/imports-csv-queue.ts
  - apps/api/src/modules/events/events-queue.ts
  - apps/api/src/modules/webhooks/enqueue.ts
  - apps/api/src/modules/flows/flow-queues.ts
  - apps/worker/src/queues/__tests__/queue-core-single-definition.test.ts
autonomous: true
requirements: [WRK-11]

must_haves:
  truths:
    - "Every module in the application process that constructs a BullMQ queue obtains its connection options and its retry and backoff values exclusively by import from the shared package"
    - "The application process and the worker process resolve identical connection options and identical send-lane retry values from the same module, so a restart of either cannot produce divergent queue configuration"
    - "An automated invariant fails if a second copy of any consolidated definition reappears in either application"
    - "The application-side Redis client that deliberately uses a different retry setting keeps its own definition and is explicitly excluded from the invariant's guarded set"
  artifacts:
    - path: "apps/worker/src/queues/__tests__/queue-core-single-definition.test.ts"
      provides: "The invariant guarding both applications against a returning duplicate definition"
  key_links:
    - from: "apps/api/src/modules/events/events-queue.ts"
      to: "packages/queue-core/src/index.ts"
      via: "imports the shared connection builder and job-options factory"
      pattern: "@mega-crm/queue-core"
    - from: "apps/api/src/modules/webhooks/enqueue.ts"
      to: "packages/queue-core/src/index.ts"
      via: "imports the shared connection builder and job-options factory"
      pattern: "@mega-crm/queue-core"
---

<objective>
Finish WRK-11 by repointing the application process's five BullMQ queue modules at `@mega-crm/queue-core`, and land the invariant that guards both applications.

Purpose: plan 12-02 closed the worker-side half of the duplication inventory. Five application-side modules still declare their own connection builder and their own retry/backoff job-option literal — the very drift WRK-11 exists to remove, and the reason the two processes could otherwise acquire different queue configuration from one deploy to the next. The invariant test lands here rather than in 12-02 because it can only pass once both halves are done.
Output: five repointed modules and one invariant test covering all nine guarded modules across both applications.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-worker-reliability-tenant-fairness/12-CONTEXT.md
</context>

## Artifacts this phase produces

See the phase-level list in `12-01-PLAN.md`. Created by **this plan**: `apps/worker/src/queues/__tests__/queue-core-single-definition.test.ts`.

<tasks>

<task type="auto">
  <name>Task 1: Repoint the application's five queue modules at the shared package</name>
  <files>apps/api/package.json, apps/api/src/modules/campaigns/campaign-queues.ts, apps/api/src/modules/contacts/imports-csv-queue.ts, apps/api/src/modules/events/events-queue.ts, apps/api/src/modules/webhooks/enqueue.ts, apps/api/src/modules/flows/flow-queues.ts</files>
  <read_first>
    - apps/api/src/modules/campaigns/campaign-queues.ts (its local connection builder, its local job-options object, and the comment that describes the duplication as an accepted convention — that comment now documents a state that no longer exists)
    - apps/api/src/modules/contacts/imports-csv-queue.ts, apps/api/src/modules/events/events-queue.ts, apps/api/src/modules/webhooks/enqueue.ts, apps/api/src/modules/flows/flow-queues.ts (the other four declaration sites, including any per-queue retention that differs from the common shape)
    - apps/api/src/server.ts (the Redis client near the top of the file with a deliberately different retry setting and its explanatory comment — read it to confirm it is NOT a BullMQ connection, then leave it exactly as it is)
    - packages/queue-core/src/queue-options.ts and packages/queue-core/src/connection.ts (the shared definitions and the two retention constants)
    - apps/api/package.json (the dependency block the package joins)
  </read_first>
  <action>
Add the shared package to `apps/api/package.json` dependencies and re-run the repository-root install.

In each of the five queue modules, delete the local connection-builder function and the local job-options object, importing the builder and the factory from the package instead. Each call site chooses its own retention: use the standard retention constant where the module's current behavior matches the common shape, and if any module's retention already differs, preserve that difference by passing the matching constant rather than flattening it — the differentiated policy that already exists elsewhere in the codebase is a precedent to protect, not an inconsistency to erase. If a module needs a retention shape neither constant expresses, stop and add it to the shared module with its own rationale comment rather than reintroducing a local literal.

Delete the comments in these files that present the duplication as a deliberate convention, and leave every other comment intact.

Leave the application server's non-BullMQ Redis client completely untouched: its retry setting differs on purpose and its own comment explains why. Collapsing it into the shared BullMQ builder would change behavior the shared builder was never meant to govern.

The application-side builders were typed against BullMQ's connection-options type while the shared builder returns the ioredis options type; those are structurally compatible, so no cast is needed. If the compiler disagrees, widen the shared builder's declared return type rather than casting at any call site.
  </action>
  <verify>
    <automated>npm install && npx tsc -p apps/api/tsconfig.json --noEmit && npm test --workspace=apps/api</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p apps/api/tsconfig.json --noEmit` exits 0 and `npm test --workspace=apps/api` exits 0
    - `apps/api/package.json` lists `@mega-crm/queue-core` in dependencies
    - All five modules named in this task's files field contain `@mega-crm/queue-core` and `buildJobOptions`
    - `apps/api/src/server.ts` is unchanged by this plan (`git diff --name-only` for this task does not list it)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Every BullMQ queue the application process constructs resolves its connection options and job options from the shared package, and the deliberately divergent non-BullMQ client is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-application single-definition invariant</name>
  <files>apps/worker/src/queues/__tests__/queue-core-single-definition.test.ts</files>
  <read_first>
    - apps/worker/src/queues/__tests__/rollup-enum-migration-invariant.test.ts (the established source-reading invariant style in this repository: read files from disk, assert on their contents, and carry a header comment naming what future change the test defends against)
    - packages/queue-core/src/queue-options.ts and packages/queue-core/src/connection.ts (the canonical definitions the invariant pins)
    - apps/worker/src/queues/flows/flow-queues.ts and apps/api/src/modules/events/events-queue.ts (one repointed module from each application, to model the expected import shape)
    - .planning/phases/12-worker-reliability-tenant-fairness/12-02-PLAN.md (the duplication inventory listing every guarded module and the one deliberate non-target)
  </read_first>
  <action>
Write a source-reading invariant test over the guarded set: the six worker-side modules and the five application-side modules that construct BullMQ queues. For each, read the file, strip its comments first so a future explanatory sentence cannot fail the gate on comment text alone, and then assert two things — the module imports from the shared package, and the module's remaining source declares neither a function that parses a Redis URL into connection options nor an object literal carrying a numeric retry-attempt count.

Add a separate positive case asserting the cross-process claim directly: the values both applications consume are read from the shared module in one place, so the application and worker processes cannot drift into divergent queue configuration.

Explicitly exclude the application server's non-BullMQ Redis client from the guarded set, with an inline comment stating that its retry setting differs deliberately and that it is not a BullMQ connection — otherwise a future reader will "fix" the exclusion.

Give the file a header comment naming the requirement and the retention pitfall, and stating that the correct remedy when it fails is to import from the shared package, never to add a second definition. Before finishing, temporarily reintroduce a local job-option literal into one guarded module, confirm the test fails, and revert.
  </action>
  <verify>
    <automated>vitest run --root apps/worker src/queues/__tests__/queue-core-single-definition.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run --root apps/worker src/queues/__tests__/queue-core-single-definition.test.ts` exits 0
    - The test enumerates all eleven guarded modules by path across both applications
    - Each assertion strips comments from the source before matching
    - The test carries an explicit, commented exclusion for the application server's non-BullMQ Redis client
    - Reintroducing a local job-option literal into one guarded module makes the test fail (verified once during execution, then reverted, and the observation recorded in the plan summary)
    - `npm test --workspace=apps/worker` exits 0
  </acceptance_criteria>
  <done>An automated invariant proves connection options, job options and TTL values have exactly one definition across both applications, and fails loudly if a copy returns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application process → Redis | Producer queues in the application process now resolve options from the same module the worker uses |
| guarded set → deliberate exception | An over-broad invariant would push a future engineer to change a client whose divergence is intentional |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-11-01 | Tampering | queue configuration drift between the two processes | medium | mitigate | Both processes import connection options and job options from one module, and the invariant test fails if a local copy reappears in either. |
| T-12-11-02 | Denial of Service | the deliberately divergent non-BullMQ client collapsed into the shared builder | medium | mitigate | That client is excluded from the guarded set with an inline comment stating why, and the plan's acceptance criteria assert the file is unchanged. |
| T-12-11-03 | Information Disclosure | Redis credentials in the repointed modules | low | accept | Credentials continue to come from the process's validated environment and are passed to BullMQ exactly as before; no new logging or exposure is introduced. |
</threat_model>

<verification>
- `npx tsc -p apps/api/tsconfig.json --noEmit` exits 0
- `npm test --workspace=apps/api` exits 0
- `vitest run --root apps/worker src/queues/__tests__/queue-core-single-definition.test.ts` exits 0
- `npm run lint` exits 0
</verification>

<success_criteria>
- No module in either application declares its own Redis connection builder or job-option retry literal for a BullMQ queue
- Both processes resolve identical queue configuration from one module
- An automated invariant guards the state and names the correct remedy on failure
- The one deliberately divergent Redis client is untouched and explicitly excluded
</success_criteria>

<output>
Create `.planning/phases/12-worker-reliability-tenant-fairness/12-11-SUMMARY.md` when done
</output>
