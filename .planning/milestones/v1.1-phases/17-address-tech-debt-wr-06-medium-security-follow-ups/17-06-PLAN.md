---
phase: 17-address-tech-debt-wr-06-medium-security-follow-ups
plan: 06
type: execute
wave: 4
depends_on: ["17-05"]
files_modified:
  - SPECIFICATION.md
  - .planning/phases/14-deployment-database-durability/14-SECURITY.md
  - .planning/v1.1-MILESTONE-AUDIT.md
autonomous: true
# No requirement IDs are mapped to Phase 17 (ROADMAP: "Requirements: TBD").
# Scope authority is CONTEXT.md's locked decisions D-01..D-12; this plan discharges
# D-10 (documentation trail) and stages D-12 (auditor-signed register flips).
requirements: []

must_haves:
  truths:
    - "SPECIFICATION.md describes the system as actually built after this phase: the pool-level TimeZone pin, the CI-built GHCR postgres image, the changed POSTGRES_IMAGE_TAG semantics, the new RESTORE_DRILL_METRICS_FILE override, and the growth query's UTC anchor (D-10)."
    - "T-14-58, T-14-73 and T-14-88 carry cited, checkable closure evidence — file paths, commands, and the 17-05 SUMMARY sections holding the pasted live output."
    - "Those three register rows' status fields are NOT flipped to closed by this plan; they stay open pending a security-auditor pass (D-12), and the register says so explicitly."
    - "v1.1-MILESTONE-AUDIT.md's WR-06 entry, its three-medium-security-items entry, and its alloy/Loki operator entry are annotated as addressed by Phase 17, each citing the plan and evidence that addressed it."
    - "Every evidence citation resolves: the named file exists and the named SUMMARY section contains the pasted output being cited."
  artifacts:
    - "SPECIFICATION.md updated at every anchor this phase's changes touch, with a Phase 17 entry in the divergence/review-summary sections."
    - "14-SECURITY.md carries an evidence annotation on each of the three rows plus a dated Phase 17 note recording that the flips await an auditor pass."
    - "v1.1-MILESTONE-AUDIT.md carries closure annotations for WR-06, the three medium security items, and the alloy/Loki item."
  key_links:
    - "17-05-SUMMARY.md's pasted live output -> the evidence citations in 14-SECURITY.md -> the auditor's re-verification target. A citation pointing at a section that holds no pasted output is a broken link, not evidence."
    - "CLAUDE.md's SPECIFICATION.md maintenance rule -> this plan's SPECIFICATION.md edits (see the objective for why they land here rather than in each preceding plan)."
  prohibitions:
    - statement: "This plan must not change any of the three register rows' status column values."
      status: resolved
      verification: "`grep -c 'open (below block threshold)' .planning/phases/14-deployment-database-durability/14-SECURITY.md` returns the same count before and after this plan; D-12 reserves the flip for a security-auditor re-run."
    - statement: "No figure or evidence claim may be written that is not present in a committed SUMMARY."
      status: resolved
      verification: "Each citation names a SUMMARY file and section; the acceptance criteria require confirming each named section exists and contains the cited output."
---

<objective>
Discharge D-10's documentation trail and stage D-12's auditor sign-off: bring `SPECIFICATION.md`
into line with what this phase actually built, attach cited closure evidence to
`14-SECURITY.md`'s three open medium rows **without flipping their status**, and annotate the
milestone audit's carried items as addressed.

Purpose: the code and the live evidence exist after plans 17-01 through 17-05, but a security
review reads `SPECIFICATION.md` and `14-SECURITY.md`, not this phase's git log.
`SPECIFICATION.md` currently states that the `megacrm-postgres` image is locally built with a
`local` default tag — a sentence that is now false and would mislead exactly the reader the
document exists for. And `14-SECURITY.md`'s three rows still describe mitigations as absent.

D-12 draws the line this plan must not cross: register rows reached `verified` status originally
through a `gsd-security-auditor` pass, and they flip back to closed the same way. This plan
supplies the evidence an auditor verifies; it does not award the verdict. That is why the status
column is deliberately left untouched and the register says so in writing.

**Why all SPECIFICATION.md edits land here, in one plan.** `.claude/CLAUDE.md` requires any new
library, env var, queue, route or pipeline change to be recorded in `SPECIFICATION.md` "in the
same change". D-10, a locked user decision, assigns that update to this phase's closure step.
Both hold simultaneously because the unit of change is the phase: plans 17-01 through 17-06 land
on one phase branch and merge as one pull request, so `SPECIFICATION.md` is updated in the same
change as the code it describes. Splitting the edits across plans 17-01/17-03/17-04 would instead
force `SPECIFICATION.md` into three plans' `files_modified`, serialising three otherwise-parallel
plans into a chain purely for file ownership. One plan, one coherent as-built diff.

Output: an accurate `SPECIFICATION.md`, a register whose three rows carry citable evidence and an
explicit pending-auditor note, an annotated milestone audit, and a stated next action
(`/gsd-secure-phase`).
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-CONTEXT.md
@.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-01-SUMMARY.md
@.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-02-SUMMARY.md
@.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-03-SUMMARY.md
@.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-04-SUMMARY.md
@.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-05-SUMMARY.md
</context>

## Artifacts this phase produces

Plan 17-06 produces **no new code symbols**. Its artifacts are documentation sections:

| Artifact | Kind | Location |
|---|---|---|
| Phase 17 as-built entries (pool TimeZone pin, CI-built postgres image, POSTGRES_IMAGE_TAG semantics, RESTORE_DRILL_METRICS_FILE, growth-query UTC anchor) | edits at existing anchors + one new divergence subsection | `SPECIFICATION.md` |
| Evidence annotations on rows T-14-58 / T-14-73 / T-14-88 + a dated pending-auditor note | new register annotations (status column untouched) | `.planning/phases/14-deployment-database-durability/14-SECURITY.md` |
| Closure annotations for WR-06, the three medium security items, and the alloy/Loki operator item | new audit annotations | `.planning/v1.1-MILESTONE-AUDIT.md` |

<tasks>

<task type="auto">
  <name>Task 1: Bring SPECIFICATION.md into line with what this phase built</name>

  <files>SPECIFICATION.md</files>

  <read_first>
    - `.claude/CLAUDE.md` § "Project Specification (SPECIFICATION.md)" — the routing rules that
      decide which section each fact belongs in, and the three requirements on every entry
      (exact version from `package.json`; declared-but-unused packages flagged as such;
      anything absent from the code written as «не определено», never invented).
    - `SPECIFICATION.md` at these exact anchors, located by grep rather than by line number since
      earlier edits shift them:
      - `grep -n "POSTGRES_IMAGE_TAG" SPECIFICATION.md` — §3.8's sizing/reference-variable
        paragraph, which currently describes the tag as naming a LOCALLY built image with a
        `local` default. This sentence is now false.
      - `grep -n "RESTORE_DRILL_" SPECIFICATION.md` — §3.8's test-only/operator-override list,
        where `RESTORE_DRILL_METRICS_FILE` belongs beside its six siblings.
      - `grep -n "Единая фабрика Postgres-пулов" SPECIFICATION.md` — §5.17's canonical
        `createPgPool` paragraph, the right home for the TimeZone pin.
      - `grep -n "docker/postgres/Dockerfile" SPECIFICATION.md` — §1.3's Phase 14 paragraph and
        §2.6's base-image-tag table row.
      - `grep -n "images.yml" SPECIFICATION.md` — every place the workflow's job set is
        described (it currently says three images, build-only on pull_request).
      - `grep -n "AT TIME ZONE" SPECIFICATION.md` — the existing note contrasting
        `timestamptz` comparison against `::date` bucketing, which is where the growth query's
        anchor belongs.
      - `grep -n "^### 8\." SPECIFICATION.md` — the divergence subsections, whose numbering
        convention (8.4 = Phase 14, 8.5 = Phase 15) this phase continues.
      - `grep -n "Закрыто планом" SPECIFICATION.md` — §9's review-summary closure convention.
    - `17-01-SUMMARY.md` through `17-05-SUMMARY.md` — the authoritative source for every fact
      written below. Do not describe intended behavior; describe what the SUMMARYs record as
      landed and observed.
  </read_first>

  <action>
    Edit `SPECIFICATION.md` at the anchors above. Write in the document's existing language and
    register (Russian prose with inline code identifiers, plan/decision citations in
    parentheses), matching how the Phase 14 and Phase 15 entries around each anchor read. Use
    `Edit` with scoped replacements — never rewrite the file.

    **§3.8 — `POSTGRES_IMAGE_TAG`.** Replace the description of a locally built image with a
    `local` default. The new text states: as of Phase 17 (D-05/D-06) the `db`/`pgbackrest` image
    is built and pushed to GHCR by `.github/workflows/images.yml`'s `build-and-push-postgres` job
    on every push to master, tagged with the same `github.sha` as the three application images;
    `docker-compose.prod.yml` resolves `${GHCR_IMAGE_BASE}/postgres:${POSTGRES_IMAGE_TAG}` with
    no fallback; the `docker/prod.env.example` default is the same deliberately-invalid 40-zero
    placeholder `IMAGE_TAG` uses; and `local` is now a rejected tag name in
    `scripts/validate-prod-compose.mjs`'s `MUTABLE_TAG_NAMES`. Note that `POSTGRES_IMAGE_TAG` is
    normally the same SHA as `IMAGE_TAG` but is a separate variable because the `db`/`pgbackrest`
    cutover is a human-gated event, not part of `scripts/deploy.sh`'s routine flip.

    **§3.8 — test-only overrides.** Add `RESTORE_DRILL_METRICS_FILE` to the existing
    `RESTORE_DRILL_*` list, noting it defaults under `XDG_STATE_HOME` exactly as
    `RESTORE_DRILL_BASELINE_FILE` does (outside the repository working tree) and that unlike its
    siblings it is not test-only — it names a real operator-facing history file every drill
    appends to. Say so explicitly rather than filing it under a heading that would misdescribe it.

    **§5.17 — the pool factory.** Extend the `createPgPool` paragraph: the factory now passes
    `options: "-c TimeZone=UTC"` so every physical connection negotiates `TimeZone=UTC` during
    the Postgres startup handshake, before any query can run on it (Phase 17, D-01, closing
    13-REVIEW.md's WR-06 write-path hazard). State that the startup-parameter form was chosen over
    a `pool.on("connect")` session statement because the latter is a documented race
    (brianc/node-postgres#3265), and that the guarantee is proven behaviorally by
    `packages/db/src/__tests__/pg-timezone.test.ts` against a database whose own default timezone
    is `America/New_York`, with an unpinned pool as the negative control. Record the consequence
    that matters for the data model: every `now()`-derived value coerced into a naive
    `timestamp without time zone` column is now provably true UTC wall clock regardless of the
    server's or database's default timezone.

    **§8.4 — the PgBouncer deferral note.** Add the pin's revisit trigger to the existing D-09
    PgBouncer paragraph: transaction-mode pooling can restrict non-default startup parameters via
    its own `ignore_startup_parameters` setting, this has not been tested against this stack, and
    the pin must be re-verified against a real backend connection (or replaced with a per-checkout
    session statement) whenever SCALE-02 introduces a pooler. Recording this beside the deferral
    is the point — the pin's sufficiency argument depends on there being no intermediary today.

    **§1.3 and §2.6 — the image and the workflow.** Update the Phase 14 paragraph that lists
    `docker/postgres/Dockerfile` alongside `docker-compose.prod.yml` and `images.yml`, and the
    base-image-tag table row for `docker/postgres/Dockerfile`, so both state that this image is
    now CI-built and GHCR-published rather than built on the host. Wherever `images.yml` is
    described as building three images, correct the job inventory to include the two new
    postgres jobs and note that the pull_request one holds no registry credential
    (job-level `permissions: contents: read`, no login step), the same boundary the existing
    build-only job has.

    While at the §1.3 anchor: that paragraph also still carries the sentence stating no real VPS
    run had happened, with 14-09/14-10/14-11 sitting at blocking checkpoints. Those three were
    approved on 2026-08-14 and plan 17-05 has now performed a further live cutover and drill. Do
    not expand scope into a general audit of the document, but this specific sentence sits at an
    anchor this plan is already editing and directly contradicts this phase's own evidence — mark
    it superseded with the dates and plan references, rather than leaving a known-false statement
    at a line this plan touched. If any other stale statement is noticed elsewhere, record it in
    the SUMMARY as a follow-up instead of fixing it here.

    **§4 / the `AT TIME ZONE` anchor.** At the existing note contrasting direct `timestamptz`
    comparison with `::date` bucketing, record that Phase 17 gave the dashboard growth query
    (`apps/api/src/modules/analytics/dashboard.repository.ts`'s `GROWTH_BY_DAY_SQL`) an explicit
    double-hop `((created_at AT TIME ZONE 'UTC') AT TIME ZONE 'UTC')::date` anchor, that the
    double hop is required because `contacts.created_at` is naive `timestamp` while
    `sends.*_at` are `timestamptz` (the two column types need opposite-direction handling), and
    that the adjacent baseline count query needs no anchor because a `<` comparison against a
    date literal performs no timezone conversion. Note that no column type changed (D-04) and
    that `apps/api/src/modules/analytics/__tests__/dashboard-timezone.test.ts` locks the
    single-hop form out.

    **§8.6 — new subsection.** Add `### 8.6 Фаза 17 (tech debt: WR-06 + medium security follow-ups)`
    after §8.5, following §8.4/§8.5's shape: what changed relative to the recommended stack and
    relative to earlier phases' recorded decisions, and which earlier statements it supersedes.
    List the four as-built changes (pool pin, read-site anchor, CI-built postgres image, drill
    self-recording) with their plan numbers, and state that the three register rows' closure
    awaits the D-12 auditor pass rather than being asserted here.

    **§9 — review summary.** Add entries in the existing `Закрыто планом ...` style for WR-06 and
    for the T-14-58/T-14-73/T-14-88 group, each naming the plan that closed it and the evidence
    location, and each stating that the register status flip itself is pending the auditor pass.

    Add no new npm package anywhere in §2 — this phase introduced none. If any statement cannot
    be sourced from a committed SUMMARY, write «не определено» rather than inferring it.
  </action>

  <verify>
    <automated>npm run check:spec-env-coverage</automated>
  </verify>

  <acceptance_criteria>
    - `SPECIFICATION.md` contains `build-and-push-postgres`, `RESTORE_DRILL_METRICS_FILE`,
      `-c TimeZone=UTC`, `pg-timezone.test.ts`, `GROWTH_BY_DAY_SQL` and
      `dashboard-timezone.test.ts` (`grep -c` returns at least 1 for each).
    - The false locally-built claim is gone: `grep -c 'ЛОКАЛЬНО собранного' SPECIFICATION.md`
      returns 0.
      <!-- planner-discipline-allow: ЛОКАЛЬНО собранного -->
    - A Phase 17 divergence subsection exists:
      `grep -c '^### 8.6 ' SPECIFICATION.md` returns 1.
    - The node-postgres race citation is present: `grep -c '3265' SPECIFICATION.md` returns at
      least 1.
    - No new package was claimed: the §2 dependency tables are unchanged —
      `git diff --stat SPECIFICATION.md` shows the edit hunks, and none of them falls inside a §2
      dependency table. Confirm by reading the diff and state it in the SUMMARY.
    - `npm run check:spec-env-coverage` exits 0 (every variable documented in
      `docker/prod.env.example` still appears in `SPECIFICATION.md`).
    - Every plan/file reference newly written into `SPECIFICATION.md` resolves: for each
      referenced repository path, `test -e <path>` succeeds. List the checked paths in the SUMMARY.
    - No figure or claim appears that is absent from a committed 17-0N-SUMMARY.md.
  </acceptance_criteria>

  <done>
    A security reviewer reading `SPECIFICATION.md` sees the system as it now is: UTC-pinned pools,
    a CI-built registry-pulled database image with a fail-loud tag, a self-recording restore
    drill, and a UTC-anchored growth query — with the PgBouncer caveat that would invalidate the
    pin recorded beside the deferral it depends on.
  </done>
</task>

<task type="auto">
  <name>Task 2: Attach citable closure evidence to the register and the milestone audit — without flipping any status</name>

  <files>
    .planning/phases/14-deployment-database-durability/14-SECURITY.md,
    .planning/v1.1-MILESTONE-AUDIT.md
  </files>

  <read_first>
    - `.planning/phases/14-deployment-database-durability/14-SECURITY.md` — the file being
      modified. Read rows T-14-49, T-14-58, T-14-73 and T-14-88 verbatim (T-14-49 matters because
      its 2026-08-14 evidence was narrowed specifically to hand off the residual surface to
      T-14-88, and that hand-off note must stay coherent). Read the audit-history table row dated
      2026-08-14 and the `**Approval:**` paragraph beneath it, both of which name these three
      rows as the open medium follow-ups — both need a Phase 17 note. Note the exact status
      string used in the status column so it can be left byte-identical.
    - `.planning/v1.1-MILESTONE-AUDIT.md` — the file being modified. Read the three carried-item
      entries this phase addresses: the WR-06 warning entry (~line 21 and its restatement ~line
      132), the three-medium-security-items entry (~line 24 and ~line 135), and the alloy/Loki
      next-prod-deploy entry (~line 27 and ~line 138). Each appears twice — once in the
      structured carried-items list and once in the prose summary — and both occurrences need
      annotating or the document contradicts itself.
    - `17-05-SUMMARY.md` — the only source of live evidence. Note the exact section headings
      holding the cutover output, the alloy/Loki output, the drill output and the metrics JSON
      line; the citations written below must name those headings so an auditor can find them.
    - `17-01-SUMMARY.md` through `17-04-SUMMARY.md` — for the code-side evidence citations (test
      file paths, gate names, fixture names, commands).
    - `.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-CONTEXT.md`
      D-12 — the exact constraint on what this task may and may not do.
  </read_first>

  <action>
    **The hard constraint first: do not change any status value.** D-12 reserves the flip for a
    `gsd-security-auditor` re-run, because that is how the register reached `verified` originally.
    Leave the status column of T-14-58, T-14-73 and T-14-88 byte-identical. Add evidence, add a
    dated note, change no verdict.

    **14-SECURITY.md — evidence per row.** Extend each of the three rows' mitigation cell with a
    dated Phase 17 evidence clause. Each clause must be checkable by someone who has only this
    file and the repository — name files, commands, gates and SUMMARY sections, not conclusions:

    - T-14-58 (EoP, host-built image bypassing review): `build:` sections removed from
      `docker/docker-compose.prod.yml`'s `db`/`pgbackrest`; image reference now
      `${GHCR_IMAGE_BASE}/postgres:${POSTGRES_IMAGE_TAG}` with no fallback; built and pushed by
      `.github/workflows/images.yml`'s `build-and-push-postgres` job on the same `github.sha` as
      the application images; live cutover observed (cite 17-05-SUMMARY's cutover section,
      including that `docker inspect` reported the GHCR reference and a changed image id).
    - T-14-88 (Tampering, mutable `POSTGRES_IMAGE_TAG` outside the immutability gate): `db` and
      `pgbackrest` added to `scripts/validate-prod-compose.mjs`'s `FIRST_PARTY_IMAGE_SERVICES`;
      `local` added to `MUTABLE_TAG_NAMES`; fixture
      `scripts/__fixtures__/prod-compose/db-mutable-image-tag.yml` plus its case in
      `scripts/__tests__/validate-prod-compose.test.mjs` prove the gate rejects a mutable tag on
      `db`; `docker/prod.env.example`'s default is now the deliberately-invalid 40-zero
      placeholder; `scripts/restore-drill.sh` fail-loud guards prevent a drill from silently using
      a stale local image. Cite the plan and the test command.
    - T-14-73 (DoS, drill exhausting host disk): `scripts/restore-drill.sh` now self-records
      restore-to-ready duration and scratch-PGDATA disk high-water on every run, including the
      readiness-timeout path, to stdout and to an append-only history file; real figures captured
      from a real PITR drill against the CI-built image on the production host (cite the
      17-05-SUMMARY drill section and the metrics JSON line, and the
      `docs/runbooks/restore-drill.md` "Recorded drill runs" table where the figures and the
      derived free-disk-headroom requirement live). The derived headroom requirement is the part
      that makes this a mitigation rather than a measurement — cite it explicitly.

    Also extend T-14-49's narrowed-evidence note so it points at T-14-88's Phase 17 evidence
    rather than leaving the residual surface described as merely tracked. Do not change T-14-49's
    status either — it is already `closed`.

    **14-SECURITY.md — the pending-auditor note.** Add a dated entry to the audit-history table
    (or immediately after it, matching however that table's rows are structured) recording:
    Phase 17 supplied closure evidence for T-14-58/T-14-73/T-14-88; the three rows' status is
    deliberately UNCHANGED pending a `gsd-security-auditor` re-run per Phase 17 D-12; and the
    next action is `/gsd-secure-phase` against this register. State plainly that an executor is
    not permitted to award these verdicts. Also extend the `**Approval:**` paragraph's
    "Open medium follow-ups" sentence with a pointer to this new note, so a reader arriving at the
    old sentence is not left with stale information.

    **v1.1-MILESTONE-AUDIT.md — annotations.** Annotate all six occurrences (three items, each
    appearing once in the carried-items list and once in the prose summary):

    - WR-06: annotate as addressed by Phase 17 plans 17-01 (pool-level `TimeZone=UTC` pin, proven
      by a non-UTC behavioral test) and 17-02 (double-hop UTC anchor on the growth query plus the
      D-03 sweep audit), citing the two test files by path. WR-06 was a code warning, not a
      register row, so it may be annotated as closed here — say by which plans and on what
      evidence.
    - The three medium security items: annotate as evidenced by Phase 17 with the register status
      flip pending the auditor pass, pointing at the new 14-SECURITY.md note. Do NOT describe them
      as closed.
    - The alloy/Loki operator item: annotate as confirmed during Phase 17's live cutover session
      (D-11), citing 17-05-SUMMARY's alloy/Loki section including the two `RestartCount`
      observations and the observed Loki `service` label set.

    Keep each annotation adjacent to the original text and clearly marked as a later addition
    with its date — do not rewrite or delete the original findings. The audit's value is that it
    records what was true at audit time; an annotated trail preserves that, an edited one destroys
    it.
  </action>

  <verify>
    <automated>bash -c 'BEFORE=$(git show HEAD:.planning/phases/14-deployment-database-durability/14-SECURITY.md | grep -c "open (below block threshold)"); AFTER=$(grep -c "open (below block threshold)" .planning/phases/14-deployment-database-durability/14-SECURITY.md); test "$BEFORE" = "$AFTER" && echo "status column unchanged: $AFTER rows still open (D-12 honored)"'</automated>
  </verify>

  <acceptance_criteria>
    - The status column is untouched: the count of the open-status string in
      `14-SECURITY.md` is identical to `git show HEAD:`'s count (the verify command above
      asserts exactly this and must exit 0).
    - All three rows carry Phase 17 evidence: `grep -n 'T-14-58\|T-14-73\|T-14-88'
      .planning/phases/14-deployment-database-durability/14-SECURITY.md` shows each row's
      mitigation cell mentioning `17-05-SUMMARY` or a named Phase 17 artifact. Confirm per row and
      list the three citations in this plan's SUMMARY.
    - A dated pending-auditor note exists naming the required next action:
      `grep -c 'gsd-secure-phase' .planning/phases/14-deployment-database-durability/14-SECURITY.md`
      returns at least 1.
    - T-14-49's narrowed-evidence note points at T-14-88's Phase 17 evidence, and T-14-49's status
      is still `closed`.
    - `v1.1-MILESTONE-AUDIT.md` annotates all six occurrences:
      `grep -c 'Phase 17' .planning/v1.1-MILESTONE-AUDIT.md` returns at least 6. Verify by reading
      that each of the three items is annotated in BOTH the carried-items list and the prose
      summary.
    - The three medium security items are NOT described as closed in the audit:
      the annotation text for that item contains a pending/awaiting-auditor phrase and does not
      assert closure. Confirm by reading and quote the annotation in the SUMMARY.
    - Every SUMMARY section cited by an annotation actually exists and contains pasted output:
      for each citation, `grep -c '<cited heading>' <cited SUMMARY file>` returns at least 1, and
      the section beneath it is non-empty. List the checked citations in the SUMMARY.
    - No original finding text was deleted:
      `git diff .planning/v1.1-MILESTONE-AUDIT.md` shows only added lines and in-place annotations
      appended to existing entries — no removed finding lines. Confirm by reading the diff.
  </acceptance_criteria>

  <done>
    An auditor running `/gsd-secure-phase` against the Phase 14 register can verify each of the
    three mitigations from the register alone — every claim names a file, a command or a pasted
    evidence section — and the verdict itself is still theirs to award, exactly as D-12 requires.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| as-built reality -> `SPECIFICATION.md` | A security review trusts this document as a description of the system; a stale sentence here is worse than a missing one, because it is read as current. |
| executor -> security register verdict | D-12 places the verdict outside the executor's authority; the boundary is the status column. |
| evidence citation -> auditor verification | An auditor follows citations. A citation pointing at a section with no pasted output silently converts "verified" into "asserted". |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-32 | Repudiation | an executor flipping register rows to closed without an auditor pass, reproducing an unverified `verified` status | high | mitigate | This plan changes no status value; the verify command asserts the open-status count is byte-identical to `git show HEAD:`'s, and a dated register note plus a plan-level prohibition record that `/gsd-secure-phase` is the required next action (D-12). |
| T-17-33 | Tampering (documentation integrity) | `SPECIFICATION.md` continuing to describe a locally built image with a `local` default after the model changed | high | mitigate | Task 1 replaces that sentence at the located anchor, asserted by a zero-count grep for the false claim plus positive greps for the new facts; `check:spec-env-coverage` guards the env-var half. |
| T-17-34 | Repudiation | evidence citations that do not resolve — a named SUMMARY section that holds no pasted output | medium | mitigate | Every citation names a SUMMARY file and heading, and the acceptance criteria require confirming each heading exists with non-empty content beneath it; plan 17-05's own output contract requires the pasted output that makes those citations real. |
| T-17-35 | Tampering | rewriting the milestone audit's original findings instead of annotating them, destroying the record of what was true at audit time | medium | mitigate | Annotations are appended and dated adjacent to the original text; the diff is required to show only additions and in-place annotations, with no removed finding lines. |
| T-17-36 | Information Disclosure | pasting production hostnames, DSNs, bucket names or credentials into planning documents while citing evidence | medium | mitigate | Citations reference SUMMARY sections by heading rather than reproducing their content; the register and audit annotations name files, commands and gates, never credential material. Review each added line for secret material before committing. |
| T-17-37 | Tampering | `SPECIFICATION.md` gaining an invented fact not backed by a committed SUMMARY | medium | mitigate | CLAUDE.md's own rule applies: anything absent from the code is written as «не определено», never inferred; the acceptance criteria forbid any figure or claim absent from a committed 17-0N-SUMMARY.md and require every newly written repository path to resolve on disk. |
| T-17-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package and touches only Markdown (RESEARCH.md § Package Legitimacy Audit: not applicable to this phase). |
</threat_model>

<verification>
- `npm run check:spec-env-coverage` exits 0.
- The open-status count in `14-SECURITY.md` is unchanged from `git show HEAD:` (Task 2's verify
  command).
- `grep -c 'gsd-secure-phase' .planning/phases/14-deployment-database-durability/14-SECURITY.md`
  returns at least 1.
- `grep -c '^### 8.6 ' SPECIFICATION.md` returns 1.
- `grep -c 'ЛОКАЛЬНО собранного' SPECIFICATION.md` returns 0.
- Every repository path newly written into `SPECIFICATION.md` and into the two planning documents
  resolves with `test -e`.
- `npm test` exits 0 (this plan touches only Markdown; confirm nothing incidental broke).
</verification>

<success_criteria>
- `SPECIFICATION.md` describes the system as built after this phase, with no surviving statement
  that the postgres image is built on the host.
- The three register rows carry evidence an auditor can check from the register alone.
- No status value was changed by an executor (D-12 intact), and the register itself says why.
- The milestone audit's three carried items are annotated with what addressed them, and the three
  medium security items are explicitly NOT described as closed.
- The next action — `/gsd-secure-phase` against the Phase 14 register — is written where the
  auditor will look.
</success_criteria>

<output>
Create `.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-06-SUMMARY.md` when done.
Record in it: the list of `SPECIFICATION.md` anchors edited (with the grep used to locate each),
the three per-row evidence citations added to `14-SECURITY.md`, the quoted pending-auditor
annotation text, the confirmed-resolving citation list, and an explicit statement that
`/gsd-secure-phase` against the Phase 14 register is the required next action per D-12.
</output>