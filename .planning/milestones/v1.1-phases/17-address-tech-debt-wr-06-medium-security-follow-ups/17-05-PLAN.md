---
phase: 17-address-tech-debt-wr-06-medium-security-follow-ups
plan: 05
type: execute
wave: 3
depends_on: ["17-01", "17-02", "17-03", "17-04"]
files_modified:
  - docs/runbooks/restore-drill.md
  - docs/runbooks/backups.md
autonomous: false
# No requirement IDs are mapped to Phase 17 (ROADMAP: "Requirements: TBD").
# Scope authority is CONTEXT.md's locked decisions D-01..D-12; this plan produces
# the LIVE evidence T-14-58, T-14-73 and T-14-88 close on (D-07, D-08, D-11).
requirements: []
user_setup:
  - service: github-container-registry
    why: "Production must pull the CI-built postgres image for the merged master SHA before the cutover can run. Only a human can merge to master and only a human has VPS shell access."
    env_vars:
      - name: POSTGRES_IMAGE_TAG
        source: "The full 40-character git SHA of the merged master commit whose images.yml run published <GHCR_IMAGE_BASE>/postgres:<sha> — set in MEGA_CRM_ENV_FILE on the VPS and exported in the cutover shell"
      - name: GHCR_IMAGE_BASE
        source: "Already present on the VPS for the application images (docker/prod.env.example) — must also be exported in the cutover shell so compose interpolation resolves it"
    dashboard_config:
      - task: "Confirm the postgres image exists for the target SHA before touching production"
        location: "GitHub → Actions → Images workflow run for the merged commit, job 'build-and-push (postgres)'; or `docker manifest inspect <GHCR_IMAGE_BASE>/postgres:<sha>` from the VPS"

must_haves:
  truths:
    - "Production's db and pgbackrest containers run the CI-built, GHCR-pulled, SHA-tagged postgres image — observed live, not inferred from code landing (D-07)."
    - "After the cutover Postgres is healthy, every tenant-scoped table still has RLS both enabled and forced, and WAL archiving has resumed: archived_count strictly increases, failed_count is unchanged from its baseline reading, and last_failed_wal/last_failed_time do not advance."
    - "The alloy container stays running (not restarting) and log lines keep arriving in Loki across all expected service labels, during the same live session (D-11 — clears the last Phase 15 operator-side audit item with no extra deploy event)."
    - "A real PITR restore drill ran against the real off-host repository using the CI-built image, and passed verification (D-08) — proving the new image restores correctly, not merely that it starts."
    - "The drill's restore duration and disk high-water figures exist as real recorded numbers from a real run, replacing the runbook placeholder (T-14-73)."
    - "Production data was never modified: the only production-touching operations were a read-only baseline query and a container recreate against the same persistent named volume."
  artifacts:
    - "docs/runbooks/restore-drill.md carries the real duration and disk high-water figures from this drill, dated, with the target used."
    - "docs/runbooks/backups.md's restore-drill cross-reference carries the same figures or points at them."
    - "17-05-SUMMARY.md records the pasted command output for every verification step (the evidence 14-SECURITY.md's rows will cite in plan 17-06)."
  key_links:
    - "images.yml build-and-push-postgres -> GHCR <base>/postgres:<sha> -> docker-compose.prod.yml db/pgbackrest -> the running production containers. This chain must be observed end to end, once, live."
    - "The same live session -> the alloy/Loki confirmation (D-11), so the Phase 15 audit item closes without scheduling a second production event."
    - "scripts/restore-drill.sh's self-recorded metrics (plan 17-04) -> the runbook figures -> T-14-73's closure evidence."
  prohibitions:
    - statement: "The executor must not perform the cutover, the drill, or any production command itself."
      status: resolved
      verification: "Both live tasks are `checkpoint:human-verify` with `gate=\"blocking\"`; the executor stages commands and verification queries and then stops, exactly as plans 14-09/14-10/14-11 and 16-07 did."
    - statement: "scripts/deploy.sh must not be used to perform this cutover."
      status: resolved
      verification: "deploy.sh's pull/up sets are api/worker/web only by design (RESEARCH.md Pitfall 4); the cutover is a separate, manually-invoked, human-gated sequence staged in Task 1."
---

<objective>
Produce the **live evidence** T-14-58, T-14-88 and T-14-73 close on (D-07, D-08), and fold in the
Phase 15 alloy/Loki confirmation (D-11) so the last operator-side milestone-audit item clears
during the same session.

Purpose: plans 17-03 and 17-04 changed code. D-07 is explicit that the threat rows close on live
evidence, not on code landing — a compose file that references a registry image proves nothing
about what production is actually running. D-08 makes the drill part of that same evidence: it
simultaneously fills T-14-73's duration/disk placeholder AND proves the CI-built image restores
correctly via PITR, which is the one property a container that merely starts does not
demonstrate. Running both in one operator session, together with D-11's alloy check, means one
production event yields three closures.

This plan is `autonomous: false`. The executor's job is to stage exact commands and exact
verification queries, then stop at each blocking checkpoint. It performs no production action
itself. This is the same pattern plans 14-09, 14-10, 14-11 and 16-07 used successfully.

Output: two approved blocking checkpoints with pasted evidence, and the real drill figures
written into the runbooks.

**Sequencing (locked by D-08, remainder planner's choice):** cutover first, then the drill. The
drill must run against the image production is now running, otherwise it validates the wrong
artifact. The alloy/Loki check rides inside the cutover checkpoint because that is the
"next production deploy" moment the milestone audit was waiting for.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-CONTEXT.md
@.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-RESEARCH.md
@.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-03-SUMMARY.md
@.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-04-SUMMARY.md
@.planning/v1.1-MILESTONE-AUDIT.md
</context>

## Artifacts this phase produces

Plan 17-05 produces **no new code symbols**. Its artifacts are evidence and figures:

| Artifact | Kind | Location |
|---|---|---|
| Cutover evidence block (pasted command output) | new SUMMARY section | `17-05-SUMMARY.md` |
| Drill evidence block incl. real duration + disk high-water figures | new SUMMARY section | `17-05-SUMMARY.md` |
| Recorded drill figures (date, PITR target, duration, disk high-water) | new runbook subsection | `docs/runbooks/restore-drill.md` |
| Alloy/Loki live confirmation record (D-11) | new SUMMARY section | `17-05-SUMMARY.md` |

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: BLOCKING — cut production's db/pgbackrest over to the CI-built GHCR image, and confirm alloy/Loki in the same session</name>

  <precondition>
    The phase branch is merged to `master`, the `Images` workflow run for that merge has a green
    `build-and-push (postgres)` job, and `<GHCR_IMAGE_BASE>/postgres:<merged-sha>` is resolvable
    from the VPS (`docker manifest inspect` succeeds). Halt and report if the image does not
    exist — every step below is unreachable without it, and there is no fallback now that the
    `build:` sections are gone.
  </precondition>

  <read_first>
    - `docker/docker-compose.prod.yml` `db` and `pgbackrest` service blocks as merged — read the
      actual committed image reference rather than trusting this plan's prose, and read the
      volume list so the staged commands name the real persistent volume.
    - `.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-03-SUMMARY.md`
      — the exact image reference and the confirmed action SHAs.
    - `docs/runbooks/backups.md` § "Confirming WAL archiving is keeping up" (~lines 137-147) —
      the canonical `pg_stat_archiver` check, including the `pg_switch_wal()` trick for forcing a
      fresh data point. Use that runbook's command, not an invented one.
    - `packages/db/scripts/verify-restored-database.ts` (~lines 225-270) — the canonical RLS
      posture query (`relrowsecurity` AND `relforcerowsecurity` per tenant-scoped table) and its
      `RLS_ACCEPT_EXEMPT` set. The staged production check must use this same query shape and the
      same exemption list, so "posture intact" means the same thing it means everywhere else in
      this repo.
    - `docs/runbooks/log-shipping-and-backstop-alerts.md` § "How to confirm logs are arriving in
      Loki (OPS-10 verification)" (~lines 56-78) — the exact LogQL query and the eight expected
      `service` label values. This is D-11's check; do not invent a different one.
    - `docs/runbooks/deploy-and-rollback.md` — for the operator's existing mental model of what
      `deploy.sh` does and does not do, so the staged sequence is visibly distinct from a routine
      deploy.
    - `scripts/deploy.sh` (~lines 340-400) — read how it exports `GHCR_IMAGE_BASE`,
      `MEGA_CRM_ENV_FILE` and `IMAGE_TAG` before calling `compose`, because compose resolves
      `${VAR}` interpolation from the invoking shell. The cutover shell needs the same treatment
      for `POSTGRES_IMAGE_TAG`.
  </read_first>

  <what-built>
    Plans 17-01 through 17-04 landed, in code:
    - `packages/db/src/pool.ts` pins `TimeZone=UTC` on every pooled connection at handshake time,
      proven against an America/New_York database (17-01).
    - The dashboard growth query carries an explicit double-hop UTC anchor, with the wrong
      single-hop form locked out by an executable assertion (17-02).
    - `.github/workflows/images.yml` builds and pushes `<GHCR_IMAGE_BASE>/postgres:<github.sha>`
      on every push to master; `docker/docker-compose.prod.yml`'s `db`/`pgbackrest` have no
      `build:` section and reference that registry image only; `db`/`pgbackrest` are inside
      `scripts/validate-prod-compose.mjs`'s immutable-tag gate, with a fixture proving the gate
      rejects a mutable tag on `db`; `local` is now a rejected tag name (17-03).
    - `scripts/restore-drill.sh` records its own restore duration and disk high-water on every
      run (success and readiness-timeout alike) and launches the same GHCR image production runs,
      refusing to start on a missing tag (17-04).

    What is NOT yet true, and is what this checkpoint establishes: production's running `db` and
    `pgbackrest` containers are still the old host-built image. Nothing above changes that until
    an operator pulls and recreates them.
  </what-built>

  <how-to-verify>
    Run these on the production VPS, from the repository root, with `MEGA_CRM_ENV_FILE` exported
    as usual. Substitute `<SHA>` with the merged master commit SHA everywhere. Paste every
    command's output back into this session.

    **Choose a low-traffic window.** Recreating `db` is a brief database interruption:
    `api`/`worker` pools will see dropped connections and reconnect (their unconditional pool
    error handlers exist for exactly this), but in-flight requests can fail. This is not a
    zero-downtime operation and must not be treated as one.

    **0. Pre-flight — confirm the artifact exists and matches, BEFORE touching production.**
    ```
    docker manifest inspect "$GHCR_IMAGE_BASE/postgres:<SHA>" > /dev/null && echo "image present"
    docker pull "$GHCR_IMAGE_BASE/postgres:<SHA>"
    docker run --rm --entrypoint postgres "$GHCR_IMAGE_BASE/postgres:<SHA>" --version
    docker run --rm --entrypoint pgbackrest "$GHCR_IMAGE_BASE/postgres:<SHA>" version
    ```
    The Postgres major version MUST match what production is currently running (17) — a
    different major would refuse to start against the existing PGDATA. Report and STOP if it
    differs. Record the pgBackRest version too; it should match the version `backups.md`
    documents (2.59.0) unless a base-image change was intended.

    **1. Note the current state, so a rollback is possible.**
    ```
    docker compose -f docker/docker-compose.prod.yml ps db pgbackrest
    docker compose -f docker/docker-compose.prod.yml exec -T db psql -U postgres -c "SELECT version();"
    docker images --format '{{.Repository}}:{{.Tag}} {{.ID}}' | grep -i postgres
    ```
    Record the image id currently backing `db`. That id is the rollback artifact.

    **2. Set the tag in BOTH places, then pull.**
    Add or update `POSTGRES_IMAGE_TAG=<SHA>` in `MEGA_CRM_ENV_FILE` (so `scripts/restore-drill.sh`,
    which sources that file, picks it up in Task 2), and export it plus `GHCR_IMAGE_BASE` in this
    shell (so compose's `${VAR}` interpolation resolves — compose reads the invoking shell, which
    is why `scripts/deploy.sh` exports these explicitly rather than relying on the env file).
    ```
    export GHCR_IMAGE_BASE POSTGRES_IMAGE_TAG=<SHA>
    docker compose -f docker/docker-compose.prod.yml config | grep -A1 -E '^  (db|pgbackrest):'
    docker compose -f docker/docker-compose.prod.yml pull db pgbackrest
    ```
    The `config` output must show the resolved image as `<GHCR_IMAGE_BASE>/postgres:<SHA>` for
    both services, with no `build:` key. If it shows an empty tag, the export did not take —
    stop and fix it rather than proceeding.

    **3. Recreate the two containers.**
    ```
    docker compose -f docker/docker-compose.prod.yml up -d db pgbackrest
    docker compose -f docker/docker-compose.prod.yml ps db pgbackrest
    ```
    Wait for `db` to report healthy. `pgbackrest` depends on `db` being healthy and will start
    after it.

    **4. Verify Postgres is healthy and running the new image.**
    ```
    docker inspect --format '{{.Config.Image}} {{.Image}} {{.State.Health.Status}}' \
      "$(docker compose -f docker/docker-compose.prod.yml ps -q db)"
    docker compose -f docker/docker-compose.prod.yml exec -T db pg_isready -U postgres
    docker compose -f docker/docker-compose.prod.yml exec -T db psql -U postgres -c "SELECT version();"
    ```
    `.Config.Image` must be the GHCR reference; the image id must differ from the one recorded in
    step 1.

    **5. Verify the RLS posture is intact (enabled AND forced).**
    Run the same query shape `packages/db/scripts/verify-restored-database.ts` uses — read that
    file for the exact SQL and the exempt-table set, then execute it against production's
    database read-only via `compose exec -T db psql`. Expected result: zero non-exempt
    tenant-scoped tables with `relrowsecurity` false or `relforcerowsecurity` false. Paste the
    full row set, not a summary — Phase 10's Pitfall 11 is that a fail-open RLS posture looks
    like a successful empty result, so the evidence must show the actual flags.

    **6. Verify WAL archiving resumed.**
    ```
    docker compose -f docker/docker-compose.prod.yml exec -T db psql -U postgres -c "SELECT * FROM pg_stat_archiver;"
    docker compose -f docker/docker-compose.prod.yml exec -T db psql -U postgres -c "SELECT pg_switch_wal();"
    # wait ~30s, then re-run the pg_stat_archiver query
    docker compose -f docker/docker-compose.prod.yml exec -T pgbackrest pgbackrest --stanza=mega_crm check
    ```
    `archived_count` must advance across the two `pg_stat_archiver` reads, `failed_count` must
    remain equal to its baseline reading, `last_failed_wal`/`last_failed_time` must not advance,
    `last_archived_wal`/`last_archived_time` must move forward, and `check` must pass.
    Paste both reads so the advance is visible.

    **7. Verify the application recovered.**
    ```
    docker compose -f docker/docker-compose.prod.yml ps
    curl -fsS https://<SITE_ADDRESS>/readyz && echo
    ```
    Every service `Up`; `api` reports ready. Then load the dashboard for a real workspace in a
    browser and confirm the growth chart renders (this also gives a first live look at plan
    17-02's changed query).

    **8. D-11 — alloy and Loki, in this same session.**
    ```
    docker compose -f docker/docker-compose.prod.yml ps alloy
    docker inspect --format '{{.RestartCount}} {{.State.Status}} {{.State.StartedAt}}' \
      "$(docker compose -f docker/docker-compose.prod.yml ps -q alloy)"
    docker compose -f docker/docker-compose.prod.yml logs --tail 50 alloy
    ```
    `alloy` must be `running`, NOT restarting — a stable `RestartCount` observed twice a few
    minutes apart is the check, since a crash-looping container also reports `running` in the
    instant between restarts. Record the count at two timestamps.

    Then in Grafana Cloud → Explore, against the Loki data source alloy pushes to, run the
    runbook's own query for the last 15 minutes (see
    `docs/runbooks/log-shipping-and-backstop-alerts.md`, the `{service=~".+"}` query) and confirm
    lines are arriving across the expected `service` label values — including fresh lines from
    `db` and `pgbackrest`, which were just recreated and are therefore the most informative
    labels here. Paste the label list you observed and the time range.

    **If anything in steps 4-7 fails — rollback.** The previous image is still on the host
    (step 1 recorded its id). Re-tag it into the reference the compose file now expects so the
    reference resolves locally, then recreate:
    ```
    docker tag <recorded-image-id> "$GHCR_IMAGE_BASE/postgres:<SHA>"
    docker compose -f docker/docker-compose.prod.yml up -d --force-recreate db pgbackrest
    ```
    This deliberately breaks the immutability property as an emergency measure — report it
    immediately, do NOT leave it in place, and do not proceed to Task 2. The rollback fact and
    the reason must be recorded in the SUMMARY so the register rows stay open.
  </how-to-verify>

  <acceptance_criteria>
    - Behavior: `docker inspect --format '{{.Config.Image}}'` on the running `db` container
      returns `<GHCR_IMAGE_BASE>/postgres:<SHA>`, and the same for `pgbackrest`.
    - Behavior: the running `db` container's image id differs from the id recorded in step 1.
    - Behavior: `docker compose config` shows no `build:` key under either service, and both
      resolved images carry the 40-character SHA tag (not an empty tag, not `local`).
    - Behavior: `pg_isready` returns ready and `SELECT version()` reports PostgreSQL 17.
    - Behavior: the RLS query returns zero non-exempt tenant-scoped tables with
      `relrowsecurity` or `relforcerowsecurity` false, and the pasted output shows the actual
      per-table flags rather than a count.
    - Behavior: `pg_stat_archiver.archived_count` is strictly greater in the second read than the
      first, `failed_count` is unchanged from the first read, `last_failed_wal` and
      `last_failed_time` do not advance, and `pgbackrest --stanza=mega_crm check` exits 0.
    - Behavior: `curl -fsS https://<SITE_ADDRESS>/readyz` succeeds and every compose service
      reports `Up`.
    - Behavior (D-11): `alloy`'s `RestartCount` is identical at two observations several minutes
      apart, its state is `running`, and the Loki query returns lines from the expected `service`
      labels within the last 15 minutes — including fresh `db` and `pgbackrest` lines.
    - Every command's output above is pasted into `17-05-SUMMARY.md`. A verdict without pasted
      output is not evidence and cannot be cited by plan 17-06's register annotations.
    - Production data unchanged: no schema or row-level command was run; the same persistent
      named volume backs the recreated container (confirm with
      `docker inspect --format '{{range .Mounts}}{{.Name}} {{end}}'` on the new `db` container
      and compare against step 1's).
  </acceptance_criteria>

  <resume-signal>
    Type "approved" with the pasted evidence, or describe what failed (including whether the
    rollback was used). If the rollback was used, this checkpoint is NOT approved and Task 2 must
    not run.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING — run a real PITR restore drill against the CI-built image and capture the recorded figures</name>

  <precondition>
    Task 1 is approved (production `db`/`pgbackrest` are running the GHCR image and WAL archiving
    is confirmed resuming), `POSTGRES_IMAGE_TAG` and `GHCR_IMAGE_BASE` are present in
    `MEGA_CRM_ENV_FILE` (the drill sources that file, it does not read the invoking shell), and
    the host has free disk headroom for a full second copy of PGDATA (`df -h`, per the drill
    runbook's own prerequisite). Halt if any is unmet.
  </precondition>

  <read_first>
    - `docs/runbooks/restore-drill.md` in full as merged, including plan 17-04's updated
      prerequisites, step 8 and record-keeping guidance. The staged sequence must match the
      runbook, not diverge from it.
    - `scripts/restore-drill.sh` as merged — the argument contract, the production-name safety
      refusals, and the new metrics behavior. Run `bash scripts/restore-drill.sh --dry-run <target>`
      first and read the printed sequence: that is the authoritative command list, generated from
      the actual script rather than transcribed.
    - `.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-04-SUMMARY.md`
      — the metrics record shape and the default metrics-file location.
    - `.planning/phases/14-deployment-database-durability/14-SECURITY.md` rows T-14-73 (and the
      2026-08-14 approval note) — the exact placeholder this drill replaces.
    - `docs/runbooks/backups.md` § retention — the drill target must fall inside the retention
      window (2 full backups).
  </read_first>

  <what-built>
    `scripts/restore-drill.sh` now (plan 17-04) records its own restore-to-ready wall-clock
    duration and scratch-PGDATA disk high-water mark, prints both inline, and appends one JSON
    record per run to `RESTORE_DRILL_METRICS_FILE` — on the readiness-timeout path as well as on
    success. It launches `<GHCR_IMAGE_BASE>/postgres:<POSTGRES_IMAGE_TAG>`, the same image
    production now runs after Task 1, and refuses to start if either variable is unset.

    All of that is proven against PATH-injected `docker` stubs. What no stub can prove, and what
    this checkpoint establishes: that the CI-built image actually performs a PITR restore against
    the real off-host repository, and what the real figures are on the real host.
  </what-built>

  <how-to-verify>
    On the production VPS, from the repository root, with `MEGA_CRM_ENV_FILE` exported.

    **1. Pick a valid PITR target from live state — do not use a pre-written timestamp.**
    ```
    df -h
    docker compose -f docker/docker-compose.prod.yml exec -T pgbackrest pgbackrest --stanza=mega_crm info
    ```
    Choose a UTC timestamp inside the retention window the `info` output reports, following the
    runbook's target-selection guidance. The correct target is only knowable from this output at
    this moment, which is why this plan does not name one.

    **2. Dry run first — read the sequence the script will execute.**
    ```
    bash scripts/restore-drill.sh --dry-run "<TARGET>"
    ```
    Confirm the printed `docker run` line names the GHCR image reference (not a host-built one),
    and that the sequence includes the in-container disk-sampling command and the metrics append.

    **3. Run the real drill.**
    ```
    bash scripts/restore-drill.sh "<TARGET>"
    ```
    Capture the FULL stdout/stderr. Expected: scratch volume created, read-only baseline captured
    from production, scratch container started from the GHCR image, readiness reached, the
    inline duration + disk high-water line printed, `db:verify-restored` passing, and both scratch
    resources destroyed.

    **4. Read back the recorded metrics.**
    ```
    tail -n 3 "${RESTORE_DRILL_METRICS_FILE:-${XDG_STATE_HOME:-$HOME/.local/state}/mega-crm/restore-drill-history.ndjson}"
    ```
    Paste the JSON line for this run. Its `durationSeconds` and `diskHighWaterKb` are the figures
    T-14-73 has been waiting for since 2026-08-14.

    **5. Confirm production was untouched and nothing was left behind.**
    ```
    docker ps -a | grep -i restore-drill-scratch || echo "no scratch container remains"
    docker volume ls | grep -i restore-drill-scratch || echo "no scratch volume remains"
    docker compose -f docker/docker-compose.prod.yml ps
    curl -fsS https://<SITE_ADDRESS>/readyz && echo
    ```

    **If the drill fails:** the script deliberately leaves the scratch container and volume in
    place for inspection and prints the cleanup command. Do NOT clean up before capturing the
    failure output and the metrics line (which is written on the timeout path too). A drill
    failure against the CI-built image is a material finding about the cutover — report it, and
    treat plan 17-06's register annotations as blocked, since T-14-58/T-14-88 would then close on
    an image that starts but cannot restore.
  </how-to-verify>

  <acceptance_criteria>
    - Behavior: `bash scripts/restore-drill.sh "<TARGET>"` exits 0.
    - Behavior: the dry-run output's `docker run` line names the GHCR postgres reference, proving
      the drill exercised the CI-built image and not a leftover host-built one.
    - Behavior: the run's stdout contains an inline line naming both the duration in seconds and
      the disk high-water figure.
    - Behavior: the metrics history file gained exactly one line for this run; it parses as JSON
      and carries a non-zero integer `durationSeconds`, an integer `diskHighWaterKb`, the target
      used, and a UTC `recordedAt`.
    - Behavior: `db:verify-restored` reported a pass (row counts against the baseline, partitions
      present, RLS enabled and forced on the restored cluster).
    - Behavior: no scratch container and no scratch volume remain; every production service is
      `Up` and `/readyz` succeeds after the drill.
    - The full drill output and the metrics JSON line are pasted into `17-05-SUMMARY.md`.
    - The chosen PITR target is recorded together with the `pgbackrest info` output it was chosen
      from, so the target's validity is reconstructible.
  </acceptance_criteria>

  <resume-signal>
    Type "approved" with the pasted drill output and the metrics JSON line, or describe the
    failure. On failure, state explicitly whether the scratch resources were preserved for
    inspection.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write the real drill figures into the runbooks</name>

  <files>docs/runbooks/restore-drill.md, docs/runbooks/backups.md</files>

  <read_first>
    - `17-05-SUMMARY.md` as written so far — the pasted drill output and metrics JSON line from
      Task 2 are the only source for these figures. Use the recorded numbers verbatim; do not
      round, estimate, or reconstruct them from memory of the session.
    - `docs/runbooks/restore-drill.md` as merged after plan 17-04 — the record-keeping paragraph
      (~215-230) and the "What was NOT verified locally" list, which is where the figures belong
      and which plan 17-04 already prepared for them.
    - `.planning/phases/14-deployment-database-durability/14-SECURITY.md` row T-14-73 — the exact
      wording of the placeholder, so the runbook text closes precisely the gap the register names
      ("observed figure still runbook placeholder — capture at next scheduled drill").
    - `docs/runbooks/backups.md` — the cross-reference to the restore-drill runbook, to keep the
      two documents consistent rather than duplicating figures.
  </read_first>

  <action>
    Add a dated "Recorded drill runs" subsection to `docs/runbooks/restore-drill.md` containing a
    table with one row for this drill: the date, the PITR target used, the restore-to-ready
    duration in seconds, the disk high-water figure in KB (plus a human-readable MB/GB rendering
    alongside it, since the register's threat is about host disk exhaustion and KB is not the unit
    an operator reasons in), the image reference under test, and the outcome. State that future
    runs append a row here and that the machine-written history lives in the metrics file — so
    this table is a curated record, not the primary data store.

    Immediately after the table, state the operational consequence the T-14-73 mitigation
    actually needs: the free-disk headroom a drill requires on this host, derived from the
    measured high-water figure. That derived statement is what turns a number into a mitigation;
    a bare figure would leave the register row citing data with no decision attached.

    Update the "What was NOT verified locally" list so duration and disk high-water are no longer
    listed as unmeasured — they are now measured, with this drill named as the source.

    In `docs/runbooks/backups.md`, update the restore-drill cross-reference so it points at the
    new "Recorded drill runs" subsection rather than restating the figures. One source of truth.

    Use only figures present in `17-05-SUMMARY.md`. If a figure is missing from the pasted
    evidence, stop and say so — writing an invented number into a runbook that a capacity
    decision then rests on is worse than leaving the placeholder.
  </action>

  <verify>
    <automated>grep -c 'Recorded drill runs' docs/runbooks/restore-drill.md</automated>
  </verify>

  <acceptance_criteria>
    - `docs/runbooks/restore-drill.md` contains a `Recorded drill runs` heading and a table row
      whose duration and disk figures match the metrics JSON line pasted in `17-05-SUMMARY.md`
      exactly (compare digit for digit and state the comparison in the SUMMARY).
    - The row names the PITR target used and the GHCR image reference under test.
    - The runbook states a derived free-disk-headroom requirement for future drills, referencing
      the measured high-water figure.
    - `docs/runbooks/restore-drill.md` no longer lists the restore duration or the disk
      high-water mark among the things not verified:
      `grep -c 'the real disk high-water mark on the real VPS' docs/runbooks/restore-drill.md`
      returns 0.
      <!-- planner-discipline-allow: the real disk high-water mark on the real VPS -->
    - `docs/runbooks/backups.md` links to the new subsection and does not restate the figures.
    - No figure appears in either runbook that is absent from `17-05-SUMMARY.md`'s pasted
      evidence.
  </acceptance_criteria>

  <done>
    T-14-73's placeholder is replaced by measured figures from a named, dated, reproducible drill,
    together with the disk-headroom conclusion the threat row's mitigation actually depends on.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GHCR -> production runtime | The cutover is the moment production begins trusting a registry-served artifact for its database image. |
| operator shell -> production containers | A container recreate against a live database; the persistent named volume is the thing that must survive untouched. |
| production database -> drill scratch container | The drill reads production read-only and writes only into throwaway scratch resources. |
| live observation -> security register | The pasted evidence in the SUMMARY is what plan 17-06's register annotations cite; unpasted verdicts are not evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-24 | Tampering | closing T-14-58/T-14-88 on code landing alone, while production still runs the old host-built image | high | mitigate | D-07's blocking checkpoint requires observed live evidence: `docker inspect` shows the GHCR reference and a changed image id, `compose config` shows no build key, and the pasted output is what plan 17-06's register annotations cite. |
| T-17-25 | Denial of Service | the cutover leaving production down — a Postgres major mismatch, a failed start against the existing PGDATA, or a config regression | high | mitigate | Pre-flight verifies the image's Postgres major matches 17 and STOPS on mismatch, before any production command runs; step 1 records the currently-serving image id; a concrete re-tag-and-recreate rollback is staged; and the checkpoint is explicitly not approved if the rollback is used. |
| T-17-26 | Tampering | data loss during the container recreate | high | mitigate | Only `pull` and `up -d` for the two services are run — no volume command, no schema command, no row-level command. The recreated container's mount list is compared against the pre-cutover one as an acceptance criterion, so a volume substitution cannot pass silently. |
| T-17-27 | Elevation of Privilege | the RLS enabled-and-forced posture regressing across the image swap | high | mitigate | The staged check reuses `verify-restored-database.ts`'s own query shape and exempt set and requires the per-table flags to be pasted, not a count — Phase 10's Pitfall 11 is precisely that a fail-open posture reads as a clean empty result. |
| T-17-28 | Repudiation | WAL archiving silently not resuming after the recreate, leaving a gap in the recovery chain | high | mitigate | Two `pg_stat_archiver` reads bracketing a forced `pg_switch_wal()` must show `archived_count` advancing, `failed_count` unchanged from baseline, and `last_failed_wal`/`last_failed_time` unmoved, plus a passing `pgbackrest check`. Both reads are pasted so the advance is visible rather than asserted. |
| T-17-29 | Denial of Service | the drill exhausting host disk (T-14-73's own threat) | medium | mitigate | `df -h` is a pre-flight step, the script's own sampling now measures the actual high-water figure, and Task 3 converts that figure into a stated free-disk-headroom requirement for future drills — which is the mitigation the register row needs, not merely the number. |
| T-17-30 | Tampering | the emergency re-tag rollback silently persisting and defeating the immutability property this phase just established | medium | mitigate | The rollback is documented as an emergency measure that must be reported immediately, must not be left in place, blocks Task 2, and blocks plan 17-06's register annotations — so the register rows stay open rather than closing over a locally re-tagged image. |
| T-17-31 | Repudiation | the alloy/Loki confirmation being recorded from a single observation of a crash-looping container | medium | mitigate | `RestartCount` is observed at two timestamps several minutes apart and both values recorded; a container restarting between observations fails the criterion even though each individual observation reports `running`. |
| T-17-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package (RESEARCH.md § Package Legitimacy Audit: not applicable to this phase). The one third-party artifact newly trusted in production is the GHCR image this repository's own CI built from its own committed Dockerfile, whose supply-chain posture is registered as T-17-15/T-17-16 in plan 17-03. |
</threat_model>

<verification>
- Task 1 approved with pasted evidence for every one of its eight steps.
- Task 2 approved with the full drill output and the metrics JSON line pasted.
- `grep -c 'Recorded drill runs' docs/runbooks/restore-drill.md` returns 1.
- The runbook figures match the pasted metrics line digit for digit.
- No emergency rollback is in place on the host (if one was used, this plan is not complete and
  plan 17-06 must not run).
</verification>

<success_criteria>
- Production runs the CI-built, registry-pulled, SHA-tagged postgres image, observed live.
- Postgres healthy, RLS enabled and forced, WAL archiving advancing with zero failures.
- Alloy stable and Loki receiving, confirmed in the same session (D-11 cleared).
- A real PITR drill passed against that image, with self-recorded duration and disk figures.
- The runbook carries real measured figures and the disk-headroom conclusion drawn from them.
- Every claim above is backed by pasted command output in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/17-address-tech-debt-wr-06-medium-security-follow-ups/17-05-SUMMARY.md` when done.
It must contain, as separate labelled sections: the cutover evidence (all eight steps' output),
the D-11 alloy/Loki evidence (both RestartCount observations and the observed Loki service
labels), the full drill output, the metrics JSON line, and the digit-for-digit comparison between
that line and the runbook table row. This SUMMARY is the citable evidence plan 17-06's register
annotations depend on — an unpasted verdict blocks 17-06.
</output>
