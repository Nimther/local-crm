---
phase: 15-observability-alerting-frontend-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/package.json
  - apps/api/package.json
  - apps/web/package.json
  - packages/redaction/package.json
  - package-lock.json
  - SPECIFICATION.md
  - docker/redis.conf
autonomous: false
requirements: [OPS-06, OPS-08, OPS-14]
user_setup:
  - service: sentry
    why: "Error tracking for web/api/worker (D-05/D-06). Needed before any DSN-bearing deploy, NOT before this plan."
    env_vars:
      - name: SENTRY_DSN_API
        source: "Sentry (EU region) -> Projects -> mega-crm-api -> Client Keys (DSN)"
      - name: SENTRY_DSN_WORKER
        source: "Sentry (EU region) -> Projects -> mega-crm-worker -> Client Keys (DSN)"
      - name: VITE_SENTRY_DSN
        source: "Sentry (EU region) -> Projects -> mega-crm-web -> Client Keys (DSN). Build-time only, NOT a secret, NOT in MEGA_CRM_ENV_FILE."
    dashboard_config:
      - task: "Create org in the EU region and three projects: mega-crm-web, mega-crm-api, mega-crm-worker"
        location: "sentry.io (EU) -> Projects -> Create Project"
  - service: grafana-cloud
    why: "Loki log storage/query + alerting backstop (D-01/D-03). Needed by plan 15-17, not by this plan."
    env_vars:
      - name: GRAFANA_LOKI_URL
        source: "Grafana Cloud -> Stack -> Loki -> Details (push URL)"
      - name: GRAFANA_LOKI_USER
        source: "Grafana Cloud -> Stack -> Loki -> Details (user/instance id)"
      - name: GRAFANA_CLOUD_API_TOKEN
        source: "Grafana Cloud -> Access Policies -> create token with logs:write"

must_haves:
  truths:
    - "npm ls resolves @sentry/node in apps/api and apps/worker, @sentry/react in apps/web, @bull-board/api and @bull-board/fastify in apps/worker, and pino in apps/worker."
    - "fastify is a runtime dependency (not devDependency) of apps/worker."
    - "SPECIFICATION.md section 2 lists every newly added package with the exact version recorded in the owning package.json."
    - "npm run check:lockfile-npm10 and npm run lint exit 0 after the install."
    - statement: "A human confirmed each [SUS]-verdict package against its public registry page before it was installed."
      verification: backstop
  artifacts:
    - apps/worker/package.json
    - apps/api/package.json
    - apps/web/package.json
    - packages/redaction/package.json
    - package-lock.json
    - SPECIFICATION.md
  key_links:
    - "package-lock.json is mutated exactly once in this phase — every later plan installs nothing, so no two plans can corrupt the lockfile concurrently."
    - "packages/redaction gains @sentry/node as a devDependency ONLY (type-level import for the beforeSend signature); it must not become a runtime dependency of that package."
  prohibitions:
    - statement: "No Sentry SDK initialization call site may be created in this plan — installs only. Sentry initialization lands only after the OPS-09 redaction fixture test is a required CI check (Pitfall 18: Sentry has no retroactive redaction)."
      status: kept
      verification: flagged-unverified
    - statement: "No package on the RESEARCH.md legitimacy audit may be silently reclassified from SUS to OK without the human checkpoint below."
      status: kept
      verification: flagged-unverified
    - statement: "packages/redaction must not gain a runtime dependency on any Sentry package — it stays dependency-light by its own stated principle."
      status: kept
      verification: flagged-unverified
---

<objective>
Land every new dependency this phase needs in one lockfile mutation, behind a blocking human legitimacy check.

Purpose: RESEARCH.md's Package Legitimacy Audit returns a `SUS` (`too-new`) verdict on all four Sentry/Bull Board packages, and the fallback policy treats any package with no audit row (`pino` for apps/worker) as `[ASSUMED]`. Both classes require a human check before install. Consolidating them here also means `package-lock.json` is mutated exactly once in the phase — two concurrent `npm install` runs in one workspace tree corrupt each other (Phase 8 precedent).
Output: installed dependencies, `fastify` promoted from dev to runtime in `apps/worker`, and SPECIFICATION.md section 2 updated in the same change per the binding rule in `.claude/CLAUDE.md`.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-CONTEXT.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (blocking, never auto-approvable)</name>
  <what-built>Nothing yet. RESEARCH.md's Package Legitimacy Audit returned verdict `SUS` (reason `too-new`) for `@sentry/node`, `@sentry/react`, `@bull-board/api`, `@bull-board/fastify`. `pino` (new for `apps/worker`) and `fastify` (already present as a devDependency) have no audit row and fall back to `[ASSUMED]` under the gate's own fallback policy.</what-built>
  <how-to-verify>
    Open each page and confirm the package is the first-party, actively-maintained project it claims to be — publisher, source repository link, recent release cadence, weekly downloads:
    - https://www.npmjs.com/package/@sentry/node (expect repo getsentry/sentry-javascript)
    - https://www.npmjs.com/package/@sentry/react (expect repo getsentry/sentry-javascript)
    - https://www.npmjs.com/package/@bull-board/api (expect repo felixmosh/bull-board)
    - https://www.npmjs.com/package/@bull-board/fastify (expect repo felixmosh/bull-board)
    - https://www.npmjs.com/package/pino (expect repo pinojs/pino — already a runtime dep of apps/api at 10.3.1)
    Then run `npm view @sentry/node version`, `npm view @sentry/react version`, `npm view @bull-board/api version`, `npm view @bull-board/fastify version` and record the versions actually returned today — RESEARCH.md's numbers are 7-day-stale by its own note and must be re-read, not copied.
  </how-to-verify>
  <resume-signal>Type "approved" with the four freshly-read version numbers, or name the package that failed the check.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the batch and promote fastify to a runtime dependency</name>
  <files>apps/worker/package.json, apps/api/package.json, apps/web/package.json, packages/redaction/package.json, package-lock.json, SPECIFICATION.md</files>
  <read_first>
    - apps/worker/package.json (current dependency/devDependency split; `fastify` currently sits under devDependencies)
    - apps/api/package.json (pino is already pinned here — match its version exactly for apps/worker)
    - apps/web/package.json
    - packages/redaction/package.json (dependency-light by design — read its own doc comments before adding anything)
    - .planning/phases/15-observability-alerting-frontend-resilience/15-RESEARCH.md § Standard Stack + § Package Legitimacy Audit
  </read_first>
  <action>
    Install, from the repository root, in ONE sequential run (never two concurrent `npm install` processes — Phase 8 precedent: concurrent installs in one workspace tree corrupt `package-lock.json`), using the versions the human confirmed in Task 1:
    - `apps/worker`: add `pino` (exact same version apps/api pins), `@sentry/node`, `@bull-board/api`, `@bull-board/fastify` as dependencies; move the existing `fastify` entry from `devDependencies` to `dependencies` at the same pinned version.
    - `apps/api`: add `@sentry/node` as a dependency.
    - `apps/web`: add `@sentry/react` as a dependency.
    - `packages/redaction`: add `@sentry/node` as a devDependency ONLY. It is needed exclusively for the `ErrorEvent`/`EventHint` type imports plan 15-04's `sentry-scrub.ts` uses; a runtime dependency here would contradict this package's own stated dependency-light principle, which its header comments record.
    Do not create, edit or scaffold any Sentry initialization module, any Bull Board module, or any worker logger module in this plan. This plan changes manifests and the lockfile only.
    Then update `SPECIFICATION.md` section 2 ("Зависимости и версии"): add one row per newly declared package to the table of the owning workspace, with the exact version string as written in that workspace's `package.json` (not a research range), and mark each as "объявлен, ещё не используется в коде" since no source file imports them yet.
  </action>
  <verify>
    <automated>npm ls @sentry/node @sentry/react @bull-board/api @bull-board/fastify pino fastify --workspaces 2>&1 | tail -40 && npm run check:lockfile-npm10 && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `apps/worker/package.json` `dependencies` contains keys `pino`, `@sentry/node`, `@bull-board/api`, `@bull-board/fastify`, `fastify`.
    - `apps/worker/package.json` `devDependencies` no longer contains a `fastify` key.
    - `apps/api/package.json` `dependencies` contains `@sentry/node`; `apps/web/package.json` `dependencies` contains `@sentry/react`.
    - `packages/redaction/package.json` `devDependencies` contains `@sentry/node` and its `dependencies` does not.
    - `npm run check:lockfile-npm10` exits 0.
    - `npm run lint` exits 0.
    - `SPECIFICATION.md` section 2 contains a row for each of `@sentry/node`, `@sentry/react`, `@bull-board/api`, `@bull-board/fastify`, `pino` (apps/worker) whose version string is byte-identical to the one in the owning `package.json`.
  </acceptance_criteria>
  <done>Every dependency this phase needs is installed and recorded; the lockfile has been mutated exactly once; no application source file has changed.</done>
</task>

<task type="auto">
  <name>Task 3: Record the phase's out-of-scope scope notes</name>
  <files>docker/redis.conf, SPECIFICATION.md</files>
  <read_first>
    - docker/redis.conf (its own comment tags `maxmemory` sizing as "Phase 15's concern")
    - packages/db/migrations/0058_reputation_and_ingestion_alert_state.sql (its closing comment says a tenant-facing reputation dashboard is "deferred to Phase 15")
    - .planning/phases/15-observability-alerting-frontend-resilience/15-RESEARCH.md § Open Questions (item 1)
  </read_first>
  <action>
    Two stale in-repo comments point at Phase 15 for work that is NOT in OPS-06…OPS-19. Resolve both in writing rather than leaving them to be discovered as gaps at verification:
    - `docker/redis.conf`: amend the `maxmemory` comment to state that sizing against the real production VPS host is NOT part of Phase 15 (no OPS-06…19 requirement and no CONTEXT.md decision covers it) and to name the milestone item that owns it instead (SCALE, alongside the deferred PgBouncer/DB-14 decision). Do not change the `maxmemory` value itself.
    - `packages/db/migrations/0058_reputation_and_ingestion_alert_state.sql` is an applied migration and MUST NOT be edited. Instead record the correction in `SPECIFICATION.md` section 8 ("Расхождения"): the tenant-facing reputation dashboard referenced by that migration's comment is not in Phase 15's requirement set and remains unscheduled.
  </action>
  <verify>
    <automated>npm run lint:migrations && git diff --name-only | grep -c 'packages/db/migrations/' | grep -qx 0 && echo "no migration files touched"</automated>
  </verify>
  <acceptance_criteria>
    - `docker/redis.conf` no longer claims Phase 15 owns the `maxmemory` sizing decision, and its `maxmemory` directive value is unchanged from HEAD.
    - No file under `packages/db/migrations/` appears in `git diff --name-only`.
    - `SPECIFICATION.md` section 8 contains an entry naming the reputation dashboard as unscheduled and not part of Phase 15.
    - `npm run lint:migrations` exits 0.
  </acceptance_criteria>
  <done>Both stale "Phase 15 owns this" pointers are corrected in writing; neither expands this phase's scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Untrusted third-party code enters the build and the deployed images |
| repo → deployed image | Manifest changes propagate to production containers on the next deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-SC | Tampering | npm installs (`@sentry/node`, `@sentry/react`, `@bull-board/api`, `@bull-board/fastify`, `pino`) | high | mitigate | Blocking, non-auto-approvable `checkpoint:human-verify` (Task 1) against each package's public registry page before install; versions re-read live, never copied from research |
| T-15-01 | Elevation of Privilege | `fastify` promoted devDependency → dependency in `apps/worker` | medium | mitigate | Promotion is manifest-only in this plan; no HTTP surface is added here. The listener that will use it (plan 15-10) stays loopback-bound with no prod-compose `ports:` mapping, enforced by the existing `scripts/validate-prod-compose.mjs` CI gate |
| T-15-02 | Information Disclosure | `packages/redaction` gaining a Sentry dependency | medium | mitigate | Added as devDependency only (type imports); a runtime dependency would put SDK code inside the module that is supposed to be the redaction trust boundary |
| T-15-03 | Tampering | `package-lock.json` corruption from concurrent installs | medium | mitigate | All phase installs consolidated into this single wave-1 plan; every later plan installs nothing |
</threat_model>

<verification>
- `npm ls` resolves all six packages at the versions recorded in SPECIFICATION.md section 2.
- `npm run check:lockfile-npm10`, `npm run lint`, `npm run lint:migrations` all exit 0.
- `git diff --stat` shows changes only to `package.json` files, `package-lock.json`, `SPECIFICATION.md` and `docker/redis.conf`.
</verification>

<success_criteria>
Every dependency later plans import is installed and recorded, the human legitimacy gate is on record, and no Sentry SDK is initialized anywhere yet.
</success_criteria>

## Artifacts this phase produces

New symbols/paths introduced by THIS plan: none in source. Manifest keys only — `apps/worker` dependencies `pino`, `@sentry/node`, `@bull-board/api`, `@bull-board/fastify`, `fastify`; `apps/api` dependency `@sentry/node`; `apps/web` dependency `@sentry/react`; `packages/redaction` devDependency `@sentry/node`. New SPECIFICATION.md section 8 entry for the unscheduled reputation dashboard.

## Flagged assumptions (edge-probe fallback)

The edge-coverage report classified every OPS row `unclassified`/`unresolved` (known project behaviour: the probe classifier is English-keyed, REQUIREMENTS.md is Russian). Rows owned by this plan:

- **OPS-06 (unclassified, unresolved)** — FLAGGED ASSUMPTION: this plan only makes `pino` available to `apps/worker`; the behavioural edge (what the worker logs when the logger itself fails to construct) is owned by plan 15-02/15-05, not resolvable here.
- **OPS-08 (concurrency, unresolved)** — FLAGGED ASSUMPTION: "if interrupted or run in parallel, what is guaranteed?" is not answerable at the manifest layer. Resolved in plan 15-06 where the SDK is actually initialized.
- **OPS-14 (unclassified, unresolved)** — FLAGGED ASSUMPTION: Bull Board's access edge is resolved in plan 15-10; here only its package availability is established.

<output>
Create `.planning/phases/15-observability-alerting-frontend-resilience/15-01-SUMMARY.md` when done
</output>
