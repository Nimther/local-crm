---
phase: 16-live-sendgrid-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/uat-verify.mjs
  - scripts/__tests__/uat-verify.test.mjs
  - docs/runbooks/uat-live-sendgrid.md
  - package.json
autonomous: false
requirements: [UAT-01, UAT-02]

user_setup:
  - service: sendgrid
    why: "D-01/D-04 — the UAT tenant's BYO key and the purpose-built Dynamic Template do not exist yet; RESEARCH.md Environment Availability lists the second API key as the phase's only unmet prerequisite"
    env_vars:
      - name: (none new — the BYO key is entered through the product's key-entry UI, never an env var)
        source: "SendGrid Dashboard -> Settings -> API Keys -> Create API Key (Mail Send scope), on the same account as the Phase 14 verified sender"
    dashboard_config:
      - task: "Create a second API key with Mail Send scope on the existing SendGrid account (D-01 — no second account)"
        location: "SendGrid Dashboard -> Settings -> API Keys"
      - task: "Create a minimal UAT Dynamic Template with at least two handlebars substitutions and one visible clickable link, and record its template id (D-04)"
        location: "SendGrid Dashboard -> Email API -> Dynamic Templates"
      - task: "Confirm the operator's receiving mailbox is reachable and its domain has NO catch-all (needed later by plan 16-02's bounce leg — RESEARCH.md Pitfall 5 / A1)"
        location: "Operator's own mail provider admin console"
  - service: vps
    why: "D-03 — the UAT runs on the production VPS against the deployed release, no staging"
    dashboard_config:
      - task: "Confirm SSH access to the production VPS and that the deployed stack is healthy (/readyz 200)"
        location: "VPS shell"

must_haves:
  truths:
    - "A single command run by the operator prints, for one named send, whether the send row reached a terminal status, which SendGrid message id it carries, and every send_event attributed to it — so a UAT verdict is a query result, not an impression."
    - "The verification command exits non-zero when the expected send or event rows are absent, so a missing result cannot be read as a pass."
    - "The verification command scopes every read to one workspace id through the platform's own tenant-context helper, introducing no new database grant and no cross-tenant read."
    - "A runbook describes, in operator-executable steps, how the dedicated UAT workspace is created, how the tenant BYO key is entered, how the Dynamic Template id is attached, and how the webhook subscription is provisioned against the real account."
    - "One live campaign send, dispatched with the tenant's own BYO key through the UAT Dynamic Template, arrives in the operator's real mailbox with both handlebars substitutions rendered."
    - statement: "The received message is a genuine Dynamic Template render — no unsubstituted handlebars placeholder text is visible in the delivered mail."
      verification: backstop
    - statement: "The delivered event observed for that send originates from the live SendGrid Event Webhook and not from any replayed or synthesised payload."
      verification: backstop
  artifacts:
    - scripts/uat-verify.mjs
    - scripts/__tests__/uat-verify.test.mjs
    - docs/runbooks/uat-live-sendgrid.md
  key_links:
    - "custom_args send_id (packages/delivery-core/src/send-mail.ts) -> send_events.send_id: the whole attribution chain UAT-02 verifies rests on this one field surviving the round trip through SendGrid. The verification command must report the send_id it matched on, not just a count."
    - "The UAT workspace's provisioned webhook endpoint (apps/api/src/modules/webhooks/sendgrid-webhook-provision.ts) -> the public Caddy URL: without provisioning against the real account no event ever returns, and the failure looks identical to a send that never happened."
    - "The recorded UAT workspace id -> plans 16-03/16-04: WEBHOOK_RAW_CAPTURE_WORKSPACE_ID is that exact id; the runbook is where it gets written down."
  prohibitions:
    - statement: MUST NOT record UAT-01 as passed on the basis of a platform-key send or a non-template send — the evidence must show the tenant BYO key and the UAT Dynamic Template id were the ones actually used.
      status: flagged
      verification: unverified
    - statement: MUST NOT use a real customer's or third party's mailbox as a UAT recipient — only mailboxes the operator personally controls.
      status: flagged
      verification: unverified
---

<objective>
**This plan is the phase's tracer slice.** It wires ONE path through every layer Phase 16 touches — dedicated UAT workspace, tenant BYO key, SendGrid Dynamic Template, the deployed worker's real send path, real SendGrid, a real inbox, and the Event Webhook returning a `delivered` event onto the correct send row — and proves it end to end before any expansion work is planned around it. If the live path is broken (BYO key entry, template rendering, webhook provisioning, attribution), this plan finds it after one step instead of after six.

Closes UAT-01 and the `delivered` leg of UAT-02, and produces the evidence instrument (`scripts/uat-verify.mjs`) every later UAT test in this phase reuses.

**Execution-mode note:** per **D-13** this plan ends in a blocking real-host checkpoint (the proven Phase 14 pattern, see `14-09-PLAN.md`). This deliberately overrides `workflow.human_verify_mode: end-of-phase` — the override is structural, not stylistic: plan 16-05's committed fixture is captured *during* plan 16-04's live session, so mid-phase human action is a data dependency of the phase.

Purpose: v1.0's accepted tech debt was precisely deferred live UAT. Every mechanism this phase exercises already exists and was verified against mocks in Phases 8–15; what has never been proven is that the real provider, the real inbox and the real deployed environment agree with those mocks.
Output: a scripted-assert CLI, the UAT runbook, and one live-verified end-to-end send.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-live-sendgrid-verification/16-CONTEXT.md
@.planning/phases/16-live-sendgrid-verification/16-RESEARCH.md
@.planning/phases/16-live-sendgrid-verification/16-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols and paths created by THIS plan:

- Files: `scripts/uat-verify.mjs`, `scripts/__tests__/uat-verify.test.mjs`, `docs/runbooks/uat-live-sendgrid.md`
- npm script: `uat:verify`
- CLI subcommand: `send-attribution`
- CLI flags: `--workspace`, `--send-id`, `--message-id`, `--expect-status`, `--expect-events`, `--json`
- Exported helpers from `scripts/uat-verify.mjs`: `parseArgs`, `formatReport`, `assertExpectations`

## Planner assumptions (specless probe, no SPEC.md for this phase)

Edge-probe rows **UAT-01** and **UAT-02** returned `unresolved` (the classifier is English-keyed; REQUIREMENTS.md is Russian — a known, recurring project condition). No edge predicate was auto-derived, so the following are authored here as explicit planner assumptions rather than silently dropped:

- A UAT verdict is a query result with an exit code, never a human impression — a missing row is a failure, not an inconclusive.
- Attribution is asserted on `send_id` (the `custom_args` round-trip), not on recipient address or timestamp proximity.
- "Arrives in a real inbox" is human-observable only and is therefore carried as a `backstop` truth, not as an automated assertion.
- One live send is sufficient evidence for UAT-01; repetition adds no information and burns real sends.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/uat-verify.mjs — the scripted-assert instrument (send-attribution)</name>
  <files>scripts/uat-verify.mjs, scripts/__tests__/uat-verify.test.mjs, package.json</files>
  <read_first>
    - scripts/lint-session-state.mjs and scripts/check-spec-env-coverage.mjs (this repository's script conventions: Node built-ins only, exported pure helpers, a CLI behind an `import.meta.url` guard, and a reported checked-item count so a vacuous pass is visible rather than hidden inside a bare exit 0)
    - scripts/__tests__/check-spec-env-coverage.test.mjs (how a script's exported helpers are tested in this repository — the shape this task's test file follows)
    - scripts/vitest.config.ts (the `scripts/` test lane: node environment, no database globalSetup — this task's test must therefore exercise the pure helpers, not a live connection)
    - scripts/env-path.mjs (the `MEGA_CRM_ENV_FILE` resolution convention — the script must not invent a second config path and must not read a repo-root `.env`)
    - packages/tenant-context/src (the `withTenant` helper's signature and how a workspace id is bound to a connection — the script scopes every read through it, adding no new grant)
    - packages/db/src/webhooks/ingress-journal.ts (the `ingress_journal` column names later subcommands will need; read now so the shared query module is shaped for them)
    - packages/db/migrations/0057_send_events_dedup_rebase.sql (the `send_events` dedup columns: workspace_id, send_id, event_type, occurred_at)
    - docker/Dockerfile and docker/docker-compose.prod.yml (determine whether `scripts/` is present in the shipped api image; the answer decides the one invocation form the runbook documents in Task 2)
  </read_first>
  <behavior>
    - `parseArgs` rejects an invocation with no subcommand and one with an unknown subcommand, naming the accepted set.
    - `parseArgs` rejects a `send-attribution` invocation missing `--workspace`, and one missing both `--send-id` and `--message-id`.
    - `parseArgs` accepts `--expect-events delivered,opened` and yields the two event types as a list.
    - `assertExpectations` returns a failing result when an expected event type is absent from the observed set.
    - `assertExpectations` returns a failing result when zero send rows were observed, and its message says zero rows were observed rather than reporting success.
    - `assertExpectations` returns a failing result when the observed send status differs from `--expect-status`.
    - `assertExpectations` returns a passing result only when the send row exists, its status matches, and every expected event type is present.
    - `formatReport` renders the matched send id, the SendGrid message id, the send status, and one line per observed event with its type and `occurred_at`.
    - `formatReport` under `--json` emits parseable JSON carrying the same fields.
  </behavior>
  <action>
Create `scripts/uat-verify.mjs`: a dependency-free Node CLI (Node built-ins plus the repository's existing `pg`/tenant-context imports only — no new npm package) that is the single scripted-assert instrument for every UAT test in this phase, per **D-13** ("verification queries are scripted so 'passed' means a query returned the expected rows, not an eyeball").

Structure it exactly like `scripts/check-spec-env-coverage.mjs`: exported pure helpers plus a CLI behind an `import.meta.url` guard, so the helpers are unit-testable without a database. Export `parseArgs`, `formatReport` and `assertExpectations` as pure functions; keep all database I/O in a separate non-exported function the CLI path calls.

Implement one subcommand now — `send-attribution` — with flags `--workspace <uuid>`, `--send-id <uuid>`, `--message-id <sg_message_id>`, `--expect-status <status>`, `--expect-events <csv>`, `--json`. Later plans in this phase add `event-coverage` (16-02), `dedup` (16-04) and `uat05-state` (16-06); shape the subcommand dispatch table and the shared query module so those slot in without restructuring.

Database access: resolve the connection string through `scripts/env-path.mjs`'s `MEGA_CRM_ENV_FILE` convention and read `DATABASE_URL`; scope every query through `withTenant` from `@mega-crm/tenant-context` bound to `--workspace`. Do NOT use `SCAN_DATABASE_URL` and do NOT add any grant — the UAT workspace id is known, so a tenant-scoped read is sufficient and introduces no cross-tenant surface.

Queries for `send-attribution`: one row from `sends` matched by `id` (or by `sg_message_id` when `--send-id` is absent), selecting id, status, `sg_message_id`, `campaign_id`, `flow_run_id` if present, `queued_at`, `sent_at`; and every `send_events` row for that `send_id` selecting `event_type`, `occurred_at`, `sg_event_id`.

Exit codes: 0 only when `assertExpectations` passes; 1 when it fails; 2 on a usage error. Print the observed-row count on every run so a vacuous pass (zero rows, zero expectations) is visible rather than hidden inside exit 0 — the same anti-vacuous-pass discipline `check-spec-env-coverage.mjs` documents.

Add the npm script `uat:verify` invoking `node scripts/uat-verify.mjs`. Write the vitest file `scripts/__tests__/uat-verify.test.mjs` covering the `<behavior>` list against the exported helpers only.
  </action>
  <verify>
    <automated>npx vitest run --root scripts __tests__/uat-verify.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --root scripts __tests__/uat-verify.test.mjs` exits 0 with at least 9 passing assertions.
    - `node scripts/uat-verify.mjs` with no arguments exits 2 and prints the accepted subcommand names.
    - `node scripts/uat-verify.mjs send-attribution --workspace X` (no send selector) exits 2.
    - `scripts/uat-verify.mjs` contains `export function parseArgs(`, `export function formatReport(` and `export function assertExpectations(`.
    - `scripts/uat-verify.mjs` contains no `import` of a package absent from the repository's existing dependency tree — `npm ls` for any newly imported name resolves to an already-installed package.
    - `package.json` scripts contains a `uat:verify` entry.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>A single command answers "did this send reach the inbox pipeline and which events landed on it", with an exit code that treats a missing row as a failure.</done>
</task>

<task type="auto">
  <name>Task 2: docs/runbooks/uat-live-sendgrid.md — UAT workspace, BYO key, template, webhook</name>
  <files>docs/runbooks/uat-live-sendgrid.md</files>
  <read_first>
    - docs/runbooks/production-topology.md (the as-built VPS layout the operator is working against — service names, compose file path, how commands are invoked on the host)
    - docs/runbooks/reprovision-webhook-event-types.md (the exact `POST /api/workspaces/:slug/webhook-reconnect` call and its preconditions — the UAT workspace's webhook subscription is created this way, not by hand in the SendGrid UI)
    - docs/runbooks/deploy-and-rollback.md (the `deploy.sh <sha>` mechanism plans 16-04 and 16-06 reuse, and the redeploy loop D-16's gap closure depends on)
    - apps/api/src/modules/webhooks/sendgrid-webhook-provision.ts (EVENT_FLAGS — which event types are provisioned, and the friendly-name scoping that keeps the UAT subscription distinguishable on the shared account)
    - scripts/uat-verify.mjs (as written in Task 1 — the runbook documents its real invocation form, including the container/bind-mount form determined from the Dockerfile read)
    - docs/runbooks/ (any one existing runbook, for this repository's runbook heading and formatting conventions)
  </read_first>
  <action>
Create `docs/runbooks/uat-live-sendgrid.md` — the single operator document for this whole phase. Later plans append sections to it; this task writes the setup half plus the UAT-01 procedure.

Sections to write now:

1. **Scope and safety.** State that this runbook operates on the production VPS against a dedicated UAT workspace (D-03), that the UAT workspace is retained afterwards as a standing canary (D-15), and that no UAT step may target a mailbox the operator does not personally control (D-02).
2. **Prerequisites checklist.** Second SendGrid API key with Mail Send scope on the existing account (D-01); UAT Dynamic Template created with at least two handlebars substitutions and one visible clickable link, template id recorded (D-04); operator's receiving mailbox confirmed; SSH to the VPS; deployed stack healthy.
3. **UAT workspace creation.** Create the workspace through the product UI, record its slug AND its workspace id — state explicitly that the workspace id is the value plans 16-03/16-04 use for `WEBHOOK_RAW_CAPTURE_WORKSPACE_ID`, and provide a fill-in line for it.
4. **BYO key entry.** Enter the second API key through the product's normal tenant key-entry flow (never as an environment variable, never into `MEGA_CRM_ENV_FILE`) — the point of D-01 is that the BYO code path is exercised unchanged.
5. **Webhook provisioning.** The `webhook-reconnect` call for the UAT workspace, and how to confirm the subscription exists with all four event types enabled. Include the note from RESEARCH.md Pitfall 6 that this shared account fans events out across workspaces and that sibling-drop discards are expected, not defects.
6. **Contact and campaign setup.** One UAT contact addressed to the operator's own mailbox; one minimal campaign referencing the UAT template id with two handlebars values.
7. **Running the verification command.** The one concrete, copy-pasteable invocation form for `scripts/uat-verify.mjs` on the VPS, chosen from what the Dockerfile read in Task 1 established (in-image path versus a bind-mounted one-shot container). Document exactly one form; do not offer alternatives the operator has to choose between mid-session.
8. **UAT-01 procedure.** Launch the campaign; wait; confirm arrival in the real mailbox; run `send-attribution` with `--expect-status sent --expect-events delivered`; record the `sg_message_id`.

Every command must be copy-pasteable with placeholders clearly marked. No credential, key or token value appears anywhere in the file.
  </action>
  <verify>
    <automated>npm run check:runbook-coverage &amp;&amp; npm run check:root-hygiene</automated>
  </verify>
  <acceptance_criteria>
    - `docs/runbooks/uat-live-sendgrid.md` exists and contains the headings for scope/safety, prerequisites, workspace creation, BYO key entry, webhook provisioning, contact and campaign setup, running the verification command, and the UAT-01 procedure.
    - The file contains a fill-in line for the UAT workspace id and names `WEBHOOK_RAW_CAPTURE_WORKSPACE_ID` as its downstream consumer.
    - The file contains exactly one documented invocation form for `scripts/uat-verify.mjs` (a single fenced command block under the "running the verification command" heading).
    - `grep -nEi 'SG\.[A-Za-z0-9_-]{10,}|BEGIN [A-Z ]*PRIVATE KEY' docs/runbooks/uat-live-sendgrid.md` returns no match.
    - `npm run check:runbook-coverage` exits 0.
    - `npm run check:root-hygiene` exits 0.
  </acceptance_criteria>
  <done>An operator with SSH access and the two SendGrid prerequisites can set up the UAT workspace and run the first live send without asking a question the runbook does not answer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: TRACER — first live BYO-key send through the Dynamic Template, end to end</name>
  <precondition>The second SendGrid API key (Mail Send scope) and the UAT Dynamic Template exist on the real account, and the production VPS answers /readyz with 200. Neither is guaranteed by plan ordering — halt and report if either is unmet.</precondition>
  <read_first>
    - docs/runbooks/uat-live-sendgrid.md (as written by Task 2 — the checkpoint is walked from this file; read it before presenting the checkpoint so the staged commands and the presented steps cannot diverge)
    - scripts/uat-verify.mjs (as written by Task 1 — the exact flag names and exit-code meanings the operator will see)
  </read_first>
  <what-built>
`scripts/uat-verify.mjs send-attribution` — a scripted assert that reports a send's status, its SendGrid message id and every event attributed to it, exiting non-zero when an expected row is missing. `docs/runbooks/uat-live-sendgrid.md` — the operator procedure for creating the dedicated UAT workspace, entering the tenant BYO key, attaching the UAT Dynamic Template, provisioning the webhook subscription against the real account, and running the first live campaign send.

What automation cannot establish: UAT-01's requirement is that mail with a tenant's own BYO key, rendered from a SendGrid Dynamic Template, physically arrives in a real inbox. Every mechanism involved was verified against a mock in Phases 8-15 and against no real mailbox ever. The four things that have never been exercised together are the BYO key on the real account, the template render with real handlebars data, the real webhook round trip through Caddy, and the `custom_args` `send_id` surviving that round trip.
  </what-built>
  <how-to-verify>
Follow `docs/runbooks/uat-live-sendgrid.md` sections 2 through 8. Then confirm each of the following:

1. The UAT workspace exists, is distinct from every existing tenant workspace, and its workspace id is written into the runbook's fill-in line.
2. The tenant BYO key was entered through the product's key-entry flow (not an environment variable), and the key-check step in that flow reported success.
3. The webhook subscription for this workspace exists on the SendGrid account with delivered, opened, clicked and bounced all enabled.
4. The campaign was launched and the mail arrived in the operator's own mailbox.
5. Open the received mail and confirm BOTH handlebars values rendered as the real values supplied — no placeholder syntax and no empty substitution is visible anywhere in the body or subject.
6. Confirm the mail's visible clickable link is present (it is what plan 16-02's clicked event depends on; do not click it yet).
7. Run the runbook's `send-attribution` invocation with `--expect-status sent --expect-events delivered`. It must exit 0 and print a non-zero observed-row count, a `sg_message_id`, and a `delivered` event row.
8. Record the `sg_message_id`, the send id, the workspace id and the UTC timestamp — plan 16-07's UAT report cites them.
9. Report anything that required a manual step the runbook does not describe: that is a runbook defect to fix here, not an operator note.
  </how-to-verify>
  <acceptance_criteria>
    - The operator confirms arrival in a real mailbox they personally control, with both handlebars substitutions rendered.
    - The `send-attribution` command exited 0 with a `delivered` event attributed to the send.
    - The UAT workspace id, send id and `sg_message_id` are recorded in the plan's SUMMARY.
    - No step required a command absent from `docs/runbooks/uat-live-sendgrid.md`.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded workspace id, send id and sg_message_id, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → production VPS | UAT commands are executed against the live release environment |
| platform worker → real SendGrid API | A real tenant API key is used to send real mail to a real recipient |
| SendGrid Event Webhook → public Caddy endpoint | Untrusted signed input crosses into the platform |
| verification script → production database | A read path is added that runs against live tenant data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information Disclosure | `scripts/uat-verify.mjs` database reads | medium | mitigate | Every query is scoped through `withTenant` bound to the single `--workspace` argument; the script never uses `SCAN_DATABASE_URL` and adds no grant, so a mistyped workspace id returns zero rows rather than another tenant's data |
| T-16-02 | Information Disclosure | `docs/runbooks/uat-live-sendgrid.md` | medium | mitigate | Runbook carries placeholders only; the acceptance criterion greps the file for SendGrid key and private-key shapes and requires no match |
| T-16-03 | Tampering | UAT workspace on the shared production database | medium | mitigate | The UAT workspace is a normal tenant behind the platform's existing RLS boundary (Phase 10); no UAT step disables, bypasses or forces off any policy |
| T-16-04 | Spoofing | Tenant BYO key handling | low | mitigate | The key is entered through the product's existing key-entry flow and stored under the existing KMS envelope encryption; it is never written to `MEGA_CRM_ENV_FILE`, a runbook, a plan or a SUMMARY |
| T-16-05 | Repudiation | UAT verdicts | medium | mitigate | Verdicts come from `uat-verify.mjs` exit codes and printed row counts, recorded with timestamps in the plan SUMMARY; an eyeball impression is not an accepted verdict (D-13) |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package (RESEARCH.md Package Legitimacy Audit: none). An acceptance criterion asserts every import resolves to an already-installed package; any deviation requires the legitimacy gate plus a blocking human checkpoint before install |
</threat_model>

<verification>
- `npx vitest run --root scripts __tests__/uat-verify.test.mjs` exits 0.
- `npm run lint`, `npm run check:runbook-coverage`, `npm run check:root-hygiene` all exit 0.
- `npm test` (full aggregate) exits 0 — this plan must not regress an existing gate.
- The Task 3 blocking checkpoint is approved with a recorded workspace id, send id and `sg_message_id`.

<human-check>
Live operator verification (cannot be asserted by any automated check in this repository):
1. A campaign send using the UAT tenant's own BYO key, rendered from the UAT Dynamic Template, arrived in the operator's real mailbox.
2. Both handlebars substitutions rendered as real values; no placeholder syntax survived into the delivered mail.
3. `uat-verify send-attribution` exited 0 showing a `sent` send and a live `delivered` event attributed to it.
</human-check>
</verification>

<success_criteria>
- The end-to-end live path (BYO key → Dynamic Template → real SendGrid → real inbox → Event Webhook → `send_events`) is proven once, on the production release, with a recorded message id.
- Every later plan in this phase has a working evidence instrument and a written operator procedure to extend.
- UAT-01 is live-passed; the `delivered` leg of UAT-02 is live-passed.
</success_criteria>

<output>
Create `.planning/phases/16-live-sendgrid-verification/16-01-SUMMARY.md` when done, recording the UAT workspace id, workspace slug, UAT Dynamic Template id, send id, `sg_message_id` and the UTC timestamp of the live send.
</output>
