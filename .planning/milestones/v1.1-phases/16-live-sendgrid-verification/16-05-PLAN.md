---
phase: 16-live-sendgrid-verification
plan: 05
type: execute
wave: 4
depends_on: [16-04]
files_modified:
  - apps/api/src/modules/webhooks/__tests__/webhooks-signature-replay.test.ts
  - apps/api/src/modules/webhooks/__tests__/fixtures/README.md
autonomous: true
requirements: [UAT-03, UAT-04]

must_haves:
  truths:
    - "A committed test replays a genuinely SendGrid-signed payload, captured from a real delivery, through the full HTTP stack of the application server and asserts that signature verification accepts it."
    - "The same test asserts that a single-byte mutation of that body is rejected with the route's fail-closed response and that nothing is ingested for it."
    - "The same test asserts that posting the identical body twice produces exactly one send_events row for the dedup key while the enqueue path is invoked twice — the journal and queue layers are expected to see both deliveries."
    - "The test holds the clock at the fixture's own captured timestamp, so it neither passes only on the day it was written nor depends on an inflated tolerance value."
    - "The test fails if the fixture file is absent or missing any of its four keys, rather than skipping silently."
    - "A fixture README records where the payload came from, that it contains only throwaway UAT-workspace data, and what must be re-verified if it is ever replaced."
    - statement: "The committed payload contains no third party's personal data, as confirmed by the operator's decode-and-inspect gate in plan 16-04."
      verification: backstop
  artifacts:
    - apps/api/src/modules/webhooks/__tests__/webhooks-signature-replay.test.ts
    - apps/api/src/modules/webhooks/__tests__/fixtures/README.md
  key_links:
    - "The frozen clock -> the fixture's permanence: the captured timestamp header is signed and cannot be regenerated, so the only stale-proof mechanism that does not touch signed bytes is holding the clock at the fixture's own instant. An inflated tolerance override would instead weaken the very freshness check the test is meant to exercise."
    - "The fixture's public key -> the provisioned test endpoint: the signature covers the body and timestamp only, not the URL, so the test may provision any fresh endpoint as long as its stored public key is the fixture's."
    - "This test -> the flag carried since Phase 5: worker-layer attribution was tested; the HTTP signature layer never was. This file is what closes it, permanently and in CI."
  prohibitions:
    - statement: MUST NOT commit or retain a fixture containing a real person's address or any third party's data — the fixture's only legitimate content is the designated throwaway UAT recipient's own events.
      status: flagged
      verification: unverified
    - statement: MUST NOT make the test pass by widening the freshness tolerance, by regenerating the signature, or by editing the captured bytes — any of those would silently convert a real-payload test into a synthetic one.
      status: flagged
      verification: unverified
    - statement: MUST NOT let the test skip or pass when the fixture is missing — an absent fixture is a failure, not a no-op.
      status: flagged
      verification: unverified
---

<objective>
Turn plan 16-04's live capture into a permanent asset: a committed CI test that replays a genuinely SendGrid-signed payload through the full HTTP stack, proves the signature verifies, proves a one-byte mutation is rejected, and proves a byte-identical redelivery is deduplicated (**D-10**, **D-11**, **D-12**). This closes the gap carried since Phase 5 — worker-layer attribution has been tested since 05-13; the HTTP signature layer never has.

This plan is autonomous only because plan 16-04's blocking checkpoint left the inspected fixture on disk.

Purpose: the live UAT-03/UAT-04 pass proves the system works today, once. A committed test proves it keeps working on every future change, without another operator session and without another real send.
Output: one test file, one fixture README.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-live-sendgrid-verification/16-CONTEXT.md
@.planning/phases/16-live-sendgrid-verification/16-RESEARCH.md
@.planning/phases/16-live-sendgrid-verification/16-PATTERNS.md
@.planning/phases/16-live-sendgrid-verification/16-04-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols and paths created by THIS plan:

- File: `apps/api/src/modules/webhooks/__tests__/webhooks-signature-replay.test.ts`
- File: `apps/api/src/modules/webhooks/__tests__/fixtures/README.md`
- Test suite name: the replay describe block, covering fixture-integrity, accept, reject and dedup cases

## Planner assumptions (specless probe, no SPEC.md for this phase)

Edge-probe rows **UAT-03** and **UAT-04** returned `unresolved` (English-keyed classifier, Russian REQUIREMENTS.md). Authored here rather than dropped:

- A captured payload's value as evidence decays to zero the moment its bytes are edited; the test must therefore prove it never edits them, not merely avoid doing so.
- Fixture absence is a hard failure. A test that skips when its fixture is missing reports green on a codebase where the asset was deleted.
- The dedup assertion in CI mirrors the live one's polarity: the enqueue path sees two deliveries, `send_events` holds one row.
- The clock is frozen only around the request, never for the whole file, so unrelated timing in setup and teardown behaves normally.

<tasks>

<task type="auto">
  <name>Task 1: Fixture integrity guard + fixture README</name>
  <files>apps/api/src/modules/webhooks/__tests__/webhooks-signature-replay.test.ts, apps/api/src/modules/webhooks/__tests__/fixtures/README.md</files>
  <read_first>
    - apps/api/src/modules/webhooks/__tests__/fixtures/uat-signed-payload.json (as saved by plan 16-04's checkpoint — the four keys and their real values; confirm the base64 decodes and the decoded content is the expected event batch shape)
    - apps/api/src/modules/webhooks/__tests__/webhooks-signature.test.ts (full file — the existing suite's structure, its inline fixture constants, and the JSON import form used elsewhere in this codebase)
    - .planning/phases/16-live-sendgrid-verification/16-04-SUMMARY.md (what the operator recorded about the captured batch: which send, which event type, which recipient, and the inspection outcome)
    - apps/api/src/modules/webhooks/signature-verify.ts (that the signature covers body plus timestamp only — the README must state this, because it is why the fixture is portable across endpoints)
  </read_first>
  <action>
Create `apps/api/src/modules/webhooks/__tests__/webhooks-signature-replay.test.ts` and open it with a fixture-integrity block that runs before the behavioural cases.

That block loads the fixture by direct JSON import (the form PATTERNS.md records) and asserts: the four keys `rawBodyBase64`, `signature`, `timestamp` and `publicKey` are all present and non-empty; the base64 field decodes without throwing; the decoded content parses as a JSON array of events; and the timestamp field is a numeric string. If the fixture is absent the import fails and the suite fails — do not guard the import with a try/catch and do not call any skip helper, because an absent fixture must be a red test, not a green no-op.

Create `apps/api/src/modules/webhooks/__tests__/fixtures/README.md` recording: that the payload was captured from a real SendGrid Event Webhook delivery to the Phase 16 UAT workspace on the production deployment; the UTC date of capture; that it contains only the designated throwaway UAT recipient's own events and was decoded and inspected by the operator before commit (plan 16-04's blocking gate); that the signature covers the raw body and the timestamp header only, not the URL, which is why the test can provision any endpoint whose stored public key is the fixture's; and that replacing this fixture requires repeating that capture-and-inspect procedure, never hand-editing or re-signing. Record no credential and no recipient address in the README itself.
  </action>
  <verify>
    <automated>npx vitest run --root apps/api src/modules/webhooks/__tests__/webhooks-signature-replay.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The vitest invocation exits 0 with the fixture-integrity cases passing.
    - `apps/api/src/modules/webhooks/__tests__/webhooks-signature-replay.test.ts` imports the fixture without a try/catch and calls no skip helper.
    - Temporarily renaming the fixture file makes the suite exit non-zero (verify, then restore).
    - `apps/api/src/modules/webhooks/__tests__/fixtures/README.md` exists and states the capture provenance, the inspection gate and the no-hand-editing rule.
    - `grep -nEi 'SG\.[A-Za-z0-9_-]{10,}' apps/api/src/modules/webhooks/__tests__/fixtures/README.md` returns no match.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>The fixture cannot silently disappear, and anyone who finds it in two years knows where it came from and what replacing it costs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full-HTTP-stack replay test — accept, reject, deduplicate</name>
  <files>apps/api/src/modules/webhooks/__tests__/webhooks-signature-replay.test.ts</files>
  <read_first>
    - apps/api/src/modules/webhooks/__tests__/webhooks-signature.test.ts (full file — copy the `signUp`, `createWorkspace`, `provisionEndpoint` and `freshWorkspace` helpers verbatim, and reuse the `app.inject` request shape and the fake-timers pattern that holds the clock at the fixture's own timestamp)
    - apps/api/src/modules/webhooks/webhooks.routes.ts (the raw-body content-type override, the exact header names, and the fail-closed response the rejected case asserts on)
    - apps/api/src/modules/webhooks/enqueue.ts (the queue handle the analog spies on — the dedup case asserts the enqueue path saw both deliveries)
    - packages/db/migrations/0057_send_events_dedup_rebase.sql (the four dedup-key columns the send_events assertion filters on)
    - apps/api/src/test/db-fixture.ts (`ensureTestDbMigrated` and `getTestDatabaseUrl` — the ephemeral test-database contract this suite runs under)
    - apps/api/src/env.ts (confirm the freshness tolerance's parse site; the test must NOT override it — the frozen clock is the chosen mechanism and the choice is recorded in a code comment)
  </read_first>
  <behavior>
    - Posting the fixture's decoded bytes with both captured signature headers, against a freshly provisioned endpoint whose stored public key is the fixture's, is accepted with the route's success status.
    - Posting the same bytes with one byte mutated, and the same captured headers, is rejected with the route's fail-closed response.
    - The rejected request enqueues nothing.
    - Posting the identical bytes twice produces exactly one send_events row for the batch's dedup key.
    - Posting the identical bytes twice invokes the enqueue path twice — the second delivery is ingested, it is merely deduplicated downstream.
    - Posting the fixture against an endpoint whose stored public key is a different key is rejected.
    - The clock is held at the fixture's own captured timestamp for the duration of each request and released afterwards.
  </behavior>
  <action>
Extend `apps/api/src/modules/webhooks/__tests__/webhooks-signature-replay.test.ts` with the behavioural cases, reusing the analog suite's helpers verbatim rather than reimplementing workspace and endpoint setup.

Freshness mechanism — **make this choice explicit and record it in a code comment**: hold the clock at the fixture's own captured timestamp using the analog's fake-timers pattern, scoped tightly around each `app.inject` call and released in a `finally`. Do NOT set or override the freshness tolerance variable in this test. RESEARCH.md Pitfall 1 required picking one of the two mechanisms explicitly; this is the pick, and the reason is that widening the tolerance would disable the very freshness gate the suite exercises, while the frozen clock leaves both gates fully armed and never touches signed bytes.

Provision the test endpoint with the fixture's `publicKey`. The signature covers the body and the timestamp header only, so the path token may be freshly generated — do not attempt to reconstruct the capturing workspace's token.

For the dedup case, post the identical bytes twice and then assert both layers with the polarity RESEARCH.md Pitfall 4 establishes: exactly one `send_events` row for the batch's `(workspace_id, send_id, event_type, occurred_at)` key, and two invocations of the enqueue path. Do not assert that the enqueue count is unchanged — the ingestion layer correctly sees both deliveries, and asserting otherwise reports a false failure on a correct system.

Derive the dedup key values from the decoded fixture rather than hard-coding them, so a future fixture replacement does not silently make the assertion vacuous.
  </action>
  <verify>
    <automated>npx vitest run --root apps/api src/modules/webhooks/__tests__/webhooks-signature-replay.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The vitest invocation exits 0 covering all seven `<behavior>` cases.
    - The test file contains a code comment naming the frozen-clock choice and why the tolerance override was rejected.
    - The test file sets no freshness-tolerance environment value anywhere.
    - The dedup case asserts an enqueue invocation count of 2 and a send_events row count of 1.
    - The dedup key values used in the assertion are read from the decoded fixture, not written as literals.
    - `npx vitest run --root apps/api src/modules/webhooks/__tests__/webhooks-signature.test.ts` still exits 0.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>The HTTP signature layer has a real-payload regression test in CI, and it will still pass a year from now without anyone touching it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed repository → every developer and CI runner | A real signed payload with real event data is distributed to everyone with repository access |
| test HTTP client → application server | The test drives the same untrusted-input path the public endpoint exposes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-20 | Information Disclosure | Committed fixture distributed with the repository | high | mitigate | The fixture was decoded and inspected under plan 16-04's blocking gate and contains only the designated throwaway UAT recipient's events; the fixture README records the provenance and the inspection gate so a future replacement cannot skip it |
| T-16-21 | Tampering | A future change silently weakening the freshness gate to keep the test green | high | mitigate | The test sets no tolerance value and an acceptance criterion asserts it does not; the frozen-clock mechanism and the rejected alternative are recorded in a code comment at the point of change |
| T-16-22 | Repudiation | A vacuous test that greens on a deleted or malformed fixture | medium | mitigate | The fixture-integrity block imports without a try/catch and calls no skip helper; an acceptance criterion requires that renaming the fixture turns the suite red |
| T-16-23 | Spoofing | A verifier that accepts everything reading as a pass | high | mitigate | Two negative cases are mandatory — one-byte mutation and wrong public key — so acceptance alone cannot green the suite |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed (RESEARCH.md Package Legitimacy Audit: none); any deviation requires the legitimacy gate plus a blocking human checkpoint before install |
</threat_model>

<verification>
- `npx vitest run --root apps/api src/modules/webhooks/__tests__/webhooks-signature-replay.test.ts` exits 0.
- `npx vitest run --root apps/api src/modules/webhooks/__tests__/webhooks-signature.test.ts` exits 0 — the pre-existing suite is unaffected.
- `npm run lint` and `npm run check:root-hygiene` exit 0.
- `npm test` exits 0.
- Renaming the fixture turns the new suite red; restoring it turns it green again.
</verification>

<success_criteria>
- The Phase 5 HTTP-signature-replay gap is closed by a committed test running on every CI pass.
- The test proves acceptance, rejection and deduplication, and cannot go stale.
- Nothing in the test weakens a security gate to stay green.
</success_criteria>

<output>
Create `.planning/phases/16-live-sendgrid-verification/16-05-SUMMARY.md` when done, recording the freshness mechanism chosen, the case count, and confirmation that the fixture-absent case turns the suite red.
</output>
