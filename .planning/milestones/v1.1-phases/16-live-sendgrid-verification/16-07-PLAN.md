---
phase: 16-live-sendgrid-verification
plan: 07
type: execute
wave: 5
depends_on: [16-05, 16-06]
files_modified:
  - .planning/phases/16-live-sendgrid-verification/16-UAT-REPORT.md
  - docs/runbooks/uat-live-sendgrid.md
  - SPECIFICATION.md
autonomous: false
requirements: [UAT-01, UAT-02, UAT-03, UAT-04, UAT-05]

must_haves:
  truths:
    - "One document maps each of UAT-01 through UAT-05 to its live evidence: the UTC timestamp, the send and message identifiers, the exact verification command run, that command's output, and the checkpoint approval."
    - "Every claim in that document cites a scripted query result or an operator-observed artifact; no row is recorded as passed on an impression."
    - "Any UAT test that did not pass on its first live attempt is recorded with what failed, which gap-closure plan addressed it, and the rerun that passed — not silently overwritten by the passing run."
    - "The runbook contains a standing post-deploy smoke procedure using the retained UAT workspace, short enough to run after any risky release."
    - "Neither Phase 16 seam is active in production after the phase closes: the endpoint override is unset, the raw-capture workspace is unset, and the worker's boot warning is absent."
    - "The fault proxy is not running and its override file is not part of any deploy path."
    - "The UAT workspace still exists with its contacts, campaign, flow and webhook subscription intact, and is documented as the standing canary."
    - statement: "The webhook timestamp tolerance is at its original production value, verified by reading the effective value back rather than by assuming the restore step ran."
      verification: backstop
  artifacts:
    - .planning/phases/16-live-sendgrid-verification/16-UAT-REPORT.md
    - docs/runbooks/uat-live-sendgrid.md
  key_links:
    - "The five plan SUMMARYs -> the UAT report: the report is a compilation, not a re-narration. A claim that appears in the report but in no SUMMARY has no evidence behind it and must be removed or re-earned."
    - "The teardown -> production safety: both seams are default-off by design, but only if nothing is left set. The boot warning is the detector; this checkpoint is the last moment anyone is deliberately looking at it."
    - "The retained UAT workspace (D-15) -> future releases: the value of keeping it is only realised if the smoke procedure is written down while the setup is still fresh."
  prohibitions:
    - statement: MUST NOT record a UAT test as passed without citing the scripted query output or the operator-observed artifact that proves it — an eyeball impression is not evidence.
      status: flagged
      verification: unverified
    - statement: MUST NOT omit or overwrite a failed first attempt — the record of what failed and how it was closed is the point of the document.
      status: flagged
      verification: unverified
    - statement: MUST NOT close the phase with either seam still active, the fault proxy still running, or a temporarily widened webhook timestamp tolerance still in place.
      status: flagged
      verification: unverified
    - statement: MUST NOT delete or anonymise the UAT workspace — it is retained deliberately as the standing canary and is fenced by the platform's existing tenant isolation.
      status: flagged
      verification: unverified
---

<objective>
Close the phase: compile the single evidence artifact an auditor reads (**D-14**), write the standing-canary smoke procedure that makes keeping the UAT workspace worthwhile (**D-15**), and verify by blocking checkpoint that production carries no residue of the UAT session — no endpoint override, no raw-capture workspace, no fault proxy, no widened freshness tolerance.

**Execution-mode note:** per **D-13** this plan ends in a blocking real-host checkpoint, deliberately overriding `workflow.human_verify_mode: end-of-phase`. Teardown verification is the one checkpoint whose absence would be most costly: both seams are safe by default, and unsafe only if left set.

Purpose: v1.1's release barrier is not "the tests ran" but "someone can show that they ran, against the real provider, and that nothing dangerous was left behind". Phase 16 completes only at five live passes (**D-16**).
Output: the UAT report, the canary procedure, and a verified-clean production environment.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-live-sendgrid-verification/16-CONTEXT.md
@.planning/phases/16-live-sendgrid-verification/16-RESEARCH.md
@.planning/phases/16-live-sendgrid-verification/16-01-SUMMARY.md
@.planning/phases/16-live-sendgrid-verification/16-02-SUMMARY.md
@.planning/phases/16-live-sendgrid-verification/16-04-SUMMARY.md
@.planning/phases/16-live-sendgrid-verification/16-05-SUMMARY.md
@.planning/phases/16-live-sendgrid-verification/16-06-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols and paths created by THIS plan:

- File: `.planning/phases/16-live-sendgrid-verification/16-UAT-REPORT.md`
- New runbook section in `docs/runbooks/uat-live-sendgrid.md`: standing canary post-deploy smoke procedure, and the teardown verification checklist
- SPECIFICATION.md closing entries recording that both Phase 16 seams ship default-off

## Planner assumptions (specless probe, no SPEC.md for this phase)

All five edge-probe rows — **UAT-01** through **UAT-05** — returned `unresolved` (the classifier is English-keyed; this project's REQUIREMENTS.md is Russian, a known recurring condition). Every one is surfaced here as an explicit flagged assumption rather than dropped:

- **UAT-01:** live BYO-key send through a Dynamic Template — asserted by real-inbox arrival with both substitutions rendered (plan 16-01).
- **UAT-02:** four live event types — asserted by the four types observed and attributed to a campaign send and a flow-step send (plan 16-02).
- **UAT-03:** signature verification on a real signed payload through the full HTTP stack — asserted in both directions, accepted and rejected, live and in CI (plans 16-04, 16-05).
- **UAT-04:** dedup of a redelivered event — asserted at both the row and aggregate layers, live and in CI (plans 16-04, 16-05).
- **UAT-05:** behaviour on a real 429 and a real transient error — asserted by mailbox copy counts, not by database status alone (plan 16-06).

Additionally authored here: the phase is complete only at five live passes, and a failed first attempt is part of the record rather than something a rerun erases.

<tasks>

<task type="auto">
  <name>Task 1: 16-UAT-REPORT.md — the single evidence artifact</name>
  <files>.planning/phases/16-live-sendgrid-verification/16-UAT-REPORT.md</files>
  <read_first>
    - .planning/phases/16-live-sendgrid-verification/16-01-SUMMARY.md through 16-06-SUMMARY.md (every recorded identifier, command output, checkpoint approval and deviation — the report compiles these, it does not re-narrate them)
    - .planning/REQUIREMENTS.md (the exact Russian text of UAT-01 through UAT-05 — quote each requirement verbatim so the report maps evidence to the requirement as written, not to a paraphrase)
    - .planning/phases/16-live-sendgrid-verification/16-CONTEXT.md (D-14's "one place an auditor reads" intent, and D-16's failure protocol whose outcomes the report must record)
    - .planning/phases/14-deployment-database-durability/14-11-SUMMARY.md (how a real-host checkpoint's evidence was recorded in this project, including the discipline of recording what was NOT measured rather than inventing it)
  </read_first>
  <action>
Create `.planning/phases/16-live-sendgrid-verification/16-UAT-REPORT.md`.

Open with a status line stating how many of the five passed live and on what date, and a one-paragraph statement of what environment the tests ran against: the production VPS, a dedicated UAT workspace, a second API key on the real SendGrid account, and the operator's own mailboxes.

Then one section per requirement, UAT-01 through UAT-05, each containing: the requirement quoted verbatim from REQUIREMENTS.md; the plan and checkpoint that verified it; the UTC timestamp of the live action; the identifiers recorded in that plan's SUMMARY (workspace id, send ids, SendGrid message ids, event types, `occurred_at` values, HTTP statuses, mailbox copy counts — whichever that requirement produced); the exact verification command that was run, quoted; that command's output or exit status; and the checkpoint approval note.

Add a section recording the assumptions that were confirmed or refuted live, drawing on RESEARCH.md's Assumptions Log: whether the chosen bounce target produced a genuine hard bounce, whether the capture was retrievable inside the freshness window or the tolerance fallback was needed, and whether the dependency-free proxy sufficed. Record each as confirmed or refuted with the evidence, not as an open question.

Add a section for any test that did not pass on its first live attempt: what failed, which gap-closure plan closed it, the deploy that shipped the fix, and the rerun that passed. If every test passed first time, state that explicitly rather than omitting the section — an absent section is ambiguous between "none" and "not recorded".

Close with a residual-items section: anything observed during the session that is real but out of this phase's scope, recorded for the milestone's own record rather than fixed here.

Record no credential, key, token or public-key value anywhere in the document. Recipient addresses are the operator's own and may appear; no other person's address may.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-live-sendgrid-verification/16-UAT-REPORT.md &amp;&amp; grep -c 'UAT-0' .planning/phases/16-live-sendgrid-verification/16-UAT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/16-live-sendgrid-verification/16-UAT-REPORT.md` exists and contains a section for each of UAT-01, UAT-02, UAT-03, UAT-04 and UAT-05.
    - Each of the five sections quotes its requirement verbatim from REQUIREMENTS.md and contains a UTC timestamp, at least one recorded identifier, and a quoted verification command with its result.
    - The document contains an assumptions section recording the bounce-target, capture-window and proxy-sufficiency outcomes as confirmed or refuted.
    - The document contains a first-attempt-failures section, stating "none" explicitly if there were none.
    - `grep -nEi 'SG\.[A-Za-z0-9_-]{10,}|BEGIN [A-Z ]*PRIVATE KEY' .planning/phases/16-live-sendgrid-verification/16-UAT-REPORT.md` returns no match.
  </acceptance_criteria>
  <done>An auditor reading one file can see what was tested, against what, when, with which command, and with what result.</done>
</task>

<task type="auto">
  <name>Task 2: Standing-canary smoke procedure + teardown checklist + SPECIFICATION closing entries</name>
  <files>docs/runbooks/uat-live-sendgrid.md, SPECIFICATION.md</files>
  <read_first>
    - docs/runbooks/uat-live-sendgrid.md (the full file as built by plans 16-01, 16-02, 16-04 and 16-06 — append two sections; do not restructure what is there)
    - docs/runbooks/deploy-and-rollback.md (the deploy procedure the canary smoke test attaches to, so the smoke step is described as part of a release rather than as a separate ritual)
    - .planning/phases/16-live-sendgrid-verification/16-06-SUMMARY.md (the teardown steps as actually performed, including anything that differed from the runbook's prediction)
    - SPECIFICATION.md sections 3, 5 and 6 (the entries plan 16-03 added for both environment variables — confirm they state the production default correctly now that the seams have been exercised)
    - .planning/phases/16-live-sendgrid-verification/16-04-SUMMARY.md (whether the webhook timestamp tolerance fallback was used, and how it was restored — the teardown checklist must include reading the effective value back if it was)
  </read_first>
  <action>
Append two sections to `docs/runbooks/uat-live-sendgrid.md`.

**Section: standing canary smoke test (D-15).** A short, repeatable procedure using the retained UAT workspace: trigger one send to the operator's mailbox, confirm arrival, run the `send-attribution` assert expecting a terminal status and a delivered event. State the intended trigger — after any risky deploy — and keep it short enough that skipping it is never justified by time. Record that the UAT workspace, its BYO key, its Dynamic Template, its contacts and its webhook subscription are deliberately retained for this purpose and must not be deleted or anonymised.

**Section: teardown verification checklist.** An explicit list the operator walks with an expected observation for each item: the endpoint override is unset and the worker's boot log carries no override warning; the raw-capture workspace variable is unset and no capture line appears for a fresh delivery; the fault proxy service is not in the running stack and its port does not answer from off-host; the webhook timestamp tolerance reads back at its original production value; and the production compose file is unchanged. State that each item is verified by observation, not by having run the step that should have caused it.

Then confirm SPECIFICATION.md's section 3, 5 and 6 entries for both Phase 16 variables describe the shipped state accurately — that both default off, what the production default of the endpoint is, and that the UAT compose override is a session artifact not part of any deploy. Correct any entry that plan 16-03 wrote before the seams were exercised and that the live session showed to be imprecise. Add nothing to section 8: no new library was introduced.
  </action>
  <verify>
    <automated>npm run check:spec-env-coverage &amp;&amp; npm run check:runbook-coverage &amp;&amp; npm run verify:prod-compose</automated>
  </verify>
  <acceptance_criteria>
    - `docs/runbooks/uat-live-sendgrid.md` contains a standing-canary smoke-test section and a teardown verification checklist section.
    - The teardown checklist contains five items, each with an expected observation.
    - The canary section states that the UAT workspace and its SendGrid artifacts are retained and must not be deleted.
    - `SPECIFICATION.md` still contains both Phase 16 variable entries and states that each defaults off.
    - `npm run check:spec-env-coverage`, `npm run check:runbook-coverage`, `npm run verify:prod-compose` and `npm run check:root-hygiene` all exit 0.
  </acceptance_criteria>
  <done>Keeping the UAT workspace pays off on the next release, and the teardown is verified by observation rather than by intention.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Teardown verification and canary confirmation on the production host</name>
  <precondition>All five UAT checkpoints (plans 16-01, 16-02, 16-04, 16-06) have been approved. Per D-16 the phase completes only at five live passes — halt and report if any remains open.</precondition>
  <read_first>
    - docs/runbooks/uat-live-sendgrid.md (the teardown verification checklist and the standing-canary smoke procedure written by Task 2 — the checkpoint is walked from these two sections)
    - .planning/phases/16-live-sendgrid-verification/16-UAT-REPORT.md (as written by Task 1 — the operator corroborates each of its five sections against what they observed)
    - .planning/phases/16-live-sendgrid-verification/16-04-SUMMARY.md and 16-06-SUMMARY.md (whether the tolerance fallback was used, and what the UAT-05 teardown already removed — the checklist re-verifies rather than repeats)
  </read_first>
  <what-built>
`16-UAT-REPORT.md` — the compiled evidence artifact mapping each of UAT-01 through UAT-05 to its live proof. A standing-canary smoke procedure using the retained UAT workspace, and a five-item teardown verification checklist with an expected observation for each item.

What automation cannot establish: no check in this repository can see the production host's environment file, its running containers, or its worker's current boot log. Both Phase 16 seams are safe by default and unsafe only if left set, and the worker's boot warning is the detector — this checkpoint is the last moment anyone is deliberately looking at it. A forgotten endpoint override would route real tenant mail through a proxy that no longer exists.
  </what-built>
  <how-to-verify>
Walk the teardown verification checklist in `docs/runbooks/uat-live-sendgrid.md`, verifying each item by observation rather than by recalling that the step was performed:

1. The endpoint override variable is absent (or empty) in the production env file, and the current worker boot log carries no override warning. Restart the worker if needed to see a fresh boot.
2. The raw-capture workspace variable is absent (or empty), and a fresh webhook delivery produces no capture line under the marker grep.
3. The fault proxy service is not in the running stack, and its port does not answer from off-host.
4. The webhook timestamp tolerance reads back at its original production value — read the effective value, do not assume the restore ran.
5. The production compose file on the host matches the deployed SHA's version, with no UAT override merged into it.
6. The UAT workspace still exists with its contacts, campaign, flow and webhook subscription intact, and is recorded as the standing canary.
7. Run the standing-canary smoke procedure once, end to end, to confirm it works as written — the first person to rely on it will be under release pressure.
8. Read `16-UAT-REPORT.md` and confirm every one of the five sections cites evidence that matches what you personally observed during that test's checkpoint. Report any row whose evidence you cannot corroborate.
9. Confirm the report records any first-attempt failure and its closure, or states explicitly that there were none.
  </how-to-verify>
  <acceptance_criteria>
    - All five teardown checklist items verified by observation, with the observed value or state reported for each.
    - The standing-canary smoke procedure was run once and worked as written.
    - The UAT workspace and its SendGrid artifacts are intact and retained.
    - The operator corroborates all five UAT report sections against what they observed at each checkpoint.
    - No credential value appears in the UAT report or the runbook.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the five observed teardown states and the canary smoke result, or describe what remains active.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT session configuration → ongoing production state | Session-scoped seams could persist into normal operation |
| retained UAT workspace → production tenant data | A canary workspace lives alongside real tenants indefinitely |
| committed evidence artifact → repository readers | Identifiers and addresses from a live session are recorded permanently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-30 | Tampering / Denial of Service | Endpoint override surviving the phase | high | mitigate | Verified by observation of a fresh worker boot log rather than by recalling the unset step; the checklist item requires reporting the observed state, and the boot warning remains the standing detector afterwards |
| T-16-31 | Information Disclosure | Raw-capture variable surviving the phase | high | mitigate | Verified by observing that a fresh delivery produces no capture line under the marker grep, not merely that the variable looks absent |
| T-16-32 | Elevation of Privilege | Fault proxy or its unauthenticated control endpoint surviving the phase | high | mitigate | Verified two ways: the service is absent from the running stack, and its port does not answer from off-host; the override file is not referenced by any deploy path |
| T-16-33 | Elevation of Privilege | Widened webhook timestamp tolerance surviving the phase | high | mitigate | The effective value is read back rather than assumed; a widened window would enlarge the replay opportunity for any captured payload |
| T-16-34 | Information Disclosure | Evidence artifact carrying credentials or third-party addresses | medium | mitigate | A grep acceptance criterion rejects key and private-key shapes; only the operator's own addresses are permitted, and the report records identifiers rather than secrets |
| T-16-35 | Spoofing | The retained canary workspace being mistaken for or used as a real tenant | low | mitigate | The workspace stays behind the platform's existing tenant isolation (Phase 10) and is documented in the runbook as the canary; no special privilege is granted to it |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed (RESEARCH.md Package Legitimacy Audit: none); any deviation requires the legitimacy gate plus a blocking human checkpoint before install |
</threat_model>

<verification>
- `npm run check:spec-env-coverage`, `npm run check:runbook-coverage`, `npm run verify:prod-compose`, `npm run check:root-hygiene`, `npm run lint` all exit 0.
- `npm test` (full aggregate) exits 0.
- `.planning/phases/16-live-sendgrid-verification/16-UAT-REPORT.md` contains all five requirement sections with cited evidence.
- The Task 3 blocking checkpoint is approved with all five teardown states observed.

<human-check>
Live operator verification:
1. Neither Phase 16 seam is active: no override warning in a fresh worker boot, no capture line for a fresh delivery.
2. The fault proxy is gone and does not answer from off-host.
3. The webhook timestamp tolerance reads back at its original production value.
4. The retained UAT workspace works as a canary — the smoke procedure was run once end to end.
5. Every UAT report section matches what the operator observed at its checkpoint.
</human-check>
</verification>

<success_criteria>
- All five UAT requirements are live-passed and documented in one auditable artifact (D-16: the phase completes only at five live passes).
- Production carries no residue of the UAT session, verified by observation.
- The UAT workspace is retained and the smoke procedure that makes it useful is written and proven.
</success_criteria>

<output>
Create `.planning/phases/16-live-sendgrid-verification/16-07-SUMMARY.md` when done, recording the five observed teardown states, the canary smoke result, and the final five-of-five UAT status.
</output>
