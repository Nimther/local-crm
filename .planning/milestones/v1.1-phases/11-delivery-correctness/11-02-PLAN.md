---
phase: 11-delivery-correctness
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - packages/db/migrations/0047_send_status_reconciling.sql
  - packages/db/migrations/0048_send_status_unknown.sql
  - packages/db/migrations/0049_send_reconciliation_columns.sql
  - packages/db/migrations/0050_send_reconciler_runs.sql
  - packages/db/migrations/meta/_journal.json
  - packages/db/src/schema/sends.ts
  - packages/db/src/schema/send-reconciler-runs.ts
  - packages/db/src/index.ts
  - packages/db/scripts/audit-sends-history.ts
  - packages/db/package.json
  - packages/db/src/__tests__/send-status-enum-parity.test.ts
  - apps/worker/src/queues/__tests__/rollup-enum-migration-invariant.test.ts
  - SPECIFICATION.md
autonomous: true
requirements: [DLV-02, DLV-03, DLV-09]

must_haves:
  truths:
    - "`SELECT unnest(enum_range(NULL::send_status))` against a migrated database returns exactly six values including `reconciling` and `unknown` (D-02, DB-08 expand half)."
    - "`sends` has columns `reconciling_since timestamptz`, `dispatched_at timestamptz`, and `dispatch_duration_ms integer`, all nullable — send duration is SQL-queryable before any metrics infrastructure exists (DLV-09, D-17)."
    - "`npm run lint:migrations` exits 0 — neither enum-add migration references its own new literal, so the `enum-add-value-used-same-file` rule passes (Pitfall 2)."
    - "Applying migrations 0047-0050 to a database holding pre-existing `sends` rows leaves every `workspace_daily_rollup` count byte-identical — no historical row is reclassified (Pitfall 2, locked)."
    - "`SEND_STATUSES` from `@mega-crm/delivery-core` equals the live `send_status` enum values in the same order-insensitive set — doc, code, and database cannot drift apart (DLV-01 support)."
    - "`npm run db:audit-sends-history` performs zero writes and reports the counts a reviewer needs before the enum ships: rows per status, `failed` rows with no matching `send_events`, and rows whose `id` predates deterministic derivation."
    - statement: "Send duration is available as a metric (DLV-09, ROADMAP SC5) — this plan lands the queryable columns; 11-06 lands the writes that populate them."
      verification: backstop
  artifacts:
    - packages/db/migrations/0047_send_status_reconciling.sql
    - packages/db/migrations/0048_send_status_unknown.sql
    - packages/db/migrations/0049_send_reconciliation_columns.sql
    - packages/db/migrations/0050_send_reconciler_runs.sql
    - packages/db/src/schema/send-reconciler-runs.ts
    - packages/db/scripts/audit-sends-history.ts
  key_links:
    - "`sends_status_queued_at_idx` is the index the reconciler's cross-workspace discovery scan (11-03) depends on; without it the scan is a sequential scan of the whole ledger."
    - "`send_reconciler_runs` is the health row 11-08's watchdog reads from a different process."
    - "`sendStatusEnum` in Drizzle must stay equal to `SEND_STATUSES` in delivery-core — enforced by `send-status-enum-parity.test.ts`."
  prohibitions:
    - "No migration in this plan may UPDATE, DELETE, or otherwise reclassify an existing `sends` row; the expand step adds vocabulary only."
    - "A send with no delivery evidence must never be described, counted, or displayed to a tenant as `sent` or delivered."
---

<objective>
Ship the expand half of the schema change: two standalone `send_status` enum-value migrations, the additive `sends` columns that make send duration and reconciliation age queryable, and the reconciler's health-row table — plus the read-only history audit the ROADMAP requires before the enum ships.

Purpose: DB-08's expand/contract rule and Pitfall 2 both require the new enum values to exist in the database *before* any code references them, and require that landing them changes no historical data. Every later plan in this phase writes or reads one of these columns.
Output: migrations 0047-0050 applied, Drizzle schema updated, a read-only audit script, and two regression tests pinning "nothing historical moved".
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-delivery-correctness/11-CONTEXT.md
@.planning/phases/11-delivery-correctness/11-RESEARCH.md
@.planning/phases/11-delivery-correctness/11-PATTERNS.md
@.planning/phases/11-delivery-correctness/11-01-SUMMARY.md
</context>

## Artifacts this phase produces

Created by **this plan** (new — not pre-existing):

- Migrations `0047_send_status_reconciling.sql`, `0048_send_status_unknown.sql`, `0049_send_reconciliation_columns.sql`, `0050_send_reconciler_runs.sql` (+ their `meta/_journal.json` entries)
- `send_status` enum values `reconciling`, `unknown`
- `sends` columns `reconciling_since`, `dispatched_at`, `dispatch_duration_ms`
- Index `sends_status_queued_at_idx`, index `sends_reconciling_since_idx`
- Table `send_reconciler_runs` (singleton, `id = 1`)
- `packages/db/src/schema/send-reconciler-runs.ts` exporting `sendReconcilerRuns`
- `packages/db/scripts/audit-sends-history.ts` and npm script `db:audit-sends-history`
- `packages/db/src/__tests__/send-status-enum-parity.test.ts`
- `apps/worker/src/queues/__tests__/rollup-enum-migration-invariant.test.ts`

<tasks>

<task type="auto">
  <name>Task 1: Read-only history audit before the enum ships</name>
  <files>packages/db/scripts/audit-sends-history.ts, packages/db/package.json, package.json</files>
  <read_first>
    - packages/db/scripts/relocate-default-partition-rows.ts — the existing operator-invoked CLI in this directory: how it constructs its own `Pool` from `DATABASE_URL`, how it prints results, how it is registered as an npm script
    - packages/db/package.json — the `relocate:default-partition-rows` script entry whose `tsx` invocation shape this copies
    - packages/db/src/schema/sends.ts — the current columns available to the audit (`status`, `queued_at`, `sent_at`, `id`, `kind`, `campaign_id`, `flow_run_id`)
    - packages/db/src/schema/send-events.ts — `send_id` is nullable; the audit's "orphaned evidence" count depends on that
  </read_first>
  <action>
    Create `packages/db/scripts/audit-sends-history.ts`, a strictly read-only operator CLI. It must contain no `INSERT`, `UPDATE`, `DELETE`, `ALTER`, or `CREATE` statement and must open its connection inside a transaction it always rolls back, so a copy-paste mistake in a future edit cannot write.

    It reports, as a plain-text table to stdout:
    1. Row count per `sends.status`, plus overall total.
    2. Count of `status = 'failed'` rows with no matching row in `send_events` (join on `send_events.send_id = sends.id`) — this is the ROADMAP's "how many `failed` rows have no matching `send_events`" question, i.e. the population that would have become `reconciling`/`unknown` had this phase existed earlier.
    3. Count of `status = 'failed'` rows that DO have matching `send_events` — sends recorded failed while the provider emitted events for them.
    4. Count of `sends` rows per `kind`.
    5. Oldest and newest `queued_at`.
    6. Count of `send_events` rows with a null `send_id` (evidence that can never be correlated — the pre-existing orphan population).

    Print a closing line stating explicitly that this run made no writes and that reclassifying any of the reported rows is out of scope for this phase (Pitfall 2).

    Connect with its own `new Pool({ connectionString: process.env.DATABASE_URL })` — do not import `@mega-crm/tenant-context` (this is a platform-level operator read, exactly like `relocate-default-partition-rows.ts`). Attach a `pool.on("error", ...)` listener mirroring that file. Exit non-zero with a clear message when `DATABASE_URL` is absent.

    Register `"db:audit-sends-history": "tsx scripts/audit-sends-history.ts"` in `packages/db/package.json` and a root passthrough `"db:audit-sends-history": "npm run db:audit-sends-history -w packages/db"` in the root `package.json`, both placed next to the existing `relocate:default-partition-rows` / `db:migrate` entries.
  </action>
  <verify>
    <automated>npm run db:audit-sends-history && node -e "const s=require('fs').readFileSync('packages/db/scripts/audit-sends-history.ts','utf8').replace(/\/\*[\s\S]*?\*\//g,'').split('\n').filter(l=>!/^\s*(\/\/|\*)/.test(l)).join('\n'); for (const w of ['INSERT ','UPDATE ','DELETE ','ALTER ','CREATE ']) if (new RegExp(w,'i').test(s)) throw new Error('write statement present: '+w); console.log('read-only ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run db:audit-sends-history` exits 0 against a migrated database and prints a per-status count table.
    - The script source, with comments stripped, contains none of `INSERT`, `UPDATE`, `DELETE`, `ALTER`, `CREATE` (case-insensitive).
    - The script's SQL runs inside a transaction that is always rolled back.
    - `packages/db/package.json` and the root `package.json` both contain a `db:audit-sends-history` script entry.
    - Running the script twice produces identical output on an idle database.
  </acceptance_criteria>
  <done>An operator can answer the ROADMAP's pre-migration history questions with one command, and the command provably cannot write.</done>
</task>

<task type="auto">
  <name>Task 2: Four migrations — two standalone enum adds, additive columns, reconciler health table [BLOCKING]</name>
  <files>packages/db/migrations/0047_send_status_reconciling.sql, packages/db/migrations/0048_send_status_unknown.sql, packages/db/migrations/0049_send_reconciliation_columns.sql, packages/db/migrations/0050_send_reconciler_runs.sql, packages/db/migrations/meta/_journal.json</files>
  <read_first>
    - packages/db/migrations/0038_partition_catchup_and_maintenance_runs.sql — the `partition_maintenance_runs` CREATE TABLE (singleton `id integer PRIMARY KEY DEFAULT 1 CHECK (id = 1)`, the explicit "NO ROW LEVEL SECURITY and this is deliberate" header + `COMMENT ON TABLE`) that `send_reconciler_runs` mirrors
    - packages/db/migrations/0040_partition_maintenance_runs_seed.sql — the seed-with-`ON CONFLICT DO NOTHING` pattern and the reasoning for seeding a dead-man's-switch row at epoch
    - packages/db/migrations/meta/_journal.json — the entry shape (`idx`, `version`, `when`, `tag`, `breakpoints`) that each new `.sql` file needs
    - scripts/lint-migrations.mjs — the exact `enum-add-value-used-same-file` and `destructive-ddl-unmarked` rules these files must satisfy
    - packages/db/migrations/0042_scan_role_grants_and_policies.sql — confirms `mega_crm_scan` already holds `GRANT SELECT ON sends` and an unrestricted `sends_scan` policy, so the new `sends` columns need no additional grant
  </read_first>
  <action>
    Write four hand-written migrations and register each in `meta/_journal.json` with `idx` 47/48/49/50, `version` `"7"`, monotonically increasing `when` values continuing from 0046's, matching `tag` values, and `breakpoints: true`.

    `0047_send_status_reconciling.sql` — one statement only: `ALTER TYPE send_status ADD VALUE 'reconciling';`. No other statement, no comment containing the quoted literal in a position the linter would read as a usage (the linter strips comments, so a plain explanatory header comment is safe). Header comment: this is the DB-08 expand half; the code that references the value lands in a later plan; no historical row is touched.

    `0048_send_status_unknown.sql` — one statement only: `ALTER TYPE send_status ADD VALUE 'unknown';`. Same header discipline. Note in the comment that `unknown` is a terminal-but-re-examinable state per D-02/D-04, and that Postgres cannot drop an enum value without a type rebuild, so this value is part of the published send-status contract from here on.

    `0049_send_reconciliation_columns.sql` — additive only:
    - `ALTER TABLE sends ADD COLUMN reconciling_since timestamptz;`
    - `ALTER TABLE sends ADD COLUMN dispatched_at timestamptz;`
    - `ALTER TABLE sends ADD COLUMN dispatch_duration_ms integer;`
    - `CREATE INDEX sends_status_queued_at_idx ON sends (status, queued_at);` — serves the reconciler's cross-workspace discovery scan (both the `reconciling`/`unknown` predicate and the stale-`dispatching` age predicate) in one index.
    - `CREATE INDEX sends_reconciling_since_idx ON sends (reconciling_since) WHERE reconciling_since IS NOT NULL;` — serves Phase 15's planned webhook-lag query on `reconciling_since` and the watchdog's oldest-reconciling read.
    - `COMMENT ON COLUMN` for each of the three columns, naming: `reconciling_since` is deliberately a separate column from `queued_at` (locked — Phase 15 queries it directly and overloading `queued_at` would conflate enqueue time with ambiguity onset); `dispatched_at` is the moment the provider call started; `dispatch_duration_ms` is wall-clock milliseconds for that call.
    Do not write any enum literal in this file's SQL — the two index definitions above deliberately avoid `WHERE status IN (...)` so this file is independent of whether 0047/0048 committed in the same session.

    `0050_send_reconciler_runs.sql` — `CREATE TABLE send_reconciler_runs` mirroring `partition_maintenance_runs`'s singleton shape: `id integer PRIMARY KEY DEFAULT 1 CHECK (id = 1)`, `last_run_at timestamptz NOT NULL`, `candidates_scanned integer NOT NULL DEFAULT 0`, `rows_resolved integer NOT NULL DEFAULT 0`, `rows_marked_unknown integer NOT NULL DEFAULT 0`, `stale_dispatching_swept integer NOT NULL DEFAULT 0`, `oldest_reconciling_since timestamptz`, `last_alert_sent_at timestamptz`, `updated_at timestamptz NOT NULL DEFAULT now()`. Include the same explicit "NO ROW LEVEL SECURITY, no `workspace_id`, and this is deliberate" header comment and `COMMENT ON TABLE` that 0038 carries for `partition_maintenance_runs`, with the same reasoning (platform-level operational metadata, never tenant data) so a reviewer scanning for a table missing RLS finds the explanation instead of adding a policy. Seed the singleton with `INSERT INTO send_reconciler_runs (id, last_run_at) VALUES (1, TIMESTAMPTZ 'epoch') ON CONFLICT (id) DO NOTHING;` for exactly the reason 0040 documents: a dead-man's switch whose claim step cannot match a nonexistent row never fires.
  </action>
  <verify>
    <automated>npm run lint:migrations && npm run db:migrate && node -e "const {Pool}=require('pg');const p=new Pool({connectionString:process.env.DATABASE_URL});p.query(\"SELECT unnest(enum_range(NULL::send_status))::text AS v\").then(async r=>{const vals=r.rows.map(x=>x.v).sort();const want=['dispatching','excluded','failed','reconciling','sent','unknown'];if(JSON.stringify(vals)!==JSON.stringify(want))throw new Error('enum mismatch: '+vals.join(','));const c=await p.query(\"SELECT column_name FROM information_schema.columns WHERE table_name='sends' AND column_name IN ('reconciling_since','dispatched_at','dispatch_duration_ms')\");if(c.rowCount!==3)throw new Error('columns missing');const t=await p.query(\"SELECT to_regclass('send_reconciler_runs') AS t\");if(!t.rows[0].t)throw new Error('send_reconciler_runs missing');await p.end();console.log('schema ok')}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint:migrations` exits 0.
    - `npm run db:migrate` exits 0 against a database already at 0046.
    - `enum_range(NULL::send_status)` contains exactly `dispatching`, `sent`, `failed`, `excluded`, `reconciling`, `unknown`.
    - `information_schema.columns` reports all three new `sends` columns as nullable.
    - `to_regclass('send_reconciler_runs')` is non-null and `SELECT count(*) FROM send_reconciler_runs WHERE id = 1` returns 1.
    - `pg_indexes` contains `sends_status_queued_at_idx` and `sends_reconciling_since_idx`.
    - `meta/_journal.json` has entries for tags `0047_send_status_reconciling` through `0050_send_reconciler_runs` with strictly increasing `idx`.
    - None of the four `.sql` files contains an `UPDATE sends` or `DELETE FROM sends` statement.
  </acceptance_criteria>
  <done>The database vocabulary and columns this phase needs exist, the migration linter passes, and no historical row was touched. This is the BLOCKING schema-push gate — later plans' type checks would otherwise pass against a database that does not have these values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Drizzle schema, enum parity test, and rollup-unchanged invariant</name>
  <files>packages/db/src/schema/sends.ts, packages/db/src/schema/send-reconciler-runs.ts, packages/db/src/index.ts, packages/db/src/__tests__/send-status-enum-parity.test.ts, apps/worker/src/queues/__tests__/rollup-enum-migration-invariant.test.ts, SPECIFICATION.md</files>
  <read_first>
    - packages/db/src/schema/sends.ts — the current `sendStatusEnum` array and the column-definition style (nullable `timestamp(..., { withTimezone: true })`, `integer(...)`) the three new columns must match
    - packages/db/src/schema/partition-maintenance-runs.ts — the "logical/type-inference shape ONLY" doc-comment convention `send-reconciler-runs.ts` copies verbatim in spirit, including the explanation of why the physical CHECK constraint lives in the migration
    - packages/db/src/index.ts — the `import * as ...Schema` / spread-into-`schema` / `export * from` triple that a new schema file must be added to in all three places
    - packages/delivery-core/src/send-state-machine.ts — `SEND_STATUSES`, the union the parity test compares the live enum against
    - apps/worker/src/queues/analytics-reconciliation.worker.ts — `reconcileWorkspaceDay`'s fact-column-driven aggregation (`sent_at IS NOT NULL`, not `status = 'sent'`), which is why adding enum values cannot move a rollup count; the invariant test proves it rather than assuming it
    - apps/worker/src/test/failure-fixtures.ts — `freshWorkspaceId`, `createFixtureCampaign`, `createFixtureContact` helpers the invariant test uses to build a pre-migration-shaped ledger
    - SPECIFICATION.md §4 — the existing schema-section format the new enum values, columns, indexes and table are recorded in
  </read_first>
  <behavior>
    - Reading `sendStatusEnum.enumValues` yields the same six values as `SEND_STATUSES` from `@mega-crm/delivery-core` (set equality, order-insensitive).
    - Querying `enum_range(NULL::send_status)` against the live test database yields the same six values as `SEND_STATUSES` — schema file, delivery-core union, and database all agree.
    - Given a workspace with `sends` rows across `sent`/`failed`/`excluded` and populated fact columns, `reconcileWorkspaceDay` produces identical `workspace_daily_rollup` counts before and after a row is inserted with `status = 'reconciling'` and again with `status = 'unknown'` — neither new status contributes to any rollup count.
    - A `sends` row inserted with `status = 'unknown'` and a non-null `delivered_at` still contributes to `delivered_count` — the rollup is fact-driven, and the test pins that this is deliberate rather than accidental.
  </behavior>
  <action>
    Extend `sendStatusEnum` in `packages/db/src/schema/sends.ts` to the six values, appending `"reconciling"` then `"unknown"` after the existing four (order in the TS array is cosmetic; the physical order is set by the migrations). Add the three nullable columns using this file's existing style: `reconcilingSince: timestamp("reconciling_since", { withTimezone: true })`, `dispatchedAt: timestamp("dispatched_at", { withTimezone: true })`, `dispatchDurationMs: integer("dispatch_duration_ms")`. Extend the file's doc comment with a Phase 11 paragraph explaining what the three columns mean and that `reconciling_since` is deliberately separate from `queuedAt`.

    Create `packages/db/src/schema/send-reconciler-runs.ts` exporting `sendReconcilerRuns` as a `pgTable` matching migration 0050's columns, with the "logical/type-inference shape ONLY — the physical singleton CHECK constraint lives in the migration" doc comment and the "no `workspace_id`, no RLS, deliberately" note that `partition-maintenance-runs.ts` carries.

    Wire the new schema file into `packages/db/src/index.ts` in all three places (the `import * as sendReconcilerRunsSchema`, the `schema` object spread, and the `export * from`).

    Write `packages/db/src/__tests__/send-status-enum-parity.test.ts` covering the first two `<behavior>` items. It needs a live migrated database — follow the existing `packages/db/src/__tests__` suite's fixture usage rather than inventing a new one.

    Write `apps/worker/src/queues/__tests__/rollup-enum-migration-invariant.test.ts` covering the last two `<behavior>` items, using `freshWorkspaceId`/`createFixtureCampaign`/`createFixtureContact` from `apps/worker/src/test/failure-fixtures.ts` and calling `reconcileWorkspaceDay` directly (the exported function, not the Worker). This is the executable form of the locked Pitfall-2 requirement that `workspace_daily_rollup` historical totals are unchanged after the migration.

    Update `SPECIFICATION.md` §4 in this same change per the binding rule in `.claude/CLAUDE.md`: record the two new `send_status` values, the three new `sends` columns with their types, both new indexes, and the `send_reconciler_runs` table with its columns and its deliberate absence of RLS. Where behavior is not yet implemented (nothing writes these columns until a later plan), state that explicitly rather than implying it is live.
  </action>
  <verify>
    <automated>npx vitest run --root packages/db src/__tests__/send-status-enum-parity.test.ts && npx vitest run --root apps/worker src/queues/__tests__/rollup-enum-migration-invariant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both new test files pass via the commands above.
    - `packages/db/src/schema/sends.ts` declares `reconcilingSince`, `dispatchedAt`, `dispatchDurationMs` and a six-value `sendStatusEnum`.
    - `packages/db/src/index.ts` contains `send-reconciler-runs.js` in the import block, the `schema` spread, and the `export *` block (three occurrences).
    - `npm run typecheck` (repo `static` gate) exits 0.
    - `SPECIFICATION.md` §4 names `reconciling_since`, `dispatched_at`, `dispatch_duration_ms`, `send_reconciler_runs`, `sends_status_queued_at_idx`, and both new enum values.
    - The rollup invariant test asserts equality of all six `workspace_daily_rollup` count columns across the before/after comparison, not just `sent_count`.
  </acceptance_criteria>
  <done>Drizzle, delivery-core, and the live database agree on the send-status vocabulary; a test proves the new values move no rollup number; SPECIFICATION.md §4 records the facts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration -> production data | DDL applied to a live ledger; a stray DML here silently rewrites delivery history and every derived analytic |
| operator CLI -> database | `audit-sends-history` runs with `DATABASE_URL` (the app role) against real tenant rows |
| worker/API processes -> `send_reconciler_runs` | A new platform-level table deliberately outside RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02-01 | Tampering | migrations 0047-0050 | high | mitigate | Acceptance criteria assert no `UPDATE sends`/`DELETE FROM sends` in any file; `rollup-enum-migration-invariant.test.ts` proves derived totals are unchanged; `npm run lint:migrations` blocks enum-add-and-use in one file |
| T-11-02-02 | Tampering | `audit-sends-history.ts` | medium | mitigate | Script wraps its reads in a transaction it always rolls back, and an automated check rejects any write keyword in the comment-stripped source |
| T-11-02-03 | Information Disclosure | `audit-sends-history.ts` stdout | medium | mitigate | Reports counts, timestamps and status names only — never email addresses, contact ids, workspace ids, or payload contents |
| T-11-02-04 | Elevation of Privilege | `send_reconciler_runs` without RLS | low | accept | Platform-level operational counters only, no tenant data and no `workspace_id`; mirrors `partition_maintenance_runs`, whose migration header documents the same acceptance. `mega_crm_scan` is granted nothing on this table |
| T-11-02-05 | Denial of Service | `CREATE INDEX` on a large `sends` table | medium | accept | Plain (non-CONCURRENT) index creation takes a write lock; acceptable at this deployment's scale and consistent with every prior index migration in this repo. Phase 14's Pitfall 17 covers the CONCURRENTLY path when it becomes necessary |
</threat_model>

<verification>
- `npm run lint:migrations` exits 0.
- `npm run db:migrate` applies 0047-0050 cleanly.
- `npx vitest run --root packages/db src/__tests__/send-status-enum-parity.test.ts` passes.
- `npx vitest run --root apps/worker src/queues/__tests__/rollup-enum-migration-invariant.test.ts` passes.
- `npm run db:audit-sends-history` exits 0 and writes nothing.
- `npm run typecheck` and `npm run lint` exit 0.
</verification>

<success_criteria>
- `reconciling` and `unknown` exist in the database, in Drizzle, and in delivery-core's union, with a test proving all three agree.
- Send duration and reconciliation age are SQL-queryable columns.
- No historical `sends` row changed status and no `workspace_daily_rollup` number moved.
</success_criteria>

<output>
Create `.planning/phases/11-delivery-correctness/11-02-SUMMARY.md` when done
</output>
