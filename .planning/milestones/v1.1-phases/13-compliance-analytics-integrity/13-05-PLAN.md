---
phase: 13-compliance-analytics-integrity
plan: 05
type: execute
wave: 2
depends_on: [13-02]
files_modified:
  - packages/db/migrations/0056_workspace_daily_rollup_dirtied_at.sql
  - packages/db/migrations/meta/_journal.json
  - packages/db/src/schema/workspace-daily-rollup.ts
  - packages/db/src/analytics/daily-rollup.ts
  - apps/worker/src/queues/analytics-rollup.ts
  - apps/worker/src/queues/analytics-reconciliation.worker.ts
  - apps/worker/src/queues/__tests__/analytics-reconciliation-dirty-day.test.ts
autonomous: true
requirements: [CMP-03]

produces_schema_change: true

must_haves:
  truths:
    - An event whose UTC occurrence day is any day other than today marks that (workspace, day) rollup row dirty at increment time.
    - An event whose UTC occurrence day is today does not mark the row dirty, because today is reconciled on every tick for as long as it remains today.
    - `incrementWorkspaceDailyRollup` is importable by both `apps/api` and `apps/worker` from one shared module, so a caller in either app increments the rollup through the same code path.
    - The next reconciliation tick reconciles every dirty (workspace, day) in addition to today and yesterday, and the retroactive count is verified against a fresh scan of `sends` rather than trusted from the increment.
    - After a sweep, a dirty mark that arrived during that sweep survives and is picked up by the following tick.
    - After a sweep, a dirty mark that existed before the sweep started is cleared.
    - A day that was never marked dirty and is outside the standing window is not reconciled, so the per-tick scan is not blanket-widened.
  artifacts:
    - packages/db/migrations/0056_workspace_daily_rollup_dirtied_at.sql
    - apps/worker/src/queues/__tests__/analytics-reconciliation-dirty-day.test.ts
  key_links:
    - "incrementWorkspaceDailyRollup's day derivation -> dirtied_at write -> the reconciler's dirty-day query: the marker is written by the incremental path and consumed only by the overwrite path, so the two write paths stay distinct while sharing one signal."
    - "dirtied_at <= sweepStartTime in the conditional clear -> no lost late-event tracking: an unconditional clear would drop a mark that landed mid-sweep."
  prohibitions:
    - statement: The reconciler's standing per-tick window is never blanket-widened to cover late events; only explicitly marked dirty days are additionally swept.
      status: flagged
      verification: unverified
    - statement: A retroactive count is never trusted from the incremental increment alone; every dirty day is re-verified against a fresh scan.
      status: flagged
      verification: unverified
    - statement: The dirty marker is never cleared unconditionally; a mark that arrived during a sweep must survive it.
      status: flagged
      verification: unverified
---

<objective>
Close CMP-03 (D-14): a provider event that arrives late — after the reconciler's standing 2-day window has moved past its occurrence day — currently increments the rollup row for that day but is never verified against a fresh scan, leaving an unverified band in the daily numbers. Mark such (workspace, day) rows dirty at increment time and sweep them in addition to today and yesterday.

Purpose: the phase's acceptance lens is that a daily number means exactly one thing. "Trust the increments outside the window" contradicts that; a widened blanket scan pays for it on every tick. Dirty-day marking verifies exactly the days that need it.
Output: a `dirtied_at timestamptz` column with a race-free conditional clear, the marking write on the incremental path, the dirty-day sweep on the reconciliation path, and tests covering the mid-sweep race.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-compliance-analytics-integrity/13-CONTEXT.md
@.planning/phases/13-compliance-analytics-integrity/13-RESEARCH.md
@.planning/phases/13-compliance-analytics-integrity/13-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols and paths created by THIS plan:

- Columns: `workspace_daily_rollup.dirtied_at`
- Files: `packages/db/migrations/0056_workspace_daily_rollup_dirtied_at.sql`, `packages/db/src/analytics/daily-rollup.ts`, `apps/worker/src/queues/__tests__/analytics-reconciliation-dirty-day.test.ts`
- Functions: `findDirtyRollupDays`, `clearDirtyRollupDays`, `isNotToday`, `incrementWorkspaceDailyRollup` (relocated)
- Constants: `DIRTY_DAY_SWEEP_PAGE_LIMIT`

## Cross-app shared-module placement (decided in plan 13-01, restated here)

This plan relocates `incrementWorkspaceDailyRollup` from `apps/worker/src/queues/analytics-rollup.ts` to `packages/db/src/analytics/daily-rollup.ts`, because plan 13-08's unsubscribe route in `apps/api` must call it and cannot — REVIEWS.md MEDIUM finding on the `apps/api` ↔ `apps/worker` import boundary.

- `apps/worker` declares `@mega-crm/api` as a devDependency only; `apps/api` declares no dependency on `apps/worker`. Neither app may import the other in production code, and a relative cross-app import is a hard `tsc` TS6059 error under `apps/api/tsconfig.json`'s `rootDir: "src"` (proven in plan 12-10, which relocated `dead-letter-writer.ts` into `packages/queue-core` for the same reason).
- Shared `PoolClient`-first query helpers live under `packages/db/src/<domain>/<module>.ts`, imported as `@mega-crm/db/src/<domain>/<module>.js` — the convention `packages/db/src/partitions/maintenance-run.ts` and `packages/db/src/reconciler/reconciler-run.ts` already established.
- The move is mechanical: `analytics-rollup.ts` imports only `PoolClient` from `pg`, so `packages/db` gains no dependency edge.

This plan does the move because it is already rewriting the function's signature and doc comment for the dirty-day marker — one disruption to one function rather than two. Plan 13-08 (wave 4) only imports the result.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the dirtied_at marker and write it from the incremental path</name>
  <files>packages/db/migrations/0056_workspace_daily_rollup_dirtied_at.sql, packages/db/migrations/meta/_journal.json, packages/db/src/schema/workspace-daily-rollup.ts, packages/db/src/analytics/daily-rollup.ts, apps/worker/src/queues/analytics-rollup.ts</files>
  <read_first>
    - apps/worker/src/queues/analytics-rollup.ts (the whole file — `METRIC_COLUMN` allow-list, the `occurredAt.slice(0, 10)` UTC day derivation, the additive `ON CONFLICT ... DO UPDATE SET <col> = workspace_daily_rollup.<col> + 1` upsert this extends, and the fact that its only import is `PoolClient` from `pg`, which is what makes the relocation mechanical)
    - packages/db/src/schema/workspace-daily-rollup.ts (as edited by plan 13-02 — the two-write-path contract and the new CMP-02 day-semantics clauses)
    - packages/db/src/reconciler/reconciler-run.ts (the `packages/db` query-helper module shape and export convention the relocated module follows)
    - apps/worker/src/queues/analytics-reconciliation.worker.ts lines 9-13 (`RECONCILE_INTERVAL_MS` and `RECONCILE_WINDOW_DAYS` — the standing window the sweep covers, which the dirty-day sweep still reads, even though the marking predicate no longer depends on it)
    - packages/db/migrations/0053_flow_segment_sweep_checkpoint.sql (house style for a small additive migration on a tenant-RLS table)
    - grep for every call site of `incrementWorkspaceDailyRollup` across `apps` and `packages` before moving it, so the import rewrite is complete rather than partial
  </read_first>
  <behavior>
    - `incrementWorkspaceDailyRollup` called with an `occurredAt` 5 days in the past sets `dirtied_at` on that (workspace, day) row to a non-null timestamp.
    - `incrementWorkspaceDailyRollup` called with an `occurredAt` earlier today leaves `dirtied_at` null.
    - `incrementWorkspaceDailyRollup` called with an `occurredAt` yesterday sets `dirtied_at` to a non-null timestamp.
    - Two late increments on the same (workspace, day) do not push `dirtied_at` forward past the first mark's timestamp.
    - A late increment still performs its additive count increment exactly as before the change.
    - `isNotToday` returns false for a `YYYY-MM-DD` equal to `now`'s UTC calendar day and true for every other day, computed against an injected `now` rather than the wall clock.
    - Every existing call site of `incrementWorkspaceDailyRollup` resolves against the relocated module, and `apps/worker/src/queues/analytics-rollup.ts` no longer declares the function.
  </behavior>
  <action>
Write migration `0056_workspace_daily_rollup_dirtied_at.sql` adding a nullable `dirtied_at timestamptz` column to `workspace_daily_rollup`, plus a partial index on `(dirtied_at)` filtered to `dirtied_at IS NOT NULL` so the sweep's discovery query does not scan the whole table. Add a `COMMENT ON COLUMN` recording D-14: a timestamp rather than a boolean, because a boolean flag has a mark/clear race — the tick reads `true`, does its work, then unconditionally writes `false`, clobbering any mark that arrived in between. Append the matching `meta/_journal.json` entry (idx 56, tag `0056_workspace_daily_rollup_dirtied_at`, same field shape as the preceding entry). Do not backfill existing rows: a null `dirtied_at` correctly means "no late event has been observed for this day", and inventing dirty marks for historical rows would trigger a full re-reconciliation of every day the table has ever held.

Add `dirtiedAt` to the `workspaceDailyRollup` Drizzle table in `packages/db/src/schema/workspace-daily-rollup.ts` and extend its doc comment with a fifth contract clause: the incremental path is the sole writer of `dirtied_at`, the reconciliation path is its sole clearer, and the clear is conditional on the mark predating the sweep's start.

Relocate `incrementWorkspaceDailyRollup`, `RollupMetric`, and the `METRIC_COLUMN` allow-list from `apps/worker/src/queues/analytics-rollup.ts` into a new `packages/db/src/analytics/daily-rollup.ts`, following `packages/db/src/reconciler/reconciler-run.ts`'s module shape, and rewrite every call site to import from `@mega-crm/db/src/analytics/daily-rollup.js`. The reason is the placement decision above: plan 13-08's unsubscribe route lives in `apps/api` and must call this exact function to keep the two unsubscribe paths' counters identical, and `apps/api` cannot import `apps/worker`. Grep for every call site first and move them all — leaving `apps/worker/src/queues/analytics-rollup.ts` as a re-export shim is NOT acceptable, because a second import path is the drift this relocation exists to prevent. Delete the file if nothing else remains in it, and state in the relocated module's header that it is shared by both applications and why.

Extend the relocated `incrementWorkspaceDailyRollup` so it decides whether the derived day needs a dirty mark. Export a small pure helper — `isNotToday(day, now)` — returning whether the derived `YYYY-MM-DD` differs from `now`'s UTC calendar day, using the same UTC arithmetic `recentDays` uses so the two cannot disagree about what "today" is.

Mark dirty whenever the day is not today, rather than only when it falls outside the reconciler's standing window — REVIEWS.md LOW finding on this plan. The window-edge predicate leaves a real sliver open: an event for day D arriving in the last few minutes before D+1 UTC midnight is inside the standing window at increment time and so is never marked, but D leaves the window before the next tick runs, so that increment is never verified against a fresh scan. `day != today` closes it completely, and the cost is one extra sweep row per boundary event — dirty rows are rare by construction and the sweep is a bounded per-day re-scan the reconciler already performs for yesterday on every tick. Prefer the airtight predicate over the minimal one: the phase's whole acceptance lens is that a daily number means exactly one thing, and "verified except for events arriving in a three-minute window each midnight" does not clear it. Record in the constant's comment that this deliberately over-marks yesterday — every yesterday event marks a row the tick would have reconciled anyway — and that the redundant mark is cheap while the missed one is silent.

A second benefit worth stating in the same comment: the marking predicate now depends only on the UTC calendar, not on `RECONCILE_WINDOW_DAYS`. The sweep still reads that constant to decide which days it covers on every tick, but a future change to the window's width can no longer silently open a gap at the new edge, which was the construction argument this plan previously had to rely on.

When the day is not today, extend the upsert's `ON CONFLICT ... DO UPDATE` clause to also set `dirtied_at = COALESCE(workspace_daily_rollup.dirtied_at, now())`. The `COALESCE` is what stops a burst of late events from repeatedly pushing the mark forward and starving the sweep — the first mark wins, and the conditional clear later measures against the sweep's own start time. Set `dirtied_at` in the INSERT branch too, for the case where the late event is the first write for that (workspace, day) at all.
  </action>
  <acceptance_criteria>
    - `packages/db/migrations/0056_workspace_daily_rollup_dirtied_at.sql` contains `ADD COLUMN` for `dirtied_at` and a partial index filtered on `dirtied_at IS NOT NULL`.
    - `packages/db/migrations/meta/_journal.json` contains an entry with tag `0056_workspace_daily_rollup_dirtied_at`.
    - `packages/db/src/schema/workspace-daily-rollup.ts` declares `dirtiedAt`.
    - `packages/db/src/analytics/daily-rollup.ts` exports `incrementWorkspaceDailyRollup` and `isNotToday`.
    - `apps/worker/src/queues/analytics-rollup.ts` no longer declares `incrementWorkspaceDailyRollup`, and no module re-exports it from a second path.
    - `grep -rn "incrementWorkspaceDailyRollup" apps packages --include=*.ts` shows every call site importing from `@mega-crm/db/src/analytics/daily-rollup.js`.
    - `incrementWorkspaceDailyRollup` with an `occurredAt` 5 days old leaves that day's row with a non-null `dirtied_at`.
    - `incrementWorkspaceDailyRollup` with an `occurredAt` from earlier today leaves that day's row with a null `dirtied_at`.
    - `incrementWorkspaceDailyRollup` with an `occurredAt` from yesterday leaves that day's row with a non-null `dirtied_at`.
    - `isNotToday("2026-08-11", new Date("2026-08-11T23:59:00Z"))` is false and `isNotToday("2026-08-11", new Date("2026-08-12T00:01:00Z"))` is true.
    - Two consecutive late increments on the same day leave `dirtied_at` equal to the value written by the first.
    - `npm run build` exits 0 (the relocation's import rewrite is complete).
    - `npm run lint:migrations` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint:migrations && npm run build && npx vitest run --root apps/worker src/queues/__tests__/analytics-rollup-idempotency.test.ts src/queues/__tests__/analytics-rollup-tenant-isolation.test.ts</automated>
  </verify>
  <done>Any event for a day other than today marks that day dirty exactly once, a same-day event marks nothing, the additive count behavior is unchanged, and the increment function is importable by both applications from one module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sweep dirty days in the reconciliation tick with a race-free conditional clear</name>
  <files>apps/worker/src/queues/analytics-reconciliation.worker.ts, apps/worker/src/queues/__tests__/analytics-reconciliation-dirty-day.test.ts</files>
  <read_first>
    - apps/worker/src/queues/analytics-reconciliation.worker.ts (the whole file — `reconcileWorkspaceDay` as UTC-corrected by plan 13-02, `recentDays`, `reconcileWorkspace`'s per-workspace fresh-transaction rule, and the `withCrossWorkspaceScan` discovery in the worker processor)
    - packages/db/src/analytics/daily-rollup.ts (as relocated and edited by Task 1 — the `dirtied_at` write and the `isNotToday` predicate that decides it)
    - apps/worker/src/queues/flows/flow-segment-sweep-checkpoint.ts (bounded-page sweep discipline; the dirty-day set is expected to be small, but the page limit is what keeps a pathological backlog from turning one tick into an unbounded scan)
    - apps/worker/src/queues/__tests__/analytics-reconciliation.test.ts (existing tick-level test harness)
  </read_first>
  <behavior>
    - Given a rollup row for a 5-day-old day with `dirtied_at` set and a deliberately wrong stored count, one tick overwrites that count from a fresh `sends` scan and clears `dirtied_at`.
    - Given a rollup row for a 5-day-old day with `dirtied_at` null and a deliberately wrong stored count, one tick leaves that count untouched.
    - A dirty mark written after the sweep's start timestamp but before its clear statement survives the clear and is still dirty afterwards.
    - A dirty mark written before the sweep's start timestamp is null afterwards.
    - Today and yesterday are still reconciled on every tick regardless of any dirty mark.
    - A single tick reconciles at most `DIRTY_DAY_SWEEP_PAGE_LIMIT` dirty days per workspace, leaving the remainder for the next tick still marked.
  </behavior>
  <action>
Add two exported query helpers to `analytics-reconciliation.worker.ts`, both `PoolClient`-first and transaction-scoped like the rest of this codebase's query helpers: `findDirtyRollupDays(client, limit)` selecting `day` where `dirtied_at IS NOT NULL` ordered by `day` ascending with `LIMIT`, and `clearDirtyRollupDays(client, sweepStartedAt)` running `UPDATE workspace_daily_rollup SET dirtied_at = NULL WHERE dirtied_at IS NOT NULL AND dirtied_at <= $1`. The `<= $1` predicate is the whole point and must not be simplified to `IS NOT NULL` alone: a mark that arrived after the sweep read its day list has not been reconciled, and clearing it would lose that late event permanently.

Add a `DIRTY_DAY_SWEEP_PAGE_LIMIT` versioned constant with a rationale comment: the dirty set is bounded and small by construction rather than rare — under Task 1's `day != today` predicate, yesterday's rows are marked routinely by any workspace with midnight-adjacent traffic, and genuinely late days are the only other source — so this bound exists to make a pathological backlog degrade into "more ticks" rather than "one tick that never finishes", not because deep paging is expected in normal operation. State the expected steady state explicitly (roughly one row per active workspace per day, plus late arrivals) so a future reader can tell a healthy dirty set from a symptom.

Extend `reconcileWorkspace` to capture a `sweepStartedAt` timestamp once at the top of its transaction, reconcile the standing `recentDays(RECONCILE_WINDOW_DAYS)` list as it does today, then reconcile the days returned by `findDirtyRollupDays`, then call `clearDirtyRollupDays(client, sweepStartedAt)`. All of this stays inside the single per-workspace `withTenant`/`withTenantTransaction` scope the function already opens — do not open a second transaction for the dirty sweep, or a crash between reconcile and clear leaves a day reconciled but still marked, which is harmless, while a crash between clear and reconcile would lose the late event, which is not. Deduplicate the two day lists so a dirty day that happens to also be inside the standing window is reconciled once, not twice.

Extend the function's doc comment to record that the dirty-day sweep is the CMP-03 mechanism, that a retroactive increment is verified by the same absolute-overwrite fresh scan every other day gets (never trusted from the increment), and that the standing window is deliberately not widened.

Write `apps/worker/src/queues/__tests__/analytics-reconciliation-dirty-day.test.ts` covering every case in the behavior list. For the mid-sweep race case, arrange the interleaving deterministically by driving `findDirtyRollupDays`, an injected late-mark write, and `clearDirtyRollupDays` in explicit sequence against the same client rather than relying on timing.
  </action>
  <acceptance_criteria>
    - `analytics-reconciliation.worker.ts` exports `findDirtyRollupDays`, `clearDirtyRollupDays`, and `DIRTY_DAY_SWEEP_PAGE_LIMIT`.
    - `clearDirtyRollupDays`'s SQL constrains the update with `dirtied_at <= $1`.
    - One tick over a 5-day-old dirty day with a wrong stored `delivered_count` leaves that count equal to a fresh `COUNT(*)` over `sends` for that day.
    - One tick over a 5-day-old non-dirty day with a wrong stored `delivered_count` leaves that wrong count unchanged.
    - A mark written with a timestamp after the captured `sweepStartedAt` is still non-null after `clearDirtyRollupDays` runs.
    - `npx vitest run --root apps/worker src/queues/__tests__/analytics-reconciliation-dirty-day.test.ts` exits 0.
    - `npm run test:migrations` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:migrations && npx vitest run --root apps/worker src/queues/__tests__/analytics-reconciliation-dirty-day.test.ts src/queues/__tests__/analytics-reconciliation.test.ts src/queues/__tests__/analytics-rollup-reconciliation-invariant.test.ts src/queues/__tests__/reconcile-utc-day.test.ts</automated>
  </verify>
  <done>A late event's day is re-verified by the next tick against a fresh scan, the mark is cleared only if it predates the sweep, and days that were never late are not re-scanned.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply and verify the schema change</name>
  <files>packages/db/migrations/0056_workspace_daily_rollup_dirtied_at.sql, packages/db/migrations/meta/_journal.json</files>
  <read_first>
    - packages/db/src/__tests__/migrate-from-empty.test.ts and packages/db/src/__tests__/migrate-incremental.test.ts (this project's schema-apply mechanism: hand-written SQL migrations applied against an ephemeral database, both from zero and incrementally over an existing schema)
    - packages/db/migrations/0056_workspace_daily_rollup_dirtied_at.sql (as written by Task 1)
  </read_first>
  <action>
[BLOCKING] After all schema files in this plan are final, apply and verify the schema. This project's schema-push mechanism is the hand-written SQL migration applied through the ephemeral-database harness: run `npm run lint:migrations` and `npm run test:migrations`. Both must exit 0.

Do NOT rely on `npm run db:migrate` (drizzle-kit CLI): it is known to hang in this dev sandbox under Node v26, per the operational note in STATE.md carried from Phase 12, where migrations 0053 and 0054 were proven via `test:migrations` but never applied through the CLI. This matters because `npm run build` and type checks pass without any real apply — Drizzle's types come from the schema files, not from the live database — so a build-green state is not evidence that the column exists.

Also assert the migration is genuinely additive against real data: seed a `workspace_daily_rollup` row before applying 0056 in the incremental test path and confirm all six existing counts are byte-identical afterwards and `dirtied_at` is null. This mirrors the Phase 11 precedent for an enum migration ("verify rollup totals are unchanged afterwards") and is what distinguishes "the column was added" from "the column was added without disturbing the numbers this phase exists to protect".
  </action>
  <acceptance_criteria>
    - `npm run lint:migrations` exits 0.
    - `npm run test:migrations` exits 0, covering both apply-from-zero and apply-over-existing-schema.
    - A `workspace_daily_rollup` row seeded before 0056 is applied has identical `sent_count`, `delivered_count`, `opened_count`, `clicked_count`, `bounced_count`, and `unsubscribed_count` afterwards, and a null `dirtied_at`.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint:migrations && npm run test:migrations</automated>
  </verify>
  <done>Migration 0056 applies from zero and incrementally, and provably disturbs no existing rollup count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider event occurrence day → which rollup row is credited | A late or manipulated day selects which historical number moves. |
| Incremental write path ↔ reconciliation overwrite path | Two writers on one row; the dirty marker is the only signal crossing between them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05-01 | Tampering | Retroactive rollup increment | high | mitigate | A dirty day is re-verified by the same absolute-overwrite fresh `sends` scan every other day gets, so a bogus retroactive increment is corrected on the next tick rather than persisting as an unverified number. |
| T-13-05-02 | Repudiation | Lost late-event tracking via the clear race | high | mitigate | `clearDirtyRollupDays` clears only marks with `dirtied_at <= sweepStartedAt`; a mark arriving mid-sweep survives. Asserted by a deterministic interleaving test. |
| T-13-05-03 | Denial of Service | Unbounded dirty-day backlog turning one tick into a full-history scan | medium | mitigate | `DIRTY_DAY_SWEEP_PAGE_LIMIT` bounds the per-tick dirty set; the remainder stays marked for the next tick. |
| T-13-05-04 | Tampering | Forged `occurred_at` marking an arbitrary historical day dirty | medium | mitigate | Depends on plan 13-04's bounding: an event older than `OCCURRED_AT_MAX_PAST_DAYS` is quarantined and never reaches the increment path at all, so the markable range is bounded to the accepted window. |
| T-13-05-05 | Information Disclosure | Cross-workspace dirty-day discovery | low | accept | Unchanged by this plan: the dirty-day query runs inside the existing per-workspace `withTenant` transaction, so RLS confines it; only the workspace enumeration uses `withCrossWorkspaceScan`, exactly as today. |
| T-13-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced (RESEARCH.md Package Legitimacy Audit: zero new dependencies). Any install proposed during execution requires slopcheck plus a blocking human checkpoint. |
</threat_model>

<flagged_assumptions>
Probe row **CMP-03** came back `unclassified` / `unresolved` (no SPEC.md exists for this phase). Surfaced explicitly, not auto-backstopped:

- **CMP-03 (unresolved):** the `must_haves.truths` above were derived goal-backward from ROADMAP success criterion 2 ("a provider event that arrives late is counted on the day it occurred rather than the day it arrived") and CONTEXT D-14, not from a verified SPEC edge row. The unverified judgement is that dirty-day marking plus the existing absolute-overwrite reconciliation is sufficient to make every retroactive increment verified — specifically, that "not today" can be decided at increment time from the derived UTC day alone, with no separate arrival-time bookkeeping.
- **Narrowed during the cross-AI review pass:** this entry previously carried a second, sharper risk — that "late" was decided against the reconciler's standing window, so a change to the window's width without a matching change to the shared constant could leave days at the new edge unmarked and unverified. The review independently found a live instance of the same class of gap: an event for day D arriving in the final reconcile interval before D+1 UTC midnight was inside the window at increment time and outside it by the next tick, so it was never marked and never verified. Task 1 now marks dirty whenever the day is not today, which closes the sliver and removes the marking predicate's dependence on the window constant entirely. What remains unverified is only the much weaker claim that the UTC calendar day is the right unit — which is the CMP-02 contract plan 13-02 pins.
</flagged_assumptions>

<review_incorporation>
Cross-AI review findings (13-REVIEWS.md) addressed in this plan:

- **LOW, "A sliver of unverified band survives at day rollover"** — incorporated exactly as the reviewer suggested. Task 1's lateness predicate becomes `day != today (UTC)` via a new pure `isNotToday(day, now)` helper, replacing the standing-window comparison. Two `must_haves` truths are rewritten, the yesterday behavior case flips from "leaves `dirtied_at` null" to "sets it", and two acceptance criteria pin the midnight boundary directly. The cost — one extra sweep row per boundary event, plus redundant marks on yesterday — is recorded in the code comment as deliberate, with the reasoning that a redundant mark is cheap and a missed one is silent.
- **MEDIUM, "The apps/api ↔ apps/worker import boundary"** — incorporated. This plan relocates `incrementWorkspaceDailyRollup` to `packages/db/src/analytics/daily-rollup.ts` so plan 13-08's `apps/api` unsubscribe route can call the same function the webhook path calls, per the phase-wide placement decision in plan 13-01. This plan performs the move rather than 13-08 because it is already rewriting the function's signature — one disruption instead of two. A re-export shim is explicitly forbidden, with a grep acceptance criterion over every call site.
</review_incorporation>

<verification>
- `npm run lint:migrations` and `npm run test:migrations` exit 0.
- `npx vitest run --root apps/worker src/queues/__tests__/analytics-reconciliation-dirty-day.test.ts src/queues/__tests__/analytics-reconciliation.test.ts src/queues/__tests__/analytics-rollup-idempotency.test.ts src/queues/__tests__/analytics-rollup-reconciliation-invariant.test.ts src/queues/__tests__/analytics-rollup-tenant-isolation.test.ts src/queues/__tests__/reconcile-utc-day.test.ts` exits 0.
- `npm run lint` and `npm run build` exit 0.
</verification>

<success_criteria>
- A late provider event is counted on its occurrence day and that day is re-verified against a fresh scan.
- The mark/clear race cannot lose a late event.
- The per-tick scan is not widened for days that were never late.
</success_criteria>

<output>
Create `.planning/phases/13-compliance-analytics-integrity/13-05-SUMMARY.md` when done. Record: the relocated module's final path and exported surface, the complete list of call sites whose imports were rewritten, and confirmation that no re-export shim was left behind — plan 13-08 depends on this and will import from whatever path this SUMMARY names. Also record which module owns `RECONCILE_WINDOW_DAYS` after the relocation (the sweep still reads it even though the marking predicate no longer does), the page-limit value chosen, and how the mid-sweep race was arranged deterministically in the test.
</output>
