---
phase: 21-per-contact-dsr-export
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/delivery-core/src/send-event-payload-allowlist.ts
  - packages/delivery-core/src/index.ts
  - packages/delivery-core/src/__tests__/send-event-payload-allowlist.test.ts
  - apps/worker/src/queues/erasure-scrub.worker.ts
  - docs/PII-INVENTORY.md
autonomous: true
requirements: [DSR-03]
must_haves:
  truths:
    - "`SEND_EVENT_PAYLOAD_EVIDENCE_ALLOWLIST`, `buildScrubbedSendEventPayload`, `buildScrubbedEventProperties` and `ERASURE_SCRUB_PAGE_LIMIT` have exactly one definition in the monorepo, in `@mega-crm/delivery-core`, imported by both the API export path and the worker erasure path (D-03)."
    - "`SEND_EVENT_PAYLOAD_EXPORT_ALLOWLIST` is a strict superset of `SEND_EVENT_PAYLOAD_EVIDENCE_ALLOWLIST`, adding exactly `ip`, `useragent`, `url`, `reason` — asserted by a test, not only documented (D-02)."
    - "`buildExportSendEventPayload` constructs a new object copying only allowlisted keys forward, returns `{}` for null/array/non-object input, and never materialises an allowlisted key that was absent from the input (D-02, build-up not tear-down)."
    - "`buildScrubbedEventProperties` still returns an empty object for every input — `events.properties` has no allowlist on either the erasure or the export path (D-01)."
    - "The existing erasure-scrub worker tests pass unchanged, importing the four relocated names from `../erasure-scrub.worker.js` exactly as before, and the worker's module-source check still holds (D-03: pure relocation, zero behaviour change)."
    - "`docs/PII-INVENTORY.md` enumerates, per table, which columns count as the contact's personal data for DSR export and purge, and states an explicit reason for every table excluded (D-03, D-04)."
    - "The inventory records `suppressions` (HMAC-hashed, no plaintext to return), `send_event_quarantine`, `erasure_records` and checkpoint/plumbing tables as excluded, each with its reason (D-04)."
    - "The inventory records the journey tables `flow_runs`, `flow_run_steps` and `campaign_recipients` as included, because processing history is personal data under GDPR Art. 15 (D-04)."
  prohibitions:
    - statement: "MUST NOT let the export allowlist drift below the erasure evidence allowlist — export must always remain a superset, or export and purge diverge on what counts as the contact's personal data."
      status: unverified
    - statement: "MUST NOT filter freeform tenant JSONB with a denylist, regex or keyword scanner on either the export or the erasure path — only build-up allowlist reconstruction is permitted."
      status: unverified
  artifacts:
    - path: "packages/delivery-core/src/send-event-payload-allowlist.ts"
      provides: "the single definition of both allowlists and all four build functions"
      exports: ["SEND_EVENT_PAYLOAD_EVIDENCE_ALLOWLIST", "SEND_EVENT_PAYLOAD_EXPORT_ALLOWLIST", "buildScrubbedSendEventPayload", "buildExportSendEventPayload", "buildScrubbedEventProperties", "ERASURE_SCRUB_PAGE_LIMIT"]
    - path: "packages/delivery-core/src/__tests__/send-event-payload-allowlist.test.ts"
      provides: "superset assertion + build-up behaviour coverage for both builders"
    - path: "docs/PII-INVENTORY.md"
      provides: "per-table contact PII inventory consumed by Phase 21 export and Phase 22 purge"
      contains: "flow_runs"
  key_links:
    - from: "apps/worker/src/queues/erasure-scrub.worker.ts"
      to: "packages/delivery-core/src/send-event-payload-allowlist.ts"
      via: "import from @mega-crm/delivery-core then re-export the four names under the same module path its tests already use"
      pattern: "@mega-crm/delivery-core"
    - from: "packages/delivery-core/src/index.ts"
      to: "packages/delivery-core/src/send-event-payload-allowlist.ts"
      via: "named re-export block"
      pattern: "send-event-payload-allowlist"
---

<objective>
Give the JSONB disclosure rule one home. Relocate the erasure allowlist constants and their build-up reconstruction functions out of `apps/worker/src/queues/erasure-scrub.worker.ts` into `@mega-crm/delivery-core` — a package both `apps/api` and `apps/worker` already depend on — and add the export-side superset (`ip`, `useragent`, `url`, `reason`) beside them, with a test that asserts the superset relationship rather than trusting a comment. Then write the PII inventory that Phase 22's purge will cite, so export and purge can never diverge on what counts as a contact's personal data.

Purpose: DSR-03's plan-time decision (named by the ROADMAP) becomes an executable artifact — one definition, one inventory, one asserted relationship — instead of two copies drifting apart across two runtimes.
Output: `packages/delivery-core/src/send-event-payload-allowlist.ts` + its unit test, a thin import-and-re-export in the worker, and `docs/PII-INVENTORY.md`.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-per-contact-dsr-export/21-CONTEXT.md
@.planning/phases/21-per-contact-dsr-export/21-RESEARCH.md
@.planning/phases/21-per-contact-dsr-export/21-PATTERNS.md
</context>

## Artifacts this phase produces

See the full list in `21-01-PLAN.md` → "Artifacts this phase produces". This plan creates: `packages/delivery-core/src/send-event-payload-allowlist.ts` with `SEND_EVENT_PAYLOAD_EXPORT_ALLOWLIST` and `buildExportSendEventPayload` (plus the relocated `SEND_EVENT_PAYLOAD_EVIDENCE_ALLOWLIST`, `buildScrubbedSendEventPayload`, `buildScrubbedEventProperties`, `ERASURE_SCRUB_PAGE_LIMIT`), `packages/delivery-core/src/__tests__/send-event-payload-allowlist.test.ts`, and `docs/PII-INVENTORY.md`.

**SPECIFICATION.md is owned by plan 21-06 for this phase** (§1.2 records the relocated package exports and points at the inventory). Do not edit `SPECIFICATION.md` here — a single owner is what keeps Wave 1 file ownership exclusive, and 21-06 lands in the same phase branch/PR.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One definition of the JSONB rule — relocate the evidence allowlist and add the export superset</name>
  <reversibility rating="costly">D-03: Phase 22's purge is explicitly designed against this shared definition; moving or forking it later re-opens the export/purge divergence the ROADMAP forbids.</reversibility>
  <files>packages/delivery-core/src/send-event-payload-allowlist.ts, packages/delivery-core/src/index.ts, packages/delivery-core/src/__tests__/send-event-payload-allowlist.test.ts, apps/worker/src/queues/erasure-scrub.worker.ts</files>
  <read_first>
    - apps/worker/src/queues/erasure-scrub.worker.ts lines 60-160 — the four definitions to relocate verbatim, including their full doc comments (the reasoning is the artifact, not just the array)
    - apps/worker/src/queues/erasure-scrub.worker.ts lines 200-330 — `scrubSendEventsPage` / `scrubEventsPage`, the internal call sites that must keep resolving after the relocation
    - apps/worker/src/queues/__tests__/erasure-scrub.test.ts — the pure-function describe blocks and, at line ~154, the module-source check that reads `../erasure-scrub.worker.ts` off disk and asserts what it must not import
    - packages/delivery-core/src/index.ts — the named-re-export block style used by every sibling module
    - packages/delivery-core/src/occurred-at-bounds.ts (or any sibling) — file layout, doc-comment density and export style in this package
  </read_first>
  <behavior>
    Write `packages/delivery-core/src/__tests__/send-event-payload-allowlist.test.ts` first:
    - "export allowlist is a superset of the evidence allowlist": every member of `SEND_EVENT_PAYLOAD_EVIDENCE_ALLOWLIST` is present in `SEND_EVENT_PAYLOAD_EXPORT_ALLOWLIST`.
    - "export allowlist adds exactly the subject's own single-recipient fields": the set difference `EXPORT \ EVIDENCE` deep-equals `["ip", "useragent", "url", "reason"]` — so a future key added to the export list without a decision fails the test.
    - "buildExportSendEventPayload copies allowlisted keys forward": given a payload holding several evidence keys, all four export-only keys, and two tenant-invented keys (one of them nesting another person's email), the result contains every allowlisted key with its original value and neither tenant key, and the serialized result does not contain the other person's email.
    - "buildExportSendEventPayload returns {} for null, an array, a string and a number".
    - "buildExportSendEventPayload omits an allowlisted key absent from the input" — the result has no such property at all, not a null-valued one.
    - "buildExportSendEventPayload is idempotent" — applying it to its own output is a fixed point.
    - "buildScrubbedSendEventPayload still drops all four export-only keys" — the evidence list is unchanged by this plan, so erasure keeps removing `ip`/`useragent`/`url`/`reason`.
    - "buildScrubbedEventProperties returns {} for every input" — including an object with plausible-looking keys.
  </behavior>
  <action>
    Create `packages/delivery-core/src/send-event-payload-allowlist.ts` and MOVE (not copy) into it, verbatim including their doc comments: `SEND_EVENT_PAYLOAD_EVIDENCE_ALLOWLIST` (the 10 existing keys), `buildScrubbedSendEventPayload`, `buildScrubbedEventProperties`, `ERASURE_SCRUB_PAGE_LIMIT`. Then add beside them `SEND_EVENT_PAYLOAD_EXPORT_ALLOWLIST` declared as a spread of the evidence list followed by `"ip", "useragent", "url", "reason"` — declaring it as a spread, not a retyped 14-element literal, is what makes the superset relationship structural rather than a promise — and `buildExportSendEventPayload(payload: unknown): Record<string, unknown>` with the identical build-up body over the export list. Document why the four extra keys belong on the export path and not on the evidence path: on a per-recipient send event they are this subject's own network/device identifiers and the reason text about them, so a DSR answer without them is incomplete, whereas erasure evidence needs only the fact and status of the send. Document that tenant-defined keys (`unique_args`, `categories`, `marketing_campaign_*`, and anything else) stay out of both lists because that key space is unenumerable by construction.

    Append a named re-export block for the six symbols to `packages/delivery-core/src/index.ts`, matching the surrounding style.

    In `apps/worker/src/queues/erasure-scrub.worker.ts`, replace the four removed definitions with an **import followed by a re-export**, not a bare `export … from`. The worker's own `scrubSendEventsPage`/`scrubEventsPage` call `buildScrubbedSendEventPayload`, `buildScrubbedEventProperties` and `ERASURE_SCRUB_PAGE_LIMIT` internally, and a bare re-export creates no local bindings — the file would stop compiling. So: `import { … } from "@mega-crm/delivery-core";` for internal use, plus an explicit `export { … };` statement re-exporting the same four names, because `erasure-scrub.test.ts` imports them from `"../erasure-scrub.worker.js"` and D-03 requires those tests to pass unchanged. (PATTERNS.md's snippet shows only the re-export half — do not copy it verbatim.) The new import target is `@mega-crm/delivery-core`, which is not the module the worker's source check forbids, so that check keeps holding.

    This is a pure relocation plus one addition: change no behaviour of the erasure path, and do not renumber, reorder or reword the evidence allowlist.

    Import nothing from the deprecated regex-based scrubbing package (the one the worker's own module-source check already forbids) into either this file or the new one — build-up allowlists superseded pattern-matching for freeform JSONB in Phase 13, and that check exists to keep it that way.
  </action>
  <verify>
    <automated>npm run test -w packages/delivery-core -- src/__tests__/send-event-payload-allowlist.test.ts && npm run test -w apps/worker -- src/queues/__tests__/erasure-scrub.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass; the erasure-scrub test file is byte-unchanged by this task (`git diff --stat apps/worker/src/queues/__tests__/erasure-scrub.test.ts` reports no changes).
    - `grep -c 'SEND_EVENT_PAYLOAD_EVIDENCE_ALLOWLIST = \[' apps/worker/src/queues/erasure-scrub.worker.ts` is 0 (the definition moved, it was not duplicated).
    - `grep -c 'SEND_EVENT_PAYLOAD_EVIDENCE_ALLOWLIST = \[' packages/delivery-core/src/send-event-payload-allowlist.ts` is 1.
    - `grep -q 'import {' apps/worker/src/queues/erasure-scrub.worker.ts` and `grep -q '^export {' apps/worker/src/queues/erasure-scrub.worker.ts` both succeed, and `npm run build -w apps/worker` exits 0 (proving the internal call sites still bind).
    - `grep -q '\.\.\.SEND_EVENT_PAYLOAD_EVIDENCE_ALLOWLIST' packages/delivery-core/src/send-event-payload-allowlist.ts` succeeds (the export list is declared as a spread of the evidence list).
    - The export path imports nothing from the deprecated regex-redaction package: `grep -v '^\s*\*' packages/delivery-core/src/send-event-payload-allowlist.ts | grep -c 'redaction'` is 0.
    - `npm run test -w apps/worker` green, `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Both allowlists and all four build functions have exactly one definition in `@mega-crm/delivery-core`; the export list is structurally a superset of the evidence list and a test says so; the erasure worker and its unchanged tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: The PII inventory Phase 22 will cite</name>
  <files>docs/PII-INVENTORY.md</files>
  <read_first>
    - packages/db/src/schema/contacts.ts, events.ts, sends.ts, send-events.ts, subscription-status-history.ts, flow-runs.ts, flow-run-steps.ts, campaign-recipients.ts, erasure-records.ts — the actual column names to inventory
    - packages/delivery-core/src/send-event-payload-allowlist.ts — the two allowlists as they now exist (Task 1)
    - .planning/phases/21-per-contact-dsr-export/21-CONTEXT.md — D-01 through D-04, the rulings this document records
    - docs/webhook-live-uat.md — an existing doc in this directory, for heading depth and tone
    - .planning/ROADMAP.md → Phase 22 section — what the purge will need to look up here
  </read_first>
  <action>
    Write `docs/PII-INVENTORY.md`: the per-table statement of what counts as one contact's personal data in this system, authored once and consumed by both Phase 21's DSR export and Phase 22's purge.

    Open with a short scope paragraph: this is the shared definition; export discloses exactly what it lists as included, purge must delete or anonymize exactly the same set, and any change belongs here first. Reference DSR-03 and Phase 22's dependency note.

    Then a single "Included" table with one row per table, columns: `Table` | `Reached via` | `Personal data columns exported` | `Excluded columns + reason`. Rows and content:
    - `contacts` — reached by `(workspace_id, id)`. Exported: `id`, `external_id`, `email`, `first_name`, `last_name`, `phone`, `city`, `country`, `timezone`, `tags`, `subscription_status`, `created_at`, `updated_at`, and `properties` in full. Excluded: `anonymized_at` (a lifecycle flag, and non-null means the export refuses entirely), delivery-health counters such as `consecutive_soft_bounces` (platform state about the address, not data the subject supplied or that describes them personally).
    - `contacts.properties` gets its own explicit note: it is included WHOLE and is deliberately not treated like `events.properties`. It is the tenant's own structured record about this subject, is edited through the contact form, and is what DSR-01 means by "custom properties".
    - `subscription_status_history` — reached by `(workspace_id, contact_id)`. Exported: `id`, `old_status`, `new_status`, `source`, `reason`, `changed_at`. This is the consent history; the table is append-only, so no additional tracking exists or is needed.
    - `events` — reached by `(workspace_id, contact_id)`. Exported: `id`, `name`, `occurred_at`, `received_at`. **Excluded: `properties`, entirely (D-01).** State the reason in full: the whole key space is tenant-supplied at ingestion, so any allowlist over it would be one tenant's schema guessed and applied to every tenant, and a tenant can put another subject's email under any key name. This mirrors the Phase 13 erasure ruling verbatim — what erasure cannot defend keeping, export cannot defend disclosing. The export goes one step further than the scrub and never selects the column at all.
    - `sends` — reached by `(workspace_id, contact_id)`. Exported: the send facts and delivery-status timestamps and reasons (`id`, `campaign_id`, `kind`, `status`, `exclusion_reason`, `provider_message_id`, `queued_at`, `sent_at`, `delivered_at`, `first_opened_at`, `first_clicked_at`, `bounced_at`, `dropped_at`, `unsubscribed_at`, `spam_reported_at`, `bounce_reason`, `drop_reason`, `flow_run_id`, `node_id`, `open_count`, `click_count`, `dispatched_at`). Excluded: internal dispatch instrumentation (`reconciling_since`, `dispatch_duration_ms`) as platform telemetry about the send attempt, not personal data.
    - `send_events` — reached by `sends.contact_id` (the table carries no `contact_id`). Exported: `id`, `sg_event_id`, `event_type`, `reason`, `is_test`, `occurred_at`, `received_at`, and `payload` **only through `SEND_EVENT_PAYLOAD_EXPORT_ALLOWLIST`** (D-02). Name the two lists, state that export is a strict superset of evidence and where the assertion lives, and name the four export-only keys with their justification. State explicitly that tenant-defined keys are excluded because the key space is unenumerable.
    - `flow_runs` and `flow_run_steps` — reached by `(workspace_id, contact_id)` and then by `flow_run_id`. Exported: which automations processed this person, when, and how each step resolved. Reason for inclusion: processing history is personal data under GDPR Art. 15.
    - `campaign_recipients` — reached by `(workspace_id, contact_id)`. Exported: `id`, `campaign_id`, `created_at` — which campaigns targeted this person.

    Then an "Excluded tables" table, one row per table with a written reason (not a shrug):
    - `workspace_suppressions` — entries are HMAC hashes of the address with a per-workspace key; there is no plaintext to return, and returning the hash would disclose nothing useful to the subject while leaking the suppression mechanism.
    - `send_event_quarantine` — provider events that could not be attributed to a send; unattributed by definition, so not provably this subject's.
    - `erasure_records` — relevant only after erasure, at which point the export refuses with the typed 410 that references this row rather than exporting it.
    - checkpoint and plumbing tables (erasure-scrub cursors, partition maintenance, reconciler runs, alert state, rollups) — platform bookkeeping with no per-subject content.

    Close with a "Consumed by" section naming: Phase 21's `apps/api/src/modules/contacts/dsr-export.repository.ts` (discloses exactly the Included set) and Phase 22's purge (must delete or anonymize exactly the same set), plus the rule that a change to either allowlist or to any row of these tables updates this document in the same change.

    Write it in Russian or English consistently with the surrounding `docs/` files; do not invent a column that is not in the schema, and where something is genuinely undetermined write that rather than guessing.
  </action>
  <verify>
    <automated>test -f docs/PII-INVENTORY.md && grep -q 'SEND_EVENT_PAYLOAD_EXPORT_ALLOWLIST' docs/PII-INVENTORY.md && grep -q 'flow_run_steps' docs/PII-INVENTORY.md && grep -q 'campaign_recipients' docs/PII-INVENTORY.md && grep -q 'workspace_suppressions' docs/PII-INVENTORY.md && grep -q 'send_event_quarantine' docs/PII-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/PII-INVENTORY.md` exists and names all nine included tables/columns groups (`contacts`, `contacts.properties`, `subscription_status_history`, `events`, `sends`, `send_events`, `flow_runs`, `flow_run_steps`, `campaign_recipients`) and all four excluded groups, each excluded row carrying a written reason.
    - Every column name in the document exists in `packages/db/src/schema/` — spot-checkable by grepping each named column against the schema directory.
    - The document states that `events.properties` is excluded entirely and that `contacts.properties` is included whole, with distinct reasons for each.
    - The document names both allowlists and states the superset direction (export ⊇ evidence) and where the test asserting it lives.
    - `npm run check:root-hygiene` exits 0 (the file lands in `docs/`, not the repo root).
  </acceptance_criteria>
  <done>A reviewer, or Phase 22's planner, can read one document and know exactly which columns of which tables are this contact's personal data, and why each excluded table is excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant-supplied JSONB → exported artifact | `send_events.payload` and `events.properties` are written by tenant integrations and by the provider webhook; both cross into a file handed to an outside party |
| shared package → two runtimes | One definition consumed by `apps/api` (disclose) and `apps/worker` (erase); a divergence between them is a compliance defect in both directions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-02-01 | Information Disclosure | `send_events.payload` allowlist | high | mitigate | Build-up reconstruction over a fixed named list (`buildExportSendEventPayload`), never a denylist or regex scan; a tenant key nesting another subject's email is asserted absent by the Task 1 test. |
| T-21-02-02 | Information Disclosure | allowlist drift between export and erasure | high | mitigate | `SEND_EVENT_PAYLOAD_EXPORT_ALLOWLIST` is declared as a spread of the evidence list, so the superset relation is structural; the set-difference test fails on any undecided addition. |
| T-21-02-03 | Tampering | relocation silently changing erasure behaviour | medium | mitigate | Verbatim move; the pre-existing `erasure-scrub.test.ts` must pass byte-unchanged, and its module-source check still runs against the worker file. |
| T-21-02-04 | Information Disclosure | inventory drifting from the schema | medium | mitigate | Every inventoried column is checked against `packages/db/src/schema/`; the document carries the same-change rule that binds Phase 22. |
| T-21-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase — RESEARCH.md's Package Legitimacy Audit records "not applicable: no new external packages". Nothing to gate. |
</threat_model>

<verification>
- `npm run test -w packages/delivery-core` green.
- `npm run test -w apps/worker` green, with `erasure-scrub.test.ts` unmodified.
- `npm run build -w apps/worker` and `npm run build -w packages/delivery-core` exit 0.
- `npm run lint` exits 0; `npm run check:root-hygiene` exits 0.
</verification>

<success_criteria>
- One definition of the JSONB disclosure rule, imported by both runtimes (DSR-03, D-03).
- Export ⊇ evidence, asserted by a test that fails on undecided drift (D-02).
- `events.properties` still has no allowlist anywhere (D-01).
- A per-table PII inventory exists that Phase 22 can cite, with a written reason for every exclusion (D-03, D-04).
</success_criteria>

<output>
Create `.planning/phases/21-per-contact-dsr-export/21-02-SUMMARY.md` when done
</output>
