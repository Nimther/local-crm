---
milestone: v1.1
audited: 2026-08-20T10:30:00Z
status: tech_debt
scores:
  requirements: 95/95
  phases: 10/10
  integration: 14/14
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 09-partition-automation-boundary-safety
    items:
      - "Live operator-alert email confirmation outstanding (behavior_unverified: 1 in 09-VERIFICATION.md): a real SendGrid alert to OPERATOR_ALERT_EMAIL has never been observed by a human. Every layer up to sgMail.send() is proven by injected-seam tests (13 tests). Not covered by Phase 16's live UAT (tenant BYO-key sends and webhooks only) nor by Phase 17's live cutover (deploy/restore/observability, no platform-key send)."
  - phase: 13-compliance-analytics-integrity
    items:
      - "Remaining live compliance walkthroughs (human_verify_mode=end-of-phase): unsubscribe-specific atomicity (CMP-01 half), timezone-independence + late-event dirty-day (CMP-02/03/06), erasure end-to-end incl. quarantine survive-then-expire (CMP-04), out-of-bounds timestamp integrity (CMP-05 half), backfill replay + reputation alert emails (CMP-08/09). Already covered live by Phase 16: the CMP-07 dedup half and CMP-01's replay-idempotency half (UAT-03/04 signed-webhook replay against real SendGrid + permanent 8/8 CI regression). Code-level UTC-correctness risk has since shrunk: Phase 17 closed WR-06 (see Closed items)."
  - phase: 15-observability-alerting-frontend-resilience
    items:
      - "OPS-13 alert thresholds and STALE_DATA_LAG_THRESHOLD_MINUTES are documented assumptions pending observation under real production load."
      - "API-side Sentry workspace_id tagging gap inside route-level withTenant (~10 route modules), documented with an executable test."
      - "Two UI follow-ups: LaunchConfirmDialog hides a failed audience breakdown; CsvImportWizard conflates loading state with a dead fetch."
  - phase: post-milestone-operator
    items:
      - "Quick task 260818-aqd Task 3: production provisioning of the file-backed KEK provider + one live send through it (operator-only; Tasks 1-2 are implemented and committed). Acknowledged deferred item in the v1.1 override_closeout."
nyquist:
  compliant_phases: [08, 09, 10, 11, 12, 13, 14, 15, 16, 17]
  partial_phases: []
  not_validated_phases: []
  missing_phases: []
  overall: compliant
---

# Milestone v1.1 "Production Hardening" — Audit Report

**Audited:** 2026-08-20 (post-close re-audit; supersedes the 2026-08-19 audit — Phase 17 was inserted, executed, and the milestone completed/archived/tagged since)
**Scope:** Phases 8–17 (10 phases, 128 plans, 95 requirements)
**Definition of done (ROADMAP.md):** Take Mega CRM from a functionally complete MVP to a system operable in production — correct sends at failure boundaries, proven tenant isolation, honest compliance and analytics, bounded and fault-tolerant background work, an automated database lifecycle, and a full operational loop. No new product functionality.

## Verdict: TECH_DEBT

This is a **record update on a shipped milestone** — v1.1 was completed, archived, and tagged on 2026-08-20 (`override_closeout`, see `.planning/MILESTONES.md`). All 95 requirements are satisfied, all 10 phases (now including Phase 17) passed goal-backward verification, all 10 phases are Nyquist-compliant, all security registers are fully closed (`threats_open: 0` everywhere, including the three formerly-open medium rows), and a fresh integration pass over Phase 17's post-audit code changes found 0 gaps. What remains is a short carry-forward list of live-environment confirmations and documented assumptions for the next milestone — no code gaps, no unverified claims about the shipped system.

### Changes vs the 2026-08-19 audit

- **Scope +1 phase:** Phase 17 (`address-tech-debt-wr-06-medium-security-follow-ups`, 6 plans) executed 2026-08-19/20 and passed verification 32/32 must-haves, `behavior_unverified: 0`. Phases score 9/9 → **10/10**.
- **Tech debt −3 (each verified in code/register this pass, not just claimed):**
  - **WR-06 (naive-timestamp growth-chart bucketing) — CLOSED.** `packages/db/src/pool.ts` pins `TimeZone=UTC` at connection handshake via the startup-parameter form (`options: '-c TimeZone=UTC'`, plan 17-01); `apps/api/src/modules/analytics/dashboard.repository.ts:92–95` uses the double-hop `AT TIME ZONE 'UTC'` idiom in `GROWTH_BY_DAY_SQL` (plan 17-02, plus the D-03 bare-`::date` sweep audit with a residual gate). Both behavioral tests exist and were re-located this pass: `packages/db/src/__tests__/pg-timezone.test.ts`, `apps/api/src/modules/analytics/__tests__/dashboard-timezone.test.ts`.
  - **T-14-58 / T-14-73 / T-14-88 (three medium security follow-ups) — CLOSED.** The pending `gsd-security-auditor` re-run the 2026-08-19 audit called for has executed (`/gsd-secure-phase 17`, D-12, 2026-08-20): all three rows in `14-SECURITY.md` now read `closed`, the audit-trail row records the FLIP-TO-CLOSED ×3 verdict, and the register is fully closed (97/97, `threats_open: 0`). Underlying implementation verified independently this pass: db/pgbackrest pull `${GHCR_IMAGE_BASE}/postgres:${POSTGRES_IMAGE_TAG}` (CI-built by `images.yml`'s `build-and-push-postgres` on the same `github.sha` as the app images), zero `build:` keys in `docker/docker-compose.prod.yml`, the immutability gate covers db/pgbackrest and rejects `local` by name, and `restore-drill.sh` self-records metrics NDJSON with real figures (119 s / 170520 KB) recorded in `docs/runbooks/restore-drill.md`.
  - **Phase 15 alloy prod-redeploy confirmation — CLOSED.** Confirmed live during Phase 17's cutover session (17-05, D-11): Grafana Alloy established on production for the first time, `RestartCount` 0 at both ends of the session bracketing the app deploy and db cutover, all seven service labels arriving in Grafana Cloud Loki. `docker/alloy/config.alloy` remains committed and unchanged.
- **Integration:** fresh gsd-integration-checker pass focused on Phase 17 (the one phase the 2026-08-19 pass never covered): **14/14 integration points WIRED, 0 gaps, 0 regressions** in the previously verified seams. One new E2E flow verified (growth chart, see below).
- **Nyquist:** 9/9 → **10/10 COMPLIANT** — `17-VALIDATION.md` reads `status: validated, nyquist_compliant: true, wave_0_complete: true`.
- **Carry-forward list normalized:** the quick-task 260818-aqd Task 3 deferred item and the Phase 15 carried flags (acknowledged in the milestone close record but absent from the 2026-08-19 audit YAML) are now tracked here as tech debt so this file is the single carry-forward source for the next milestone.

## Requirements Coverage — 95/95 satisfied

3-source cross-reference re-run for every REQ-ID: (1) `milestones/v1.1-REQUIREMENTS.md` traceability table (95/95 rows Complete, 95/95 checkboxes `[x]`, 0 unchecked), (2) phase VERIFICATION.md requirement tables, (3) SUMMARY.md `requirements-completed` frontmatter (every traceability REQ-ID appears in at least one phase SUMMARY; zero orphans in either direction).

| Phase | Requirements | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Status |
|-------|--------------|--------------|--------------------|-----------------|--------|
| 8 — Quality Gates & Failure-Injection | QG-01..10, WRK-12, DB-08 (12) | passed 5/5 | all 12 listed | all [x] / Complete | **satisfied** |
| 9 — Partition Automation | DB-01..04 (4) | passed 4/4 (+1 behavior-unverified, see debt) | all 4 listed | all [x] / Complete | **satisfied** |
| 10 — Tenant Isolation | SEC-01..16 (16) | passed 21/21 (re-verified, G-10-1 closed) | all 16 listed | all [x] / Complete | **satisfied** |
| 11 — Delivery Correctness | DLV-01..09 (9) | passed 9/9 + 5/5 SC | all 9 listed | all [x] / Complete | **satisfied** |
| 12 — Worker Reliability | WRK-01..11, WRK-13 (12) | passed 16/16 (3rd pass; G-12-1/G-12-3 closed) | all 12 listed | all [x] / Complete | **satisfied** |
| 13 — Compliance & Analytics | CMP-01..09 (9) | passed 5/5 (re-verified, Gap #1 closed by 13-16) | all 9 listed | all [x] / Complete | **satisfied** |
| 14 — Deployment & DB Durability | DB-05..07, DB-09..14, OPS-01..05 (14) | passed 5/5 (security register now fully closed) | all 14 listed | all [x] / Complete | **satisfied** |
| 15 — Observability & Alerting | OPS-06..19 (14) | passed 5/5 (G-15-4 closed by 15-22) | all 14 listed | all [x] / Complete | **satisfied** |
| 16 — Live SendGrid Verification | UAT-01..05 (5) | passed 4/4 SC (39/39 truths) | all 5 listed | all [x] / Complete | **satisfied** |
| 17 — Tech-Debt Closure (insert) | — owns no REQ-IDs | passed 32/32 | `requirements-completed: []` (by design) | n/a | **n/a — debt-closure phase** |

**Orphan check:** none orphaned. Phase 17 owning zero REQ-IDs is by design (tech-debt insert after the 95-requirement scope was already satisfied) — the status matrix is not applied to it. The SEC-04..15 bundling note from the 2026-08-19 audit stands unchanged (covered via 10-VERIFICATION's 21/21 success-criteria form + SUMMARY frontmatter + traceability `[x]`).
**Unverified phases:** none — all 10 phases have VERIFICATION.md with `status: passed` (Phase 17: `verified: 2026-08-19T20:21:38Z`, 32/32, `overrides_applied: 0`).

## Phase Verification Summary — 10/10 passed

Re-verification history for phases 8–16 unchanged from the 2026-08-19 audit (Phase 8 CI merge-block gap, G-10-1, G-12-1/2/3, 13-16 quarantine retention, Phase 14 conditional security re-run, G-15-4 alloy parse gate, Phase 16 4/4 live SC — all closed). New this pass:

- **Phase 17:** passed 32/32 must-haves across 6 plans, 0 behavior-unverified. Two orchestrator-side defects were discovered live by 17-05's own checkpoints and fixed before cutover completed (charts/canvas-vendor chunk-cycle crash PR #16; deploy.sh db/redis leg-isolation hazard PR #17). SPECIFICATION.md brought to as-built state (17-06) incl. a corrected false compose-env-vars citation in the previous audit's seam #5 evidence.

## Cross-Phase Integration — prior 7/7 seams re-affirmed + Phase 17 pass 14/14 WIRED

The 2026-08-19 checker verified 7 cross-phase seams with file:line evidence (partition watchdog → unified alerting; RLS → workers; send pipeline → suppression; tick queues → replay-sweep/quarantine-prune; prod compose → alloy sidecar; CI gates → validators; live UAT ↔ seams 3/4) — that table stands as recorded in the git history of this file and none of its evidence sites were invalidated by Phase 17 (spot-checked this pass: deploy.sh untouched in its gate wiring, alloy mount intact, no stray `build:` keys).

This pass's gsd-integration-checker (2026-08-20) covered the Phase 17 delta — the only code that landed after the previous integration pass — 14/14 points WIRED, 0 gaps:

| # | Integration point | Status | Evidence |
|---|-------------------|--------|----------|
| 1 | `createPgPool` TimeZone=UTC pin, enforced repo-wide | WIRED | `packages/db/src/pool.ts` (startup-parameter form); `lint:pg-pool-factory` gate from Phase 14 still covers apps/api, apps/worker, packages/db; `pg-timezone.test.ts` + `pool-factory.test.ts` |
| 2 | Growth-chart SQL constants → dashboard route | WIRED | `dashboard.repository.ts:92,108` exported `GROWTH_BY_DAY_SQL`/`BASELINE_CONTACT_COUNT_SQL` → `getWorkspaceDashboard` → `dashboard.routes.ts`; imported unchanged by `dashboard-timezone.test.ts` |
| 3 | CI-built postgres image → prod compose | WIRED | `images.yml` `build-and-push-postgres`/`build-only-postgres` on same `github.sha` as app images → `docker-compose.prod.yml:48,174` `${GHCR_IMAGE_BASE}/postgres:${POSTGRES_IMAGE_TAG}`, pull-only |
| 4 | Immutability gate covers db/pgbackrest | WIRED | `validate-prod-compose.mjs` `FIRST_PARTY_IMAGE_SERVICES` + `MUTABLE_TAG_NAMES` rejects `local`; fixture-backed test suite; invoked on the deploy path |
| 5 | Restore-drill self-recorded metrics | WIRED | `restore-drill.sh:76,81,274–293` NDJSON on success and timeout paths, `:?`-guarded image vars; real figures in `docs/runbooks/restore-drill.md:272,275` |
| 6 | SPECIFICATION.md as-built | WIRED | §1.3, §2.6, §3.8, §4, §5.17, §8.6 reflect the TimeZone pin and image pipeline |

Affected requirement families all confirmed wired: DB-14 (pool factory), AN-*/CMP-02/03/06 protection (UTC day semantics), DB-05..07/OPS-01..05 (deploy/image immutability, restore drill), OPS-10 (alloy live).

## E2E Flows — 5/5 complete

- **A. Campaign → SendGrid → webhook → send_events → analytics rollup** — COMPLETE (corroborated live by Phase 16).
- **B. Unsubscribe click → consent history + suppression → next send refused** — COMPLETE (atomic dual-write → pre-send gate).
- **C. Contact erasure → PII nulled → scrub worker → quarantine ages out** — COMPLETE at wiring level; full live walkthrough remains in Phase 13 debt.
- **D. Unhealthy partition buffer → watchdog → operator alert email** — COMPLETE in code; real-inbox observation remains in Phase 9 debt.
- **E. Dashboard growth chart (new, this pass)** — COMPLETE: `GET /api/analytics/dashboard?period=30` → `dashboard.routes.ts` → `getWorkspaceDashboard()` → `GROWTH_BY_DAY_SQL` (double-hop UTC) → `createPgPool` (TimeZone=UTC pinned) → chart render; every link verified.

## Nyquist Compliance — 10/10 COMPLIANT

| Phase | VALIDATION.md | Status | Verdict |
|-------|---------------|--------|---------|
| 08–16 | exists | validated, nyquist_compliant: true | COMPLIANT (unchanged from 2026-08-19) |
| 17 | exists | validated, nyquist_compliant: true, wave_0_complete: true | COMPLIANT |

10/10 COMPLIANT, 0 PARTIAL, 0 NOT-VALIDATED, 0 MISSING.

## Tech Debt (non-blocking) — 6 open items across 4 areas

All items are live-environment confirmations or documented assumptions; none are code gaps. This list is the carry-forward input for the next milestone.

**Phase 9 — Partition Automation**
- Live operator-alert email never observed by a human (real SendGrid call to `OPERATOR_ALERT_EMAIL`, platform key). All layers up to `sgMail.send()` proven by injected-seam tests (13 tests). Not covered by Phase 16's live UAT (tenant BYO-key paths only) nor by Phase 17's live cutover (no platform-key send exercised).

**Phase 13 — Compliance & Analytics**
- Remaining live walkthroughs: unsubscribe-specific atomicity, timezone-independence + late-event dirty-day, erasure end-to-end (incl. quarantine survive-then-expire), out-of-bounds timestamp integrity, backfill replay + reputation alert emails. Phase 16 covered the CMP-07 dedup half and CMP-01's replay-idempotency half live (with a permanent 8/8 CI regression); Phase 17 closed the WR-06 UTC-correctness code risk underneath the timezone walkthrough, but the walkthroughs themselves remain unexercised live.

**Phase 15 — Observability & Alerting** (carried flags, previously only in the close record)
- OPS-13 alert thresholds and `STALE_DATA_LAG_THRESHOLD_MINUTES` are assumptions pending real production load.
- API-side Sentry `workspace_id` tagging gap inside route-level `withTenant` (~10 route modules; documented with an executable test).
- Two UI follow-ups: LaunchConfirmDialog hides a failed audience breakdown; CsvImportWizard conflates loading with a dead fetch.

**Post-milestone / operator**
- Quick task 260818-aqd Task 3: production provisioning of the file-backed KEK provider + one live send through it (operator-only; Tasks 1–2 implemented and committed). The single acknowledged deferred item of the v1.1 `override_closeout`.

**Closed since the 2026-08-19 audit (each verified this pass, not inherited):**
- ~~WR-06 naive-timestamp growth-chart bucketing~~ — closed by Phase 17 plans 17-01/17-02 (code + tests re-located this pass).
- ~~T-14-58 / T-14-73 / T-14-88 medium security follow-ups~~ — closed by the gsd-security-auditor re-run 2026-08-20 (`/gsd-secure-phase 17`, D-12); register fully closed, `threats_open: 0`.
- ~~Phase 15 alloy prod-redeploy confirmation~~ — closed by 17-05's live cutover evidence (RestartCount 0 bracketing, 7/7 service labels in Loki).
