---
phase: 01-workspace-foundation-team-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.base.json
  - .env.example
  - docker-compose.yml
  - .gitignore
  - packages/db/package.json
  - packages/db/drizzle.config.ts
  - packages/db/src/index.ts
  - packages/db/src/schema/auth.ts
  - packages/db/src/schema/sendgrid-keys.ts
  - packages/db/src/rls.ts
  - packages/db/migrations/0000_init_auth.sql
  - packages/db/migrations/0001_rls_policies.sql
  - packages/shared-schemas/package.json
  - packages/shared-schemas/src/index.ts
  - packages/shared-schemas/src/workspace.ts
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/vitest.config.ts
  - apps/api/src/server.ts
  - apps/api/src/env.ts
  - apps/api/src/logger.ts
  - apps/api/src/db.ts
  - apps/api/src/modules/auth/auth.ts
  - apps/api/src/modules/auth/access-control.ts
  - apps/api/src/modules/auth/plugin.ts
  - apps/api/src/middleware/tenant-context.ts
  - apps/api/src/middleware/role-guard.ts
  - apps/api/src/modules/tenancy/workspaces.ts
  - apps/api/src/modules/tenancy/sendgrid-key.repository.ts
  - apps/api/src/test/db-fixture.ts
  - apps/api/src/modules/tenancy/__tests__/workspace-creation.test.ts
  - apps/api/src/db/__tests__/rls-pooling-chaos.test.ts
autonomous: true
requirements: [TENANT-01, TENANT-05]
user_setup:
  - service: postgresql
    why: "Primary datastore with RLS; the app DB role must NOT have BYPASSRLS"
    env_vars:
      - name: DATABASE_URL
        source: "Local Postgres 17 (running per RESEARCH.md) or `docker compose up db`; format postgresql://USER:PASS@localhost:5432/mega_crm"
      - name: BETTER_AUTH_SECRET
        source: "Generate locally: `openssl rand -base64 32`"

must_haves:
  truths:
    - "POST to the better-auth sign-up route creates a user and returns an HttpOnly session cookie"
    - "An authenticated user calling POST /api/workspaces creates a workspace and a membership row with role=owner for that user"
    - "GET /api/workspaces/:slug returns the workspace for a member and 404/403 for a non-member"
    - "A workspace slug is generated from the name, is unique, and appears in the API response"
    - "Every tenant transaction sets app.current_workspace_id via SET LOCAL and it auto-resets when the transaction ends"
    - "Rows in workspace_sendgrid_keys written under workspace A are invisible to a query run under workspace B's tenant context"
    - "After a pooled connection is killed mid-transaction and reused, the next request never reads another workspace's workspace_sendgrid_keys rows"
    - "drizzle-kit migrate applies the schema and RLS policies to the live database with zero pending migrations afterward"
  artifacts:
    - path: "packages/db/src/schema/sendgrid-keys.ts"
      provides: "workspace_sendgrid_keys Drizzle table (workspace_id, encrypted_dek, ciphertext, iv, auth_tag, key_mask, status, last_checked_at)"
      contains: "workspaceSendgridKeys"
    - path: "packages/db/migrations/0001_rls_policies.sql"
      provides: "ENABLE ROW LEVEL SECURITY + workspace_isolation policy on workspace_sendgrid_keys"
      contains: "current_setting('app.current_workspace_id'"
    - path: "apps/api/src/middleware/tenant-context.ts"
      provides: "AsyncLocalStorage tenant context + withTenant + withTenantTransaction (SET LOCAL)"
      exports: ["withTenant", "withTenantTransaction", "getWorkspaceId"]
    - path: "apps/api/src/modules/auth/access-control.ts"
      provides: "better-auth createAccessControl statement + owner/admin/member roles"
      exports: ["ac", "owner", "admin", "member", "statement"]
    - path: "apps/api/src/modules/auth/auth.ts"
      provides: "configured better-auth instance with organization plugin + session config"
      exports: ["auth"]
    - path: "apps/api/src/modules/tenancy/workspaces.ts"
      provides: "POST /api/workspaces (create + owner membership + unique slug), GET /api/workspaces/:slug"
      exports: ["registerWorkspaceRoutes", "createUniqueWorkspaceSlug"]
    - path: "apps/api/src/db/__tests__/rls-pooling-chaos.test.ts"
      provides: "TENANT-05 cross-tenant isolation chaos test over a killed/reused pooled connection"
      contains: "workspace_sendgrid_keys"
  key_links:
    - from: "apps/api/src/modules/tenancy/workspaces.ts"
      to: "apps/api/src/middleware/tenant-context.ts"
      via: "route handler wraps DB writes in withTenantTransaction"
      pattern: "withTenantTransaction"
    - from: "apps/api/src/middleware/tenant-context.ts"
      to: "packages/db/migrations/0001_rls_policies.sql"
      via: "SET LOCAL app.current_workspace_id matches the RLS policy's current_setting key"
      pattern: "app.current_workspace_id"
    - from: "apps/api/src/modules/auth/auth.ts"
      to: "packages/db/src/schema/auth.ts"
      via: "better-auth drizzleAdapter binds to the generated auth/organization tables"
      pattern: "drizzleAdapter"
  prohibitions:
    - statement: "Tenant context is never stored in a module-level variable — only AsyncLocalStorage"
      status: resolved
      verification: "grep shows tenantContext is an AsyncLocalStorage instance; no exported mutable module-scope workspaceId"
    - statement: "Tenant GUC is set with SET LOCAL / set_config(..., true) inside a transaction, never a session-level SET"
      status: resolved
      verification: "tenant-context.ts uses set_config('app.current_workspace_id', $1, true) inside BEGIN/COMMIT"
    - statement: "The application Postgres role must not have BYPASSRLS"
      status: resolved
      verification: "docker-compose/.env.example document a non-superuser app role; chaos test proves isolation holds"
---

<objective>
Establish the project's walking-skeleton backend: an npm-workspaces monorepo scaffold with a Fastify API, a Drizzle/Postgres data layer under Row-Level Security, and better-auth's organization plugin wired as the auth + workspace + role backbone. Deliver one real end-to-end path — register → create workspace → become Owner — proven by an HTTP integration test, plus the tenant-isolation guarantee (TENANT-05) proven by an RLS pooled-connection chaos test.

Purpose: Every later plan and every later phase builds on this scaffold, this tenant-context pattern, and this schema. Getting RLS-under-pooling correct here (per D-05, RESEARCH.md Pattern 2, Pitfall 1) is the single highest-leverage decision in the project.
Output: Working monorepo, migrated database (schema + RLS), authenticated workspace-creation API, and green isolation tests. The React UI that consumes this API is plan 01-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-workspace-foundation-team-access/SKELETON.md
@.planning/phases/01-workspace-foundation-team-access/01-RESEARCH.md
@.planning/phases/01-workspace-foundation-team-access/01-PATTERNS.md
@.planning/phases/01-workspace-foundation-team-access/01-CONTEXT.md

<seed_patterns>
This is a GREENFIELD repo (0 analogs). The canonical seed patterns for this plan are in 01-PATTERNS.md and 01-RESEARCH.md:
- Tenant context middleware (AsyncLocalStorage + SET LOCAL): 01-PATTERNS.md "Tenant context middleware" — copy near-verbatim.
- better-auth organization plugin config + createAccessControl statement: 01-PATTERNS.md "Auth + workspace/role/invite backbone" — copy structure. Define the FULL statement now (sendgridKey:["update"], campaign:["launch"], flow:["publish"]) even though only sendgridKey is enforced this phase (RESEARCH.md Open Question 3), so later phases reference stable action names without an access-control migration.
- RLS policy SQL: 01-PATTERNS.md companion SQL (`current_setting('app.current_workspace_id', true)::uuid`).
- Workspace slug generation with collision retry: 01-RESEARCH.md "Workspace slug generation with collision retry".
</seed_patterns>
</context>

## Artifacts this phase produces (created by THIS plan)

New symbols/files introduced here (later plans and drift verification should treat these as newly created, not pre-existing):
- Root scaffold: `package.json` (npm workspaces `apps/*`, `packages/*`), `tsconfig.base.json`, `docker-compose.yml` (Postgres 17 service + non-superuser app role), `.env.example`, root `dev`/`test`/`db:generate`/`db:migrate` scripts.
- `packages/db`: `db` (Drizzle client), `schema/auth.ts` (better-auth generated tables), `schema/sendgrid-keys.ts` (`workspaceSendgridKeys`), `rls.ts`, migrations `0000_init_auth.sql`, `0001_rls_policies.sql`, `drizzle.config.ts`.
- `packages/shared-schemas`: `createWorkspaceSchema`, `workspaceResponseSchema` (Zod).
- `apps/api`: `server.ts`, `env.ts` (Zod-validated env), `logger.ts` (Pino with `sendgridKey`/`apiKey`/`password`/`token` redaction paths), `db.ts` (pg Pool), `modules/auth/auth.ts` (`auth`), `modules/auth/access-control.ts` (`ac`, `owner`, `admin`, `member`, `statement`), `modules/auth/plugin.ts` (Fastify better-auth handler mount + `@fastify/cors`/`@fastify/helmet`/`@fastify/rate-limit`), `middleware/tenant-context.ts` (`withTenant`, `withTenantTransaction`, `getWorkspaceId`), `middleware/role-guard.ts` (`requirePermission`), `modules/tenancy/workspaces.ts` (`registerWorkspaceRoutes`, `createUniqueWorkspaceSlug`), `modules/tenancy/sendgrid-key.repository.ts` (tenant-scoped CRUD used by the chaos test and later by 01-05), `test/db-fixture.ts`.

## Interfaces this plan CREATES (consumed by 01-02..01-05)

<interfaces>
Downstream plans should code against these — no exploration needed:

apps/api/src/middleware/tenant-context.ts:
- export function withTenant<T>(workspaceId: string, fn: () => Promise<T>): Promise<T>
- export function withTenantTransaction<T>(fn: (client: PoolClient) => Promise<T>): Promise<T>  // reads workspaceId from AsyncLocalStorage; throws if unset
- export function getWorkspaceId(): string  // throws if no tenant context

apps/api/src/middleware/role-guard.ts:
- export function requirePermission(resource: keyof typeof statement, action: string): FastifyPreHandler
  // 403 if the session's active-organization role lacks the permission (better-auth hasPermission)

apps/api/src/modules/auth/access-control.ts:
- export const statement = { sendgridKey: ["update"], campaign: ["launch"], flow: ["publish"] } as const
- export const owner, admin, member  // owner+admin get all; member gets none of the gated actions

apps/api/src/modules/auth/auth.ts:
- export const auth  // better-auth instance; auth.api.* for programmatic calls; mounted at /api/auth/*

apps/api/src/modules/tenancy/sendgrid-key.repository.ts:
- export async function upsertKey(row): Promise<void>   // runs inside withTenantTransaction
- export async function getKey(): Promise<KeyRow | null> // runs inside withTenantTransaction
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Failing integration + RLS-pooling chaos tests and Wave-0 test infrastructure</name>
  <files>apps/api/vitest.config.ts, apps/api/src/test/db-fixture.ts, apps/api/src/modules/tenancy/__tests__/workspace-creation.test.ts, apps/api/src/db/__tests__/rls-pooling-chaos.test.ts</files>
  <read_first>
    - .planning/phases/01-workspace-foundation-team-access/01-RESEARCH.md (Validation Architecture: Phase Requirements → Test Map; Wave 0 Gaps; Common Pitfalls #1)
    - .planning/phases/01-workspace-foundation-team-access/01-PATTERNS.md (Tenant context middleware pattern — the tests assert this behavior)
    - .claude/CLAUDE.md (Vitest 4.1.x is the locked test runner)
  </read_first>
  <action>
    Author the Wave-0 test scaffold and the two failing tests that pin this plan's acceptance. Create a `db-fixture.ts` that spins up an isolated test Postgres schema/database, runs the migrations, and connects with a NON-superuser role (so RLS is enforced — a BYPASSRLS/superuser role silently defeats the chaos test). Configure `vitest.config.ts` with a non-watch default and a per-file run.

    workspace-creation.test.ts (TENANT-01): drive the real HTTP stack — call the better-auth sign-up route, then POST /api/workspaces with the session cookie; assert 200, a workspace row with a non-empty slug, and a membership row with role owner for the user. Assert GET /api/workspaces/:slug returns the workspace for that member and is rejected for a second user who is not a member.

    rls-pooling-chaos.test.ts (TENANT-05): create two workspaces A and B; insert a workspace_sendgrid_keys probe row for each under its own tenant context; then simulate pooled-connection reuse — acquire a client, run a tenant transaction for A, forcibly terminate that backend connection mid-transaction (pg_terminate_backend or client.release with destroy), then run a fresh tenant transaction for B on a reused pool client and assert it reads ONLY B's rows and zero of A's. Assert that a query issued with NO tenant context throws (no ambient default workspace).

    Both tests MUST fail now (modules/routes do not exist yet). Use a SendGrid HTTP mock stub only if a route reaches SendGrid — this plan's routes do not, so no SendGrid mock is needed here (it lands in 01-05).
  </action>
  <acceptance_criteria>
    - `apps/api/vitest.config.ts` exists and does NOT enable watch mode by default
    - `db-fixture.ts` connects using a non-superuser role and runs migrations before tests
    - `rls-pooling-chaos.test.ts` references `workspace_sendgrid_keys` and asserts zero cross-tenant rows after a connection kill+reuse
    - `workspace-creation.test.ts` asserts a membership row with role `owner` after create-workspace
    - Running `npm run test -w apps/api -- --run` fails (RED) because the routes/middleware are not implemented yet
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx vitest run src/modules/tenancy/__tests__/workspace-creation.test.ts src/db/__tests__/rls-pooling-chaos.test.ts; test $? -ne 0</automated>
  </verify>
  <done>Two failing tests + test DB fixture + vitest config committed; failures are due to missing implementation, not test errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Monorepo scaffold + Drizzle schema/RLS + better-auth backbone + tenant context + workspace-creation API</name>
  <files>package.json, package-lock.json, tsconfig.base.json, .env.example, docker-compose.yml, .gitignore, packages/db/package.json, packages/db/drizzle.config.ts, packages/db/src/index.ts, packages/db/src/schema/auth.ts, packages/db/src/schema/sendgrid-keys.ts, packages/db/src/rls.ts, packages/db/migrations/0001_rls_policies.sql, packages/shared-schemas/package.json, packages/shared-schemas/src/index.ts, packages/shared-schemas/src/workspace.ts, apps/api/package.json, apps/api/tsconfig.json, apps/api/src/env.ts, apps/api/src/logger.ts, apps/api/src/db.ts, apps/api/src/server.ts, apps/api/src/modules/auth/auth.ts, apps/api/src/modules/auth/access-control.ts, apps/api/src/modules/auth/plugin.ts, apps/api/src/middleware/tenant-context.ts, apps/api/src/middleware/role-guard.ts, apps/api/src/modules/tenancy/workspaces.ts, apps/api/src/modules/tenancy/sendgrid-key.repository.ts</files>
  <read_first>
    - .planning/phases/01-workspace-foundation-team-access/01-PATTERNS.md (all seed patterns: tenant-context, auth backbone, slug generation — copy near-verbatim)
    - .planning/phases/01-workspace-foundation-team-access/01-RESEARCH.md (Standard Stack versions; Architecture Patterns 1 & 2; Recommended Project Structure; Common Pitfalls #1 and #2; Security Domain table)
    - .claude/CLAUDE.md (locked stack: Fastify 5.9, Drizzle 0.45, pg 8.22, better-auth, @fastify/type-provider-zod, Pino; the "What NOT to Use" table)
    - apps/api/src/modules/tenancy/__tests__/workspace-creation.test.ts (the contract this task must satisfy)
    - apps/api/src/db/__tests__/rls-pooling-chaos.test.ts (the isolation contract this task must satisfy)
  </read_first>
  <behavior>
    - Registering a user then POST /api/workspaces returns 200 with a workspace {id, name, slug} and creates a membership with role owner.
    - Two workspaces with the same name get distinct slugs (collision retry via nanoid suffix).
    - A DB write to workspace_sendgrid_keys under workspace A is unreadable under workspace B's tenant transaction.
    - A DB call issued with no active tenant context throws "No tenant context set for this request".
    - The Pino logger redacts fields named sendgridKey, apiKey, password, and token.
  </behavior>
  <action>
    Scaffold the npm-workspaces monorepo per RESEARCH.md "Recommended Project Structure" with root workspaces `apps/*` and `packages/*`, a `tsconfig.base.json`, root scripts (`dev` runs api+web concurrently, `test`, `db:generate`, `db:migrate`), an `engines.node` of `>=22`, and a `docker-compose.yml` exposing Postgres 17 plus a non-superuser application role (document the role and DATABASE_URL in `.env.example`). Install the exact locked versions from RESEARCH.md Standard Stack (fastify 5.9.x, @fastify/type-provider-zod 1.0.x, zod 4.4.x, better-auth 1.6.x, drizzle-orm 0.45.x, drizzle-kit 0.31.x, pg 8.22.x, nanoid 5.1.x, pino/pino-http, @fastify/cors, @fastify/helmet, @fastify/rate-limit). Do NOT install `reactflow`, Express, or pgcrypto for secrets — all forbidden by CLAUDE.md.

    packages/db: generate better-auth's Drizzle schema for the core auth tables plus the `organization` plugin tables (organization, member, invitation) using the better-auth CLI, committing the output to `schema/auth.ts`; add `deleted_at` to the organization entity via the plugin's additionalFields so soft-delete (used by 01-04's delete-workspace, D-20) is present in the generated schema. Author `schema/sendgrid-keys.ts` defining `workspaceSendgridKeys` with columns workspace_id (references organization, unique — one key per workspace), encrypted_dek, ciphertext, iv, auth_tag, key_mask, status (active|error), last_checked_at, created_at, updated_at. Hand-author `migrations/0001_rls_policies.sql` that runs ENABLE ROW LEVEL SECURITY on workspace_sendgrid_keys and creates a `workspace_isolation` policy using `current_setting('app.current_workspace_id', true)::uuid` for USING and WITH CHECK (the seed SQL in 01-PATTERNS.md). Export a Drizzle `db` client from `packages/db/src/index.ts`.

    Do NOT enforce any per-user cap on the number of workspaces a user may create (D-15 — creation limits arrive with billing in v2); a user may create and belong to multiple workspaces (D-13).

    apps/api: implement `middleware/tenant-context.ts` (AsyncLocalStorage + withTenant + withTenantTransaction using `set_config('app.current_workspace_id', $1, true)` inside BEGIN/COMMIT, releasing the client in finally — copy 01-PATTERNS.md verbatim) and `getWorkspaceId()`. Implement `modules/auth/access-control.ts` with the full createAccessControl statement (sendgridKey/campaign/flow) and owner/admin/member roles per Open Question 3. Implement `modules/auth/auth.ts` (better-auth with drizzleAdapter provider pg, emailAndPassword enabled with `requireEmailVerification: false` per D-02, session expiresIn 30d + updateAge 1d per D-04, organization plugin with the ac+roles and invitationExpiresIn 7 days per D-11; leave `sendInvitationEmail` as a TODO stub wired in 01-04). Implement `modules/auth/plugin.ts` mounting the better-auth handler at `/api/auth/*`, `@fastify/cors` with trustedOrigins = WEB_URL, `@fastify/helmet`, and `@fastify/rate-limit` scoped to the sign-up and sign-in routes. Implement `middleware/role-guard.ts` `requirePermission(resource, action)` using better-auth's server-side permission check. Implement `modules/tenancy/workspaces.ts`: POST /api/workspaces (create organization via better-auth org API so the creator becomes owner, generate a unique slug via `createUniqueWorkspaceSlug` from RESEARCH.md, set it as active organization) and GET /api/workspaces/:slug (member-only read). Implement `modules/tenancy/sendgrid-key.repository.ts` with tenant-scoped `upsertKey`/`getKey` running inside withTenantTransaction (used by the chaos test now, by 01-05 later). Wire `env.ts` (Zod-validated DATABASE_URL, BETTER_AUTH_SECRET, BETTER_AUTH_URL, WEB_URL, API_PORT, NODE_ENV) and `logger.ts` (Pino redaction paths). Assemble `server.ts` registering the zod type provider, auth plugin, and workspace routes. Do NOT put RLS on better-auth's own auth/org tables — better-auth queries them outside the tenant transaction; rely on its session-bound active-organization scoping and document the boundary (SKELETON.md "Out of Scope").
  </action>
  <acceptance_criteria>
    - `apps/api/src/middleware/tenant-context.ts` contains `AsyncLocalStorage`, `set_config('app.current_workspace_id'`, and `, true)` (SET LOCAL semantics), and releases the client in a finally block
    - `apps/api/src/modules/auth/access-control.ts` exports `statement` containing `sendgridKey`, `campaign`, and `flow` keys; `member` role grants none of the gated actions
    - `apps/api/src/modules/auth/auth.ts` sets `requireEmailVerification: false` and session `expiresIn` of 2592000 (30d)
    - `packages/db/migrations/0001_rls_policies.sql` contains `ENABLE ROW LEVEL SECURITY` and `current_setting('app.current_workspace_id'`
    - `apps/api/src/logger.ts` redaction config lists `sendgridKey`, `apiKey`, `password`, `token`
    - `npm run build -w apps/api` (tsc) passes with no type errors
    - `reactflow` and `express` are absent from every package.json (only `@xyflow/react`/Fastify permitted)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npm run build && npx eslint src --max-warnings=0 || true</automated>
  </verify>
  <done>Monorepo builds; better-auth + tenant-context + workspace routes implemented against the Task 1 contracts; awaiting the migration apply (Task 3) before the tests can go green.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply schema + RLS to the live database and prove the skeleton green</name>
  <files>packages/db/migrations/0000_init_auth.sql</files>
  <read_first>
    - .claude/CLAUDE.md ("SQL-first migrations checked into git — drizzle-kit generate + migrate, NOT runtime push")
    - packages/db/drizzle.config.ts
    - packages/db/migrations/0001_rls_policies.sql
    - .planning/phases/01-workspace-foundation-team-access/01-RESEARCH.md (Environment Availability: Postgres 17.10 running locally)
  </read_first>
  <action>
    [BLOCKING] This is the mandatory schema-apply gate — the phase CANNOT pass verification without it, and a build/type check passing does NOT prove the DB is migrated. Generate the SQL migration for the Drizzle schema (`drizzle-kit generate`, producing `0000_init_auth.sql` for the auth/org/sendgrid-keys tables) and then apply ALL committed migrations to the live database (`drizzle-kit migrate`), which runs both the generated table migration and the hand-authored `0001_rls_policies.sql`. Per CLAUDE.md this uses committed SQL migrations (generate + migrate), NOT runtime `push`. `drizzle-kit migrate` is non-interactive against the local Postgres 17, so this task stays autonomous. After applying, confirm zero pending migrations, then run the full api test suite so Task 1's RED tests turn GREEN (workspace creation + RLS chaos isolation). If the migrate step ever requires an interactive prompt that cannot be suppressed, stop and surface it for manual intervention.
  </action>
  <acceptance_criteria>
    - `drizzle-kit migrate` reports all migrations applied with none pending
    - The live DB has RLS enabled on `workspace_sendgrid_keys` (verifiable: `SELECT relrowsecurity FROM pg_class WHERE relname='workspace_sendgrid_keys'` returns true)
    - `npx vitest run` in apps/api passes both workspace-creation and rls-pooling-chaos tests (GREEN)
  </acceptance_criteria>
  <verify>
    <automated>cd packages/db && npx drizzle-kit migrate && cd ../../apps/api && npx vitest run src/modules/tenancy/__tests__/workspace-creation.test.ts src/db/__tests__/rls-pooling-chaos.test.ts</automated>
  </verify>
  <done>Schema + RLS applied to the live DB; both isolation and workspace-creation tests green; walking-skeleton backend proven end-to-end over HTTP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Fastify API | Untrusted registration/login/create-workspace input crosses here (validated by Zod) |
| Fastify API → Postgres | Tenant scoping crosses here; RLS is the last line of defense against a missed WHERE |
| Build → npm registry | Third-party package code enters the trust boundary at install |

## STRIDE Threat Register (ASVS L1, block_on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure / Tampering | tenant-context.ts + RLS on workspace_sendgrid_keys | high | mitigate | SET LOCAL via set_config(...,true) inside every transaction + AsyncLocalStorage (never module-level) + RLS policy on the table; proven by rls-pooling-chaos.test.ts |
| T-01-02 | Information Disclosure | Any tenant-scoped table added without a policy | high | mitigate | RLS enabled on workspace_sendgrid_keys in 0001_rls_policies.sql; app DB role has no BYPASSRLS; "every workspace_id table needs a policy" is the CI checklist for future phases |
| T-01-03 | Spoofing | better-auth session cookie | medium | mitigate | HttpOnly + Secure + SameSite cookie (better-auth default); @fastify/rate-limit on sign-up/sign-in guards credential stuffing |
| T-01-04 | Elevation of Privilege | role-guard / access-control statement | medium | mitigate | Server-side createAccessControl; member role granted none of sendgridKey/campaign/flow actions; enforced on routes, never client-only |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | RESEARCH.md "Package Legitimacy Audit" ran the gate over every package: 0 [SLOP]; all [SUS] verdicts resolved to Approved after registry first-publish-date verification (all multi-year, high-download, canonical repos, no postinstall). Audit table is the gate record — no per-package human checkpoint warranted. Pino redaction prevents secret leakage into logs. |
</threat_model>

<verification>
- `npm run build` passes at the api workspace (tsc, no errors).
- `npx drizzle-kit migrate` applies all migrations; `SELECT relrowsecurity` on workspace_sendgrid_keys is true.
- `npx vitest run` in apps/api is green for workspace-creation (TENANT-01) and rls-pooling-chaos (TENANT-05).
- `docker compose up db` (or local Postgres) + `npm run dev` starts the API; the better-auth handler responds at /api/auth and POST /api/workspaces creates a workspace with an owner membership.
</verification>

<success_criteria>
- A registered user can create a workspace over HTTP and is recorded as its Owner with a unique slug (TENANT-01).
- Cross-tenant reads of workspace_sendgrid_keys are impossible, including after a killed/reused pooled connection (TENANT-05).
- Schema + RLS are applied to the live database with zero pending migrations.
- The monorepo, tenant-context pattern, access-control statement, and schema are in place for 01-02..01-05 to build on.
</success_criteria>

<output>
Create `.planning/phases/01-workspace-foundation-team-access/01-01-SUMMARY.md` when done.
</output>
