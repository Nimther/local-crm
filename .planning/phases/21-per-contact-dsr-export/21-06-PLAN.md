---
phase: 21-per-contact-dsr-export
plan: 06
type: execute
wave: 4
depends_on: [21-04, 21-05]
files_modified:
  - apps/api/src/modules/contacts/dsr-export.repository.ts
  - packages/shared-schemas/src/dsr-export.ts
  - apps/api/src/modules/contacts/__tests__/dsr-export.test.ts
  - packages/db/migrations/0067_dsr_export_contact_indexes.sql
  - packages/db/migrations/meta/_journal.json
  - SPECIFICATION.md
  - .planning/phases/21-per-contact-dsr-export/COVERAGE.md
autonomous: true
requirements: [DSR-02, DSR-03]
must_haves:
  truths:
    - "The export document contains a `flowParticipation` section listing every `flow_runs` row for this contact, each nesting its `flow_run_steps` in chronological order — which automations processed this person, when, and how each step resolved (D-04)."
    - "The section includes completed, exited and ejected runs, not only active ones — processing history is personal data under GDPR Art. 15 (D-04)."
    - "The export document contains a `campaignMemberships` section listing every `campaign_recipients` row for this contact — which campaigns targeted them (D-04)."
    - "Both journey walks are keyset-paginated to completion in bounded pages inside the same REPEATABLE READ transaction as every other section (D-10, D-15)."
    - "`metadata.sectionRowCounts` now carries an entry for all eight document sections, each equal to the real array length, so a reader can verify nothing was truncated (D-06)."
    - "Migration 0067 creates a leading `(workspace_id, contact_id)` index on `flow_runs` and on `campaign_recipients`, and an index on `flow_run_steps (flow_run_id)` — closing the index gap that the REQUIREMENTS.md out-of-scope note assumed already existed for every table."
    - "`SPECIFICATION.md` documents the new route with its permission gate and its four typed responses, the new access-control resource, the new index migration, and the relocated allowlist package exports plus a pointer to the PII inventory — all in this phase's branch."
    - "`COVERAGE.md` records that this phase integrates no external API, with a stated reason."
  prohibitions:
    - statement: "MUST NOT describe the export in SPECIFICATION.md as anything other than what the code does — an as-built document that overstates a compliance guarantee is worse than no document."
      status: unverified
  artifacts:
    - path: "packages/db/migrations/0067_dsr_export_contact_indexes.sql"
      provides: "contact-scoped indexes for the journey-table reads"
      contains: "idx_flow_runs_workspace_contact"
    - path: "apps/api/src/modules/contacts/dsr-export.repository.ts"
      provides: "flow-participation and campaign-membership walks completing the eight-section document"
      exports: ["selectFlowRunsPage", "selectFlowRunStepsPage", "selectCampaignRecipientsPage"]
    - path: "SPECIFICATION.md"
      provides: "as-built record of the export route, the permission resource, the indexes and the shared allowlist package"
      contains: "dsr-export"
    - path: ".planning/phases/21-per-contact-dsr-export/COVERAGE.md"
      provides: "external-API coverage declaration for this phase"
  key_links:
    - from: "apps/api/src/modules/contacts/dsr-export.repository.ts"
      to: "packages/db/migrations/0067_dsr_export_contact_indexes.sql"
      via: "the journey-table filters ride the new leading (workspace_id, contact_id) indexes"
      pattern: "idx_flow_runs_workspace_contact|FROM flow_runs"
    - from: "SPECIFICATION.md"
      to: "apps/api/src/modules/contacts/dsr-export.routes.ts"
      via: "section 6 route table entry naming the path, the gate and the typed responses"
      pattern: "dsr-export"
---

<objective>
Finish the document and leave the repository's as-built record true. The journey sections (`flowParticipation`, `campaignMemberships`) complete the eight-section export — processing history is personal data under GDPR Art. 15, and D-04 puts it in scope. Migration 0067 gives those two tables the leading `(workspace_id, contact_id)` index that REQUIREMENTS.md's own justification for keeping the export synchronous already assumed existed, and gives `flow_run_steps` the FK index Postgres does not create on its own. Then `SPECIFICATION.md` records the route, the permission resource, the indexes and the relocated allowlist package, and `COVERAGE.md` states plainly that this phase integrates no external API.

Purpose: DSR-02's journey half and DSR-03's workspace+contact scoping close, the ROADMAP's synchronous-export justification stops resting on a half-true index claim, and CLAUDE.md's same-change documentation rule is honoured within this phase's branch.
Output: three more page readers, the two final sections, `0067_dsr_export_contact_indexes.sql` with its journal entry, `SPECIFICATION.md` edits, and `COVERAGE.md`.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-per-contact-dsr-export/21-CONTEXT.md
@.planning/phases/21-per-contact-dsr-export/21-RESEARCH.md
@.planning/phases/21-per-contact-dsr-export/21-PATTERNS.md
@.planning/phases/21-per-contact-dsr-export/21-01-SUMMARY.md
@.planning/phases/21-per-contact-dsr-export/21-02-SUMMARY.md
@.planning/phases/21-per-contact-dsr-export/21-05-SUMMARY.md
</context>

## Artifacts this phase produces

See `21-01-PLAN.md` → "Artifacts this phase produces". This plan adds `selectFlowRunsPage`, `selectFlowRunStepsPage` and `selectCampaignRecipientsPage` to `apps/api/src/modules/contacts/dsr-export.repository.ts`, the `flowParticipation` and `campaignMemberships` sections to `dsrExportDocumentSchema`, `packages/db/migrations/0067_dsr_export_contact_indexes.sql` (`idx_flow_runs_workspace_contact`, `idx_campaign_recipients_workspace_contact`, `idx_flow_run_steps_flow_run_id`), and `.planning/phases/21-per-contact-dsr-export/COVERAGE.md`.

**This plan is the single owner of `SPECIFICATION.md` for phase 21** — plans 21-01 through 21-05 deliberately left it untouched so Wave 1–3 file ownership stayed exclusive. Task 3 lands every SPECIFICATION.md change for this phase in the same branch/PR as the code, which is what CLAUDE.md's same-change rule requires.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Journey sections — which automations and campaigns processed this person</name>
  <files>apps/api/src/modules/contacts/__tests__/dsr-export.test.ts, apps/api/src/modules/contacts/dsr-export.repository.ts, packages/shared-schemas/src/dsr-export.ts</files>
  <read_first>
    - apps/api/src/modules/contacts/dsr-export.repository.ts — the four existing page readers and walks, to mirror exactly
    - packages/db/src/schema/flow-runs.ts — the columns (`flowId`, `flowVersionId`, `status`, `currentNodeId`, `enteredAt`, `lastEntryAt`, `exitedAt`, `exitReason`) and the `flowRunStatusEnum` values
    - packages/db/src/schema/flow-run-steps.ts — the columns (`flowRunId`, `nodeId`, `nodeType`, `outcome`, `sendId`, `createdAt`)
    - packages/db/src/schema/campaign-recipients.ts — the columns and its `(campaign_id, contact_id)` unique index (which does NOT lead with contact)
    - docs/PII-INVENTORY.md — the `flow_runs`, `flow_run_steps` and `campaign_recipients` rows
    - apps/api/src/modules/flows or apps/worker flow-run tests — how existing tests seed a flow run and its steps for a contact
  </read_first>
  <behavior>
    Extend `dsr-export.test.ts` first:
    - "flow participation: runs of every status are exported, oldest first, with their steps" — seed two flow runs for the contact with different statuses (at least one terminal: exited or completed, plus one waiting) and two steps under one of them; export; assert both runs appear ordered ascending by `enteredAt`, each carrying `flowId`, `flowVersionId`, `status`, `currentNodeId`, `enteredAt`, `lastEntryAt`, `exitedAt`, `exitReason`; the run with steps nests them in ascending `createdAt` order with `nodeId`, `nodeType`, `outcome`, `sendId`; `metadata.sectionRowCounts.flowParticipation` equals 2 and `.flowRunSteps` equals 2.
    - "flow participation: a terminal run is not filtered out" — assert explicitly that the non-active run is present, since the only pre-existing contact-scoped index on this table covers active statuses only and an implementation that leaned on it would silently drop history.
    - "campaign memberships: every campaign that targeted this contact is exported" — seed two `campaign_recipients` rows; assert both appear with `campaignId` and `createdAt`, ordered ascending by `createdAt`, with a matching count.
    - "journey sections: another contact's runs and memberships are absent".
    - "journey sections: a contact with neither exports two empty arrays with counts of 0".
    - "the document now has all eight sections" — assert `Object.keys(document)` contains exactly `metadata`, `profile`, `customProperties`, `consentHistory`, `events`, `sends`, `flowParticipation`, `campaignMemberships`, and that every key of `metadata.sectionRowCounts` equals the corresponding real array length (or 1/property count for the two single-value sections).
  </behavior>
  <action>
    Add three readers to `dsr-export.repository.ts`, all client-first like their siblings:

    `selectFlowRunsPage(client, workspaceId, contactId, cursor, limit)` — SELECT `id`, `flow_id as "flowId"`, `flow_version_id as "flowVersionId"`, `status`, `current_node_id as "currentNodeId"`, `entered_at::text`, `last_entry_at::text`, `exited_at::text`, `exit_reason as "exitReason"` FROM `flow_runs` WHERE `workspace_id = $1 AND contact_id = $2`, conditional `AND (entered_at, id) > ($3::timestamptz, $4::uuid)`, `ORDER BY entered_at ASC, id ASC`, `LIMIT $N`. Put NO status predicate on this query: every status is processing history, and the only pre-existing contact-scoped index on this table is partial over the active statuses, so an implementation shaped by that index would silently omit exactly the history D-04 asks for. Migration 0067 (Task 2) supplies the unconditional index.

    `selectFlowRunStepsPage(client, workspaceId, flowRunIds, cursor, limit)` — SELECT `id`, `flow_run_id as "flowRunId"`, `node_id as "nodeId"`, `node_type as "nodeType"`, `outcome`, `send_id as "sendId"`, `created_at::text` FROM `flow_run_steps` WHERE `workspace_id = $1 AND flow_run_id = ANY($2::uuid[])`, conditional `AND (created_at, id) > (…)`, `ORDER BY created_at ASC, id ASC`, `LIMIT $N`. Batch the run ids in groups no larger than the page limit so the array parameter stays bounded when a contact has many runs.

    `selectCampaignRecipientsPage(client, workspaceId, contactId, cursor, limit)` — SELECT `id`, `campaign_id as "campaignId"`, `created_at::text` FROM `campaign_recipients` WHERE `workspace_id = $1 AND contact_id = $2`, conditional `AND (created_at, id) > (…)`, `ORDER BY created_at ASC, id ASC`, `LIMIT $N`.

    Walk all three to exhaustion on the same client inside the same transaction. Nest the steps under their run as `steps`, keyed by `flowRunId` in memory (same grouping shape as the send-events nesting), and drop `flowRunId` from the nested entry. Add `flowParticipation` and `campaignMemberships` to the document, and add `flowParticipation`, `flowRunSteps` and `campaignMemberships` entries to `metadata.sectionRowCounts` — the step count is reported separately from the run count so a reader can verify the nested total independently.

    Add the matching schemas to `packages/shared-schemas/src/dsr-export.ts`, and confirm `dsrExportDocumentSchema` now declares exactly the eight sections named in D-05 (camelCased per D-07) and no more.
  </action>
  <verify>
    <automated>npm run test -w apps/api -- src/modules/contacts/__tests__/dsr-export.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six new cases pass alongside every earlier case, including the eight-section completeness assertion.
    - `awk '/FROM flow_runs/,/LIMIT/' apps/api/src/modules/contacts/dsr-export.repository.ts | grep -c "status IN" ` is 0 (no status predicate narrows the run history).
    - `grep -q 'ORDER BY entered_at ASC, id ASC' apps/api/src/modules/contacts/dsr-export.repository.ts` succeeds.
    - `grep -c 'withTenantTransactionRepeatableRead' apps/api/src/modules/contacts/dsr-export.repository.ts` is still 1.
    - Every key of `metadata.sectionRowCounts` is asserted against a real length in the completeness test — no count is asserted against a hardcoded literal alone.
    - `npm run lint` exits 0, `npm run build -w apps/api` exits 0.
  </acceptance_criteria>
  <done>The exported document tells the subject which automations processed them, when, how each step resolved, and which campaigns targeted them — complete across every run status — and reports a verifiable count for all eight sections.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migration 0067 — the contact-scoped indexes the journey reads need</name>
  <precondition>`packages/db/migrations` contains `0066_campaigns_version.sql` as its highest-numbered file and `meta/_journal.json`'s last entry has `idx` 66 — confirm before choosing the new number.</precondition>
  <files>packages/db/migrations/0067_dsr_export_contact_indexes.sql, packages/db/migrations/meta/_journal.json</files>
  <read_first>
    - packages/db/migrations/0066_campaigns_version.sql — the header-comment convention (phase, requirement, plan, and the reasoning) and the trailing `COMMENT ON` style
    - packages/db/migrations/0036_analytics_status_history_counts.sql — an existing plain `CREATE INDEX` migration, and the fact that these performance indexes live only in SQL and are deliberately NOT declared in the drizzle schema files
    - packages/db/migrations/meta/_journal.json — the entry shape (`idx`, `version`, `when`, `tag`, `breakpoints`) and the `when` values already used
    - scripts/lint-migrations.mjs — the three rules it enforces: no enum ADD VALUE plus usage in one file, destructive DDL needs a reason-bearing marker, and the drizzle statement-delimiter literal must never appear inside prose
    - packages/db/src/__tests__/migrate-from-empty.test.ts and migrate-incremental.test.ts — the tests that actually apply the migration set and are this repo's proof a migration works
    - .planning/phases/21-per-contact-dsr-export/21-RESEARCH.md → Common Pitfalls → Pitfall 2 — the verified index gap and why it matters to the ROADMAP's synchronous-export justification
  </read_first>
  <action>
    Create `packages/db/migrations/0067_dsr_export_contact_indexes.sql` with three statements:
    - `CREATE INDEX idx_flow_runs_workspace_contact ON flow_runs (workspace_id, contact_id);`
    - `CREATE INDEX idx_campaign_recipients_workspace_contact ON campaign_recipients (workspace_id, contact_id);`
    - `CREATE INDEX idx_flow_run_steps_flow_run_id ON flow_run_steps (flow_run_id);`

    Use plain `CREATE INDEX`, never the concurrent form: this repo applies each migration file as a single `client.query(sql)` call inside one transaction, and the concurrent form cannot run inside a transaction block.

    Write a header comment in the same voice as 0066: name Phase 21, DSR-02/DSR-03 and this plan; state precisely what was verified — `events` and `sends` already carry leading `(workspace_id, contact_id)` indexes, while `flow_runs`' only contact-scoped index is a partial unique index restricted to the active statuses (so completed/exited/ejected runs, exactly the processing history the DSR export must show, were covered by nothing), `campaign_recipients`' only index leads with `campaign_id`, and Postgres creates no index for a foreign-key column, leaving the step lookup by run id unsupported. Note that REQUIREMENTS.md's out-of-scope entry justifies a synchronous export by asserting leading `(workspace_id, contact_id)` indexes on all tables, and that this migration is what makes that assertion true. Note that Phase 22's purge will scan the same tables by contact and inherits the benefit. Add no destructive DDL, so no destructive marker is needed. Do not write the drizzle statement-delimiter literal inside any comment.

    Append the matching entry to `packages/db/migrations/meta/_journal.json`: `idx` 67, `version` "7", `when` 1786598400004 (one past the 0066 entry), `tag` "0067_dsr_export_contact_indexes", `breakpoints` true.

    Do NOT declare these indexes in `packages/db/src/schema/*.ts`: this repository's convention, verified against `idx_subscription_status_history_workspace_contact_changed`, is that performance indexes live in the SQL migrations only.

    Apply and prove the migration through the paths that work in this environment: `npm run lint:migrations`, then `npm run test:migrations` (which applies the full set from empty and incrementally). Per STATE.md the drizzle-kit `db:migrate` CLI hangs under this machine's Node version, so do not gate on it; the api integration suite additionally applies migrations to the test database through `ensureTestDbMigrated`, so re-running the DSR export suite after this task is a second, independent apply.
  </action>
  <verify>
    <automated>npm run lint:migrations && npm run test:migrations && npm run test -w apps/api -- src/modules/contacts/__tests__/dsr-export.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint:migrations` exits 0 and `npm run test:migrations` is green (migrate-from-empty and migrate-incremental both apply 0067).
    - `grep -c 'CONCURRENTLY' packages/db/migrations/0067_dsr_export_contact_indexes.sql` is 0.
    - The three index names appear once each in the migration; `psql` (or the test DB after `test:migrations`) reports all three in `pg_indexes`.
    - `packages/db/migrations/meta/_journal.json` parses as JSON and its last entry has `idx` 67 and tag `0067_dsr_export_contact_indexes`.
    - `git diff --stat packages/db/src/schema/` reports no changes (the index declaration convention is SQL-only).
    - `npm run test -w packages/db` green, including `migration-journal.test.ts` and `migration-empty-diff.test.ts`.
  </acceptance_criteria>
  <done>The three missing contact-scoped indexes exist, are applied by both migration test paths, and the ROADMAP's justification for a synchronous export is no longer resting on a half-true claim.</done>
</task>

<task type="auto">
  <name>Task 3: As-built record — SPECIFICATION.md and the external-API coverage declaration</name>
  <files>SPECIFICATION.md, .planning/phases/21-per-contact-dsr-export/COVERAGE.md</files>
  <read_first>
    - .claude/CLAUDE.md → "Project Specification (SPECIFICATION.md)" — the same-change rule and the section-routing table it defines
    - SPECIFICATION.md section 1.2 «Внутренние пакеты» — the per-package responsibility/export entries to extend for the relocated allowlist
    - SPECIFICATION.md section 4.5 «Индексы» and 4.6 «Миграции» — the existing row/entry formats for a new index and a new migration
    - SPECIFICATION.md section 6 «Публичные точки входа», specifically the session-authed route subsection — the existing route-table format, including how permission gates and typed error bodies are recorded
    - SPECIFICATION.md section 2 «Зависимости и версии» — confirm there is nothing to add (this phase installs no package)
    - The five preceding SUMMARY files in this phase directory — the authoritative record of what was actually built
    - apps/api/src/modules/contacts/dsr-export.routes.ts and dsr-export.repository.ts — the code being described; describe what it does, not what the plan intended
  </read_first>
  <action>
    Update `SPECIFICATION.md` in four places, in the language and format the surrounding sections already use:

    Section 6 (session-authed routes): add `GET /api/workspaces/:slug/contacts/:id/dsr-export`. Record: the permission gate `requirePermission("contact", "export")` (Owner/Admin only, `member` denied); membership resolution via `resolveWorkspaceMember`; the uuid validation of `:id` before it reaches the response header; the four response shapes — 200 with `Content-Type: application/json` and `Content-Disposition: attachment; filename="dsr-export-{contactId}-{YYYY-MM-DD}.json"`; 400 on a malformed id; 404 carrying the shared anti-enumeration body for a foreign or nonexistent contact; 410 carrying `{ code: "contact_erased", erasedAt, erasureRecordId }` for an anonymized contact. Record that the whole read runs in one `REPEATABLE READ` transaction whose first statement is the `anonymized_at` gate, that every section is keyset-paginated to completion in 500-row pages, and that a successful export emits one structured log line with requester/workspace/contact ids and per-section counts and no personal data. Name the eight document sections. State explicitly that no durable export-audit table exists (deliberately deferred).

    Section 6 (same subsection or adjacent): record the new access-control resource `contact: ["export"]` in `apps/api/src/modules/auth/access-control.ts` — granted to `owner` and `admin`, empty for `member` — since it is the enforcement mechanism for the route above.

    Sections 4.5 and 4.6: add the three indexes from migration 0067 and the migration itself, matching the existing entry formats.

    Section 1.2: record that `@mega-crm/delivery-core` now owns the send-event payload allowlists and their build functions (both lists, both builders, the events-properties function and the scrub page limit), consumed by `apps/api`'s export path and `apps/worker`'s erasure path, and point at `docs/PII-INVENTORY.md` as the per-table definition of a contact's personal data that Phase 21's export and Phase 22's purge both consume.

    Add a line to section 2 confirming this phase introduced no new dependency, rather than leaving the reader to infer it.

    Describe only what the code does. If a SUMMARY records a deviation from the plan, the SUMMARY is the truth — write the deviation. Where something is genuinely undetermined, write «не определено» rather than filling it in.

    Then create `.planning/phases/21-per-contact-dsr-export/COVERAGE.md` containing exactly one declaration line, because this phase adds routes to the project's own Fastify server and integrates no third-party API:

    `No external API integration: this phase adds one internal Fastify route reading the project's own Postgres tables — no third-party API, SDK or service is called.`

    Keep it to that single line (the seal-time gate requires either one table or one declaration line, with the reason under 200 characters).
  </action>
  <verify>
    <automated>test -f .planning/phases/21-per-contact-dsr-export/COVERAGE.md && grep -q 'No external API integration:' .planning/phases/21-per-contact-dsr-export/COVERAGE.md && grep -q 'dsr-export' SPECIFICATION.md && grep -q 'idx_flow_runs_workspace_contact' SPECIFICATION.md && grep -q 'PII-INVENTORY' SPECIFICATION.md && npm run check:spec-env-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `SPECIFICATION.md` contains the route path, its permission gate, and all four status codes (200/400/404/410) with the 410's typed `code` value named.
    - `SPECIFICATION.md` contains all three index names from migration 0067 and the migration tag itself.
    - `SPECIFICATION.md` section 1.2 names `@mega-crm/delivery-core` as the owner of both allowlists and points at `docs/PII-INVENTORY.md`.
    - `SPECIFICATION.md` states that this phase introduced no new dependency.
    - `.planning/phases/21-per-contact-dsr-export/COVERAGE.md` is exactly one declaration line beginning `No external API integration:` with a reason under 200 characters.
    - `npm run check:spec-env-coverage` exits 0 and `npm run check:runbook-coverage` exits 0.
    - Every claim in the new SPECIFICATION.md text is checkable against `apps/api/src/modules/contacts/dsr-export.routes.ts` or `dsr-export.repository.ts` — spot-check the status codes, the filename format and the page limit against the code, not against this plan.
  </acceptance_criteria>
  <done>SPECIFICATION.md describes the export exactly as it was built — route, gate, typed refusals, indexes, migration, shared allowlist package and PII inventory pointer — and COVERAGE.md records with a reason that this phase integrates no external API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| journey tables → exported artifact | `flow_runs` / `flow_run_steps` / `campaign_recipients` rows describing how the platform processed this person cross into a file handed to them |
| index-less scan → synchronous request | Without a contact-scoped index these two tables are scanned on every export, inside a request that must complete synchronously |
| as-built documentation → security review | `SPECIFICATION.md` is what a reviewer reads instead of the code; an overstated guarantee there is a real risk, not a documentation nit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06-01 | Information Disclosure | journey-table contact scoping | high | mitigate | Explicit `workspace_id = $1 AND contact_id = $2` on the run and membership walks; steps reached only through this contact's own run ids; RLS underneath; "another contact's runs and memberships are absent" test. |
| T-21-06-02 | Tampering | silent omission of terminal flow runs | high | mitigate | No status predicate on the run walk, plus an explicit test asserting a terminal run is present; migration 0067 supplies the unconditional index so no future change is tempted to lean on the partial one. |
| T-21-06-03 | Denial of Service | uncovered sequential scan on every export | medium | mitigate | Migration 0067 adds the two leading `(workspace_id, contact_id)` indexes and the `flow_run_steps` FK index; applied and proven by both migration test paths. |
| T-21-06-04 | Repudiation | as-built record overstating the guarantee | medium | mitigate | Every documented claim is spot-checked against the route/repository source, and the SUMMARY files (not the plans) are the authority on what shipped. |
| T-21-06-05 | Tampering | migration applied under a broken path | medium | mitigate | Plain (non-concurrent) `CREATE INDEX` so the single-transaction apply works; `lint:migrations` + `test:migrations` + the api suite's own `ensureTestDbMigrated` are three independent applies; the drizzle-kit CLI is deliberately not gated on (it hangs on this machine's Node version per STATE.md). |
| T-21-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase (RESEARCH.md Package Legitimacy Audit: not applicable); Task 3 records that absence in SPECIFICATION.md section 2 rather than leaving it implicit. |
</threat_model>

<verification>
- `npm run test -w apps/api -- src/modules/contacts/__tests__/dsr-export.test.ts` green with all eight sections and the completeness assertion.
- `npm run lint:migrations` and `npm run test:migrations` green; `npm run test -w packages/db` green.
- `npm run check:spec-env-coverage` and `npm run check:runbook-coverage` exit 0.
- **Phase gate:** `npm run test` green across all workspaces, `npm run lint` exits 0, `npm run build` succeeds — run this after Task 3, before `/gsd-verify-work`.
</verification>

<success_criteria>
- `flowParticipation` (with nested steps) and `campaignMemberships` sections present, complete across every run status, contact-scoped (DSR-02, D-04).
- The document has exactly the eight sections D-05 names, each with a verifiable row count (D-05, D-06).
- The three missing contact-scoped indexes exist and are applied (RESEARCH.md Pitfall 2 closed; Phase 22 inherits them).
- `SPECIFICATION.md` records the route, gate, typed refusals, indexes, migration and shared allowlist package in this phase's branch (CLAUDE.md same-change rule).
- `COVERAGE.md` declares, with a reason, that this phase integrates no external API.
</success_criteria>

<output>
Create `.planning/phases/21-per-contact-dsr-export/21-06-SUMMARY.md` when done
</output>