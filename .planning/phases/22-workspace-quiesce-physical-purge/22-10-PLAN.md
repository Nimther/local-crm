---
phase: 22-workspace-quiesce-physical-purge
plan: 10
type: execute
wave: 4
depends_on: [22-03, 22-04, 22-05, 22-06, 22-07, 22-08]
files_modified:
  - SPECIFICATION.md
  - docker/prod.env.example
  - docs/runbooks/backups.md
  - docs/runbooks/workspace-purge-and-restore.md
autonomous: true
requirements: [PRG-01, PRG-02]
must_haves:
  truths:
    - "SPECIFICATION.md §3 documents WORKSPACE_PURGE_RETENTION_DAYS (name, source, purpose, default, floor) and records that AUTH_DATABASE_URL is now also read by apps/worker (CLAUDE.md same-change rule)."
    - "SPECIFICATION.md §4 documents the purge_records table, the organization purgedAt column, the erasure_records contact_id FK relaxation and the three amended scan policies (CLAUDE.md same-change rule)."
    - "SPECIFICATION.md §5 documents the workspace-purge queue, its tick cadence, its report-then-destroy state machine and the dedicated pools it opens (CLAUDE.md same-change rule)."
    - "SPECIFICATION.md §6 documents the two new operator CLIs and the three new typed refusals on the ingress surfaces (CLAUDE.md same-change rule)."
    - "SPECIFICATION.md §7 documents the stuck-purge alert and the structured purge log lines (CLAUDE.md same-change rule)."
    - "docker/prod.env.example declares WORKSPACE_PURGE_RETENTION_DAYS, and npm run check:spec-env-coverage passes — the env example and the spec moved in one commit (operational gate)."
    - "docs/runbooks/backups.md states that a workspace purge's irreversibility claim is bounded by the same pgBackRest retention window as the partition-drop caveat already documented there — purged data remains recoverable from an encrypted backup until that backup rolls off (PT-02)."
    - "SPECIFICATION.md carries a one-line pointer to that backup caveat, so a security reviewer reading only the specification sees the qualification on the purge's irreversibility claim (PT-02)."
    - "An operator runbook describes the whole workspace lifecycle end to end: soft delete, quiesce, the report-only tick, the point of no return, restore, and what a completed purge does and does not remove."
    - "The runbook states explicitly which four evidence sets survive a purge and why, so a future reader cannot mistake their presence for an incomplete purge (D-10)."
  prohibitions:
    - statement: "MUST NOT claim in any document that a purge makes data unrecoverable without the backup-window qualification — the encrypted pgBackRest repository still holds it until that backup expires."
      status: unverified
    - statement: "MUST NOT record an environment variable's value in SPECIFICATION.md or in prod.env.example — names, sources and purposes only."
      status: unverified
    - statement: "MUST NOT restate the pgBackRest backup cadence in a second file; reference the single source so the two cannot drift."
      status: unverified
  artifacts:
    - path: "SPECIFICATION.md"
      provides: "the as-built record of everything this phase added, across sections 3, 4, 5, 6, 7 and 8"
    - path: "docker/prod.env.example"
      provides: "the declared retention variable, in the same commit as its specification row"
    - path: "docs/runbooks/backups.md"
      provides: "the PT-02 caveat that a purge's irreversibility is bounded by backup retention"
    - path: "docs/runbooks/workspace-purge-and-restore.md"
      provides: "the operator runbook for the full workspace lifecycle and both CLIs"
  key_links:
    - from: "docker/prod.env.example"
      to: "SPECIFICATION.md"
      via: "check:spec-env-coverage requires every declared env name to appear in the specification"
      pattern: "WORKSPACE_PURGE_RETENTION_DAYS"
    - from: "SPECIFICATION.md"
      to: "docs/runbooks/backups.md"
      via: "a one-line pointer to the purge-vs-backup-horizon caveat"
      pattern: "pgBackRest"
---

<objective>
File the phase as built, and qualify the one claim that would otherwise be overstated. `.claude/CLAUDE.md`'s same-change rule says every new env var, schema object, queue, entry point and observability signal lands in the matching `SPECIFICATION.md` section in the same change; this plan is that change for all of Phase 22, deliberately concentrated in one owner so nine parallel plans could not collide on one file and so `check:spec-env-coverage` — which fails CI whenever `docker/prod.env.example` names a variable the specification does not — never sees the two halves apart.

The qualification is PT-02. A purged workspace's rows remain recoverable from the encrypted pgBackRest repository until that backup itself rolls off the count-based retention window. `docs/runbooks/backups.md` already carries a structurally identical caveat for partition-drop retention (Phase 14's D-08), so this is a parallel note in the same place and the same voice, not a new mechanism — and the specification gets a one-line pointer so a security reviewer reading only that document sees the qualification without needing to know pgBackRest exists.

Purpose: PRG-01 and PRG-02's documentation obligations, plus PT-02.
Output: the specification sections, the env example, the backup caveat, and an operator runbook covering the whole workspace lifecycle.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-workspace-quiesce-physical-purge/22-CONTEXT.md
@.planning/phases/22-workspace-quiesce-physical-purge/22-RESEARCH.md
@.planning/phases/22-workspace-quiesce-physical-purge/22-01-SUMMARY.md
@.planning/phases/22-workspace-quiesce-physical-purge/22-05-SUMMARY.md
@.planning/phases/22-workspace-quiesce-physical-purge/22-06-SUMMARY.md
@.planning/phases/22-workspace-quiesce-physical-purge/22-07-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols and files created by Phase 22 (new — not pre-existing drift; the plan-review source-grounding pass must exclude these from drift verification):

- `packages/db/src/schema/purge-records.ts` — `purgeRecords` (table `purge_records`)
- `packages/db/migrations/0068_workspace_purge_records.sql`, `0069_erasure_records_contact_fk_relax.sql`, `0070_scan_policies_exclude_deleted_workspaces.sql` and their `meta/_journal.json` entries
- `apps/worker/src/env.ts` — `workerEnv`, `parseWorkerEnv`, `WORKSPACE_PURGE_RETENTION_DAYS_FLOOR`
- `apps/worker/src/queues/workspace-purge.worker.ts` — `WORKSPACE_PURGE_QUEUE`, `WORKSPACE_PURGE_JOB_OPTIONS`, `findEligibleWorkspaces`, `processWorkspacePurge`, `createWorkspacePurgeWorker`, `waitForWorkspacePurgeRegistration`, `WorkspaceRestoredError`, `tombstoneOrganization`
- `apps/worker/src/queues/workspace-purge-checkpoint.ts` — `loadWorkspacePurgeProgress`, `advanceWorkspacePurgeCheckpoint`, `markPurgeTableDone`, `WorkspacePurgeProgress`
- `apps/worker/src/queues/workspace-purge-tables.ts` — `PurgeTable`, `PURGE_TABLE_ORDER`, `PURGE_TABLE_SPECS`, `PURGE_EVIDENCE_TABLES`, `PURGE_SECRET_TABLES`, `PURGE_BATCH_SIZE`, `PURGE_ADVISORY_LOCK_NAMESPACE`, `deletePurgeBatch`, `countPurgeTableRows`
- `apps/worker/src/queues/workspace-purge-auth.ts` — `createAuthPurgePool`, `closeAuthPurgePool`, `deleteWorkspaceAuthRows`
- `packages/delivery-core/src/workspace-quiesce.ts` — `isWorkspaceSoftDeleted`, `WORKSPACE_DELETED_EXCLUSION_REASON`
- `isWorkspaceSoftDeletedById` added to `apps/api/src/modules/tenancy/workspace-lookup.ts`
- `packages/db/src/workspace-restore.ts` — `restoreWorkspace`, `WorkspacePurgeStartedError`, `WorkspaceNotDeletedError`, `RestoreWorkspaceResult`
- `packages/db/src/workspace-purge-report.ts` — `buildWorkspacePurgeReport`, `formatWorkspacePurgeReport`, `WorkspacePurgeReport`
- `packages/db/scripts/restore-workspace.ts`, `packages/db/scripts/workspace-purge-report.ts`, npm scripts `db:restore-workspace` and `db:workspace-purge-report`
- `apps/api/src/modules/ops/purge-watchdog.ts` — `WORKSPACE_PURGE_STUCK_ALERT_NAME`, `WORKSPACE_PURGE_STUCK_THRESHOLD_HOURS`, `WORKSPACE_PURGE_ALERT_DEDUP_HOURS`, `WORKSPACE_PURGE_WATCHDOG_INTERVAL_MS`, `evaluateWorkspacePurgeHealth`, `renderWorkspacePurgeAlertText`, `checkWorkspacePurgeHealthAndAlert`, `startWorkspacePurgeWatchdog`
- `docs/runbooks/workspace-purge-stuck-alert.md`, `docs/runbooks/workspace-purge-and-restore.md`
- env var `WORKSPACE_PURGE_RETENTION_DAYS`; `AUTH_DATABASE_URL` newly read by `apps/worker`
- exclusion-reason literal `workspace_deleted` written to `sends.exclusion_reason`
- tests: `apps/worker/src/queues/__tests__/workspace-purge.test.ts`, `workspace-purge-tables.test.ts`, `workspace-purge-neighbour-safety.test.ts`, `workspace-quiesce-dispatch.test.ts`, `workspace-quiesce-ingest.test.ts`, `workspace-quiesce-scan.test.ts`, `workspace-purge-auth.test.ts`, `failure-injection/workspace-purge-resume.test.ts`; `apps/api/src/modules/events/__tests__/events-api-quiesce.test.ts`, `apps/api/src/modules/webhooks/__tests__/webhooks-quiesce.test.ts`, `apps/api/src/modules/ops/__tests__/purge-watchdog.test.ts`; `packages/db/src/__tests__/workspace-restore.test.ts`
- `.planning/phases/22-workspace-quiesce-physical-purge/COVERAGE.md`

<document_contract>
This plan is the **sole owner** of `SPECIFICATION.md` and `docker/prod.env.example` for Phase 22 — every earlier plan deferred its same-change filing here, so this plan must file all of it, not only its own.

`docs/PII-INVENTORY.md` is owned by **22-05** and `docs/runbooks/workspace-purge-stuck-alert.md` by **22-08**; do not edit either. The root `package.json` is owned by **22-09** in this wave; do not edit it.

`COVERAGE.md` was written at plan time and already exists — do not regenerate it.
</document_contract>

<tasks>

<task type="auto">
  <name>Task 1: File the whole phase into SPECIFICATION.md, and declare the retention variable in the same commit</name>
  <precondition>All code plans (22-01 through 22-09) are merged onto the phase branch, so the as-built facts being filed are the ones actually shipped rather than the ones planned.</precondition>
  <files>SPECIFICATION.md, docker/prod.env.example</files>
  <read_first>
    - .claude/CLAUDE.md — the "Project Specification (SPECIFICATION.md)" rule: which kind of change belongs in which numbered section, and the requirement that versions be exactly those in package.json and that anything absent from the code be written as "не определено" rather than invented
    - SPECIFICATION.md — sections 3 (Секреты, line ~309), 4 (Схема данных, ~554), 5 (Планировщик и пайплайн отправки, ~844), 6 (Публичные точки входа, ~1171), 7 (Наблюдаемость, ~1494) and 8 (Расхождения, ~1616): the existing table shapes and the document's Russian voice, which the new rows must match
    - docker/prod.env.example — the declaration style (name, blank value, a comment naming the source and purpose), and the existing DATABASE_URL / AUTH_DATABASE_URL / SCAN_DATABASE_URL block around lines 190-200
    - scripts/check-spec-env-coverage.mjs — the one-directional rule: every uncommented `KEY=` line in the env example must appear somewhere in SPECIFICATION.md; a name only in the spec is fine
    - the SUMMARY files of plans 22-01 through 22-09 — the authoritative as-built facts, including any name or default that changed during execution
    - apps/worker/src/env.ts, apps/worker/src/queues/workspace-purge.worker.ts, packages/db/src/schema/purge-records.ts — read the shipped code for the actual constant values rather than copying them from this plan
  </read_first>
  <action>
    File each fact into its section, in the document's existing Russian voice and table shapes. Read the shipped code and the plan SUMMARYs for every value — this document's whole purpose is to be as-built, so a value copied from a plan that execution then changed is worse than no row at all.

    **§3 Секреты** — a row for `WORKSPACE_PURGE_RETENTION_DAYS`: name, where it is read (the worker's new boot-validated env module), its purpose (the platform retention window before a soft-deleted workspace is physically purged), its default and its validated floor. Names, sources and purposes only — never a value. Also record that `AUTH_DATABASE_URL`, already documented for the API, is now additionally read by `apps/worker`, lazily at purge time, for the `mega_crm_auth`-scoped deletion of `member` and `invitation` rows.

    **§4 Схема данных** — the new `purge_records` table with its columns and the two facts that make it unusual: no row-level security and no foreign key to `organization`, both deliberate so the checkpoint and the evidence outlive the tenant tables. The new `purgedAt` column on `organization`. The `erasure_records.contact_id` relaxation from a mandatory cascade reference to a nullable set-null one, with the reason. The three amended scan policies with their new exclusion predicate. Reference each by migration number.

    **§5 Планировщик** — the new purge queue and its tick worker: cadence, the report-then-destroy ordering that gives D-07 its announce-then-act guarantee, the per-workspace advisory lock, the batch size, the FK-ordered table walk, the checkpoint location, and the dedicated pools the worker opens (the discovery pool and the auth pool).

    **§6 Публичные точки входа** — the two new operator CLIs and their npm script names; the typed refusal now returned by API-key-authenticated surfaces for a soft-deleted workspace; and the webhook route's behaviour change, described precisely: a deleted workspace's inbound webhook receives the same generic 404 as an unknown token, before signature verification, with no journal write.

    **§7 Наблюдаемость** — the stuck-purge alert (its alert name, its threshold constant and its runbook path) and the structured log lines the purge emits on report and on completion.

    **§8 Расхождения** — anything this phase shipped that diverges from the Technology Stack section of `.claude/CLAUDE.md`, plus the one-line PT-02 pointer: the purge's irreversibility claim is bounded by the pgBackRest retention window documented in `docs/runbooks/backups.md`. Keep it a pointer; the cadence numbers live in one file only.

    Then add `WORKSPACE_PURGE_RETENTION_DAYS` to `docker/prod.env.example` beside the other worker-facing knobs, with a comment naming its purpose, its default and its floor, and no value. Both edits must be in the same commit: `check:spec-env-coverage` fails the moment the env example names something the specification does not.
  </action>
  <verify>
    <automated>npm run check:spec-env-coverage && npm run verify:prod-compose</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check:spec-env-coverage` exits 0 with a non-zero checked-name count.
    - `npm run verify:prod-compose` exits 0.
    - `grep -c "WORKSPACE_PURGE_RETENTION_DAYS" SPECIFICATION.md` is at least 1 and `grep -c "^WORKSPACE_PURGE_RETENTION_DAYS=" docker/prod.env.example` is exactly 1.
    - `grep -c "purge_records" SPECIFICATION.md` is at least 1, and the §4 entry names both the absence of row-level security and the absence of a foreign key to the organization table.
    - `grep -c "0068\|0069\|0070" SPECIFICATION.md` is at least 3 — all three migrations are referenced.
    - `grep -q "pgBackRest" SPECIFICATION.md` succeeds (the PT-02 pointer is present).
    - No environment variable value appears in either edited file: the env example's new line ends at the equals sign.
  </acceptance_criteria>
  <done>Every env var, schema object, queue, entry point and observability signal this phase added is recorded in the matching specification section, and the env example and the specification passed CI's coverage gate in the same commit.</done>
</task>

<task type="auto">
  <name>Task 2: The PT-02 backup caveat and the operator runbook for the workspace lifecycle</name>
  <files>docs/runbooks/backups.md, docs/runbooks/workspace-purge-and-restore.md</files>
  <read_first>
    - docs/runbooks/backups.md — the "Cadence and retention" section (lines ~91-124), and in particular the existing Phase 14 D-08 note at its end stating that the retention window is the actual recovery horizon for anything the partition-drop tick removes; the new note is that note's structural twin and must sit with it
    - docker/pgbackrest/pgbackrest.conf — `repo1-retention-full=2`, count-based, and `docker/pgbackrest/crontab` for the cadence, so the new note can point at them instead of restating numbers
    - docs/runbooks/relocate-default-partition-rows.md and docs/runbooks/data-retention.md — the house structure for an operator runbook covering a destructive maintenance path
    - docs/runbooks/workspace-purge-stuck-alert.md — plan 22-08's alert runbook, so the two cross-reference rather than duplicate; that one is triage for a stuck purge, this one is the lifecycle
    - the SUMMARY files of plans 22-05, 22-06 and 22-07 — the shipped table list, the CLI names and behaviour, and the auth-row scope
    - .planning/phases/22-workspace-quiesce-physical-purge/COVERAGE.md — the recorded decision not to deprovision the tenant's SendGrid webhook, which the runbook should mention so an operator seeing an orphaned subscription knows it is expected
  </read_first>
  <action>
    **`docs/runbooks/backups.md`** — add a note in or immediately after "Cadence and retention", beside the existing partition-drop caveat and in the same voice. State that a workspace purge's own irreversibility claim (the point of no return, after which restore refuses) is bounded by this same retention window: purged rows remain recoverable from an encrypted pgBackRest backup until the backup holding them rolls off. Point at `docker/pgbackrest/pgbackrest.conf` and `docker/pgbackrest/crontab` for the actual numbers rather than restating them — two files carrying the same cadence is how they drift. Say plainly what this means for a compliance answer: the platform's own copy is gone at purge time, and the encrypted backup copy ages out on the documented schedule.

    **`docs/runbooks/workspace-purge-and-restore.md`** (new) — the operator's end-to-end view:
    - **The lifecycle** — soft delete sets the deletion timestamp only; quiesce takes effect immediately at every layer (discovery, dispatch, ingestion); the retention window runs; the tick announces the workspace with a full census one tick before touching it; the destructive walk runs; the workspace ends as an anonymized tombstone.
    - **What an operator can do at each stage** — print the census on demand with the report CLI; restore with the restore CLI right up to the first destroyed row, and never after; watch for the stuck-purge alert, cross-referencing 22-08's runbook for triage rather than repeating it.
    - **What a purge removes** — the tenant tables (name the list, or point at the shipped constant as the single source), the three secret rows, and the membership and invitation rows.
    - **What survives, and why** — the four evidence sets, each with its D-10 reason, plus the tombstoned organization row. State this prominently: an operator or auditor finding these rows after a purge must be able to tell "correct by design" from "purge incomplete" at a glance.
    - **Cryptographic erasure** — the per-workspace suppression key is destroyed with the other secrets, so the surviving hashed suppression rows still prove suppression was honoured but can never be matched against an address again, by anyone including the platform.
    - **Known leftovers outside our control** — the tenant's own SendGrid account may retain an event-webhook subscription pointing at a path that now returns 404 forever; deprovisioning it was deliberately opted out of and the reason is recorded in the phase's coverage matrix.
    - **The backup horizon** — one line pointing at the new note in `backups.md`.
  </action>
  <verify>
    <automated>npm run check:runbook-coverage && npm run check:root-hygiene</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check:runbook-coverage` exits 0 with a non-zero checked count (unchanged by the new non-alert runbook).
    - `npm run check:root-hygiene` exits 0.
    - `test -f docs/runbooks/workspace-purge-and-restore.md` succeeds, and the file contains a section naming all four surviving evidence sets.
    - `grep -c "pgBackRest" docs/runbooks/workspace-purge-and-restore.md` is at least 1 (the backup-horizon pointer).
    - The backup cadence numbers appear in only one document: `grep -c "repo1-retention-full" docs/runbooks/workspace-purge-and-restore.md` is 0.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>An operator has one document describing the whole workspace lifecycle and both CLIs, and the backups runbook states plainly that a purge's irreversibility is bounded by the backup retention window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → security reviewer / compliance answer | These files are what an outside reviewer reads; an overstated claim here is a false assurance, not a typo |
| documentation → operator under pressure | The runbook is read during an incident, when the wrong instruction causes the damage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-10-01 | Repudiation | overstated irreversibility claim | high | mitigate | PT-02 caveat in `backups.md` beside its structural twin, plus a one-line pointer in the specification so a reviewer reading only that document still sees it. |
| T-22-10-02 | Information Disclosure | a secret value written into a committed document | high | mitigate | Names, sources and purposes only; the env-example line ends at the equals sign, asserted by the acceptance criteria. |
| T-22-10-03 | Repudiation | surviving evidence rows mistaken for an incomplete purge | medium | mitigate | The runbook names all four evidence sets and the D-10 reason for each, prominently rather than as a footnote. |
| T-22-10-04 | Tampering | documentation drifting from the shipped code | medium | mitigate | Values are read from the shipped code and the plan SUMMARYs rather than copied from plans; `check:spec-env-coverage` enforces the env half mechanically. |
| T-22-10-05 | Repudiation | two files carrying the same backup cadence, drifting apart | low | mitigate | The new note points at the pgBackRest config and crontab; a zero-count grep asserts the numbers were not restated. |
| T-22-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase — RESEARCH.md's Package Legitimacy Audit records "no new packages introduced". Nothing to gate. |
</threat_model>

<verification>
- `npm run check:spec-env-coverage`, `npm run check:runbook-coverage`, `npm run check:root-hygiene` and `npm run verify:prod-compose` all exit 0.
- `npm run lint` exits 0.
- Full-suite regression at the end of the phase: `npm test` green, and `npm run failure:all` green.
</verification>

<success_criteria>
- Every env var, schema object, queue, entry point and observability signal this phase added is filed in the matching SPECIFICATION.md section (CLAUDE.md same-change rule, PRG-01/PRG-02 documentation half).
- The purge's irreversibility claim is qualified by the backup retention window in both the runbook and the specification (PT-02).
- An operator can run the whole workspace lifecycle — soft delete, report, restore, purge, triage — from one document.
</success_criteria>

<output>
Create `.planning/phases/22-workspace-quiesce-physical-purge/22-10-SUMMARY.md` when done
</output>
