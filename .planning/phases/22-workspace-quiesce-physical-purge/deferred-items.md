# Deferred Items — Phase 22 (workspace-quiesce-physical-purge)

Out-of-scope discoveries logged during plan execution, per the executor's SCOPE BOUNDARY rule. Not fixed here.

## Plan 22-04

- **Pre-existing, out of scope**: `apps/worker/src/queues/__tests__/failure-injection/erasure-enqueue-crash.test.ts` -- "a crash strictly between deleteContact's commit and the enqueue call is recovered end to end by one reclaim tick" fails with `"No tenant context set for this request"` instead of the expected `/INJECTED FAILURE/` match. Reproduces in complete isolation (`vitest run erasure-enqueue-crash` alone), unrelated to scan policies, campaigns, flows, flow_runs or analytics-reconciliation -- none of which this test touches. Not fixed here per the deviation-rules scope boundary (only auto-fix issues directly caused by the current task's changes). Orchestrator note: this test passed on the merged wave-1 tree and in the pre-phase baseline of the main checkout, so the failure is likely worktree-environment-specific; re-verify on the merged tree.
- **Pre-existing, machine-specific (per STATE.md/project memory)**: `apps/worker/src/__tests__/sentry.test.ts` -- "with no DSN configured, does not throw and leaves the SDK uninitialized" fails deterministically on this machine because `~/.config/mega-crm/.env` carries real Sentry DSNs. Documented project-wide constraint, not caused by this plan.
- **Pre-existing, environment setup**: `apps/worker/src/__tests__/stop-grace-period-publish.test.ts` requires `apps/worker/dist/shutdown-budget.js` to exist (`npm run build -w apps/worker` was not run in this worktree). Unrelated to this plan's changes.

## Plan 22-09

- Re-observed, not caused by this plan: `apps/worker/src/queues/__tests__/failure-injection/erasure-enqueue-crash.test.ts`'s "a crash strictly between deleteContact's commit and the enqueue call is recovered end to end by one reclaim tick" and `apps/worker/src/__tests__/sentry.test.ts`'s "with no DSN configured..." both failed during this plan's `npm run test -w apps/worker` regression run, exactly as documented under Plan 22-04 above. Both reproduce in complete isolation with none of this plan's files loaded, confirming they are pre-existing and unrelated to `workspace-purge.worker.ts`/`workspace-purge-checkpoint.ts`/the new failure-injection scenario. Not fixed here.
- `npm run failure:all` could not be verified end-to-end in one run: the `&&`-chained script halts at the pre-existing `failure:erasure-enqueue-crash` failure above, before ever reaching `failure:workspace-purge-resume` at the end of the chain. Verified equivalently instead: `failure:workspace-purge-resume` passes standalone (all seven cases), and every script listed AFTER the pre-existing failure point (`failure:migrate-unclean-death`, `failure:two-version-compat`, `failure:sigterm-mid-load`) passes when run individually, confirming the new scenario is correctly wired and the chain's only failure is the already-documented pre-existing one.
- `npm run lint` (full repo) still fails on `apps/web/src/lib/sentry.ts`'s 4 pre-existing `@typescript-eslint/no-unsafe-*` errors, exactly as documented under Plan 22-06 above. Scoped lint (`npx eslint` against every file this plan touched) passes cleanly with zero errors.

## Plan 22-10

- **Out-of-scope discovery, not fixed (outside `files_modified`):** `apps/worker/src/queues/events-ingest.worker.ts` and `apps/worker/src/queues/webhook-events.worker.ts` still each carry a local, `TODO(22-02)`-marked duplicate of `isWorkspaceSoftDeleted` (`isWorkspaceSoftDeletedForIngest`/`isWorkspaceSoftDeletedForWebhookEvents`), per plan 22-03's own SUMMARY.md disclosure of its documented fallback for a not-yet-merged parallel-wave dependency (`packages/delivery-core/src/workspace-quiesce.ts`, plan 22-02). Both plans have since merged onto the phase branch, but neither local copy has been deleted/replaced with an import of the now-available shared helper — grep-confirmed still present at merge time (`grep -n "TODO(22-02)"` on both files). Functionally correct (both copies implement the identical fail-closed rule), but it is dead duplication a future edit to the shared rule could silently miss. Not fixed here — both files are `apps/worker` source, outside this plan's `files_modified` (`SPECIFICATION.md`, `docker/prod.env.example`, `docs/runbooks/backups.md`, `docs/runbooks/workspace-purge-and-restore.md`).
- **Out-of-scope discovery, not fixed:** `scripts/validate-prod-compose.mjs`'s `POOL_SUM_FLOOR` constant is hardcoded at `84` (plan 14-03's own connection-budget sum), not recalculated from `packages/db/src/pool.ts`'s `PG_POOL_SIZES`. Phase 22 added two new permanent `apps/worker` pools (`worker-workspace-purge`, `worker-workspace-purge-auth`, plans 22-01/22-07, both falling to `PG_POOL_DEFAULT_MAX = 2` since neither is named in `PG_POOL_SIZES`) that raise the real one-instance-each sum from 84 to 88 — no plan in this phase updated `POOL_SUM_FLOOR` to match. The CI gate itself still passes (`docker-compose.prod.yml`'s `PG_MAX_CONNECTIONS = 200` exceeds both 84 and the real 88), so nothing is functionally broken, but the constant's own doc comment ("Sum of maxima across every LONG-RUNNING production process... `max_connections` must exceed this") is now stated against a stale number. Filed as a known discrepancy in `SPECIFICATION.md` §5.17 rather than silently corrected — fixing the hardcoded constant is a `scripts/validate-prod-compose.mjs` code change, outside this plan's `files_modified`.
- **Re-confirmed, not caused by this plan:** `npm run lint` (full repo) still fails with the same 4 pre-existing `@typescript-eslint/no-unsafe-*` errors in `apps/web/src/lib/sentry.ts`, exactly as documented under Plans 22-02/22-06/22-09 above (unresolved `import.meta.env.*` types, this worktree's own `apps/web` has no `vite`/`@vitejs/plugin-react` resolution path). This plan's own touched files (`SPECIFICATION.md`, `docker/prod.env.example`, two `docs/runbooks/*.md` files) contain no JS/TS for ESLint to lint at all — the failure is unconditionally unrelated.

## Plan 22-06

- **`apps/web/src/lib/sentry.ts` fails `npm run lint`** with 4 `@typescript-eslint/no-unsafe-*` errors on `import.meta.env.*` member accesses (`.MODE`, `.VITE_SENTRY_RELEASE`, `.VITE_SENTRY_DSN`). Pre-existing, unrelated to this plan's changes (this plan touches only `packages/db`). Reproduces in this worktree because Vite's ambient `ImportMetaEnv` types are unresolvable here -- consistent with the documented worktree limitation "vite/Playwright are unresolvable in worktrees" (this project's `apps/web` has no local `vite`/`@vitejs/plugin-react` resolution path from inside a git worktree with no `node_modules` of its own). Not reproduced or fixed by this plan; verify against the main checkout (which has real `node_modules`) before treating as a real regression.
