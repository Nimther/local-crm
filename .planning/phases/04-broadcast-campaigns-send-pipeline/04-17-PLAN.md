---
phase: 04-broadcast-campaigns-send-pipeline
plan: 17
type: execute
wave: 1
depends_on: ["04-04", "04-08", "04-12"]
files_modified:
  - apps/worker/src/queues/send-dispatch.ts
  - apps/worker/src/queues/__tests__/send-dispatch-durability.test.ts
  - apps/web/src/features/campaigns/TestSendPanel.tsx
autonomous: true
gap_closure: true
requirements: [SEND-07, CAMP-04]

must_haves:
  truths:
    - "A test send whose SendGrid call returns a non-retryable 4xx (400/401/403/413) is reported as outcome 'failed', not silently swallowed as 'sent' — the observability defect that made a rejected test email look delivered is closed (matches the kind='campaign' disposition)."
    - "The test-send panel makes clear that the auto-filled dynamic_template_data is SAMPLE data pulled from a real segment contact — so the marketer no longer reads the sample contact's email as a bug versus the recipient address typed above (closes the D-18/D-19 as-designed 'wrong email' complaint)."
  artifacts:
    - "apps/worker/src/queues/send-dispatch.ts kind='test' branch returning outcome 'failed' on a >=400 SendGrid status"
    - "a regression test in send-dispatch-durability.test.ts pinning kind='test' 4xx → failed"
  key_links:
    - "the kind='test' response handler applies the same >=400 → failed disposition already used by the kind='campaign' branch (the two branches no longer diverge on 4xx)"
---

<objective>
Close the two remaining items from the UAT Test 4 diagnosis that are NOT the env root cause (that is 04-16): a latent worker observability defect and a UX-copy mismatch. Both are as-diagnosed in .planning/debug/test-send-no-delivery.md.

1. **Silent 4xx swallow (SEND-07 observability).** In apps/worker/src/queues/send-dispatch.ts the kind='test' branch (~lines 406–410) treats only `status === 429 || status >= 500` as a failure and returns `{ outcome: "sent" }` for everything else — including 400/401/403/413. So once the env fix lands and SendGrid IS reached, a rejected test send (unverified sender, bad template) would STILL be reported as sent. The kind='campaign' branch already does the right thing (~line 333: `status >= 400 → failed`). Mirror that disposition in the test branch.

2. **Sample-data mismatch (as-designed, UX copy).** The user reported the auto-filled `dynamic_template_data` email differs from the recipient input. This is working-as-designed per D-18/D-19: the test-sample endpoint deliberately previews a real segment contact's merge data; the recipient input only controls the delivery address. The fix is copy: label the JSON as sample data from a segment contact so the behavior reads as intentional.

Purpose: A rejected test send surfaces as failed (the marketer sees a real error instead of a false success), and the sample-JSON panel explains itself so the as-designed behavior stops looking like a bug (CAMP-04).
Output: A one-branch correctness fix in the worker + its regression test, and a clarifying label in the test-send panel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-broadcast-campaigns-send-pipeline/04-UAT.md
@.planning/debug/test-send-no-delivery.md

# The worker branch to fix (test path) + the campaign path to mirror (~line 333)
@apps/worker/src/queues/send-dispatch.ts
# The durability suite to extend — reuse its fixtures verbatim
@apps/worker/src/queues/__tests__/send-dispatch-durability.test.ts
# The test-send panel copy
@apps/web/src/features/campaigns/TestSendPanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Surface SendGrid 4xx on a test send as failed (mirror the campaign branch) + regression test</name>
  <files>apps/worker/src/queues/send-dispatch.ts, apps/worker/src/queues/__tests__/send-dispatch-durability.test.ts</files>
  <behavior>
    - processSendJob({ kind: "test", testTo, workspaceId, campaignId }) with an injected sendMail returning status 400 resolves to outcome "failed" (not "sent").
    - SendGrid is still called exactly once for that test send (the branch reaches the fetch, then classifies the response).
    - A 429/5xx test response still resolves to "rate_limited" (unchanged) and a <300 response still resolves to "sent" (unchanged).
  </behavior>
  <action>
    In apps/worker/src/queues/send-dispatch.ts, in the kind='test' branch, AFTER the existing `if (response.status === 429 || response.status >= 500) return { outcome: "rate_limited", ... }` check and BEFORE the final `return { outcome: "sent", sendId, providerMessageId }`, add a guard that returns `{ outcome: "failed", sendId }` when `response.status >= 400`. The test path skips the send-ledger insert entirely (D-12), so there is no row to record — only the returned outcome changes (from a false "sent" to "failed"). This mirrors the kind='campaign' branch's existing `status >= 400 → failed` disposition; add a short comment noting the two branches now agree on 4xx (the SendJobResult union already includes `{ outcome: "failed"; sendId }`, so no type change is needed).

    Add ONE regression test to the existing describe block in send-dispatch-durability.test.ts, reusing its fixtures verbatim (freshWorkspaceId, connectFixtureSendgridKey, createFixtureCampaign, countingSendMail — the vitest config already injects UNSUBSCRIBE_TOKEN_SECRET/PUBLIC_APP_URL so signing succeeds). The test arranges a fixture campaign, then calls `processSendJob({ workspaceId, campaignId, kind: "test", testTo: "probe@fixture.test" }, { sendMail: countingSendMail(400).fn, redisClient })` and asserts `result.outcome === "failed"` and the send-mail call count is exactly 1. Name it to pin SEND-07: a test-send 4xx is reported failed, never sent. Do not create a contactId (the test path defaults contactId to "test-send") and do not assert on the sends table (the test path writes no ledger row).
  </action>
  <verify>
    <automated>cd /Users/primeropanther/Projects/mega-crm && npm run test -w @mega-crm/worker 2>&1 | tail -30</automated>
    Confirm the test branch now has a >=400 failed guard distinct from the 429/5xx branch:
    grep -n "outcome" apps/worker/src/queues/send-dispatch.ts | tail -8
  </verify>
  <done>
    The worker test suite passes including the new SEND-07 regression; a kind='test' job whose SendGrid response is 400 resolves to outcome 'failed' with the SendGrid call made exactly once; 429/5xx and <300 test outcomes are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clarify the test-send panel sample-data copy (close the as-designed mismatch complaint)</name>
  <files>apps/web/src/features/campaigns/TestSendPanel.tsx</files>
  <action>
    In apps/web/src/features/campaigns/TestSendPanel.tsx, add a short Russian helper line clarifying that the dynamic_template_data JSON is SAMPLE data taken from a real contact in the campaign's segment — its email/fields are the sample contact's, and only the «Получатель» field above controls where the test email is actually delivered. Place it as muted helper text (className "text-sm text-muted-foreground") directly under the `<Label htmlFor="test-send-json">dynamic_template_data</Label>` (before the Skeleton/Textarea), or extend the existing CardDescription — executor's choice, but it MUST visually associate with the JSON block, not the recipient input. Keep it concise (one sentence). Change no logic: the query, mutation, state, and handlers stay identical; this is copy only. This reflects D-18/D-19 (the server's buildContactTemplateData sample), turning the reported "wrong email" from a perceived bug into self-explanatory behavior.
  </action>
  <verify>
    <automated>cd /Users/primeropanther/Projects/mega-crm && npm run build -w @mega-crm/web 2>&1 | tail -15</automated>
    Confirm the panel gained clarifying copy near the JSON block (expect a muted helper or extended description referencing the segment/sample):
    grep -niE "образец|сегмент|пример|получател" apps/web/src/features/campaigns/TestSendPanel.tsx
  </verify>
  <done>
    The web app typechecks/builds; TestSendPanel shows a concise clarification that the JSON is sample data from a segment contact and the «Получатель» field controls delivery; no behavior changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → SendGrid (test send) | The worker classifies SendGrid's HTTP status; mis-classifying a 4xx rejection as success hides real send failures from the operator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-17-01 | Repudiation | kind='test' 4xx handling in send-dispatch.ts | medium | mitigate | A non-retryable 4xx now returns outcome 'failed', so a rejected test send is truthfully reported instead of a false 'sent' — the operator can trust the result. Pinned by the new regression test. |
| T-04-17-02 | Information disclosure | test-send sample copy | low | accept | The sample JSON already exposes a real segment contact's merge data by design (D-18/D-19); the copy change only labels it. No new data is surfaced. |

No packages are installed by this plan (no npm/pip/cargo install tasks), so no supply-chain (T-*-SC) threat applies.
</threat_model>

<verification>
- worker: `npm run test -w @mega-crm/worker` green, including the new SEND-07 test-branch regression.
- web: `npm run build -w @mega-crm/web` typechecks/builds.
- Human (phase UAT re-run, after 04-16 env fix lands): send a test to an address whose sender/template is misconfigured → the UI surface reflects failure rather than a false success; the sample-JSON helper text explains the email shown is the sample contact's.
</verification>

<success_criteria>
- A test send rejected by SendGrid with a 4xx is reported failed, never sent.
- The test-send panel's sample-data behavior is self-explanatory, closing the "wrong email in JSON" complaint as as-designed.
- No change to the campaign send path or to test-send delivery semantics beyond the 4xx classification.
</success_criteria>

<output>
Create `.planning/phases/04-broadcast-campaigns-send-pipeline/04-17-SUMMARY.md` when done.
</output>
