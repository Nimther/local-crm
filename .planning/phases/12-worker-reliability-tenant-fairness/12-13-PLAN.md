---
phase: 12-worker-reliability-tenant-fairness
plan: 13
type: execute
wave: 9
depends_on: ["12-12"]
files_modified:
  - ARCHITECTURE.md
  - SPECIFICATION.md
autonomous: true
requirements: [WRK-09, WRK-13]
gap_closure: true
gap_ids: [G-12-2]

must_haves:
  truths:
    - "The forward-looking section names only work that genuinely has not shipped — bounded failed-job retention and the durable dead-letter path are no longer listed there, and the one item still open is stated on its own"
    - "The two documents agree with each other about failed-job retention and the dead-letter path instead of contradicting each other"
    - "The worker-scheduling sections describe consumption as an observed fact backed by a named test, not as an inference from a registration log line"
    - "The as-built mechanism that makes the run loop start under the production call shape is written down where the next person editing those factories will read it"
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "A forward-looking Phase 12 entry that lists only the genuinely unshipped item"
    - path: "SPECIFICATION.md"
      provides: "Worker-scheduling sections re-verified against observed consumption, including the run-loop option's as-built handling"
  key_links:
    - from: "SPECIFICATION.md §5.1/§5.2"
      to: "apps/worker/src/queues/__tests__/worker-autorun-default.test.ts"
      via: "the claim that every registered worker consumes is only true because a test constructs each factory with the composition root's call shape and asserts pickup — the document must cite that test as its evidence, not the boot log"
      pattern: "worker-autorun-default.test.ts"
---

<objective>
Close G-12-2: two documentation staleness defects. The forward-looking section still lists work that shipped inside this phase, and the worker-scheduling sections describe every registered worker as operational — a claim that was written from a registration log line rather than from observed consumption, and which was false until plan 12-12 landed.

Purpose: these two documents are the security-review and onboarding surface for this system. One of them currently contradicts the other about failed-job retention, and both describe a runtime that did not exist. A forward-looking section that lists shipped work trains readers to distrust it, which is worse than having no such section — and a specification that infers behaviour from a boot log is exactly the reasoning that let five dead workers pass review.
Output: a forward-looking entry reduced to what is genuinely still open, and worker-scheduling sections whose consumption claim cites the test that proves it.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-worker-reliability-tenant-fairness/12-UAT.md
</context>

## Gap being closed

`G-12-2` (major, documentation). Two independent staleness defects, both already diagnosed:

1. The forward-looking Phase 12 entry in `ARCHITECTURE.md` still lists bounded failed-job retention as unshipped. Plan 12-09 shipped it and plans 12-07 and 12-10 shipped the durable dead-letter path that made bounding it safe, and `SPECIFICATION.md` already documents both — so the two files directly contradict each other today. Only the backing store's memory-ceiling behaviour is genuinely still open.
2. `SPECIFICATION.md` §5.1 and §5.2 present every registered worker as operational. That was written from the worker-registration log line rather than from observed consumption, and it was false for five of the sixteen until plan 12-12 landed.

Fix order is causal, not stylistic: 12-12 must be green before §5.1/§5.2 can truthfully say what they say. The forward-looking entry is stale regardless of 12-12 and could have been fixed at any time.

**Language:** `ARCHITECTURE.md` is written in English and `SPECIFICATION.md` in Russian. Keep each file in its own language — do not introduce English prose into the Russian document.

**Editing discipline:** both files are large, long-lived documents that many phases have written into. Every change here is a scoped replacement of a specific passage. Never rewrite either file wholesale.

<tasks>

<task type="auto">
  <name>Task 1: Reduce the forward-looking Phase 12 entry to what is genuinely still open</name>
  <files>ARCHITECTURE.md</files>
  <read_first>
    - ARCHITECTURE.md, the forward-looking section near the end and its introductory sentence establishing that everything above it describes code that exists today (the Phase 12 bullet is the one to change; read its Phase 10, Phase 11 and Phase 14–15 siblings for the house voice and the level of specificity each carries)
    - SPECIFICATION.md §5.3, which already documents the bounded failed-job retention, the seven-day rationale and why bounding it was only safe once terminal failures were durably recorded — this is the passage the stale bullet contradicts
    - .planning/phases/12-worker-reliability-tenant-fairness/12-09-SUMMARY.md and 12-07-SUMMARY.md (what actually shipped, so the rewritten bullet cites the right plans)
  </read_first>
  <action>
Rewrite the forward-looking Phase 12 bullet in place, as a scoped replacement of that one bullet. It currently carries three claims: that per-tenant concurrency caps now exist, that queue retention policy is unshipped, and that the backing store's behaviour at its memory ceiling is unshipped. The middle claim is false — bounded failed-job retention shipped in plan 12-09, and the durable dead-letter path that made shortening it safe shipped in plans 12-07 and 12-10. Drop that claim from this section entirely, and instead point the reader at the specification section that documents the shipped policy, so the two files stop disagreeing.

What remains genuinely unshipped is the backing store's behaviour when it reaches its memory ceiling. State it on its own, in the same voice as the neighbouring bullets, and keep the existing cross-reference to the concurrency caps that do exist.

While you are in this section, read the Phase 10 and Phase 11 bullets and confirm they still describe unshipped work — both of those phases have since completed, and a section whose purpose is to prevent misreading loses that purpose if any of its entries describe the present. If either is now stale, say so in the summary as a finding; do not silently rewrite them, as they are outside this gap's scope and belong to their own phases' documentation.

Scan the rest of `ARCHITECTURE.md` for any other passage claiming failed jobs are retained indefinitely or that terminal failures have no durable record. Correct any you find, each as its own scoped replacement.
  </action>
  <verify>
    <automated>grep -q '^- \*\*Phase 12 — worker reliability.*memory ceiling' ARCHITECTURE.md && test "$(grep '^- \*\*Phase 12 — worker reliability' ARCHITECTURE.md | grep -c 'remain open')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0
    - The forward-looking Phase 12 bullet names the memory-ceiling item as the only unshipped one and points at the specification section documenting the shipped retention policy
    - The bullet no longer describes queue retention policy as unshipped
    - The bullet reads in the same voice and at the same specificity as its Phase 10, Phase 11 and Phase 14–15 siblings
    - Any stale Phase 10 or Phase 11 forward-looking entry is reported as a finding in the summary rather than rewritten here
    - `git diff --stat ARCHITECTURE.md` shows a bounded edit, not a whole-file rewrite
  </acceptance_criteria>
  <done>The forward-looking section lists only work that genuinely has not shipped, and no longer contradicts the specification about failed-job retention.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify the worker-scheduling sections against observed consumption</name>
  <files>SPECIFICATION.md</files>
  <precondition>Plan 12-12 must be landed and green: run `vitest run --root apps/worker src/queues/__tests__/worker-autorun-default.test.ts` and confirm it exits 0 before editing §5.1 or §5.2. If it does not, halt — this task's whole content is a claim about consumption that is only true once that fix is in, and writing it against a red suite would recreate the exact defect this gap exists to close.</precondition>
  <read_first>
    - SPECIFICATION.md §5.1 (what starts the worker: the six repeatable ticks, the scheduler-id table, the registration idempotency paragraph and the graceful-shutdown paragraph) and §5.2 (the sixteen registered workers)
    - SPECIFICATION.md §5.3 and §9, so the added text agrees with the retention and dead-letter passages already there and follows the existing convention of citing plan numbers and requirement ids inline
    - .planning/phases/12-worker-reliability-tenant-fairness/12-12-SUMMARY.md (the fix as landed, the idiom chosen, and the name of the test that pins it)
    - apps/worker/src/queues/campaign-scheduler.worker.ts and apps/worker/src/queues/flows/flow-segment-sweep.worker.ts (the fixed factory and the reference factory, so the described mechanism matches the code)
    - The 12-UAT.md diagnosis of G-12-1, for the precise statement of what registration does and does not imply
  </read_first>
  <action>
Amend §5.1 and §5.2 in Russian, as scoped replacements of the affected passages — do not restructure either section and do not touch the scheduler-id table's rows, which remain correct.

In §5.1, add a short passage recording the as-built handling of the factories' run-loop option, which this document does not currently mention at all. State three things: that the option exists only as a test-only factory argument and is never read from configuration or the environment; that each factory omits the key from the worker's options object entirely unless a caller supplied a value, because forwarding it with an undefined value would overwrite the library's own enabling default rather than fall back to it; and that this was a real defect, closed by plan 12-12, under which five of the six tick workers registered their schedulers correctly and then consumed nothing — name the affected files and the two phases the pattern originated in and spread from, in the same as-built-history voice the section already uses for the earlier registration-form migration.

In §5.1 and §5.2, correct the standing of the operational claim itself. Registration is not consumption: state explicitly that scheduler registration succeeding and a worker's processing loop running are two separate facts, that a clean boot log proves only the first, and that what backs the second is the named regression test constructing every tick factory with the composition root's exact single-argument call shape and asserting a queued job is picked up. Cite that test file by name, exactly as the section already cites the registration test as the mechanical proof of the single-registration-form claim.

Keep the count of sixteen registered workers in §5.2 and add to it that all sixteen are now confirmed as consuming, with the evidence being that test plus the post-fix boot observation, rather than the registration log line the previous wording rested on.

Add a one-line entry to the divergences or history section, following whatever convention that section already uses for entries of this kind, so that the fact this claim was once wrong is itself recorded rather than quietly overwritten.
  </action>
  <verify>
    <automated>grep -q 'autorun' SPECIFICATION.md && grep -q 'worker-autorun-default.test.ts' SPECIFICATION.md && grep -q '12-12' SPECIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0
    - §5.1 records the run-loop option as a test-only factory argument, never read from configuration, and states the omission mechanism and why forwarding an undefined value was the defect
    - §5.1 names the five affected factory files and the plan that closed the defect
    - §5.1 and §5.2 state that registration and consumption are separate facts and cite the regression test by filename as the evidence for consumption
    - §5.2 still records sixteen workers and now attributes the operational claim to that test rather than to the registration log line
    - The divergences or history section carries an entry recording that this claim was previously stated from registration alone
    - All added prose is in Russian, matching the surrounding text
    - `git diff --stat SPECIFICATION.md` shows a bounded edit, not a whole-file rewrite
  </acceptance_criteria>
  <done>The specification's worker-scheduling sections describe the runtime as observed, cite the test that proves consumption, and record the mechanism a future edit to those factories must not undo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → security review | These two files are the as-built surface a reviewer reads instead of the code; a false claim here is a review that checks the wrong thing |
| forward-looking section → present tense | The section's only value is that a reader can trust nothing in it describes today's system |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-13-01 | Repudiation | the two documents contradicting each other about failed-job retention | medium | mitigate | The forward-looking entry drops the shipped claim and points at the specification passage that documents the policy, so one file is the single statement of it. |
| T-12-13-02 | Repudiation | an operational claim inferred from a boot log | high | mitigate | The consumption claim is restated as an observed fact citing the regression test by filename, and the sections state explicitly that registration and consumption are separate facts — the reasoning error, not only its result, is corrected. |
| T-12-13-03 | Information Disclosure | as-built prose describing internals | low | accept | Both files already describe the same subsystems at this level of detail by design; this plan adds no secret, credential, endpoint or key material, only a factory-argument mechanism already visible in the repository. |
| T-12-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no packages — it edits two Markdown files. If an install becomes necessary, stop and route it through the package legitimacy gate. |
</threat_model>

<verification>
- `vitest run --root apps/worker src/queues/__tests__/worker-autorun-default.test.ts` exits 0 (the precondition for task 2)
- The forward-looking Phase 12 bullet names only the memory-ceiling item and no longer describes retention as unshipped
- `SPECIFICATION.md` §5.1 documents the run-loop option's as-built handling and cites the regression test by filename
- `SPECIFICATION.md` §5.2 attributes the sixteen-workers-operational claim to that test rather than to the registration log line
- `git diff --stat ARCHITECTURE.md SPECIFICATION.md` shows two bounded edits, no whole-file rewrites
- All added `SPECIFICATION.md` prose is in Russian

</verification>

<success_criteria>
- The forward-looking section lists only genuinely unshipped work
- The two documents no longer contradict each other about failed-job retention or the dead-letter path
- The consumption claim is backed by a named test and the registration-is-not-consumption distinction is written down
- The mechanism keeping the run loop alive is documented where a future edit to those factories will read it
</success_criteria>

<output>
Create `.planning/phases/12-worker-reliability-tenant-fairness/12-13-SUMMARY.md` when done
</output>
