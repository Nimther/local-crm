---
phase: 13-compliance-analytics-integrity
plan: 10
type: execute
wave: 5
depends_on: [13-08]
files_modified:
  - packages/db/migrations/0059_contact_erasure.sql
  - packages/db/migrations/meta/_journal.json
  - packages/db/src/schema/contacts.ts
  - packages/db/src/schema/erasure-records.ts
  - packages/db/src/schema/index.ts
  - packages/shared-schemas/src/queues.ts
  - apps/api/src/modules/contacts/contact.repository.ts
  - apps/api/src/modules/contacts/__tests__/contact-erasure.test.ts
  - apps/api/src/modules/contacts/__tests__/contact-crud.test.ts
autonomous: true
requirements: [CMP-04]

produces_schema_change: true

must_haves:
  truths:
    - Deleting a contact leaves its row present with `email`, `first_name`, `last_name`, `phone` null, `attributes` emptied, and `anonymized_at` set.
    - Foreign keys from `sends`, `subscription_status_history`, and `events` to the anonymized contact still resolve after the delete, so delivery and consent evidence stays provably linked.
    - Deleting a contact whose status was unsubscribed or suppressed leaves that address on the workspace suppression list, so mail stops immediately and the address cannot be resurrected by re-import.
    - Deleting a contact writes exactly one `erasure_records` row in the pending state, in the same transaction as the anonymization.
    - Deleting a contact enqueues exactly one erasure-scrub job with a deterministic job id, so a retried request does not queue a second scrub.
    - An anonymized contact does not appear in the workspace's contact list or contact detail reads, so the API's delete semantics are unchanged from the caller's point of view.
    - Creating a new contact with the email of a previously anonymized contact does not collide with the anonymized row.
    - Two anonymized contacts in the same workspace can coexist despite the workspace-plus-email unique constraint.
  artifacts:
    - packages/db/migrations/0059_contact_erasure.sql
    - packages/db/src/schema/erasure-records.ts
    - apps/api/src/modules/contacts/__tests__/contact-erasure.test.ts
  key_links:
    - "deleteContact's UPDATE replacing the DELETE -> every read path that assumed row-absence meant deleted: contact list, contact detail, and the email-taken check all shift from absence semantics to anonymized-row semantics, and all must move together."
    - "erasure_records row (pending) -> the scrub job's deterministic jobId -> plan 13-13's worker marking it complete: the record is the auditable proof the scrub ran, so it must be written in the same transaction as the anonymization, not after."
    - "contacts_workspace_email_unique on (workspace_id, email) + NULL email: Postgres treats NULL as distinct, which is what lets many anonymized rows coexist without a constraint change."
  prohibitions:
    - statement: A contact row is never hard-deleted; erasure is an in-place anonymization that preserves the row and its foreign keys.
      status: flagged
      verification: unverified
    - statement: Mail never continues to an erased address; the suppression entry and the status resolution happen synchronously in the delete request, not in the background scrub.
      status: flagged
      verification: unverified
    - statement: An anonymized contact is never presented to a tenant as a live contact.
      status: flagged
      verification: unverified
    - statement: The erasure record is never written outside the transaction that anonymizes the row.
      status: flagged
      verification: unverified
---

<objective>
Build CMP-04's synchronous half (D-01, D-04): replace `deleteContact`'s hard `DELETE FROM contacts` with an in-place anonymization that scrubs the PII columns, sets an `anonymized_at` marker, keeps the row and every foreign key pointing at it, writes an auditable erasure record, and enqueues the background scrub that plan 13-13 consumes. Mail stops in the request, not in the job.

Purpose: the erasure-versus-evidence tension resolves via anonymisation-with-retained-evidence (the ICO direction recorded in PROJECT.md). A hard delete destroys the ability to later prove a send or a suppression was lawful; keeping plaintext PII is not lawful. Anonymizing in place is what satisfies both.
Output: migration 0059 (`anonymized_at`, `erasure_records`), the rewritten `deleteContact`, the read-path changes that keep the API's delete semantics intact, and the scrub job's queue contract.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-compliance-analytics-integrity/13-CONTEXT.md
@.planning/phases/13-compliance-analytics-integrity/13-RESEARCH.md
@.planning/phases/13-compliance-analytics-integrity/13-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols and paths created by THIS plan:

- Columns: `contacts.anonymized_at`
- Tables: `erasure_records`
- Files: `packages/db/migrations/0059_contact_erasure.sql`, `packages/db/src/schema/erasure-records.ts`, `apps/api/src/modules/contacts/__tests__/contact-erasure.test.ts`
- Drizzle exports: `erasureRecords`
- Functions: `anonymizeContact`, `createErasureRecord`
- Types: `ErasureRecordStatus`
- Constants: `ERASURE_SCRUB_QUEUE`, `ERASURE_SCRUB_SCHEMA_VERSION`
- Schemas: `erasureScrubJobSchema`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Erasure schema — anonymized_at, erasure_records, and [BLOCKING] apply</name>
  <files>packages/db/migrations/0059_contact_erasure.sql, packages/db/migrations/meta/_journal.json, packages/db/src/schema/contacts.ts, packages/db/src/schema/erasure-records.ts, packages/db/src/schema/index.ts</files>
  <read_first>
    - packages/db/src/schema/contacts.ts (the whole file — the PII columns `email`/`first_name`/`last_name`/`phone`/`attributes`, their nullability, and the `contacts_workspace_email_unique` constraint on `(workspace_id, email)`)
    - packages/db/migrations/0004_contacts_rls_policies.sql and 0044_workspace_isolation_fail_closed.sql (the fail-closed `workspace_isolation` policy form every tenant table uses — `erasure_records` is tenant data and needs it)
    - packages/db/migrations/0053_flow_segment_sweep_checkpoint.sql (house style for a small tenant-RLS table added by a hand-written migration)
    - packages/db/src/schema/dead-letter-jobs.ts (Drizzle type-inference file convention)
    - packages/db/src/__tests__/migrate-from-empty.test.ts and migrate-incremental.test.ts (this project's schema-apply mechanism)
  </read_first>
  <behavior>
    - Applying 0059 from an empty schema and incrementally over an existing schema both succeed.
    - Two `contacts` rows in the same workspace with null `email` and `anonymized_at` set can coexist.
    - A `contacts` row with a non-null `email` still collides with a second row having the same workspace and email.
    - `erasure_records` rejects a row whose `status` is outside the allowed set.
    - `erasure_records` rows for a workspace are unreadable from a tenant transaction scoped to a different workspace.
    - Deleting a workspace cascades away its `erasure_records` rows.
    - Pre-existing `contacts` rows have a null `anonymized_at` after the migration, and no other column is altered.
  </behavior>
  <action>
Write migration `0059_contact_erasure.sql` and append the matching `meta/_journal.json` entry (idx 59, tag `0059_contact_erasure`).

Add a nullable `anonymized_at timestamptz` column to `contacts`, plus a partial index on `(workspace_id)` filtered to `anonymized_at IS NOT NULL` so evidence queries over anonymized rows do not scan the whole contacts table. Add a `COMMENT ON COLUMN` recording D-01: a non-null value means this contact was erased on request, its PII columns were scrubbed, and the row is retained solely so the foreign keys from `sends`, `subscription_status_history`, and `events` still resolve — which is what makes it possible to later prove a send or a suppression was lawful.

Do not alter `contacts_workspace_email_unique`. Confirm and record in the migration comment why no change is needed: Postgres treats NULL as distinct in a unique constraint, so any number of anonymized rows with a null `email` coexist in one workspace, while two live rows with the same non-null email still collide exactly as before. This is the discretion item CONTEXT flagged (`contacts` unique-constraint handling for anonymized rows) and the answer is that the existing constraint already has the right behavior — assert both halves of that in the tests rather than asserting only the anonymized half.

Create an `erasure_records` table: `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `workspace_id uuid NOT NULL REFERENCES organization(id) ON DELETE CASCADE`, `contact_id uuid NOT NULL REFERENCES contacts(id) ON DELETE CASCADE`, `requested_at timestamptz NOT NULL DEFAULT now()`, `anonymized_at timestamptz NOT NULL`, `scrub_started_at timestamptz`, `scrub_completed_at timestamptz`, `status text NOT NULL DEFAULT 'pending'` with a CHECK constraining it to `pending`, `scrubbing`, `complete`, `failed`, `scrub_error text`, `sends_scrubbed integer NOT NULL DEFAULT 0`, `events_scrubbed integer NOT NULL DEFAULT 0`. Give it `ENABLE` + `FORCE ROW LEVEL SECURITY` and the fail-closed `workspace_isolation` policy — this is tenant data describing a tenant's own compliance action, not platform-ops metadata, so it does not follow `dead_letter_jobs`' no-RLS precedent. Add a `COMMENT ON TABLE` stating that this row is the auditable proof the scrub ran, which is why it is written in the same transaction as the anonymization rather than by the scrub job itself.

Record one known limitation in the migration comment, because it is a real gap and silence would read as an oversight: contacts hard-deleted before this migration are already gone, so no anonymized-evidence trail can be retrofitted for them. CMP-04's evidence guarantee applies prospectively from this migration onward. There is nothing to migrate — the rows do not exist — so this is a documentation obligation, not a backfill.

Add `anonymizedAt` to the `contacts` Drizzle table, create `packages/db/src/schema/erasure-records.ts`, and export it from `packages/db/src/schema/index.ts`.

[BLOCKING] After both schema files are final, apply and verify the schema: run `npm run lint:migrations` and `npm run test:migrations`, both of which must exit 0. This project's schema-push mechanism is the hand-written SQL migration applied through the ephemeral-database harness. Do NOT rely on `npm run db:migrate` (drizzle-kit CLI) — it hangs in this dev sandbox under Node v26 per the STATE.md note carried from Phase 12 — and do not treat a green `npm run build` as evidence of a real apply, since Drizzle's types come from the schema files rather than the live database. Additionally assert the migration is non-destructive against real data: seed a `contacts` row before applying 0059 in the incremental path and confirm every pre-existing column value is byte-identical afterwards with `anonymized_at` null.
  </action>
  <acceptance_criteria>
    - `packages/db/migrations/0059_contact_erasure.sql` contains `ADD COLUMN` for `anonymized_at` on `contacts` and `CREATE TABLE erasure_records`.
    - That migration contains `FORCE ROW LEVEL SECURITY` and `CREATE POLICY workspace_isolation ON erasure_records`.
    - That migration contains no `ALTER` touching `contacts_workspace_email_unique`, and its comment explains why none is needed.
    - `packages/db/migrations/meta/_journal.json` contains an entry with tag `0059_contact_erasure`.
    - Inserting two `contacts` rows in one workspace with null `email` succeeds.
    - Inserting two `contacts` rows in one workspace with the same non-null `email` is rejected.
    - Inserting an `erasure_records` row with `status = 'bogus'` is rejected.
    - An `erasure_records` row is unreadable from `withTenant` scoped to another workspace.
    - A `contacts` row seeded before 0059 has identical column values afterwards and a null `anonymized_at`.
    - `npm run lint:migrations` exits 0.
    - `npm run test:migrations` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint:migrations && npm run test:migrations</automated>
  </verify>
  <done>The anonymization marker and the auditable erasure record exist with fail-closed RLS, the existing email uniqueness behavior is proven correct for both live and anonymized rows, and 0059 applies both ways without disturbing existing data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite deleteContact as anonymize-in-place with an erasure record and a scrub job</name>
  <files>packages/shared-schemas/src/queues.ts, apps/api/src/modules/contacts/contact.repository.ts, apps/api/src/modules/contacts/__tests__/contact-erasure.test.ts</files>
  <read_first>
    - apps/api/src/modules/contacts/contact.repository.ts lines 380-407 (the current `deleteContact`: the hard `DELETE ... RETURNING email, subscription_status`, the conditional plaintext `workspace_suppressions` insert, and the boolean return contract callers depend on) plus `createContact` and `updateContact` (the `withTenantTransaction` + `getWorkspaceId()` + typed-error conventions this must follow)
    - packages/contacts-core/src/contact-repository.ts (`isEmailSuppressed` and `isEmailTaken` — the reads that participate in re-import protection)
    - packages/shared-schemas/src/queues.ts lines 80-115 (the `schemaVersion` deploy-safety convention) and the `eventsIngestJobSchema` doc comment (the deterministic-idempotency-key convention: a job id derived from the work, not from the clock)
    - apps/api/src/modules/contacts/__tests__/contact-crud.test.ts and subscription-status.test.ts (existing delete assertions, including the D-08/D-11 suppression-on-delete expectations that must keep holding)
    - apps/worker/src/queues/flows/flow-segment-sweep.worker.ts (how a job is enqueued with a deterministic `jobId` from an API-side caller, if such a precedent exists there; otherwise follow `events-ingest`'s deterministic-id rationale)
  </read_first>
  <behavior>
    - Deleting a subscribed contact leaves the row present with null `email`, `first_name`, `last_name`, `phone`, an emptied `attributes`, and a non-null `anonymized_at`.
    - Deleting an unsubscribed contact additionally leaves that address on `workspace_suppressions`.
    - Deleting a subscribed contact does not add a suppression row, preserving the existing D-08 rule that only unsubscribed or suppressed addresses are preserved.
    - Deleting a contact writes exactly one `erasure_records` row with status `pending`, `anonymized_at` set, and `scrub_completed_at` null.
    - Deleting a contact enqueues exactly one erasure-scrub job whose id is derived from the erasure record, so calling delete twice for the same contact does not enqueue a second scrub.
    - Deleting an already-anonymized contact returns the same boolean result as before and does not write a second erasure record.
    - Deleting a nonexistent contact returns false and writes nothing.
    - A `sends` row that referenced the deleted contact still resolves its `contact_id` after the delete.
    - A `subscription_status_history` row for the deleted contact still resolves after the delete.
  </behavior>
  <action>
Add `ERASURE_SCRUB_QUEUE`, `ERASURE_SCRUB_SCHEMA_VERSION = 1`, and `erasureScrubJobSchema` (carrying `schemaVersion`, `workspaceId`, `contactId`, `erasureRecordId`) to `packages/shared-schemas/src/queues.ts`, following the `schemaVersion` deploy-safety doc-comment convention. The queue contract is defined here rather than in plan 13-13 because this plan is the producer and this wave owns the schema file; 13-13 implements the consumer.

Rewrite `deleteContact` in `apps/api/src/modules/contacts/contact.repository.ts`, keeping its existing `withTenantTransaction` + `getWorkspaceId()` shape and its boolean return contract so route-level callers do not change. Inside the transaction, in this order:

1. Replace the hard `DELETE FROM contacts` with an anonymizing UPDATE setting `email = NULL, first_name = NULL, last_name = NULL, phone = NULL, attributes = '{}'::jsonb, anonymized_at = now(), updated_at = now()`, scoped to `workspace_id` and `id`, gated on `anonymized_at IS NULL` so a repeated request is a no-op, and returning the pre-update `email` and `subscription_status`. Capturing the email from the UPDATE's own returning clause matters: after this statement the address is gone from the row, and the suppression insert in step 2 needs it. If the UPDATE returns no row, distinguish "no such contact" (return false) from "already anonymized" (return true, write nothing further) — the second must not create a duplicate erasure record.
2. Keep the existing conditional suppression insert exactly as it is, including its `reason` of `contact_deleted` and its unsubscribed-or-suppressed status gate. It still writes the plaintext address at this point in the phase; plan 13-12 converts this column and all four of its call sites to a hash in one change, because a column the send pipeline compares against on every send cannot hold a mix of plaintext and hashed values. Sequencing the column swap after the anonymization is expand/contract ordering, and the suppression guarantee is unbroken throughout: an erased unsubscribed address is unmailable before 13-12 and unmailable after it.
3. Insert one `erasure_records` row with `status = 'pending'`, `anonymized_at` set to the value written in step 1, and `contact_id`. This must be in the same transaction as the anonymization — the record is the auditable proof the erasure happened, and a record written afterwards could be lost by a crash that left the row anonymized, producing an erasure with no evidence it occurred.
4. Enqueue one erasure-scrub job with a `jobId` deterministically derived from the erasure record's id, so a retried HTTP request or a duplicated call cannot queue a second scrub over the same contact. Enqueue after the transaction commits, or inside it if the existing codebase convention for API-side enqueues does so — check how other API-side enqueues in this repo order the commit and the enqueue, and follow that. State which you did and why in the SUMMARY, because the two have different failure modes: enqueueing inside risks a job for a rolled-back transaction, enqueueing after risks a committed erasure with no scrub queued. The second is recoverable (the erasure record is `pending` and visible), the first is not.

Update `deleteContact`'s doc comment to state the new semantics plainly: this is an erasure, not a row removal; the row and its foreign keys survive; mail stops in this request because the suppression entry and status are resolved here; and the JSONB PII in linked `send_events` and `events` rows is scrubbed asynchronously by the job this enqueues, tracked to completion by the erasure record.

Write `apps/api/src/modules/contacts/__tests__/contact-erasure.test.ts` covering every case in the behavior list, including the two foreign-key-still-resolves assertions and the deterministic-job-id double-delete case.
  </action>
  <acceptance_criteria>
    - After deleting a seeded contact, `SELECT count(*) FROM contacts WHERE id = <that id>` inside its workspace returns 1.
    - `packages/shared-schemas/src/queues.ts` exports `erasureScrubJobSchema` and `ERASURE_SCRUB_QUEUE`.
    - After deleting a seeded contact, its `contacts` row is present with null `email` and non-null `anonymized_at`.
    - After deleting a seeded unsubscribed contact, `workspace_suppressions` contains a row for that address with reason `contact_deleted`.
    - After deleting a seeded subscribed contact, `workspace_suppressions` contains no row for that address.
    - After deleting a seeded contact, exactly one `erasure_records` row exists for it with status `pending`.
    - Calling delete twice for the same contact leaves exactly one `erasure_records` row and one enqueued job.
    - After deleting a contact that had a `sends` row, that `sends` row's `contact_id` still resolves to the anonymized contact.
    - `npx vitest run --root apps/api src/modules/contacts/__tests__/contact-erasure.test.ts` exits 0.
    - `npx vitest run --root apps/api src/modules/contacts/__tests__/contact-crud.test.ts src/modules/contacts/__tests__/subscription-status.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --root apps/api src/modules/contacts/__tests__/contact-erasure.test.ts src/modules/contacts/__tests__/contact-crud.test.ts src/modules/contacts/__tests__/subscription-status.test.ts src/modules/contacts/__tests__/subscription-status-history.test.ts</automated>
  </verify>
  <done>A delete request erases the person's data in place, stops mail immediately, leaves the evidence chain intact, and queues exactly one tracked scrub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep the API's delete semantics — anonymized rows are invisible to tenant reads</name>
  <files>apps/api/src/modules/contacts/contact.repository.ts, apps/api/src/modules/contacts/__tests__/contact-crud.test.ts</files>
  <read_first>
    - apps/api/src/modules/contacts/contact.repository.ts (every read function: the list query with its filters and pagination, the by-id detail read, `updateContact`, and whatever the CSV-import upsert path reads to decide create-versus-update)
    - packages/contacts-core/src/contact-repository.ts (`isEmailTaken` and `isEmailSuppressed`, and the shared column-selection constant the reads project)
    - apps/api/src/modules/contacts/__tests__/contact-crud.test.ts and upsert-priority.test.ts (the existing list/detail/upsert expectations that define what callers currently observe after a delete)
    - apps/api/src/modules/contacts/__tests__/csv-import.test.ts (the import path's create-versus-update decision, which must not resurrect an anonymized row)
  </read_first>
  <behavior>
    - After deleting a contact, the workspace contact list does not include it.
    - After deleting a contact, the contact-detail read for its id behaves as it did when the row was hard-deleted.
    - After deleting a contact, the workspace contact count excludes it.
    - Creating a contact with the deleted contact's former email succeeds and produces a new row, not a revival of the anonymized one.
    - Importing a CSV row with the deleted contact's former email creates a new contact rather than updating the anonymized row.
    - Updating an anonymized contact by id does not resurrect it or repopulate any PII column.
    - Segment and audience reads that count contacts do not count anonymized rows.
  </behavior>
  <action>
Add `anonymized_at IS NULL` to every tenant-facing `contacts` read predicate: the list query, the by-id detail read, the count used for the workspace contact total, the `isEmailTaken` check, and the import path's create-versus-update lookup. Also audit the segment evaluation and campaign audience read paths for `contacts` predicates and exclude anonymized rows there — an erased person must not receive mail, and an audience that still counts them would both mis-report the audience size and attempt a send to a null address.

This is the read-side half of the semantics shift the previous task started: the API's contract with a tenant is unchanged ("I deleted this contact and it is gone"), while the database keeps the row for evidence. Both halves must land together, because a delete that anonymizes without also filtering the reads shows the tenant a nameless, emailless contact in their list, which is worse than either behavior alone. Record in a comment on the shared read predicate that `anonymized_at IS NULL` is a tenant-visibility filter, not a soft-delete convention, and that evidence queries over `sends`/`send_events`/`subscription_status_history` deliberately do NOT apply it.

Make `updateContact` refuse to modify an anonymized row rather than silently updating zero rows or repopulating a scrubbed column. Use the existing typed-error convention (`ContactValidationError` or a sibling) so the route surfaces it the way every other validation failure surfaces, and record in the SUMMARY which error type you used and what HTTP status it produces.

Extend `apps/api/src/modules/contacts/__tests__/contact-crud.test.ts` with the cases in the behavior list. For the segment and audience assertions, add them to whichever existing test file owns those reads rather than creating a new one, and list in the SUMMARY every read path you found and filtered — an unfiltered read path discovered later is a leak of a scrubbed row into a tenant surface, so the enumeration itself is part of the deliverable.
  </action>
  <acceptance_criteria>
    - Every tenant-facing `contacts` read in `apps/api/src/modules/contacts/contact.repository.ts` constrains on `anonymized_at IS NULL`.
    - After deleting a seeded contact, the list read returns zero matching rows for it and the workspace count excludes it.
    - Creating a contact with the deleted contact's former email succeeds and yields a different contact id.
    - A CSV import row carrying the deleted contact's former email creates a new contact row.
    - Calling `updateContact` on an anonymized contact raises the chosen typed error and leaves every PII column null.
    - A segment or audience count over the workspace excludes the anonymized contact.
    - `npx vitest run --root apps/api src/modules/contacts/__tests__` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --root apps/api src/modules/contacts/__tests__ && npx vitest run --root apps/api src/modules/campaigns/__tests__ && npx vitest run --root apps/api src/modules/segments/__tests__</automated>
    <human-check>In a dev workspace, delete a contact through the UI and confirm it disappears from the contacts list and from a segment preview that previously matched it, then confirm a campaign audience count for that segment decreases by one, and finally confirm the contact's former send rows are still visible in the send log.</human-check>
  </verify>
  <done>A tenant sees exactly the delete behavior they saw before, while the evidence row and its foreign keys remain queryable behind the scenes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenant delete request → contact PII | The erasure action itself; a partial erasure is both a compliance failure and an evidence failure. |
| Anonymized row → tenant-facing reads | A scrubbed row leaking into a list or an audience is a data-quality and a mailing hazard. |
| Anonymized row → send pipeline audience | An erased person must not be mailed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-10-01 | Information Disclosure | Incomplete PII scrub on the contacts row | high | mitigate | The anonymizing UPDATE nulls every PII column named in the schema (`email`, `first_name`, `last_name`, `phone`) and empties `attributes`; asserted column-by-column after a real delete. |
| T-13-10-02 | Repudiation | Erasure with no auditable proof it occurred | high | mitigate | The `erasure_records` row is written in the same transaction as the anonymization, so a crash cannot leave an anonymized row with no record. Asserted by the exactly-one-record test. |
| T-13-10-03 | Information Disclosure | Scrubbed row leaking into a tenant surface | high | mitigate | `anonymized_at IS NULL` is added to every tenant-facing contacts read, including segment and audience paths, and the full enumeration is recorded in the SUMMARY. |
| T-13-10-04 | Tampering | Mail continuing to an erased address | high | mitigate | Suppression and status are resolved synchronously in the delete request, not in the background scrub; the audience reads additionally exclude anonymized rows. |
| T-13-10-05 | Tampering | Resurrecting an erased contact via re-import or update | medium | mitigate | The import path's create-versus-update lookup excludes anonymized rows so a re-import creates a new contact, and `updateContact` refuses an anonymized row outright. |
| T-13-10-06 | Denial of Service | Duplicate scrub jobs from a retried request | medium | mitigate | The scrub job's id is derived deterministically from the erasure record, so a repeated delete cannot queue a second scrub. Asserted by the double-delete test. |
| T-13-10-07 | Repudiation | No evidence trail for contacts deleted before this phase | low | accept | Those rows are already gone; there is nothing to retrofit. Recorded as a documented limitation in the migration comment — CMP-04's guarantee is prospective from this migration onward. |
| T-13-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced (RESEARCH.md Package Legitimacy Audit: zero new dependencies). Any install proposed during execution requires slopcheck plus a blocking human checkpoint. |
</threat_model>

<flagged_assumptions>
Probe row **CMP-04** came back `unclassified` / `unresolved` (no SPEC.md exists for this phase). Surfaced explicitly, not auto-backstopped:

- **CMP-04 (unresolved):** the `must_haves.truths` above were derived goal-backward from ROADMAP success criterion 3 and CONTEXT D-01/D-04, not from a verified SPEC edge row. Three unverified judgements are recorded rather than buried: (1) that nulling `email`/`first_name`/`last_name`/`phone` and emptying `attributes` constitutes adequate anonymization of the contacts row — this is a legal-sufficiency judgement inherited from the ICO direction recorded in PROJECT.md, not one this plan can verify, and it depends on `attributes` being the only other PII-bearing column on the row, which Task 1's read of the schema must confirm; (2) that hiding anonymized rows from tenant reads is the right resolution of "delete" semantics rather than surfacing them as tombstones — chosen because it preserves the API contract callers already depend on; (3) that the "mail stops immediately" guarantee is fully discharged by the synchronous suppression plus status resolution plus audience filtering, with no in-flight send able to slip through — an already-enqueued send job for that contact is out of this plan's reach, and whether the pre-send gate catches it depends on plan 13-12's suppression check running against the freshly written suppression row. Flagged so 13-12 carries that assertion.
- **Interim state, deliberately sequenced, not a reduction:** step 2 of Task 2 keeps writing the plaintext address to `workspace_suppressions`. Plan 13-12 converts that column and all four of its call sites to an HMAC in one atomic change, because a column the send pipeline compares against on every send cannot hold a mix of plaintext and hashed values. The suppression guarantee is unbroken across both plans; what changes in 13-12 is that no plaintext address survives.
</flagged_assumptions>

<verification>
- `npm run lint:migrations` and `npm run test:migrations` exit 0.
- `npx vitest run --root apps/api src/modules/contacts/__tests__ src/modules/campaigns/__tests__ src/modules/segments/__tests__` exits 0.
- `npx vitest run --root packages/contacts-core` exits 0.
- `npm run lint` and `npm run build` exit 0.
- Human check on the UI delete flow per Task 3.
</verification>

<success_criteria>
- A deleted contact's PII is gone from the row while the row and its foreign keys remain, provably.
- Exactly one erasure record is written, in the same transaction, and exactly one scrub is queued.
- The tenant's observed delete behavior is unchanged, including for segments and audiences.
- Mail stops in the request that erases.
</success_criteria>

<output>
Create `.planning/phases/13-compliance-analytics-integrity/13-10-SUMMARY.md` when done. Record: the complete enumeration of contacts read paths that were filtered on `anonymized_at IS NULL`, whether the scrub enqueue happens inside or after the transaction and why, the typed error chosen for updating an anonymized contact, and confirmation of whether `attributes` is the only remaining PII-bearing column on `contacts`.
</output>
