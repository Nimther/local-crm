---
phase: 13-compliance-analytics-integrity
plan: 12
type: execute
wave: 6
depends_on: [13-10]
files_modified:
  - packages/db/migrations/0060_suppression_hash_expand.sql
  - packages/db/migrations/0061_suppression_hash_contract.sql
  - packages/db/migrations/meta/_journal.json
  - packages/db/src/schema/suppressions.ts
  - packages/db/src/schema/workspace-suppression-keys.ts
  - packages/db/src/schema/index.ts
  - packages/contacts-core/src/suppression-hash.ts
  - packages/contacts-core/src/contact-repository.ts
  - packages/contacts-core/src/index.ts
  - packages/contacts-core/src/__tests__/suppression-hash.test.ts
  - apps/api/src/modules/contacts/contact.repository.ts
  - apps/worker/src/queues/webhook-events.worker.ts
  - packages/db/scripts/rehash-suppressions.ts
  - packages/db/package.json
  - package.json
  - packages/db/src/__tests__/suppression-hash-migration.test.ts
autonomous: true
requirements: [CMP-04]

produces_schema_change: true

must_haves:
  truths:
    - The workspace suppression list stores an HMAC of the normalized email and no plaintext address.
    - An address suppressed before this change is still suppressed after it — the pre-send and pre-create suppression checks find it by hash.
    - An erased contact's address remains unmailable even though no plaintext form of it survives anywhere.
    - Two addresses differing only in letter case or surrounding whitespace hash to the same value, so `A@B.com` cannot be re-imported past a suppression on `a@b.com`.
    - Each workspace's suppression hashes are computed under that workspace's own key, so a hash from one workspace never matches a hash in another.
    - The HMAC key is never stored in plaintext at rest; it is wrapped by the existing envelope-encryption provider.
    - The suppression check does not perform a key-management round trip on every candidate recipient.
    - The contract migration refuses to run while any suppression row still lacks a hash, so a partially converted column cannot reach the send pipeline.
    - Every read and write site of the suppression list uses the hash; none compares a plaintext address.
  artifacts:
    - packages/db/migrations/0060_suppression_hash_expand.sql
    - packages/db/migrations/0061_suppression_hash_contract.sql
    - packages/contacts-core/src/suppression-hash.ts
    - packages/db/scripts/rehash-suppressions.ts
  key_links:
    - "normalizeSuppressionEmail -> hashSuppressionEmail -> the unique index on (workspace_id, email_hash): the normalization is part of the identity, so a change to it silently breaks every stored hash."
    - "The per-workspace wrapped key -> the in-process TTL cache -> the pre-send suppression check: without the cache this adds a key-unwrap round trip to the hottest path in the platform."
    - "0061's fail-closed assertion -> the send pipeline: a column holding a mix of plaintext and hashed values would make the suppression check silently miss real suppressions, so the contract step must refuse rather than proceed."
  prohibitions:
    - statement: No plaintext email address survives in the workspace suppression list after this change.
      status: flagged
      verification: unverified
    - statement: The suppression column is never left holding a mix of plaintext and hashed values in a state the send pipeline can read.
      status: flagged
      verification: unverified
    - statement: The HMAC key material is never written to disk, to Redis, or to a log, and is never committed to the repository or an env file.
      status: flagged
      verification: unverified
    - statement: A suppression comparison is never performed against a plaintext address after this change.
      status: flagged
      verification: unverified
---

<objective>
Complete CMP-04's evidence-hygiene half (D-02): `workspace_suppressions` stores plaintext email addresses today, so a contact erased by plan 13-10 leaves their address readable in the suppression list — the row that exists specifically to keep them unmailable. Replace the plaintext column with an HMAC of the normalized address, computed under a per-workspace key wrapped by the existing envelope-encryption provider, and convert every read and write site in one change.

Purpose: erasure must not weaken suppression. The erased person's address has to stay unmailable while no plaintext form of it survives — evidence and suppression both outlive the PII.
Output: a per-workspace suppression key, a pure normalize-and-hash module, an expand/backfill/contract migration sequence whose contract step fails closed, and every call site converted.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-compliance-analytics-integrity/13-CONTEXT.md
@.planning/phases/13-compliance-analytics-integrity/13-RESEARCH.md
@.planning/phases/13-compliance-analytics-integrity/13-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols and paths created by THIS plan:

- Tables: `workspace_suppression_keys`
- Columns: `workspace_suppressions.email_hash`, `workspace_suppressions.suppressed_at`, `workspace_suppressions.source`
- Files: `packages/db/migrations/0060_suppression_hash_expand.sql`, `packages/db/migrations/0061_suppression_hash_contract.sql`, `packages/db/src/schema/workspace-suppression-keys.ts`, `packages/contacts-core/src/suppression-hash.ts`, `packages/db/scripts/rehash-suppressions.ts`
- Drizzle exports: `workspaceSuppressionKeys`
- Functions: `normalizeSuppressionEmail`, `hashSuppressionEmail`, `ensureWorkspaceSuppressionKey`, `loadWorkspaceSuppressionKey`, `clearSuppressionKeyCache`
- Constants: `SUPPRESSION_KEY_CACHE_TTL_MS`
- npm scripts: `db:rehash-suppressions`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-workspace suppression key and the pure normalize-and-hash function</name>
  <files>packages/db/src/schema/workspace-suppression-keys.ts, packages/contacts-core/src/suppression-hash.ts, packages/contacts-core/src/index.ts, packages/contacts-core/src/__tests__/suppression-hash.test.ts</files>
  <read_first>
    - packages/kms/src/client.ts (the whole file — `encryptTenantSecret`/`decryptTenantSecret`, `loadProvider()`'s dispatch by `KMS_PROVIDER`, the `generateDataKey` shape, and the `plaintextDek.fill(0)` zeroing in a `finally` that must be copied for this key too)
    - packages/db/src/schema/sendgrid-keys.ts (how an existing envelope-encrypted per-workspace secret is stored — column shape for the wrapped key and its metadata; this table follows the same convention)
    - packages/contacts-core/src/contact-repository.ts lines 40-60 (`isEmailSuppressed`'s current plaintext equality read, and `isEmailTaken` beside it)
    - packages/delivery-core/src/event-normalize.ts (the pure-module convention this hash module follows: no database, no network, fully unit-testable)
    - apps/api/src/modules/contacts/contact.repository.ts (how email is normalized on create/update today, if at all — the suppression normalization must not contradict whatever `contacts.email` already does, or the same person could be suppressed under one form and re-created under another)
  </read_first>
  <behavior>
    - `normalizeSuppressionEmail` lowercases and trims, so `"  A@B.com "` and `"a@b.com"` produce the same output.
    - `hashSuppressionEmail` returns the same digest for the same address and key, and a different digest for a different key.
    - `hashSuppressionEmail` returns a fixed-length hex string regardless of input length.
    - `hashSuppressionEmail` produces different digests for the same address under two different workspaces' keys.
    - `ensureWorkspaceSuppressionKey` creates and stores a wrapped key on first call for a workspace and returns the existing one on subsequent calls, never rotating silently.
    - `loadWorkspaceSuppressionKey` returns the same key material within the cache TTL without a second unwrap call.
    - `loadWorkspaceSuppressionKey` performs a fresh unwrap after the cache TTL expires.
    - The unwrapped key buffer is zeroed after use in every path, including the error path.
  </behavior>
  <action>
Create a `workspace_suppression_keys` table shape in `packages/db/src/schema/workspace-suppression-keys.ts` following `sendgrid-keys.ts`'s convention for an envelope-encrypted per-workspace secret: `workspace_id uuid PRIMARY KEY REFERENCES organization(id) ON DELETE CASCADE`, the wrapped-key columns in whatever shape `sendgrid-keys.ts` already uses for an `EncryptedSecret`, and `created_at timestamptz NOT NULL DEFAULT now()`. Its DDL lands in migration 0060 in Task 2; this task defines the Drizzle shape and the access functions. Export it from `packages/db/src/schema/index.ts`.

Create `packages/contacts-core/src/suppression-hash.ts` with three concerns kept separate:

`normalizeSuppressionEmail(email)` — pure, no key. Lowercase and trim. Document exactly what it does and does not do, and why the "does not" matters: it deliberately does not strip Gmail-style dots or plus-tags, because those are provider-specific aliasing rules and applying them would suppress addresses the tenant never asked to suppress. Cross-check against how `contacts.email` is normalized on create and update, and if the two differ, make them agree — a mismatch means the same person could be suppressed under one form and re-created under another, which silently defeats the re-import protection this table exists for.

`hashSuppressionEmail(normalizedEmail, keyMaterial)` — pure HMAC-SHA256 via `node:crypto`, returning hex. Never invent a hashing construction: HMAC-SHA256 from the Node built-in, exactly as `packages/kms/src/client.ts` uses `createCipheriv` from the same module. Take the already-normalized string, so normalization cannot be accidentally skipped at one call site and applied at another.

`ensureWorkspaceSuppressionKey(client, workspaceId)` and `loadWorkspaceSuppressionKey(client, workspaceId)` — the key lifecycle. Generate 32 bytes of key material once per workspace, wrap it through the existing `@mega-crm/kms` envelope-encryption path exactly as tenant SendGrid keys are wrapped, and store only the wrapped form. Do not re-derive the KMS integration: `loadProvider()` already dispatches by `KMS_PROVIDER`, already handles the AWS-versus-local split, and already establishes the `plaintextDek.fill(0)` zeroing discipline in a `finally` — copy that discipline for this key too, in every path including the error path.

Choose per-workspace keys over a single platform-wide secret, and record the reasoning: a leaked platform secret would let an attacker build one rainbow table against every tenant's suppression list simultaneously, whereas a per-workspace key confines that to one workspace. The email addresses being hashed are not high-entropy secrets, so the hash is a PII-minimization measure rather than a cryptographic guarantee — which is exactly why the blast-radius argument, not the strength argument, decides this.

Add an in-process TTL cache of unwrapped key material keyed by workspace id, with `SUPPRESSION_KEY_CACHE_TTL_MS` as a versioned constant, and export `clearSuppressionKeyCache` for tests. The cache is not an optimization to add later: the suppression check runs against every candidate recipient before dispatch is even attempted, so an unwrap per check would put a key-management round trip on the hottest path in the platform, which is the same path Phase 12's per-tenant rate limiter exists to protect. Never persist the unwrapped material — not to disk, not to Redis, not to a log. Set the TTL short enough that a key deletion takes effect promptly and long enough that a broadcast's worth of checks share one unwrap; state the number you chose and the reasoning in the constant's comment.

Export the module from `packages/contacts-core/src/index.ts`. Write `packages/contacts-core/src/__tests__/suppression-hash.test.ts` covering every case in the behavior list, including the two-workspaces-different-digest case and both cache-TTL cases.
  </action>
  <acceptance_criteria>
    - `packages/contacts-core/src/suppression-hash.ts` exports `normalizeSuppressionEmail`, `hashSuppressionEmail`, `ensureWorkspaceSuppressionKey`, `loadWorkspaceSuppressionKey`, and `clearSuppressionKeyCache`.
    - `packages/contacts-core/src/suppression-hash.ts` imports `createHmac` from `node:crypto` and does not implement any hashing arithmetic itself.
    - `normalizeSuppressionEmail("  A@B.com ")` equals `normalizeSuppressionEmail("a@b.com")`.
    - `hashSuppressionEmail` returns equal digests for the same address and key, and unequal digests for the same address under two different keys.
    - `ensureWorkspaceSuppressionKey` called twice for one workspace leaves exactly one `workspace_suppression_keys` row and returns the same key material both times.
    - `loadWorkspaceSuppressionKey` called twice inside the TTL performs exactly one provider unwrap.
    - `loadWorkspaceSuppressionKey` called after the TTL performs a second provider unwrap.
    - `npx vitest run --root packages/contacts-core src/__tests__/suppression-hash.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --root packages/contacts-core src/__tests__/suppression-hash.test.ts && npx vitest run --root packages/kms</automated>
  </verify>
  <done>A per-workspace suppression key exists, wrapped at rest and cached in process, and the normalize-then-HMAC path is pure and pinned by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the column, convert every call site, and backfill existing rows</name>
  <files>packages/db/migrations/0060_suppression_hash_expand.sql, packages/db/migrations/meta/_journal.json, packages/db/src/schema/suppressions.ts, packages/contacts-core/src/contact-repository.ts, apps/api/src/modules/contacts/contact.repository.ts, apps/worker/src/queues/webhook-events.worker.ts, packages/db/scripts/rehash-suppressions.ts, packages/db/package.json, package.json</files>
  <read_first>
    - packages/db/src/schema/suppressions.ts (the whole file — the `email` column, `reason`, `created_at`, and the `workspace_suppressions_workspace_email_unique` constraint)
    - packages/contacts-core/src/contact-repository.ts lines 40-60 (`isEmailSuppressed` — read site 1)
    - apps/api/src/modules/contacts/contact.repository.ts (the `deleteContact` suppression insert as rewritten by plan 13-10 — write site 1 — and the `createContact` path that consults `isEmailSuppressed`)
    - apps/worker/src/queues/webhook-events.worker.ts lines 178-218 (`applySuppression`'s `INSERT INTO workspace_suppressions ... ON CONFLICT (workspace_id, email) DO NOTHING` reading the address from the contact UPDATE's own RETURNING — write site 2)
    - packages/db/scripts/relocate-default-partition-rows.ts (operator-invoked batched data-migration script precedent, including its bounded loop and progress reporting)
    - packages/db/scripts/audit-sends-history.ts (the cross-tenant script precedent — a platform-wide walk over a tenant-RLS table needs the scan role, and this backfill has the same problem)
    - packages/contacts-core/src/suppression-hash.ts (as written by Task 1)
  </read_first>
  <behavior>
    - After 0060, `workspace_suppressions` has both `email` and `email_hash`, with a unique index on `(workspace_id, email_hash)`.
    - A newly suppressed address writes `email_hash` and does not write `email`.
    - `isEmailSuppressed` finds an address whose hash is stored, and does not find one whose hash is absent.
    - `isEmailSuppressed` finds an address supplied in a different letter case than the one originally suppressed.
    - A hard bounce through the webhook worker writes exactly one suppression row carrying a hash.
    - Suppressing the same address twice writes exactly one row.
    - The backfill script computes a hash for every row with a null `email_hash` and leaves already-hashed rows untouched.
    - The backfill script pages in bounded batches and reports progress.
    - The backfill script is idempotent — a second run changes nothing.
    - An address suppressed before the backfill is still found by `isEmailSuppressed` after it.
  </behavior>
  <action>
Write migration `0060_suppression_hash_expand.sql` and append the matching `meta/_journal.json` entry (idx 60, tag `0060_suppression_hash_expand`). It must: create the `workspace_suppression_keys` table with the same fail-closed `workspace_isolation` RLS treatment every tenant-secret table gets (follow whatever migration created the SendGrid-key table); add `email_hash text`, `suppressed_at timestamptz NOT NULL DEFAULT now()`, and `source text` to `workspace_suppressions`; make `email` nullable; and add a unique index on `(workspace_id, email_hash)`. Leave the existing `(workspace_id, email)` unique constraint in place for now — this is the expand step, and dropping it here would break the still-plaintext rows before the backfill has run. Add a `COMMENT ON COLUMN` for `email_hash` recording D-02: an HMAC of the normalized address under the workspace's own key, so the pre-send and pre-create checks can still refuse a suppressed address while no plaintext form of it survives.

Add `emailHash`, `suppressedAt`, and `source` to the `workspaceSuppressions` Drizzle table and rewrite its doc comment: this table now proves that an address was suppressed without recording what the address was. State that the hash is workspace-scoped by key, so a hash is meaningless outside its workspace and cannot be cross-referenced between tenants.

Convert all three call sites in one change — grep confirms exactly three today, and the SUMMARY must record the count found so a fourth added later is visibly out of contract:
- `isEmailSuppressed` in `packages/contacts-core/src/contact-repository.ts`: normalize the candidate, load the workspace key, hash, and compare against `email_hash`. Keep the function's boolean signature so callers do not change.
- The `deleteContact` suppression insert in `apps/api/src/modules/contacts/contact.repository.ts`: hash the address captured from the anonymizing UPDATE's returning clause and insert the hash. This is the erasure path, and it is the reason the whole conversion exists — after this change the erased person's address is unmailable and unrecoverable at the same time.
- `applySuppression` in `apps/worker/src/queues/webhook-events.worker.ts`: hash the address read from the contact UPDATE's returning clause and insert the hash, changing the `ON CONFLICT` target to `(workspace_id, email_hash)`.

At every one of the three sites, stop writing the plaintext `email` column entirely. A column that holds plaintext for some rows and nothing for others is fine; a column the send pipeline compares against that holds plaintext for some rows and hashes for others is not, which is why the comparison moves to `email_hash` in the same change that starts populating it.

Create `packages/db/scripts/rehash-suppressions.ts`: a bounded, batched, idempotent backfill that walks every workspace's `workspace_suppressions` rows with a null `email_hash`, computes the hash from the stored plaintext under that workspace's key, and writes it. Use the cross-tenant read approach `audit-sends-history.ts` established rather than deriving a new one — under the fail-closed predicate `mega_crm_app` cannot walk this table across workspaces — and introduce no new grant. Page in bounded batches with progress output, per the operator-CLI precedent. Make it safe to run repeatedly: only rows with a null hash are touched. Register it as `db:rehash-suppressions` in `packages/db/package.json` and surface it from the root `package.json`.

Extend whichever existing test files own the suppression assertions (`apps/worker/src/queues/__tests__/webhook-events-suppression.test.ts`, `apps/api/src/modules/contacts/__tests__/subscription-status.test.ts`) to assert the hash behavior rather than the plaintext behavior, and add the case-insensitive-match case at the `isEmailSuppressed` level. Do not delete the existing assertions about which events produce a suppression row and which do not — the D-11/D-13 distinction between unsubscribe and suppression is unrelated to this change and must keep holding.
  </action>
  <acceptance_criteria>
    - `packages/db/migrations/0060_suppression_hash_expand.sql` adds `email_hash` to `workspace_suppressions`, makes `email` nullable, and creates a unique index on `(workspace_id, email_hash)`.
    - That migration creates `workspace_suppression_keys` with `FORCE ROW LEVEL SECURITY`.
    - `packages/db/migrations/meta/_journal.json` contains an entry with tag `0060_suppression_hash_expand`.
    - `grep -rn "workspace_suppressions" apps packages --include=*.ts` shows no INSERT that supplies a value for the `email` column.
    - A hard bounce processed through the webhook worker leaves one `workspace_suppressions` row with a non-null `email_hash` and a null `email`.
    - `isEmailSuppressed(client, workspaceId, "A@B.COM")` returns true for an address suppressed as `a@b.com`.
    - Suppressing the same address twice leaves exactly one row.
    - Seeding a plaintext-only suppression row, running the backfill, and then calling `isEmailSuppressed` for that address returns true.
    - Running the backfill twice leaves every `email_hash` value identical to after the first run.
    - `npm run lint:migrations` exits 0.
    - `npx vitest run --root apps/worker src/queues/__tests__/webhook-events-suppression.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint:migrations && npx vitest run --root packages/contacts-core && npx vitest run --root apps/worker src/queues/__tests__/webhook-events-suppression.test.ts && npx vitest run --root apps/api src/modules/contacts/__tests__</automated>
  </verify>
  <done>New suppressions are stored and compared as workspace-scoped hashes at all three sites, and every pre-existing plaintext row has a hash without losing its suppression effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Contract the column fail-closed, and [BLOCKING] apply the full sequence</name>
  <files>packages/db/migrations/0061_suppression_hash_contract.sql, packages/db/migrations/meta/_journal.json, packages/db/src/schema/suppressions.ts, packages/db/src/__tests__/suppression-hash-migration.test.ts</files>
  <read_first>
    - packages/db/migrations/0060_suppression_hash_expand.sql (as written by Task 2 — the expand state this contracts)
    - packages/db/scripts/rehash-suppressions.ts (as written by Task 2 — the backfill that must run between the two migrations on any populated database)
    - packages/db/src/__tests__/migrate-from-empty.test.ts and migrate-incremental.test.ts (both apply paths; note that the from-empty path has zero suppression rows and therefore passes the fail-closed assertion trivially, while the incremental path is where a seeded plaintext row must be handled)
    - packages/db/migrations/0038_partition_catchup_and_maintenance_runs.sql and packages/db/src/__tests__/migration-0038-deadline-guard.test.ts (the in-repo precedent for a migration that refuses to proceed on an unsafe precondition, and how that refusal is tested)
    - packages/db/src/schema/suppressions.ts (as edited by Task 2)
  </read_first>
  <behavior>
    - Applying 0061 against a database where every suppression row has a hash succeeds, drops the `email` column, and makes `email_hash` NOT NULL.
    - Applying 0061 against a database where any suppression row has a null `email_hash` raises an error naming the backfill script, and leaves the `email` column in place.
    - Applying 0061 against an empty suppression table succeeds.
    - After 0061, the `(workspace_id, email)` unique constraint no longer exists and the `(workspace_id, email_hash)` unique constraint does.
    - After 0061, no column named `email` exists on `workspace_suppressions`.
    - The full sequence — seed plaintext rows, apply 0060, run the backfill, apply 0061 — leaves every seeded address still suppressed and no plaintext anywhere in the table.
  </behavior>
  <action>
Write migration `0061_suppression_hash_contract.sql` and append the matching `meta/_journal.json` entry (idx 61, tag `0061_suppression_hash_contract`). It must, in this order: assert that no `workspace_suppressions` row has a null `email_hash`, raising a clear exception naming `npm run db:rehash-suppressions` if any does; then set `email_hash` NOT NULL; then drop the `(workspace_id, email)` unique constraint; then drop the `email` column.

The assertion must fail closed, not warn. A suppression column holding a mix of plaintext and hashed values is a silent-miss surface for the send pipeline — the check would hash a candidate address, find no matching hash on a row that stores plaintext, and conclude the address is not suppressed. Mailing a person who asked not to be mailed is the failure this whole requirement exists to prevent, so the migration must refuse rather than proceed. Follow the in-repo precedent for a migration that guards an unsafe precondition (migration 0038's deadline guard) including how that refusal is tested.

Document the operator sequence in the migration's header comment, because it is a two-migration change with an application-level step in the middle and that step cannot be expressed in SQL: apply through 0060, run `npm run db:rehash-suppressions`, then apply 0061. If 0061 is reached before the backfill has run, it raises and stops; the operator runs the backfill and re-runs the migration, which resumes at 0061 because 0060 is already recorded as applied. State that this is the intended behavior, not a failure to handle.

Update the `workspaceSuppressions` Drizzle table to drop `email` and make `emailHash` non-null, and change its unique constraint declaration to `(workspaceId, emailHash)`.

Write `packages/db/src/__tests__/suppression-hash-migration.test.ts` covering every case in the behavior list. The full-sequence test is the important one: seed plaintext rows into a database at the pre-0060 schema, apply 0060, run the backfill's own exported function against that database, apply 0061, and then assert both halves of the guarantee — every seeded address is still found by `isEmailSuppressed`, and a query over `information_schema.columns` shows no `email` column on the table.

[BLOCKING] After every schema file in this plan is final, apply and verify the schema: run `npm run lint:migrations` and `npm run test:migrations`, both of which must exit 0. This project's schema-push mechanism is the hand-written SQL migration applied through the ephemeral-database harness. Do NOT rely on `npm run db:migrate` (drizzle-kit CLI) — it hangs in this dev sandbox under Node v26 per the STATE.md note carried from Phase 12 — and do not treat a green `npm run build` as evidence of a real apply, since Drizzle's types come from the schema files rather than the live database.
  </action>
  <acceptance_criteria>
    - `packages/db/migrations/0061_suppression_hash_contract.sql` contains a guard that raises when any `workspace_suppressions` row has a null `email_hash`, and that guard precedes the `DROP COLUMN`.
    - That migration contains `ALTER COLUMN email_hash SET NOT NULL` and `DROP COLUMN email`.
    - `packages/db/migrations/meta/_journal.json` contains an entry with tag `0061_suppression_hash_contract`.
    - Applying 0061 against a database with one null-`email_hash` suppression row raises, and `email` still exists afterwards.
    - Applying 0061 against a fully hashed database succeeds and `information_schema.columns` reports no `email` column on `workspace_suppressions`.
    - The full sequence test leaves every seeded address returning true from `isEmailSuppressed`.
    - `packages/db/src/schema/suppressions.ts` declares no `email` column.
    - `npm run lint:migrations` exits 0.
    - `npm run test:migrations` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint:migrations && npm run test:migrations && npm run lint && npm run build</automated>
    <human-check>On a dev database with existing plaintext suppression rows, apply migrations through 0060, run `npm run db:rehash-suppressions`, apply 0061, then attempt to create a contact with a previously suppressed address and confirm it is refused, and query `workspace_suppressions` to confirm no readable address remains.</human-check>
  </verify>
  <done>The plaintext suppression column is gone, every address is still unmailable by hash, and the contract step provably refuses to run over a partially converted table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Erased contact → suppression list | The one place an erased person's address previously survived in plaintext. |
| Wrapped key at rest → unwrapped key in process | Key material crosses into memory; it must not cross any further. |
| Suppression check → send dispatch | A missed suppression means mailing someone who asked not to be mailed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-12-01 | Information Disclosure | Plaintext addresses surviving erasure in the suppression list | high | mitigate | The `email` column is dropped after every row is hashed; asserted against `information_schema.columns` and by the full-sequence test. |
| T-13-12-02 | Tampering | Suppression silently missing during a partial conversion | high | mitigate | Migration 0061 fails closed on any null `email_hash`, and all three call sites move to the hash in the same change that begins populating it. |
| T-13-12-03 | Information Disclosure | Rainbow-table attack against a leaked hashing key | medium | mitigate | Per-workspace keys confine a single leaked key to one workspace's list, rather than a platform-wide secret exposing every tenant simultaneously. |
| T-13-12-04 | Information Disclosure | Unwrapped key material leaking to disk, Redis, or logs | high | mitigate | The cache is in-process only and never persisted; the unwrapped buffer is zeroed in a `finally` in every path, copying the discipline already established in `packages/kms/src/client.ts`. |
| T-13-12-05 | Denial of Service | Key-unwrap round trip on the pre-send hot path | high | mitigate | An in-process TTL cache keyed by workspace; without it, a key-management round trip lands on the path Phase 12's rate limiter exists to protect. |
| T-13-12-06 | Tampering | Case or whitespace variation bypassing a suppression | medium | mitigate | Normalization is part of the hashed identity and is applied at one place, before hashing; asserted by a case-insensitive match test. |
| T-13-12-07 | Information Disclosure | Cross-workspace hash correlation | low | mitigate | Hashes are computed under per-workspace keys, so the same address produces different digests in different workspaces; asserted directly. |
| T-13-12-08 | Repudiation | Backfill run twice corrupting hashes | low | mitigate | The backfill touches only rows with a null hash and is asserted idempotent. |
| T-13-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced — HMAC comes from Node's built-in `node:crypto`, matching the prior human decision recorded in CLAUDE.md to prefer built-ins over a package for this class of primitive (RESEARCH.md Package Legitimacy Audit: zero new dependencies). Any install proposed during execution requires slopcheck plus a blocking human checkpoint. |
</threat_model>

<flagged_assumptions>
Probe row **CMP-04** came back `unclassified` / `unresolved` (no SPEC.md exists for this phase); plan 13-10 carries its primary flag, and the suppression-specific judgements are recorded here:

- **CMP-04 (unresolved), suppression half:** three unverified judgements. First, that an HMAC of an email address under a per-workspace key is adequate PII minimization for this purpose — an email address is low-entropy, so a party holding the key can confirm a guessed address; the protection is against a database read without the key, not against a targeted check, and whether that clears the compliance bar is a legal judgement this plan cannot make. Second, that `normalizeSuppressionEmail` should lowercase and trim but NOT apply provider-specific aliasing (Gmail dots and plus-tags) — applying them would suppress addresses the tenant never suppressed, and not applying them means an alias can slip past; the plan chooses the conservative direction and writes it down. Third, RESEARCH.md assumption A2 — that an in-process TTL cache sufficiently amortizes the key-unwrap cost on the pre-send path — is unmeasured here; the local KMS provider has no network round trip, so this risk only surfaces under the AWS provider, and RESEARCH.md's recommended load-test comparison against Phase 12's `loadtest:tenant-rps` is NOT part of this plan and remains outstanding.
- **RESEARCH.md assumption A3, resolved during planning:** A3 asked whether any tenant-facing surface renders `workspace_suppressions.email` and would break when the column becomes a hash. A grep across `apps` and `packages` found exactly three touch points — `isEmailSuppressed` (equality read), `deleteContact` (insert), and `applySuppression` (insert) — and no rendering site anywhere in `apps/web`. A3 is therefore closed, not carried: there is no suppression-list UI to adjust. Task 2 re-counts during execution so a site added between planning and execution is caught.
</flagged_assumptions>

<verification>
- `npm run lint:migrations` and `npm run test:migrations` exit 0.
- `npx vitest run --root packages/contacts-core` exits 0.
- `npx vitest run --root packages/kms` exits 0.
- `npx vitest run --root apps/api src/modules/contacts/__tests__` exits 0.
- `npx vitest run --root apps/worker src/queues/__tests__/webhook-events-suppression.test.ts src/queues/__tests__/webhook-events-status.test.ts` exits 0.
- `npm run lint` and `npm run build` exit 0.
- Human check on the dev-database migrate/backfill/migrate sequence per Task 3.
</verification>

<success_criteria>
- No plaintext email address remains in the workspace suppression list.
- Every previously suppressed address is still unmailable, found by hash, case-insensitively.
- The contract migration refuses to run over a partially converted table.
- The pre-send suppression check does not perform a key-unwrap round trip per candidate.
</success_criteria>

<output>
Create `.planning/phases/13-compliance-analytics-integrity/13-12-SUMMARY.md` when done. Record: the count of suppression call sites found and converted, the cache TTL chosen and why, whether `contacts.email` normalization already matched `normalizeSuppressionEmail` or had to be aligned, and the explicit note that the AWS-KMS hot-path load-test recommended by RESEARCH.md assumption A2 remains outstanding.
</output>
