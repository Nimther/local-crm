---
phase: 13-compliance-analytics-integrity
plan: 14
type: execute
wave: 8
depends_on: [13-01, 13-02, 13-03, 13-04, 13-05, 13-06, 13-07, 13-08, 13-09, 13-10, 13-11, 13-12, 13-13]
files_modified:
  - SPECIFICATION.md
  - ARCHITECTURE.md
  - CONVENTIONS.md
  - .planning/phases/13-compliance-analytics-integrity/COVERAGE.md
autonomous: true
requirements: [CMP-01, CMP-02, CMP-03, CMP-04, CMP-05, CMP-06, CMP-07, CMP-08, CMP-09]

must_haves:
  truths:
    - Every table, column, queue, scheduled job, route change, and watchdog this phase added is described in SPECIFICATION.md in the section the project's own maintenance rule assigns it to.
    - The CMP-02 day-semantics contract is stated once in ARCHITECTURE.md and referenced, not restated differently, elsewhere.
    - The erasure-and-evidence model is documented end to end: what is anonymized synchronously, what is scrubbed asynchronously, what is deliberately not scrubbed, and why.
    - The webhook backfill and replay flow is documented including the fail-closed journal write and the operator range-replay tool.
    - Any convention this phase established is recorded in CONVENTIONS.md, or CONVENTIONS.md is left unchanged with that decision stated.
    - SPECIFICATION.md records that this phase introduced zero new runtime dependencies.
    - The API coverage matrix reflects the SendGrid capability surface as actually built.
    - A human has confirmed the phase's five ROADMAP success criteria against a running system.
  artifacts:
    - SPECIFICATION.md
    - ARCHITECTURE.md
    - .planning/phases/13-compliance-analytics-integrity/COVERAGE.md
  key_links:
    - "The .claude/CLAUDE.md maintenance rule -> SPECIFICATION.md sections 2/3/4/5/6/7/8: the rule assigns each kind of change to a specific section, and this plan is where the phase's accumulated changes are filed."
    - "ARCHITECTURE.md's day-semantics contract -> workspace-daily-rollup.ts's doc comment (plan 13-02): one contract, two readerships; they must agree word for word on which field decides a send's day."
  prohibitions:
    - statement: A technology, table, column, queue, or route added by this phase is never left undocumented in SPECIFICATION.md.
      status: flagged
      verification: unverified
    - statement: SPECIFICATION.md never describes intended behavior that the code does not have; anything absent from the code is recorded as not implemented rather than assumed.
      status: flagged
      verification: unverified
    - statement: A version recorded in SPECIFICATION.md is never a range from research; it is the exact value in package.json.
      status: flagged
      verification: unverified
---

<objective>
File the phase's accumulated changes into the project's as-built documentation and confirm the whole thing works. `.claude/CLAUDE.md` binds every new library, table, column, migration, RLS policy, queue, scheduled job, route, parser, and observability hook to a specific SPECIFICATION.md section in the same change that introduced it; thirteen plans deferred that filing to keep their own scope tight, and this is where it lands. Then a human verifies the five ROADMAP success criteria against a running system.

Purpose: SPECIFICATION.md is the as-built description used for security review. A phase that adds six migrations, three queues, two watchdogs, and a change to how every daily number is computed, without updating it, leaves the review reading a document about a different system.
Output: updated SPECIFICATION.md, ARCHITECTURE.md, and CONVENTIONS.md, a final coverage matrix, and a human-verified phase.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-compliance-analytics-integrity/13-CONTEXT.md
@.planning/phases/13-compliance-analytics-integrity/13-RESEARCH.md
</context>

## Artifacts this phase produces

This plan produces no new source symbols. It documents the symbols the preceding thirteen plans produced; each of those plans carries its own artifacts section, and the SUMMARY files are the authoritative list this plan reconciles against.

<tasks>

<task type="auto">
  <name>Task 1: File every change into SPECIFICATION.md</name>
  <files>SPECIFICATION.md</files>
  <read_first>
    - .claude/CLAUDE.md, the "Project Specification (SPECIFICATION.md)" section (the binding maintenance rule and its section-by-section routing table: packages to section 2, secrets and env to 3, tables/columns/migrations/RLS/indexes/partitions to 4, queues/workers/repeatable jobs/intervals/concurrency to 5, routes/plugins/body parsers/auth/rate limits to 6, logging/metrics/tracing/healthchecks to 7, and anything diverging from the recommended stack to 8)
    - SPECIFICATION.md (the whole file — its existing section structure, level of detail, and the exact table formats each section uses)
    - Every `13-*-SUMMARY.md` in this phase directory (the authoritative record of what was actually built, including every deviation each plan was required to record — file what the SUMMARYs say, not what the PLANs proposed)
    - packages/db/migrations/ (the 0055 through 0061 files as actually written, for exact table, column, index, and policy names)
    - package.json, packages/db/package.json (for the exact new npm scripts, and to confirm no dependency changed)
  </read_first>
  <action>
Work from the thirteen SUMMARY files, not from the plans. Several plans required the executor to make and record a decision — where the shared journal helpers were placed, whether a constraint promotion was supported, which module owns the reconciliation window constant, whether the Phase 12 checkpoint table was reused. SPECIFICATION.md must describe what was built, so a discrepancy between a plan and its SUMMARY resolves to the SUMMARY, and a discrepancy between a SUMMARY and the code resolves to the code.

Section 4 (schema): add `ingress_journal`, `send_event_quarantine`, `erasure_records`, `reputation_alert_state`, `ingestion_alert_state`, `workspace_suppression_keys`; the new columns `contacts.anonymized_at`, `workspace_daily_rollup.dirtied_at`, `workspace_suppressions.email_hash`/`suppressed_at`/`source`; the removal of `workspace_suppressions.email`; the `send_events` dedup constraint change from `(workspace_id, sg_event_id, occurred_at)` to `(workspace_id, send_id, event_type, occurred_at)` including whether it is enforced by a named constraint or by an attached unique index; and every RLS policy added, naming which tables deliberately have none and why. List migrations 0055 through 0061 by tag.

Section 5 (scheduler and send pipeline): add the `webhook-replay-sweep`, `reputation-tick`, and `erasure-scrub` queues with their intervals, page limits, attempt caps, retention, and scheduler ids; the `analytics-reconcile` tick's new dirty-day sweep; and the `webhook-events` payload's new `schemaVersion` and `journalId` fields together with the legacy-payload handling.

Section 6 (public entry points): the webhook route's journal write and its fail-closed 5xx; the unsubscribe route's new send-fact write; and the contact delete route's changed semantics from row removal to anonymization.

Section 7 (observability): the two new watchdogs, their intervals, their dedup windows, and the alert channels they use, noting that the reputation watchdog is the first to send to tenant members and the first with tier escalation.

Section 3 (secrets): the per-workspace suppression HMAC key, how it is wrapped, and where it is stored. State explicitly that no new environment variable was introduced and that `OPERATOR_ALERT_EMAIL`, `PLATFORM_SENDGRID_API_KEY`, and `PLATFORM_MAIL_FROM` gained two more consumers.

Section 2 (dependencies): record that this phase introduced zero new runtime or dev dependencies, and that HMAC uses Node's built-in `node:crypto` — consistent with the prior human decision recorded in the project's decision log to prefer built-ins over a package for this class of primitive. If any dependency did change during execution, record the exact version from `package.json`, never a range from research.

Section 8 (divergences): record anything that diverged from the recommended stack or from this phase's own research, in particular the `applyUnsubscribeWithSendFact` helper living in `contacts-core` rather than `delivery-core` because of the workspace dependency direction, and whichever path the CMP-07 migration actually took for the constraint promotion and the concurrent index build.

Where the code does not have something, write that it is not implemented. Do not describe intended behavior as built behavior — this document exists to be read by a security review, and an aspirational line in it is worse than a missing one.
  </action>
  <acceptance_criteria>
    - SPECIFICATION.md section 4 names `ingress_journal`, `send_event_quarantine`, `erasure_records`, `reputation_alert_state`, `ingestion_alert_state`, and `workspace_suppression_keys`.
    - SPECIFICATION.md section 4 names `contacts.anonymized_at`, `workspace_daily_rollup.dirtied_at`, and `workspace_suppressions.email_hash`.
    - SPECIFICATION.md section 4 states the new `send_events` dedup key columns.
    - SPECIFICATION.md section 5 names the three new queues and the `webhook-events` payload's `journalId` field.
    - SPECIFICATION.md section 6 states that the webhook route writes the journal before enqueueing and fails closed.
    - SPECIFICATION.md section 6 states that deleting a contact anonymizes rather than removes the row.
    - SPECIFICATION.md section 7 names both new watchdogs.
    - SPECIFICATION.md section 3 states that no new environment variable was introduced.
    - SPECIFICATION.md section 2 states that this phase introduced zero new dependencies.
    - Every migration tag from 0055 through 0061 appears in SPECIFICATION.md.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>for t in 0055 0056 0057 0058 0059 0060 0061; do grep -q "$t" SPECIFICATION.md || { echo "missing migration $t in SPECIFICATION.md"; exit 1; }; done; for s in ingress_journal send_event_quarantine erasure_records reputation_alert_state ingestion_alert_state workspace_suppression_keys anonymized_at dirtied_at email_hash; do grep -q "$s" SPECIFICATION.md || { echo "missing $s in SPECIFICATION.md"; exit 1; }; done; echo "SPECIFICATION.md coverage OK"</automated>
  </verify>
  <done>SPECIFICATION.md describes the system as it is after this phase, filed into the sections the project's own maintenance rule assigns.</done>
</task>

<task type="auto">
  <name>Task 2: Document the three contracts in ARCHITECTURE.md and settle CONVENTIONS.md</name>
  <files>ARCHITECTURE.md, CONVENTIONS.md, .planning/phases/13-compliance-analytics-integrity/COVERAGE.md</files>
  <read_first>
    - ARCHITECTURE.md (the whole file — its section structure and the level at which it describes flows; section 9's send-status state machine is the closest analog for how a contract is written there)
    - CONVENTIONS.md (the existing conventions and the bar for what qualifies as one — Phase 9's `ensurePartitions` rule is the precedent for a rule that earns a place here)
    - packages/db/src/schema/workspace-daily-rollup.ts (the day-semantics contract as written into the schema doc by plan 13-02 — ARCHITECTURE.md must agree with it word for word on which field decides a send's day)
    - .planning/phases/13-compliance-analytics-integrity/COVERAGE.md (the plan-time API coverage matrix, to be reconciled against what was built)
    - Every `13-*-SUMMARY.md` in this phase directory
  </read_first>
  <action>
Add three contract sections to ARCHITECTURE.md, each stated once and referenced rather than restated.

The daily-metric day-semantics contract (CMP-02/CMP-03): `day` is always a UTC calendar day; `sent_count` is bucketed by `sends.sent_at`; event-derived counters are bucketed by the provider event's `occurred_at` UTC day on the incremental path and by the corresponding `sends` fact column's UTC day on the reconciliation path; every cast forces `AT TIME ZONE 'UTC'`; the reconciliation path is an absolute overwrite while the incremental path is additive; a late event marks its day dirty and the next tick re-verifies it against a fresh scan; and `unknown` sends are excluded from rollups but shown as their own count in campaign stats. This must agree word for word with the version in `workspace-daily-rollup.ts` — two documents describing the same contract differently is the failure this contract exists to prevent.

The erasure-and-evidence model (CMP-04): what happens synchronously in the delete request (anonymize the row, resolve suppression and status so mail stops, write the erasure record, queue the scrub), what happens asynchronously (bounded resumable JSONB scrub over the linked partitioned tables, completion tracked on the record), what is deliberately NOT scrubbed and why (`ingress_journal` and `send_event_quarantine`, because their days-long pruning horizon expires them faster than an erasure's own completion window), what survives on purpose (foreign keys, event types, timestamps — the evidence a send or a suppression was lawful), and the known limitation that contacts hard-deleted before this phase have no retrofittable evidence trail.

The webhook ingestion, backfill, and replay flow (CMP-05/CMP-07/CMP-08): verify signature and header timestamp, journal the verified batch fail-closed, enqueue with the journal id, bound each event's own `occurred_at` before it routes a partition or enters the dedup key, quarantine out-of-range events per event without failing the batch, dedupe on the server-observed compound key, mark the journal row ingested, and recover post-receipt loss through the scheduled replay sweep or the operator range-replay tool with the provider's own retry window covering true unreachability.

For CONVENTIONS.md, decide and act rather than defaulting to adding text. Two candidates were flagged during discussion: the hash-based suppression comparison and the journal-first ingress ordering. Add a convention only if it is a rule a future change could plausibly violate and that a reviewer should catch — the Phase 9 `ensurePartitions` entry is the bar. If neither clears it, leave CONVENTIONS.md unchanged and record that decision with its reasoning in the SUMMARY; an unchanged file with a stated reason is a better outcome than a diluted conventions list.

Reconcile `.planning/phases/13-compliance-analytics-integrity/COVERAGE.md` against what was built. The matrix was produced at plan time from `normalizeEventType`'s event-type surface plus the SendGrid API surfaces the phase touches. If execution changed which event types are handled, or if a capability moved between INTEGRATE and OPT-OUT, update the row and its reason. Every OPT-OUT must still carry a reason; a row without one is an undecided hole, which is the failure this matrix exists to close.
  </action>
  <acceptance_criteria>
    - ARCHITECTURE.md contains a section stating that `sends.sent_at` decides a send's day for `sent_count`.
    - ARCHITECTURE.md states that `unknown` sends are excluded from daily rollups and counted separately in campaign stats.
    - ARCHITECTURE.md contains a section describing the synchronous and asynchronous halves of contact erasure, including what is deliberately not scrubbed.
    - ARCHITECTURE.md contains a section describing the journal-write ordering relative to signature verification and to enqueueing.
    - ARCHITECTURE.md states the `send_events` dedup key columns.
    - The day-semantics statement in ARCHITECTURE.md and the one in `packages/db/src/schema/workspace-daily-rollup.ts` name the same field for `sent_count`.
    - `COVERAGE.md` contains a decision for every listed capability, and every `OPT-OUT` row has a non-empty reason.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "sent_at" ARCHITECTURE.md && grep -q "anonymiz" ARCHITECTURE.md && grep -q "ingress_journal" ARCHITECTURE.md && grep -q "send_id, event_type, occurred_at" ARCHITECTURE.md && node -e "const t=require('fs').readFileSync('.planning/phases/13-compliance-analytics-integrity/COVERAGE.md','utf8');const rows=t.split('\n').filter(l=>l.includes('OPT-OUT'));const bad=rows.filter(l=>{const c=l.split('|').map(s=>s.trim());return !c[3]||c[3].length<5});if(bad.length){console.error('OPT-OUT rows without a reason:',bad);process.exit(1)}console.log('coverage matrix OK:',rows.length,'opt-outs, all with reasons')"</automated>
  </verify>
  <done>The three contracts are written down once each, the schema doc and the architecture doc agree, and the coverage matrix matches what shipped.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full phase gate and stage the end-of-phase human verification</name>
  <files>SPECIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 13 (the five success criteria this gate exists to establish)
    - package.json (the `failure:all`, `coverage`, `coverage:gate`, `test:migrations`, and `lint:migrations` scripts that constitute the phase gate)
    - Every `13-*-SUMMARY.md` in this phase directory (any deviation recorded there that the human verification steps below need to account for)
  </read_first>
  <action>
Run the complete phase gate and record the result of each command in the SUMMARY: `npm run lint`, `npm run build`, `npm run test --workspaces --if-present`, `npm run lint:migrations`, `npm run test:migrations`, `npm run failure:all`, `npm run coverage`, and `npm run coverage:gate`. Every one must exit 0. If any fails, stop and report it rather than proceeding — a documentation plan that seals over a red gate is the worst possible place for this phase to end.

Then append a short "Phase 13 verification checklist" note to the SUMMARY reproducing the human-check steps in this task's verify block, so the operator running end-of-phase verification has them in one place without re-reading fourteen plans.

Record the operational caveat carried from Phase 12 alongside that checklist: `npm run db:migrate` (drizzle-kit CLI) hangs in this dev sandbox under Node v26. Migrations 0055 through 0061 are proven by `npm run test:migrations` against an ephemeral database; applying them to a real database goes through `psql` or the CI migration step. Plan 13-12 additionally requires an application-level step between two migrations — apply through 0060, run `npm run db:rehash-suppressions`, then apply 0061, which fails closed if the backfill has not run.
  </action>
  <acceptance_criteria>
    - `npm run lint` exits 0.
    - `npm run build` exits 0.
    - `npm run test --workspaces --if-present` exits 0.
    - `npm run lint:migrations` exits 0.
    - `npm run test:migrations` exits 0.
    - `npm run failure:all` exits 0, including `failure:unsubscribe-atomic` and `failure:erasure-scrub-resume`.
    - `npm run coverage` exits 0 and `npm run coverage:gate` exits 0.
    - `13-14-SUMMARY.md` contains a "Phase 13 verification checklist" section reproducing the seven human-check steps and the migration caveat.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run build && npm run lint:migrations && npm run test:migrations && npm run test --workspaces --if-present && npm run failure:all && npm run coverage && npm run coverage:gate</automated>
    <human-check>
Confirm the phase's five ROADMAP success criteria against a running system. What the preceding thirteen plans built, by requirement:

- **Atomic unsubscribe (CMP-01):** one shared helper writes subscription status, consent history, and the originating send's `unsubscribed_at` in one transaction, called from the public unsubscribe route, the webhook unsubscribe events, and the dropped-with-unsubscribe-outcome path. A failure-injection scenario asserts no partial state at three interior boundaries.
- **Daily metric semantics (CMP-02/CMP-03/CMP-06):** every reconciliation day-cast forced to UTC; `sends.sent_at` pinned as the field that decides a send's day; late events mark their day dirty and the next reconciliation tick re-verifies it against a fresh scan; `unknown` sends given their own visible count in campaign stats.
- **Contact erasure (CMP-04):** delete now anonymizes the contact row in place and keeps its foreign keys, resolves suppression and status synchronously so mail stops immediately, writes an auditable erasure record, and queues a bounded resumable scrub that strips PII from linked `send_events.payload` and `events.properties`. The suppression list now stores an HMAC of the normalized address under a per-workspace wrapped key, with no plaintext column remaining.
- **Event integrity (CMP-05/CMP-07):** each event's provider timestamp is bounded before it routes a partition or enters the dedup key, out-of-range events are quarantined per event without failing the batch, and dedup is re-based on `(workspace_id, send_id, event_type, occurred_at)` with `sg_event_id` demoted to a forensic column.
- **Backfill and alerting (CMP-08/CMP-09):** verified webhook batches are journaled fail-closed before enqueue and marked ingested after processing; a scheduled sweep replays stuck rows and an operator CLI replays an explicit range; complaint and hard-bounce rates are computed per tenant over a rolling window and alert both the operator and the tenant's members at two tiers, with escalation bypassing the cooldown.

Steps — run against a dev environment with migrations applied through 0061 and the suppression backfill run.

1. **Unsubscribe atomicity and convergence.** Send yourself a campaign email. Click its unsubscribe link. Confirm in the send log that the send's unsubscribe fact is set, that the contact's status is unsubscribed, that the contact's consent history shows exactly one change, and that the campaign's unsubscribe count incremented by one. Then replay the corresponding SendGrid unsubscribe webhook event for the same send and confirm none of those four numbers changed.

2. **Daily numbers.** Open the workspace dashboard and note a day's sent and delivered counts. Trigger a reconciliation tick. Confirm the numbers are unchanged. Then, in a database session, run `SET TIME ZONE 'Asia/Tokyo'` and trigger reconciliation again; confirm the numbers are still unchanged.

3. **Late event.** Inject a webhook event for a send whose occurrence day is 4 days in the past. Confirm that day's rollup row is marked dirty, that the next reconciliation tick clears the mark, and that the day's count reflects the late event.

4. **Erasure.** Delete a contact that has at least one send and one event. Confirm it disappears from the contacts list and from a segment that previously matched it. Confirm the send log still shows its sends. Query the contact row directly and confirm the PII columns are null and `anonymized_at` is set. Wait for the scrub to complete, then confirm the erasure record shows complete with non-zero counts and that the contact's email address no longer appears in any `send_events.payload`. Finally, attempt to create a new contact with that address and confirm it is refused as suppressed, and confirm `workspace_suppressions` holds no readable address.

5. **Event integrity.** Send a webhook event whose `timestamp` is 30 days in the past. Confirm it produces a quarantine row and no `send_events` row, and that no metric moved. Then send the same event twice with two different `sg_event_id` values and confirm exactly one `send_events` row and one counter increment.

6. **Backfill.** Stop the worker, deliver a signed webhook batch, and confirm a journal row exists with no ingestion mark. Restart the worker, wait for the replay sweep, and confirm the row is marked ingested and its events were processed exactly once.

7. **Alerts.** With `OPERATOR_ALERT_EMAIL` pointed at a real inbox, seed a workspace above the complaint warn threshold and confirm one operator email and one email to a workspace member arrive, that both name the metric, the rate, and the sample size, and that neither contains a recipient address. Confirm a second check within the cooldown sends nothing, and that raising the workspace to the critical tier sends immediately.

Note the operational caveat carried from Phase 12: `npm run db:migrate` hangs in this sandbox under Node v26. Apply migrations through the path this phase's plans used (`npm run test:migrations` proves them; apply to a real database through `psql` or the CI migration step). Plan 13-12 additionally requires an application-level step between two migrations — apply through 0060, run `npm run db:rehash-suppressions`, then apply 0061.
    </human-check>
  </verify>
  <done>The whole phase gate is green, and the operator has a single reproduced checklist for end-of-phase verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPECIFICATION.md → external security review | The document is the review's model of the system; a gap in it is a blind spot in the review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-14-01 | Repudiation | Undocumented tables, queues, and routes | high | mitigate | Every table, column, queue, job, route, and watchdog added by the phase is filed into its assigned SPECIFICATION.md section, verified by a grep gate over the migration tags and object names. |
| T-13-14-02 | Repudiation | Aspirational documentation describing unbuilt behavior | high | mitigate | The task works from the SUMMARY files and the code rather than the plans, and requires anything absent from the code to be recorded as not implemented. |
| T-13-14-03 | Repudiation | Two documents stating the day-semantics contract differently | medium | mitigate | ARCHITECTURE.md and `workspace-daily-rollup.ts` must name the same field for `sent_count`; asserted by the verify gate. |
| T-13-14-04 | Repudiation | An undecided capability hole in the API coverage matrix | medium | mitigate | The verify gate rejects any `OPT-OUT` row lacking a reason. |
| T-13-14-05 | Information Disclosure | A worked example in documentation carrying real tenant data | low | mitigate | The human-verification steps operate on a dev environment; no production identifiers are to be pasted into the documents. |
| T-13-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced (RESEARCH.md Package Legitimacy Audit: zero new dependencies). Any install proposed during execution requires slopcheck plus a blocking human checkpoint. |
</threat_model>

<flagged_assumptions>
This plan carries no new requirement-level assumptions of its own; it documents and verifies what the preceding thirteen plans built. The nine unresolved edge-probe rows are surfaced in the plans that own each requirement: CMP-01 in 13-08, CMP-02 in 13-02 and 13-03, CMP-03 in 13-05, CMP-04 in 13-10, 13-12, and 13-13, CMP-05 in 13-04, CMP-06 in 13-02, CMP-07 in 13-07, CMP-08 in 13-01 and 13-06, CMP-09 in 13-09 and 13-11.

One documentation-level assumption is recorded: that SPECIFICATION.md's existing section structure (2 through 8) accommodates every change this phase made without a new section. If execution finds a change with no natural home — the most likely candidate being the per-workspace HMAC key, which is neither purely a secret nor purely a schema object — add it to the section the `.claude/CLAUDE.md` routing rule assigns and note the ambiguity in the SUMMARY rather than inventing a section.
</flagged_assumptions>

<verification>
- The SPECIFICATION.md coverage gate in Task 1 exits 0.
- The ARCHITECTURE.md and coverage-matrix gate in Task 2 exits 0.
- `npm run lint`, `npm run build`, and `npm run test --workspaces --if-present` exit 0.
- `npm run test:migrations` exits 0.
- `npm run failure:all` exits 0.
- `npm run coverage` exits 0 and `npm run coverage:gate` exits 0.
- The human checkpoint in Task 3 is approved.
</verification>

<success_criteria>
- SPECIFICATION.md, ARCHITECTURE.md, and CONVENTIONS.md describe the system as built after this phase.
- The coverage matrix has a decided, reasoned row for every capability.
- A human has confirmed all five ROADMAP success criteria against a running system.
</success_criteria>

<output>
Create `.planning/phases/13-compliance-analytics-integrity/13-14-SUMMARY.md` when done. Record: every discrepancy found between a plan and its SUMMARY or between a SUMMARY and the code, the CONVENTIONS.md decision and its reasoning, any coverage-matrix row that changed during execution, and the human checkpoint's outcome including any check that failed.
</output>
