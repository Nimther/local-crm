---
phase: 13-compliance-analytics-integrity
plan: 07
type: execute
wave: 3
depends_on: [13-04]
files_modified:
  - packages/db/migrations/0057_send_events_dedup_rebase.sql
  - packages/db/migrations/meta/_journal.json
  - packages/db/src/schema/send-events.ts
  - packages/db/src/__tests__/send-events-dedup-rebase.test.ts
  - apps/worker/src/queues/webhook-events.worker.ts
  - apps/worker/src/queues/__tests__/webhook-events-dedup-rebase.test.ts
autonomous: true
requirements: [CMP-07]

produces_schema_change: true

assumption_delta_decision:
  noun: send event occurrence
  decision: promote
  rationale: "The dedup identity of a send event was `sg_event_id` (singular, provider-controlled, verified unstable across retries). The compound key `(workspace_id, send_id, event_type, occurred_at)` is PROMOTED to the primary identity and `sg_event_id` is DEMOTED to a stored forensic-correlation column, not added alongside a still-authoritative `sg_event_id`. `occurred_at` stays in the key because Postgres requires every unique constraint on a partitioned table to include all partition-key columns — a structural constraint, not a design choice to revisit."

must_haves:
  truths:
    - Two webhook deliveries of the same occurrence carrying DIFFERENT `sg_event_id` values insert exactly one `send_events` row and apply side effects exactly once.
    - Two webhook deliveries of the same occurrence carrying the SAME `sg_event_id` still insert exactly one row (the previous guarantee is not lost).
    - Two genuinely different event types on the same send at the same second insert two rows, because `event_type` is part of the key.
    - Two events on different sends at the same second with the same type insert two rows, because `send_id` is part of the key.
    - An orphan event with a null `send_id` is still stored, and repeated redelivery of an orphan event is documented as not deduping — it drives zero counters, so the cost is bounded to storage.
    - `sg_event_id` remains a stored, queryable column on every row after the migration.
    - Pre-existing rows that are duplicates under the new key are resolved before the new unique index is built, and `workspace_daily_rollup` totals for a sampled workspace-day are unchanged by that resolution.
    - The migration refuses to proceed while any duplicate under the new key remains, naming the operator script that resolves them, rather than failing part-way through an index build.
    - The duplicate resolution is performed in bounded committed batches by an operator-invoked script, never by a single unbounded DELETE inside the migration.
    - The dedup insert's `ON CONFLICT` target matches the enforced unique index, so the insert path cannot raise a "no unique or exclusion constraint matching" error.
  artifacts:
    - packages/db/migrations/0057_send_events_dedup_rebase.sql
    - packages/db/src/__tests__/send-events-dedup-rebase.test.ts
    - apps/worker/src/queues/__tests__/webhook-events-dedup-rebase.test.ts
  key_links:
    - "The enforced unique index on send_events -> the ON CONFLICT column list in processWebhookEventBatch: these two must name the same columns in a form Postgres can match, or every insert fails at runtime while every type check passes."
    - "classifyOccurredAt's bound (plan 13-04) -> occurred_at's role in the new key: the two fixes compose — bounding closes the vary-the-timestamp bypass, re-basing closes the vary-the-sg_event_id bypass. Either alone leaves a bypass open."
    - "Pre-migration duplicate resolution -> the unique index build: the index build is what fails if duplicates survive, and it fails mid-migration on a large partitioned table."
  prohibitions:
    - statement: "`sg_event_id` is never part of the enforced uniqueness key after this migration; it is retained only as a stored forensic column."
      status: flagged
      verification: unverified
    - statement: The pre-existing duplicate cleanup is never a single unbounded DELETE over the partitioned table; it must be bounded and batched, and it never runs inside the migration.
      status: flagged
      verification: unverified
    - statement: The migration never begins a destructive or long-running step while duplicates under the new key remain; it asserts zero and stops.
      status: flagged
      verification: unverified
    - statement: A concurrent index build is never issued directly against the partitioned parent table.
      status: flagged
      verification: unverified
    - statement: The old dedup constraint is never dropped before the new enforced index is in place and the insert path has been swapped to it.
      status: flagged
      verification: unverified
---

<objective>
Close Pitfall 14's second half (CMP-07). The current dedup key `(workspace_id, sg_event_id, occurred_at)` rests on an assumption the project has since verified false: `sg_event_id` is not reliably stable across SendGrid webhook retries, so a redelivery with a fresh event id inserts a second row and double-counts delivered/opened/clicked. Re-base the key on server-observed fields — `(workspace_id, send_id, event_type, occurred_at)` — keeping `sg_event_id` as a stored forensic column.

Purpose: this is the phase's highest-risk migration. It combines two independent hazards: a concurrent index build is unsupported on a partitioned parent table, and the historical duplicates that motivate the fix will violate the new index at build time. Sequenced as its own migration, with a duplicate count taken before anything destructive runs.
Output: migration 0057 (pre-check, bounded duplicate resolution, per-partition concurrent unique index build, attach, expand/contract constraint swap), the swapped `ON CONFLICT` target, and tests proving a varying `sg_event_id` no longer bypasses dedup.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-compliance-analytics-integrity/13-CONTEXT.md
@.planning/phases/13-compliance-analytics-integrity/13-RESEARCH.md
@.planning/phases/13-compliance-analytics-integrity/13-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

## Artifacts this phase produces

New symbols and paths created by THIS plan:

- Indexes/constraints: `send_events_dedup_v2_idx` (parent), `send_events_dedup_v2_<partition>_idx` (per partition)
- Files: `packages/db/migrations/0057_send_events_dedup_rebase.sql`, `packages/db/src/__tests__/send-events-dedup-rebase.test.ts`, `apps/worker/src/queues/__tests__/webhook-events-dedup-rebase.test.ts`
- Scripts: `packages/db/scripts/count-send-event-duplicates.ts` (with both a read-only count mode and a `--resolve` batched-deletion mode)
- npm scripts: `db:count-send-event-duplicates`, `db:resolve-send-event-duplicates`

<assumption_delta_decision>
**Detector result:** `pluralization` signal fired on "A compound-key fallback is required" — a second dedup identity was introduced where there had been one.

**Noun now primary:** the *send event occurrence*, identified by `(workspace_id, send_id, event_type, occurred_at)`.

**Decision: promote.** The compound key becomes the primary dedup identity of a send event, and `sg_event_id` is demoted to a stored forensic-correlation column with no role in uniqueness. This is deliberately NOT `add-alongside`: keeping `sg_event_id` in the enforced key alongside the new columns would preserve exactly the bypass CMP-07 exists to close, because a redelivery varying only `sg_event_id` would still produce a distinct key tuple. The generalization ("an occurrence is identified by what the server observed about it") replaces the specific ("an occurrence is identified by the provider's event id"), and the old specific becomes one non-authoritative attribute of the row.

**Structural constraint on the promotion:** `occurred_at` cannot be dropped from the key even though it is provider-supplied. `send_events` is partitioned by range on `occurred_at`, and Postgres requires every unique constraint on a partitioned table to include all partition-key columns. What makes this safe is plan 13-04's bounding: `occurred_at` is no longer arbitrary provider input by the time it reaches the key.

**Suggested invariant test (advisory, encoded in this plan's tasks):** a redelivery of the same occurrence with a different `sg_event_id` inserts one row — this is the contract test for the generalized intent, and it fails under the old key.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Duplicate blast-radius count, and the operator-invoked batched resolution</name>
  <files>packages/db/scripts/count-send-event-duplicates.ts, packages/db/package.json, package.json</files>
  <read_first>
    - packages/db/scripts/relocate-default-partition-rows.ts (the whole file — the Phase 9 D-08 operator-invoked read-then-act CLI precedent: argument parsing, the dry-run-then-act shape, `SKIP LOCKED` batching with a page limit, per-batch commits, and how it reports progress. This is the shape the `--resolve` mode copies; the migration is deliberately not where batched deletion happens)
    - packages/db/scripts/audit-sends-history.ts (the cross-tenant read precedent from Phase 11 plan 11-02: `mega_crm_app` cannot read `sends`/`send_events` cross-tenant at all under the fail-closed RLS predicate, so a platform-wide audit needs the scan role — this script has the same problem and must reuse that solution rather than rediscover it)
    - packages/db/migrations/0020_send_events_partitioned.sql (the current inline `UNIQUE (workspace_id, sg_event_id, occurred_at)` declaration and the partition layout)
    - packages/db/package.json (script registration convention)
  </read_first>
  <action>
Create `packages/db/scripts/count-send-event-duplicates.ts`: a read-only script that reports, per workspace and in total, how many `(workspace_id, send_id, event_type, occurred_at)` groups have more than one row and how many rows would need resolving (`sum(count - 1)`). Filter to `send_id IS NOT NULL`, because null `send_id` rows are exempt from the new key's dedup guarantee by construction — Postgres treats NULL as distinct in a unique key — and including them would inflate the number with rows the migration will not touch.

Reuse the cross-tenant read approach `audit-sends-history.ts` established rather than deriving a new one: under the fail-closed `workspace_isolation` predicate, `mega_crm_app` cannot read `send_events` across workspaces at all, so this script must go through the scan role. Introduce no new database grant.

Add a `--resolve` mode to the same script, which is where the duplicate deletion actually happens — REVIEWS.md MEDIUM finding, which correctly established that the batched, per-batch-committing DELETE this plan requires is **not expressible in a hand-written SQL migration under this runner**: a `DO` block is one transaction, so it cannot commit per batch, and the `--> statement-breakpoint` convention gives no loop construct. Rather than quietly downgrading to a single unbounded DELETE inside the migration — which is exactly what the prohibition above forbids, on a partitioned table that may be large — the resolution moves to this operator-invoked script, following the Phase 9 D-08 precedent that row-level bulk mutation over a partitioned table is operator-invoked and never scheduled.

`--resolve` deletes, in bounded committed batches with a `--page-size` override, all but the earliest-`received_at` row within each `(workspace_id, send_id, event_type, occurred_at)` group where `send_id IS NOT NULL`. Earliest-`received_at` wins because it is the first observation of the occurrence and `received_at` is server-controlled — the only field in the row a redelivery cannot vary. Print a running deleted count and a final summary. Make it idempotent: a second run finds nothing to do. Default to the read-only count mode so an operator who forgets the flag gets the dry run, matching the sibling script's bias.

Register both modes: `db:count-send-event-duplicates` and `db:resolve-send-event-duplicates` in `packages/db/package.json`, surfaced from the root `package.json`.

Run the count against the most production-like dataset available in this environment and record the exact numbers in the SUMMARY. This is a gate, not a formality: RESEARCH.md flags this migration as combining two hazards (an unsupported concurrent build on a partitioned parent, and a unique index built over pre-existing violations), and the duplicate count is what tells the executor whether Task 2's resolution step is touching tens of rows or millions. If the count cannot be taken because no populated dataset is reachable, say so explicitly in the SUMMARY and record that Task 2's resolution step is therefore unvalidated against real data volume — do not silently proceed as if the number were zero.
  </action>
  <acceptance_criteria>
    - `packages/db/scripts/count-send-event-duplicates.ts` exists and filters to `send_id IS NOT NULL`.
    - `packages/db/package.json` contains `db:count-send-event-duplicates` and `db:resolve-send-event-duplicates` scripts.
    - Running the script without `--resolve` performs no DELETE.
    - Running the script against a seeded dataset containing two rows sharing `(workspace_id, send_id, event_type, occurred_at)` but differing in `sg_event_id` reports 1 duplicate group and 1 row to resolve.
    - Running the script against a seeded dataset with two null-`send_id` rows sharing everything else reports 0 duplicate groups.
    - Running `--resolve` against that seeded duplicate pair leaves exactly one row, the one with the earlier `received_at`, and a second `--resolve` run deletes nothing.
    - Running `--resolve` over more duplicate groups than the page size issues more than one committed batch.
    - The script introduces no new `GRANT` statement.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run build && npm run db:count-send-event-duplicates && npx vitest run --root packages/db src/__tests__/send-events-dedup-rebase.test.ts</automated>
  </verify>
  <done>The blast radius of the constraint swap is a recorded number, and the bounded batched resolution exists as an operator tool with a dry-run default — not as a shape the migration runner cannot express.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0057 — resolve duplicates, build the new unique index per partition, swap, drop the old</name>
  <files>packages/db/migrations/0057_send_events_dedup_rebase.sql, packages/db/migrations/meta/_journal.json, packages/db/src/schema/send-events.ts, packages/db/src/__tests__/send-events-dedup-rebase.test.ts</files>
  <read_first>
    - packages/db/migrations/0020_send_events_partitioned.sql (the whole file — the existing inline `UNIQUE (workspace_id, sg_event_id, occurred_at)` and the exact auto-generated constraint name Postgres assigned it, which must be confirmed against the live catalog rather than guessed)
    - packages/db/src/partitions/ensure-partitions.ts (the single source of partition DDL from Phase 9, including how it enumerates attached monthly partitions and the CHECK-constraint-first attach discipline — the per-partition index build must enumerate partitions the same way rather than hard-coding a month list)
    - packages/db/migrations/0039_partition_relocation_admin_scan.sql (the Phase 9 precedent for DDL that has to work under FORCE RLS, including the admin-scan policy interaction)
    - packages/db/src/__tests__/migrate-incremental.test.ts and migrate-from-empty.test.ts (both apply paths this migration must survive)
    - packages/db/src/schema/send-events.ts (the doc comment asserting the OLD dedup-key rationale — it states that `occurred_at` is stable across redeliveries and that `sg_event_id` is "the sole natural key that matters", which is exactly the assumption CMP-07 invalidates and must be rewritten, not merely appended to)
    - .planning/research/PITFALLS.md (Pitfall 14) and 13-RESEARCH.md Pitfalls 2 and 3
  </read_first>
  <behavior>
    - Applying 0057 from an empty schema succeeds and leaves an enforced unique constraint over `(workspace_id, send_id, event_type, occurred_at)`.
    - Applying 0057 over an existing schema seeded with two rows that are duplicates under the new key raises an exception naming `npm run db:resolve-send-event-duplicates`, and leaves both rows and the old constraint in place.
    - Applying 0057 over that same schema after `--resolve` has run succeeds, and the surviving row of the resolved duplicate pair is the one with the earliest `received_at`.
    - After 0057, inserting a second row with the same `(workspace_id, send_id, event_type, occurred_at)` is rejected by the database.
    - After 0057, inserting two rows differing only in `event_type` succeeds.
    - After 0057, inserting two rows differing only in `sg_event_id` is rejected.
    - After 0057, `sg_event_id` is still a `NOT NULL` column holding its original values on pre-existing rows.
    - `workspace_daily_rollup` totals for a seeded workspace-day are byte-identical before and after 0057 is applied.
  </behavior>
  <action>
Write `packages/db/migrations/0057_send_events_dedup_rebase.sql` as its own migration, never combined with another table's DDL. Append the matching `meta/_journal.json` entry (idx 57, tag `0057_send_events_dedup_rebase`).

Structure the migration in this order, with each step's hazard named in a SQL comment:

Step 0 — assert zero duplicates, fail closed. Do NOT delete anything in this migration. REVIEWS.md established that the bounded, per-batch-committing DELETE this change needs cannot be expressed here: a `DO` block is a single transaction so it cannot commit per batch, and the `--> statement-breakpoint` convention provides no loop. The deletion therefore lives in Task 1's operator script (`npm run db:resolve-send-event-duplicates`), and this step's job is to make it impossible to skip. Raise an exception naming that command if any `(workspace_id, send_id, event_type, occurred_at)` group with `send_id IS NOT NULL` still has more than one row.

Fail closed rather than warn, and place the assertion before every subsequent step, because the alternative is worse than a refused migration: a unique index build over surviving violations fails part-way through a per-partition loop on a potentially large partitioned table, leaving some partitions indexed and others not and an invalid parent index that enforces nothing while reporting no error. Follow the in-repo precedent for a migration that guards an unsafe precondition — migration 0038's deadline guard, and its test in `packages/db/src/__tests__/migration-0038-deadline-guard.test.ts` — including how that refusal is asserted by test. Plan 13-12's migration 0061 uses the same fail-closed shape for the same class of reason.

Document the operator sequence in the migration's header comment, because it is a migration with an application-level step in front of it that cannot be expressed in SQL: run `npm run db:count-send-event-duplicates` to see the blast radius, run `npm run db:resolve-send-event-duplicates` if it is non-zero, then apply 0057. If 0057 is reached first it raises and stops; the operator resolves and re-applies, which resumes at 0057 because 0056 is already recorded as applied. State that this is the intended behavior, not an unhandled case.

Step 1 — create the parent index metadata-only. Issue `CREATE UNIQUE INDEX send_events_dedup_v2_idx ON ONLY send_events (workspace_id, send_id, event_type, occurred_at)`. It must be `UNIQUE`: a non-unique index cannot back dedup and cannot be an `ON CONFLICT` target, so a plain `CREATE INDEX` here produces a migration that applies cleanly and then fails every insert at runtime. `ON ONLY` makes this metadata-only and instant; the parent index stays invalid until every partition's index is attached.

Step 2 — per attached partition, build the matching unique index with `CREATE UNIQUE INDEX CONCURRENTLY` and then `ALTER INDEX send_events_dedup_v2_idx ATTACH PARTITION <partition index>`. A concurrent build issued directly against the partitioned parent errors immediately — this is an unsupported operation, not a lock or permission problem. Enumerate partitions from the catalog the way `ensure-partitions.ts` does rather than hard-coding a month list, so a database with a different set of attached partitions than this dev environment still migrates. Once every partition's index is attached, Postgres marks the parent index valid automatically; assert `pg_index.indisvalid` for the parent afterwards rather than assuming it — this is the same class of silent failure as Phase 14's Pitfall 17, where an invalid index enforces nothing and reports no error.

Note that `CREATE INDEX CONCURRENTLY` cannot run inside a transaction block. Check how this project's migration runner wraps statements (the `--> statement-breakpoint` convention in existing migrations and what `lint:migrations` enforces) and structure the file so the concurrent builds are not wrapped in a transaction. If the runner cannot express that, fall back to a non-concurrent per-partition `CREATE UNIQUE INDEX` and record the trade in the SUMMARY: a brief write lock per partition at migration time, in exchange for a migration that actually applies. Do not silently emit CONCURRENTLY inside a transaction — it fails at apply time.

Step 3 — promote to an enforced constraint if and only if Postgres supports it here. `ALTER TABLE ... ADD CONSTRAINT ... UNIQUE USING INDEX` is documented as not supported on partitioned tables; verify this against the PostgreSQL version in use before writing the statement. If unsupported, do not attempt it: an attached, valid unique index on a partitioned table enforces uniqueness on its own, and `ON CONFLICT (workspace_id, send_id, event_type, occurred_at)` matches a unique index directly without needing a named constraint. Record which of the two paths this database supports in the SUMMARY, because it determines whether a named constraint exists for future migrations to reference.

Step 4 — drop the old dedup constraint. Confirm its actual name from the live catalog first: migration 0020 declares it inline as `UNIQUE (workspace_id, sg_event_id, occurred_at)`, so Postgres auto-generated the name and it must be read rather than assumed. This step is last, after the new index is enforced and valid — dropping first would leave a window with no dedup protection at all.

Record the deploy-ordering assumption this step rests on in the migration's header comment — REVIEWS.md MEDIUM finding. Dropping the old constraint in the same migration that Task 3's new `ON CONFLICT` target requires creates a window in which **old** worker code, whose insert still names `ON CONFLICT (workspace_id, sg_event_id, occurred_at)`, matches no constraint and fails at runtime on every insert. That window is empty under this milestone's actual deploy mode: ROADMAP R-05 adopts stop-old-then-start-new for the worker in Phase 14, so no old worker process is running when the new schema is live. State that dependency explicitly in the header rather than leaving it implicit — a future move to a rolling worker deploy would silently reintroduce a total ingestion outage, and the header is where that reader will be. Do NOT split the drop into a follow-on migration to buy rolling-deploy safety: it would leave the old constraint enforcing a key the code no longer targets for a full phase, and the deploy mode that makes the drop safe is already the committed one.

Rewrite the dedup-key section of `packages/db/src/schema/send-events.ts`'s doc comment rather than appending to it. The existing text asserts that `sg_event_id` is "the sole natural key that matters" and that its stability across redeliveries is what makes dedup work; that claim is now known false and leaving it in place next to a correction is worse than either alone. State the new key, state that `sg_event_id` is retained for forensic and log correlation only, state the partition-key structural requirement that keeps `occurred_at` in the key, and state the two accepted trade-offs (null `send_id` rows do not dedupe against each other; two same-second events of the same type on the same send collapse to one row).

Write `packages/db/src/__tests__/send-events-dedup-rebase.test.ts` covering every case in the behavior list, including the apply-over-seeded-duplicates path and the rollup-totals-unchanged assertion.
  </action>
  <acceptance_criteria>
    - `packages/db/migrations/0057_send_events_dedup_rebase.sql` contains `CREATE UNIQUE INDEX` (not a non-unique `CREATE INDEX`) for both the parent and per-partition builds.
    - `packages/db/migrations/0057_send_events_dedup_rebase.sql` contains `ON ONLY send_events` and `ATTACH PARTITION`.
    - `packages/db/migrations/meta/_journal.json` contains an entry with tag `0057_send_events_dedup_rebase`.
    - After applying 0057, a query against `pg_index` reports `indisvalid = true` for the parent index.
    - After applying 0057, inserting two `send_events` rows identical except for `sg_event_id` raises a unique violation.
    - After applying 0057, inserting two `send_events` rows identical except for `event_type` succeeds.
    - `packages/db/migrations/0057_send_events_dedup_rebase.sql` contains no `DELETE`, and contains a guard that raises and names `db:resolve-send-event-duplicates`, placed before the first `CREATE` statement.
    - That migration's header comment names the operator sequence and the stop-old-then-start-new deploy assumption behind the Step 4 drop.
    - Applying 0057 over a schema seeded with a duplicate pair raises, and both rows plus the old constraint survive.
    - Applying 0057 over that schema after the resolve script has run succeeds, leaves exactly one row, and it is the one with the earlier `received_at`.
    - `workspace_daily_rollup` counts for a seeded workspace-day are identical before and after applying 0057.
    - `send_events.sg_event_id` is still `NOT NULL` after 0057.
    - `npm run lint:migrations` exits 0.
    - `npm run test:migrations` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint:migrations && npm run test:migrations</automated>
  </verify>
  <done>The new key is enforced and valid on every partition, historical duplicates are resolved without disturbing any rollup total, and the old constraint is gone only after the replacement was proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Swap the insert path's ON CONFLICT target and prove the bypass is closed</name>
  <files>apps/worker/src/queues/webhook-events.worker.ts, apps/worker/src/queues/__tests__/webhook-events-dedup-rebase.test.ts</files>
  <read_first>
    - apps/worker/src/queues/webhook-events.worker.ts lines 490-622 (`processWebhookEventBatch`'s multi-row INSERT and its `ON CONFLICT (workspace_id, sg_event_id, occurred_at) DO NOTHING RETURNING id`, plus the `insertedIds`/`newRows` gate that every side effect depends on) and lines 255-400 (`applyEventSideEffects`, specifically the `open` and `click` cases where `open_count`/`click_count` increment for every genuinely-new ROW, inside the `newRows` loop)
    - packages/db/migrations/0057_send_events_dedup_rebase.sql (as written by Task 2 — the enforced key whose column list the `ON CONFLICT` target must match)
    - apps/worker/src/queues/__tests__/webhook-events-idempotency.test.ts (the existing dedup assertions, which currently prove the OLD key's behavior and must keep passing under the new one)
  </read_first>
  <behavior>
    - Processing the same event twice with two different `sg_event_id` values inserts one `send_events` row, sets the fact column once, increments the campaign counter once, and increments the rollup once.
    - Processing the same event twice with the same `sg_event_id` behaves identically (the previous guarantee is preserved).
    - Processing two different event types on the same send at the same second inserts two rows and applies both sets of side effects.
    - Processing two events on different sends at the same second with the same type inserts two rows.
    - Processing two orphan events (null `send_id`) that are otherwise identical inserts two rows and moves no counter.
    - Processing two `open` events on the same send at the same second inserts one row and increments `open_count` by exactly 1.
    - Processing two `open` events on the same send one second apart inserts two rows and increments `open_count` by exactly 2.
  </behavior>
  <action>
Change the dedup insert's conflict target in `processWebhookEventBatch` from `ON CONFLICT (workspace_id, sg_event_id, occurred_at)` to `ON CONFLICT (workspace_id, send_id, event_type, occurred_at)`. The column list must match the enforced unique index from Task 2 exactly; a mismatch produces a runtime "no unique or exclusion constraint matching the ON CONFLICT specification" error that no type check or build catches. Leave the `DO NOTHING RETURNING id` shape and the `insertedIds`/`newRows` side-effect gate untouched — those are what make double-processing safe, and they are not what this plan is changing.

Record the repeat-counter trade-off in a code comment at the `open`/`click` cases, because the research's stated assumption is only half right and the correction matters. `open_count` and `click_count` increment outside the `justSet` fact-column gate but INSIDE the `newRows` loop — they climb once per genuinely-new row, not once per delivered event. Under the new key, two genuine opens on the same send within the same second collapse to one row and therefore one `open_count` increment, where the old key would have counted two if the provider assigned different event ids. This is accepted and is arguably the better behavior: SendGrid timestamps have one-second granularity, so two opens on the same send in the same second are far more likely to be one open reported twice than two distinct human actions. State it as a decision with that reasoning, and assert both halves of it in the test (same-second collapses to 1, one-second-apart counts 2) so the behavior is pinned rather than incidental.

Update `processWebhookEventBatch`'s doc comment to name the new key and to state that CMP-05's bounding (plan 13-04) and CMP-07's re-base compose: bounding closes the vary-the-timestamp bypass of the partition and dedup key, re-basing closes the vary-the-event-id bypass of the dedup key, and either fix alone leaves the other bypass open.

Write `apps/worker/src/queues/__tests__/webhook-events-dedup-rebase.test.ts` covering every case in the behavior list. The varying-`sg_event_id` case is the contract test for this requirement — it must fail against the old key, so if it passes before the migration is applied, the test is wrong.
  </action>
  <acceptance_criteria>
    - `apps/worker/src/queues/webhook-events.worker.ts` contains `ON CONFLICT (workspace_id, send_id, event_type, occurred_at)`.
    - Processing one occurrence twice under two different `sg_event_id` values yields exactly 1 `send_events` row, 1 campaign counter increment, and 1 rollup increment.
    - Processing two `open` events on the same send at the same second yields `open_count = 1`.
    - Processing two `open` events on the same send one second apart yields `open_count = 2`.
    - Processing two otherwise-identical null-`send_id` events yields 2 rows and no counter movement.
    - `npx vitest run --root apps/worker src/queues/__tests__/webhook-events-dedup-rebase.test.ts` exits 0.
    - `npx vitest run --root apps/worker src/queues/__tests__/webhook-events-idempotency.test.ts src/queues/__tests__/webhook-events-status.test.ts src/queues/__tests__/webhook-events-suppression.test.ts src/queues/__tests__/webhook-events-attribution.test.ts src/queues/__tests__/webhook-events-processed.test.ts src/queues/__tests__/webhook-events-sibling-drop.test.ts src/queues/__tests__/webhook-events-occurred-at-bounds.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --root apps/worker src/queues/__tests__/webhook-events-dedup-rebase.test.ts src/queues/__tests__/webhook-events-idempotency.test.ts src/queues/__tests__/webhook-events-status.test.ts src/queues/__tests__/webhook-events-suppression.test.ts src/queues/__tests__/webhook-events-attribution.test.ts src/queues/__tests__/webhook-events-processed.test.ts src/queues/__tests__/webhook-events-sibling-drop.test.ts src/queues/__tests__/webhook-events-occurred-at-bounds.test.ts</automated>
  </verify>
  <done>A redelivery with an unstable event id is counted once, every other dedup guarantee is preserved, and the repeat-counter trade-off is pinned by a test rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider event id → dedup identity | `sg_event_id` is provider-controlled and verified unstable; trusting it for uniqueness is the vulnerability. |
| Migration DDL → live partitioned table | A mid-migration failure on a partitioned table can leave a non-enforcing index that reports no error. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07-01 | Tampering | Dedup key based on `sg_event_id` | high | mitigate | Promote the compound server-observed key and demote `sg_event_id` out of uniqueness entirely. Contract test: a redelivery with a different event id inserts one row. |
| T-13-07-02 | Tampering | Non-enforcing invalid index after a partial build | high | mitigate | Assert `pg_index.indisvalid = true` for the parent index after all partitions attach, rather than assuming the build succeeded. Same failure class as Phase 14's Pitfall 17. |
| T-13-07-03 | Denial of Service | Unbounded DELETE resolving duplicates on a large partitioned table | high | mitigate | The migration performs no DELETE at all; resolution is a bounded, per-batch-committing operator script with a dry-run default, and the migration asserts zero duplicates and refuses otherwise. The blast radius is counted first by the same script's read-only mode. |
| T-13-07-08 | Denial of Service | Total ingestion failure during a rolling worker deploy | medium | accept | Dropping the old constraint in the same migration the new `ON CONFLICT` target requires means old worker code would fail every insert. Accepted because ROADMAP R-05 commits the worker to stop-old-then-start-new, so the window is empty; the assumption is recorded in the migration header so a future move to rolling deploys sees it. |
| T-13-07-04 | Repudiation | Losing forensic correlation to the provider's own event id | medium | mitigate | `sg_event_id` remains a `NOT NULL` stored column; only its role in uniqueness changes. Asserted after migration. |
| T-13-07-05 | Denial of Service | A dedup window with no protection at all | medium | mitigate | Expand/contract ordering: the old constraint is dropped only after the new index is valid and the insert path has been swapped to it. |
| T-13-07-06 | Repudiation | Under-counting repeat engagement | low | accept | Two same-second opens on one send now collapse to one `open_count` increment. Accepted: at SendGrid's one-second timestamp granularity this is far more likely one open reported twice than two distinct actions. Pinned by a test so it cannot drift silently. |
| T-13-07-07 | Repudiation | Orphan events not deduping against each other | low | accept | Postgres treats NULL as distinct in a unique key, so repeated redelivery of an unattributable event inserts repeatedly. Accepted: orphan events drive zero counters, so the cost is bounded to storage on traffic the platform cannot attribute anyway. Documented in the schema doc comment. |
| T-13-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced (RESEARCH.md Package Legitimacy Audit: zero new dependencies). Any install proposed during execution requires slopcheck plus a blocking human checkpoint. |
</threat_model>

<flagged_assumptions>
Probe row **CMP-07** came back `unclassified` / `unresolved` (no SPEC.md exists for this phase). Surfaced explicitly, not auto-backstopped:

- **CMP-07 (unresolved):** the `must_haves.truths` above were derived goal-backward from ROADMAP success criterion 4 and CONTEXT's locked `sg_event_id`-instability fact, not from a verified SPEC edge row. Three unverified judgements are load-bearing: (1) that `occurred_at` IS stable across redeliveries of the same occurrence while `sg_event_id` is not — the whole key rests on this asymmetry, and it is inherited from CONTEXT's provenance rather than re-verified against live SendGrid traffic here; (2) that `ALTER TABLE ... ADD CONSTRAINT ... UNIQUE USING INDEX` is unsupported on partitioned tables — RESEARCH.md's own migration sketch uses it, the advisory review flagged it as probably wrong, and Task 2 therefore verifies against the live PostgreSQL version instead of copying either claim; (3) that the concurrent-build path is expressible under this project's migration runner — Task 2 carries an explicit non-concurrent fallback with a recorded trade rather than assuming it.
- **Correction to RESEARCH.md assumption A1 (recorded here so it is not lost):** A1 claims `open_count`/`click_count` "increment independently of the dedup insert", implying no repeat-engagement loss. Direct reading of `applyEventSideEffects` shows they increment independently of the `justSet` fact-column gate but *inside* the `newRows` loop, so they are gated on the dedup insert after all. The consequence is a real, bounded fidelity change under the new key, quantified and accepted as T-13-07-06 above rather than left as a false reassurance.
- **Resolved during the cross-AI review pass:** judgement (3) — that the batched duplicate-resolution step was expressible under this project's migration runner — was false, and the review established why: a `DO` block is one transaction, so it cannot commit per batch, and the `--> statement-breakpoint` convention provides no loop. The resolution moved out of the migration into Task 1's operator script and the migration now asserts zero and refuses, so the plan no longer contains a step the runner cannot express. Judgement (2) — whether `ADD CONSTRAINT ... UNIQUE USING INDEX` is supported on partitioned tables — and the concurrent-build question remain as written: Task 2 still verifies both against the live PostgreSQL version rather than trusting either claim, and still carries the non-concurrent fallback with a recorded trade.
</flagged_assumptions>

<review_incorporation>
Cross-AI review findings (13-REVIEWS.md) addressed in this plan:

- **MEDIUM, "Two migration-mechanics gaps" (1) the batched DELETE is not expressible under this runner** — incorporated by choosing the mechanism now, as the reviewer's suggestion 6 asked, rather than leaving it to be discovered mid-execution. The deletion moves to a `--resolve` mode on Task 1's operator script (the Phase 9 D-08 `relocate-default-partition-rows` precedent: dry-run default, `--page-size`, per-batch commits, idempotent), and migration Step 0 becomes a fail-closed zero-duplicates assertion naming that command (the migration-0038 deadline-guard precedent, which plan 13-12's 0061 also follows). Two new `must_haves` truths, a tightened and one added prohibition, five new Task 1 acceptance criteria, three changed Task 2 behavior cases, and a strengthened T-13-07-03.
- **MEDIUM, "Two migration-mechanics gaps" (2) the constraint drop creates a deploy window** — incorporated as the second half of the reviewer's option: the stop-old-then-start-new assumption is recorded in the migration header citing ROADMAP R-05, with an explicit warning that a future move to rolling worker deploys would reintroduce a total ingestion outage. The alternative — deferring the drop to a follow-on migration — is considered and declined in the plan text, with the reason. Recorded as accepted threat T-13-07-08 and pinned by an acceptance criterion on the header comment.
</review_incorporation>

<verification>
- `npm run db:count-send-event-duplicates` produces a recorded number before Task 2 runs, and `npm run db:resolve-send-event-duplicates` has been run if that number is non-zero.
- `npm run lint:migrations` and `npm run test:migrations` exit 0.
- The full `webhook-events-*` suite in `apps/worker/src/queues/__tests__/` exits 0.
- `npx vitest run --root packages/db` exits 0.
- `npm run lint` and `npm run build` exit 0.
</verification>

<success_criteria>
- A redelivered event with an unstable `sg_event_id` is stored and counted exactly once.
- The new unique index is valid on every attached partition, asserted from the catalog.
- Historical duplicates are resolved boundedly, with rollup totals provably undisturbed.
- Every accepted trade-off is written down and pinned by a test.
</success_criteria>

<output>
Create `.planning/phases/13-compliance-analytics-integrity/13-07-SUMMARY.md` when done. Record: the duplicate count from Task 1 (or that it could not be taken); whether `--resolve` was run and how many rows it deleted; whether `ADD CONSTRAINT ... UNIQUE USING INDEX` was supported on this PostgreSQL version; whether the concurrent build path was usable or the non-concurrent fallback was taken, with the trade; the exact old constraint name that was dropped; and the operator sequence as written into the migration header, since plan 13-14 must reproduce it in the phase checklist alongside plan 13-12's own two-migration sequence.
</output>
