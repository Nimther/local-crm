---
phase: 14-deployment-database-durability
plan: 13
type: execute
wave: 8
depends_on: [14-05, 14-07, 14-09, 14-11, 14-12]
files_modified:
  - SPECIFICATION.md
  - ARCHITECTURE.md
  - CONVENTIONS.md
  - scripts/check-spec-env-coverage.mjs
  - scripts/__tests__/check-spec-env-coverage.test.mjs
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [DB-09, DB-11, DB-13, DB-14]

must_haves:
  truths:
    - "SPECIFICATION.md describes the deployed system as built: every new dependency and image tag, every new secret name, the new schema objects and retention policy, the migrate runner and retention tick, the health routes, the backup observability, and the deployment topology."
    - "Every environment variable name the production configuration requires appears in SPECIFICATION.md's secrets section, and a machine gate fails when one is missing."
    - "The connection budget is written down as a table: each pool's maximum, the per-process totals, the sum, and the configured max_connections — so D-09's deferral of PgBouncer rests on an auditable number."
    - "ARCHITECTURE.md gains the deployment topology, the migration gating flow, the backup and point-in-time-recovery model, and the retention model."
    - "CONVENTIONS.md gains the rules this phase established: pools come from the factory, TLS is driven by the connection string alone, and the container stop-grace-period is derived rather than typed."
    - "Every version recorded is the exact value shipped, not a range from the research document, and any divergence from the Technology Stack section is recorded as a divergence."
    - "The two deferred decisions — PgBouncer and Postgres TLS verify-full — are recorded with their owners and revisit triggers where a reader of the as-built document will find them."
  artifacts:
    - SPECIFICATION.md
    - ARCHITECTURE.md
    - scripts/check-spec-env-coverage.mjs
  key_links:
    - "docker/prod.env.example -> the env-coverage gate -> SPECIFICATION.md section 3: the binding rule in .claude/CLAUDE.md says a new secret must be recorded in the same change; a gate comparing the two files is what makes that rule survive the next phase."
    - "PG_POOL_SIZES (plan 14-03) + max_connections (plan 14-08) -> the connection budget table: D-09 deferred PgBouncer on a headroom argument, and the table is the argument. Without it the deferral is an opinion."
    - "The pgBackRest retention window (plan 14-10) + PARTITION_RETENTION_MONTHS (plan 14-12) -> the recovery-horizon statement: neither number alone tells a reader what is recoverable."
  prohibitions:
    - statement: No version in SPECIFICATION.md is a range or an approximation; every one is the exact shipped value.
      status: flagged
      verification: unverified
    - statement: No secret value is written into SPECIFICATION.md — names, sources and purposes only.
      status: flagged
      verification: unverified
    - statement: Nothing absent from the code is described as present; anything undetermined is recorded as undetermined.
      status: flagged
      verification: unverified
---

<objective>
Bring the as-built documentation up to date with everything this phase deployed, and make the parts that decay machine-checked. `.claude/CLAUDE.md` binds this repository to recording every new dependency, secret, schema object, queue, route and observability hook in `SPECIFICATION.md` in the same change; this phase adds all six categories at once, across twelve plans, and Phase 13's precedent consolidated that filing into a dedicated late plan rather than fragmenting one shared file across a dozen parallel branches.

Purpose: `SPECIFICATION.md` exists for security review — it is the document a reviewer reads instead of the repository. A deployment phase that leaves it describing a system that only ever ran on a developer machine makes the next review worthless. The connection budget matters for a second reason: D-09 deferred PgBouncer on the argument that connection pressure is theoretical, and that argument is only checkable if the arithmetic is written down.
Output: `SPECIFICATION.md` sections 2 through 8 updated, `ARCHITECTURE.md` gaining four sections, `CONVENTIONS.md` gaining three rules, and an env-coverage gate wired into CI.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-deployment-database-durability/14-CONTEXT.md
@.planning/phases/14-deployment-database-durability/14-RESEARCH.md
</context>

## Artifacts this phase produces

New symbols and paths created by THIS plan:

- Files: `scripts/check-spec-env-coverage.mjs`, `scripts/__tests__/check-spec-env-coverage.test.mjs`
- npm scripts: `check:spec-env-coverage`
- CI: a "SPECIFICATION env coverage" step in the `static` job
- New sections: `ARCHITECTURE.md` deployment topology, migration gating, backup/PITR, retention, and the connection budget table; `CONVENTIONS.md` pool-factory, TLS-source-of-truth and derived-stop-grace rules

## Planner assumptions (specless probe, no SPEC.md for this phase)

Edge-probe rows **DB-09**, **DB-11**, **DB-13** and **DB-14** returned `unresolved` (English-keyed classifier, Russian REQUIREMENTS.md). Their behavioural truths are authored in the plans that implement them (14-10, 14-12, 14-03/14-08, 14-03). This plan authors the documentation half of DB-14 specifically: the connection budget as a table whose sum is compared against the configured `max_connections`, which is the evidence D-09's deferral requires.

<tasks>

<task type="auto">
  <name>Task 1: SPECIFICATION.md sections 2 through 8, with a machine gate on the secrets section</name>
  <files>SPECIFICATION.md, scripts/check-spec-env-coverage.mjs, scripts/__tests__/check-spec-env-coverage.test.mjs, package.json, .github/workflows/ci.yml</files>
  <read_first>
    - SPECIFICATION.md (the whole document's section structure and register — it is written in Russian and describes the system as built; match the existing style, including how "не определено" is used for anything undetermined)
    - .claude/CLAUDE.md § "Project Specification (SPECIFICATION.md)" (the binding rule and its per-section routing table, which is the checklist this task executes)
    - every `.planning/phases/14-deployment-database-durability/14-*-SUMMARY.md` from plans 14-01 through 14-12 (these are the source of truth for exact values: base image tags, the installed pgBackRest version, env var names, pool sizes and their sum, `max_connections`, the stanza name, the backup cadence and retention window, the retention horizon and its flag, the health routes and the migrate runner)
    - docker/prod.env.example (as extended by plans 14-08 and 14-10 — the authoritative list of env var names the gate compares against)
    - docker/docker-compose.prod.yml (the deployed topology, image references and the services list)
    - scripts/lint-session-state.mjs and scripts/__tests__/lint-session-state.test.mjs (the guard-script class and its test convention, including the repo-wide clean-run assertion that prevents a vacuous pass)
    - .github/workflows/ci.yml lines 43-90 (the `static` job's step list and comment convention)
  </read_first>
  <action>
Work section by section through the routing table in `.claude/CLAUDE.md`, taking every value from the phase SUMMARYs rather than from this phase's research document — RESEARCH.md's version numbers are explicitly flagged as assumed, and the repository's own rule is that a recorded version must be the exact shipped value.

Section 2 (dependencies and versions): the base image tags for node, caddy, postgres and redis; the installed pgBackRest version; and an explicit note that this phase added no npm package, since a reader comparing against `package.json` will otherwise wonder what a deployment phase installed. Where a tool is container- or OS-level rather than an npm entry, say so in those words.

Section 3 (secrets): every new environment variable name with what it is and where its value comes from — the six pgBackRest values, the GHCR pull token, the deploy-time variables, the retention enable flag, the TLS-related paths — all under the existing `MEGA_CRM_ENV_FILE` convention. Names, sources and purposes only; never a value.

Section 4 (schema): the `member` unique constraint from plan 14-02, and the retention policy — which tables are subject to it, the horizon, and which evidence tables are explicitly excluded.

Section 5 (scheduler and send pipeline): the one-shot migrate runner and how it gates, and the retention step joining the existing partition-maintenance tick.

Section 6 (public entry points): `/healthz` and `/readyz` on both the API and the worker, including that the worker's listener is loopback-bound and unpublished, and that neither route is authenticated.

Section 7 (observability): what the backup path reports — pgBackRest's own check and info commands — and the explicit note that backup-failure alerting is deferred to Phase 15 with the channel it will use.

Section 8 (divergences): the Node 22 images against the repository's Node 26 `.nvmrc`, with the recorded reason; the actual pgBackRest and Caddy versions against RESEARCH.md's assumed ones; PgBouncer's absence against the Technology Stack section that recommends it, recorded as D-09's deferral with owner and revisit trigger; and Postgres TLS stopping at `sslmode=require` with a self-signed certificate rather than `verify-full`, as D-10's deferral with its revisit trigger.

Then write `scripts/check-spec-env-coverage.mjs`: it extracts environment variable names from `docker/prod.env.example` and asserts each appears in `SPECIFICATION.md`, failing with the list of missing names. Node built-ins only, in the class of `lint-session-state.mjs`, with exported helpers and a reported count of names checked so a vacuous pass is visible — the same lesson `lint:floor` encodes. This gate is the mechanism that makes CLAUDE.md's same-change rule survive future phases: the next person who adds a secret to the compose configuration and forgets the specification will be told by CI instead of by a reviewer.

Write `scripts/__tests__/check-spec-env-coverage.test.mjs` with fixtures for the passing case, a missing-name case, and the real files. Register `check:spec-env-coverage` in the root `package.json` and add a step to the `static` job in `.github/workflows/ci.yml`, beside the other audits. Touch no other job.
  </action>
  <acceptance_criteria>
    - `SPECIFICATION.md` sections 2, 3, 4, 5, 6, 7 and 8 each contain the new content listed above.
    - Every version recorded for a new tool is an exact value taken from the phase SUMMARYs: for each of node, caddy, postgres, redis and pgBackRest, the version string in SPECIFICATION.md matches the value the corresponding SUMMARY recorded, character for character. No entry uses a range, a caret, a tilde, an `x` placeholder or "or later".
    - `SPECIFICATION.md` names the six `PGBACKREST_*` variables, the GHCR pull token, the retention enable flag and the deploy-time variables, and contains no value for any of them.
    - Section 8 records the Node 22 versus `.nvmrc` divergence, the pgBackRest and Caddy actual versions, PgBouncer's deferral with owner and revisit trigger, and the TLS `verify-full` deferral with its revisit trigger.
    - `npm run check:spec-env-coverage` exits 0 and reports the number of names checked.
    - The gate exits non-zero against a fixture whose specification is missing one name, and names that variable.
    - `npx vitest run --root scripts __tests__/check-spec-env-coverage.test.mjs` exits 0.
    - `.github/workflows/ci.yml`'s `static` job contains a step running `npm run check:spec-env-coverage`; the `test`, `failure-injection` and `e2e` jobs are unchanged.
    - Anything not determinable from the SUMMARYs is recorded as undetermined in the document's existing idiom rather than guessed.
  </acceptance_criteria>
  <verify>
    <automated>npm run check:spec-env-coverage && npx vitest run --root scripts</automated>
  </verify>
  <done>The as-built document describes the deployed system, and the secrets section can no longer silently fall behind the configuration.</done>
</task>

<task type="auto">
  <name>Task 2: ARCHITECTURE.md's four new sections including the connection budget, and CONVENTIONS.md's three new rules</name>
  <files>ARCHITECTURE.md, CONVENTIONS.md</files>
  <read_first>
    - ARCHITECTURE.md (the whole document's section structure, especially §9's delivery state machine and §10's drain-timeout section which states that Phase 14 must set the container stop-grace-period from the published constant — that statement is now satisfied and should say so, naming the mechanism)
    - CONVENTIONS.md (the existing rules' format, particularly the versioned-constants rule and the transaction-local-session-state rule with its paired machine gate — the three new rules follow the same written-rule-plus-machine-gate shape)
    - .planning/phases/14-deployment-database-durability/14-03-SUMMARY.md (the full `PG_POOL_SIZES` table and its sum — the raw material for the budget table)
    - .planning/phases/14-deployment-database-durability/14-08-SUMMARY.md (the configured `max_connections` and the compose topology)
    - .planning/phases/14-deployment-database-durability/14-10-SUMMARY.md and 14-12-SUMMARY.md (the backup cadence and repository retention window, and the partition retention horizon — the two numbers the recovery-horizon statement combines)
    - .planning/phases/14-deployment-database-durability/14-01-SUMMARY.md (the migration gating flow: the advisory lock on a dedicated connection, the one-shot step, and the applied-versus-shipped readiness check)
    - docs/runbooks/production-topology.md, docs/runbooks/backups.md, docs/runbooks/restore-drill.md, docs/runbooks/data-retention.md, docs/runbooks/deploy-and-rollback.md, docs/runbooks/migration-rollback-and-roll-forward.md, docs/runbooks/container-images.md (all written earlier in this phase — ARCHITECTURE.md describes the models and cross-references these for the procedures; do not duplicate procedural content)
  </read_first>
  <action>
Add four sections to `ARCHITECTURE.md`, each describing a model rather than a procedure, and cross-referencing the runbook that owns the procedure.

Deployment topology: the six containers on one VPS, what is reachable from where, that only Caddy publishes ports, the image-per-SHA model and why the tags are immutable. Include the connection budget as a table — one row per pool with its maximum, subtotals per process, the sum across everything that runs in production, and the configured `max_connections` with the resulting headroom. State plainly that this table is the evidence for D-09's deferral of PgBouncer and that the revisit trigger is real pressure against this number, so a future reader can check the claim instead of trusting it.

Migration gating: the one-shot step, the advisory lock on a dedicated short-lived connection and why the connection discipline is half the guarantee, the bounded retry with a loud failure, and readiness independently verifying applied-versus-shipped so a mis-sequenced container start cannot serve a stale schema.

Backup and point-in-time recovery: continuous WAL archiving plus scheduled backups to an off-host encrypted repository, what a restore actually restores, and the recovery-point and recovery-time expectations the cadence produces.

Retention: partition drop as the deletion mechanism, the horizon, the excluded evidence tables and what they prove, and the combined recovery horizon — the partition horizon together with the repository retention window. Give the arithmetic, because neither number alone tells a reader what is recoverable.

Then update §10's drain-timeout section: it currently instructs Phase 14 to set the container stop-grace-period from the published constant. That is now done, so record the mechanism — the publish script, the compose interpolation and the validation gate that fails on drift — so the instruction becomes a description of how the requirement is met.

Add three rules to `CONVENTIONS.md`, each in the existing written-rule-plus-machine-gate format that the session-state rule established. First: every Postgres pool is created through the factory, never directly, with the pool-factory audit named as the gate. Second: TLS for a Postgres connection is driven by the connection string alone and never by a separately-constructed options object, with the reason — the query-string parameter silently wins, so code carrying both can believe it configured a posture it never applied. Third: a container's stop-grace-period is derived from the published constant and never typed, with the compose validation gate named. For each, state the failure the rule prevents rather than only the rule, because that is what makes a convention survive a reviewer who disagrees with it.
  </action>
  <acceptance_criteria>
    - `ARCHITECTURE.md` contains new sections for deployment topology, migration gating, backup/PITR and retention.
    - The deployment topology section contains a connection budget table with a row per pool, the sum, the configured `max_connections` and the resulting headroom, and states that it is the evidence for D-09's deferral.
    - The retention section states the combined recovery horizon arithmetic using both the partition horizon and the repository retention window.
    - `ARCHITECTURE.md` §10's drain-timeout section records that the stop-grace-period is now set from the published constant and names the mechanism and the gate.
    - `CONVENTIONS.md` contains three new rules — pool factory, TLS source of truth, derived stop-grace-period — each naming its machine gate and the failure it prevents.
    - Each new ARCHITECTURE.md section cross-references the runbook that owns the corresponding procedure and does not duplicate procedural steps.
    - `npm run lint` exits 0 and no source file was modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'max_connections' ARCHITECTURE.md && grep -qi 'connection budget' ARCHITECTURE.md && grep -qi 'createPgPool' CONVENTIONS.md && grep -qi 'stop.grace' CONVENTIONS.md && npm run lint && echo docs-OK</automated>
  </verify>
  <done>The architecture document describes the deployed system's four new models with the connection budget written down as an auditable table, and the three conventions this phase established are recorded with their gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPECIFICATION.md → an external security reviewer | The reviewer reads this document instead of the repository; an inaccuracy here is an unreviewed surface |
| documentation → the running system | A document that drifts becomes a false statement that people act on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-83 | Information Disclosure | secret values written into documentation | high | mitigate | Names, sources and purposes only, asserted by a criterion; the env-coverage gate compares names and never reads values. A credential committed into a document is disclosed permanently, exactly like one committed into code. |
| T-14-84 | Repudiation | an as-built document that omits a deployed secret or dependency | high | mitigate | `check:spec-env-coverage` compares every name in the production example env file against SPECIFICATION.md and runs in the `static` job, with a reported count so a vacuous pass is visible. This is CLAUDE.md's same-change rule made mechanical for future phases. |
| T-14-85 | Repudiation | recording an assumed version as a shipped one | medium | mitigate | Every version comes from a phase SUMMARY (which recorded what was actually installed) rather than from RESEARCH.md, whose version numbers are explicitly flagged as assumed; range and approximation patterns are grep-asserted absent. |
| T-14-86 | Repudiation | describing PgBouncer's absence as an oversight rather than a decision | medium | mitigate | Section 8 records both deferrals — PgBouncer (D-09) and TLS `verify-full` (D-10) — with owner and revisit trigger, and the connection budget table gives the PgBouncer deferral an auditable basis rather than an assertion. |
| T-14-87 | Repudiation | documenting a capability the code does not have | high | mitigate | Anything not determinable from the SUMMARYs is recorded as undetermined in the document's existing idiom. A security reviewer treating a documented control as present when it is absent is the worst outcome this document can produce. |
| T-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installed: documentation plus one Node-built-ins gate script. RESEARCH.md's Package Legitimacy Audit records zero new packages this phase; if execution finds one is needed, stop and raise a blocking human checkpoint before installing. |
</threat_model>

<verification>
- `npm run check:spec-env-coverage` exits 0 with a non-zero checked-name count.
- `npx vitest run --root scripts` exits 0.
- `npm run lint && npm run build --workspaces --if-present` exits 0.
- `.github/workflows/ci.yml`'s `static` job has the new step; no other job changed.
- No source file outside `scripts/` was modified.
</verification>

<success_criteria>
- SPECIFICATION.md describes the deployed system across all seven affected sections, with exact values and no secrets.
- The connection budget is an auditable table, giving D-09's PgBouncer deferral a checkable basis.
- ARCHITECTURE.md carries the four new models and records §10's stop-grace instruction as satisfied.
- CONVENTIONS.md carries the three new rules with their machine gates.
- A CI gate prevents the secrets section from silently falling behind the configuration in future phases.
</success_criteria>

<output>
Create `.planning/phases/14-deployment-database-durability/14-13-SUMMARY.md` when done. Record: the connection budget table as filed, anything a SUMMARY failed to supply that had to be recorded as undetermined, and the list of divergences filed into section 8.
</output>
