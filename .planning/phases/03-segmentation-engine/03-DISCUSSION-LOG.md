# Phase 3: Segmentation Engine - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-05
**Phase:** 3-Segmentation Engine
**Areas discussed:** Выразительность билдера, Глубина поведенческих условий, Поведение live-превью, Страница сегментов и жизненный цикл

**⚠ Session note:** Пользователь выбрал все 4 области для обсуждения, но не ответил на первый вопрос (нет ответа за 60с — away from keyboard). Все последующие решения выбраны Claude автоматически (рекомендованные варианты), по прецеденту Phase 1/2 и yolo-режиму конфига. Пользователь уведомлён в итоговой сводке; CONTEXT.md помечен «auto-selected».

---

## Выразительность билдера

| Option | Description | Selected |
|--------|-------------|----------|
| Группы AND/OR (рекомендую) | Два уровня, как у Klaviyo: внутри группы OR, между группами AND | ✓ (auto) |
| Плоский AND-список | Простой список условий, все должны выполняться | |
| Произвольная вложенность | Группы внутри групп без ограничения глубины | |

**Выбор:** auto — Группы AND/OR (вопрос был задан через AskUserQuestion, ответа не последовало; выбран рекомендованный вариант).
**Дополнительно (auto, вопросы не задавались):** отрицания first-class (требуются success criteria); операторы типизированы по реестру свойств; статус подписки доступен как условие профиля.

---

## Глубина поведенческих условий

**Решения (auto, вопросы не задавались):**
- Выбор события из наблюдаемых имён + свободный ввод (реестра типов событий нет).
- Форма условия: «{событие} {≥N раз | ни разу} за {последние N дней | всё время}».
- Фильтры по свойствам события — отложены в v2 (freeform JSON без реестра схем; scope роадмапа требует только count/timeframe).

---

## Поведение live-превью

**Решения (auto, вопросы не задавались):**
- Debounced автопересчёт при изменении определения, точный count, индикатор загрузки.
- В билдере только счётчик; список участников — на странице сегмента.
- Производительность count на 100k–1M — бенчмарк ресёрчера (STATE.md flag).

---

## Страница сегментов и жизненный цикл

**Решения (auto, вопросы не задавались):**
- «Сегменты» — top-level раздел рядом с «Контактами».
- Список: имя, число участников (+ метка времени), создан/обновлён, автор.
- Страница сегмента: определение + пагинированный список участников (паттерн таблицы контактов).
- Сегменты всегда динамические; удаление свободное в Phase 3, схема закладывается под restrict-when-referenced (Phase 4/6).

---

## Claude's Discretion

- Движок: on-demand vs materialized — по бенчмарку ресёрчера; SQL-генерация, индексы, кэширование, debounce-интервал.
- Definition-JSON формат/версионирование, Zod-схемы, RLS.
- Контракт движка для Phase 4 (resolve состава) и Phase 6 (проверка принадлежности).
- Пустые состояния, русские тексты, UX-детали билдера (UI-SPEC отдельно).

## Deferred Ideas

- Фильтры по свойствам события — v2 (после EVNT-V2-01)
- Произвольная вложенность групп — v2
- Статические сегменты/снапшоты, ручные списки — v2
- Приблизительный count — только если бенчмарк потребует
- Папки/теги для сегментов — v2
