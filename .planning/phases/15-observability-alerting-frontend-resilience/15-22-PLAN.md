---
phase: 15-observability-alerting-frontend-resilience
plan: 22
type: execute
wave: 17
depends_on: ["15-17", "15-18"]
files_modified:
  - scripts/validate-alloy-config.mjs
  - scripts/__tests__/validate-alloy-config.test.mjs
  - scripts/__fixtures__/alloy-config/hash-comment-header.alloy
  - scripts/__fixtures__/alloy-config/hash-trailing-comment.alloy
  - scripts/__fixtures__/alloy-config/valid-with-slash-comments.alloy
  - docker/alloy/config.alloy
  - package.json
  - .github/workflows/ci.yml
  - SPECIFICATION.md
  - docs/runbooks/log-shipping-and-backstop-alerts.md
autonomous: true
requirements: [OPS-10]
gap_closure: true
gap_ids: [G-15-4]

must_haves:
  truths:
    - "The committed docker/alloy/config.alloy is accepted by the pinned grafana/alloy image's own parser — `alloy fmt` against it exits 0 instead of reporting an illegal character at 1:1 — so the production alloy sidecar reaches a running state rather than restart-looping (G-15-4)."
    - "A comment token of the rejected class reintroduced anywhere in docker/alloy/config.alloy fails `npm run verify:alloy-config` locally and fails the CI `static` job, naming the offending line and column."
    - "The gate parses the config with the SAME image reference docker/docker-compose.prod.yml pins for the `alloy` service, read from that file at run time — never a tag hardcoded in the gate."
    - "On a machine with no reachable Docker daemon the gate still runs its static scan and prints an explicit skipped-binary notice; where ALLOY_VALIDATE_REQUIRE_BINARY is set (the CI step), an unavailable binary is a violation, not a silent pass."
    - "A `//` comment or a quoted string inside config.alloy may contain the rejected character as ordinary text without tripping the gate — the scanner is comment-aware and string-aware, not a blind character search."
  artifacts:
    - scripts/validate-alloy-config.mjs
    - scripts/__tests__/validate-alloy-config.test.mjs
    - scripts/__fixtures__/alloy-config/hash-comment-header.alloy
    - scripts/__fixtures__/alloy-config/hash-trailing-comment.alloy
    - scripts/__fixtures__/alloy-config/valid-with-slash-comments.alloy
    - docker/alloy/config.alloy
  key_links:
    - "scripts/validate-alloy-config.mjs → validate-prod-compose.mjs's exported `parseEnvFile`/`resolveViaYamlFallback` → docker-compose.prod.yml's `alloy` service `image:` — the single path that keeps the validated image identical to the deployed image."
    - ".github/workflows/ci.yml `static` job step → `npm run verify:alloy-config` → package.json script entry — the gate is blocking only while all three are wired, so the wiring itself is asserted by a test."
    - "ALLOY_VALIDATE_REQUIRE_BINARY set on the CI step → runValidation's fail-closed branch — the difference between a real gate and a vacuous one on a runner without Docker."
---

<objective>
Close gap G-15-4: the production Grafana Alloy sidecar restart-loops because docker/alloy/config.alloy is written with a comment token Alloy's lexer rejects, so no logs leave the host and both backstop alert rules (dead-man's-switch, error-rate spike) sit on an empty stream.

Purpose: OPS-10's whole value — logs reaching the hosted provider — is currently zero in production. The fix itself is mechanical; the durable part is the recurrence guard. 15-17-SUMMARY.md pre-flagged the exact hole this fell through ("This file is not executed by any automated check in this repository (no real Alloy binary runs against it in CI)"), so this plan builds the guard FIRST, proves it catches the shipped defect, then applies the fix.

Output: a new `verify:alloy-config` gate (static comment-token scanner + a real-binary parse under the image the prod compose file pins), the corrected config.alloy, the gate wired into the CI `static` required check fail-closed, and SPECIFICATION.md / the log-shipping runbook updated.

Scope discipline: docker/docker-compose.prod.yml needs NO change (the UAT artifact list records it as context only — its `restart: unless-stopped` merely makes the parse failure visible as a loop). The live half of the gap's truth (logs arriving in Loki, both rules provisioned) was already confirmed by the operator during UAT against a corrected config; this plan makes that corrected config the committed one and stops it regressing.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/15-observability-alerting-frontend-resilience/15-UAT.md
@.planning/debug/alloy-config-hash-comments.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-17-SUMMARY.md

@docker/alloy/config.alloy
@scripts/validate-prod-compose.mjs
@scripts/__tests__/validate-prod-compose.test.mjs
@scripts/vitest.config.ts
@.github/workflows/ci.yml
@docs/runbooks/log-shipping-and-backstop-alerts.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the alloy-config gate and prove it catches the shipped defect (RED)</name>
  <files>scripts/validate-alloy-config.mjs, scripts/__tests__/validate-alloy-config.test.mjs, scripts/__fixtures__/alloy-config/hash-comment-header.alloy, scripts/__fixtures__/alloy-config/hash-trailing-comment.alloy, scripts/__fixtures__/alloy-config/valid-with-slash-comments.alloy</files>
  <read_first>scripts/validate-prod-compose.mjs (exported helpers, `isDirectInvocation` CLI guard, violation-record shape), scripts/__tests__/validate-prod-compose.test.mjs (pure-helper + real-subprocess test mix), scripts/vitest.config.ts, docker/alloy/config.alloy (the defect, as committed)</read_first>
  <behavior>
    Pure scanner `scanIllegalCommentTokens(text)` — a four-state character walk (code / double-quoted string / line comment / block comment), returning one record per illegal token with `{ rule: "illegal-comment-token", line, column, lineText }`, 1-based line and column:
    - A rejected marker as the first non-whitespace character of a line (the committed file's header shape) is reported at that line, at its own column.
    - A rejected marker appearing mid-line in code position (whitespace-preceded trailing comment) is reported at its line and column.
    - A rejected marker occurring inside a `//` line comment is NOT reported — comment text may name the character.
    - A rejected marker occurring inside a block comment is NOT reported.
    - A rejected marker occurring inside a double-quoted string value (including a backslash-escaped quote earlier in that string) is NOT reported.
    - A file with only Alloy-legal comments and blocks returns an empty array.
    Image resolution `resolveAlloyImageRef(baseDir)`:
    - Returns the `alloy` service's `image:` value from docker/docker-compose.prod.yml, resolved through validate-prod-compose.mjs's own exported `parseEnvFile` + `resolveViaYamlFallback` against docker/prod.env.example — currently the pinned `grafana/alloy` reference in that file, and equal to it by assertion, not by a copy.
    - The returned tag is not a mutable tag (asserted with validate-prod-compose.mjs's exported `extractImageTag`/`isMutableTag`).
    - Throws a named error when the compose file declares no `alloy` service or no image for it.
    Fail-closed orchestration `runValidation({ baseDir, requireBinary, dockerAvailable, runFmt })` (both probes injectable seams, defaults real):
    - Docker reachable: the binary layer runs and its failure becomes a violation carrying the parser's own stderr.
    - Docker unreachable and `requireBinary` false: no violation, and the result records a skip reason naming that the binary layer did not run.
    - Docker unreachable and `requireBinary` true: exactly one violation with rule `alloy-binary-check-unavailable`, regardless of the static scan's outcome.
    - The static scan always runs, in every one of those three states.
    CLI wiring, asserted as a real subprocess (this is what CI runs, not the imported function): invoked with a PATH containing no `docker` and `ALLOY_VALIDATE_REQUIRE_BINARY=1`, the script exits non-zero and prints the `alloy-binary-check-unavailable` rule name.
    Regression fixture (the RED lock): `hash-comment-header.alloy` — a byte-faithful excerpt of the committed file's opening lines followed by a valid `discovery.docker` block — yields a violation at line 1, column 1. This fixture is the permanent, re-runnable proof that the gate catches the exact defect that shipped, and survives Task 2 correcting the real file.
  </behavior>
  <action>
    Create `scripts/validate-alloy-config.mjs` — Node built-ins only, no new npm dependency of any kind (this plan installs nothing; see T-15-SC). Mirror `scripts/validate-prod-compose.mjs` structurally: an ES module of exported pure helpers plus an `isDirectInvocation()`-guarded CLI, so importing it from a test never executes it.

    Export `ALLOY_CONFIG_REL` (docker/alloy/config.alloy), `COMPOSE_FILE_REL`, `ENV_FILE_REL`, and `CONTAINER_CONFIG_PATH` — the in-container path `/etc/alloy/config.alloy`, mirroring both the read-only bind mount and the `command:` argument the alloy service already declares in docker-compose.prod.yml.

    Implement `scanIllegalCommentTokens(text)` as the four-state walk described in behavior. Comment-awareness and string-awareness are load-bearing, not polish: Task 2 adds a `//` comment to config.alloy whose prose names the rejected character, and quoted values may legitimately carry it — a blind character search would false-positive on both and make the gate unusable, which is how gates get deleted. Do NOT reimplement Alloy's grammar; this scanner owns exactly one defect class (the comment token), and the real-binary layer below owns everything else, the same way this repo's other hand-rolled scoped parsers (lint-session-state.mjs, lint-pg-pool-factory.mjs, validate-prod-compose.mjs's YAML subset) stay scoped rather than becoming general parsers.

    Implement `resolveAlloyImageRef(baseDir)` by importing `parseEnvFile` and `resolveViaYamlFallback` from `./validate-prod-compose.mjs` and reading `model.services.alloy.image`. Never hardcode a tag here — reading it from the compose file is what guarantees the gate validates against the image production actually runs, and keeps it correct through a future version bump. The fallback resolver (not `resolveViaDockerCompose`) is deliberate: it is deterministic, needs no daemon, and this service's image line is a plain literal.

    Implement `isDockerAvailable()` as a capability probe in the shape of validate-prod-compose.mjs's `isDockerComposeAvailable()` — `execFileSync("docker", ["version", "--format", "{{.Server.Version}}"], { stdio: "ignore" })` inside try/catch, so an installed CLI with no reachable daemon reads as unavailable (a container cannot run without one).

    Implement `runAlloyFmt({ baseDir, imageRef })`: `docker run --rm --network none -v <absolute host path to config.alloy>:<CONTAINER_CONFIG_PATH>:ro <imageRef> fmt <CONTAINER_CONFIG_PATH>`, stdout captured and discarded, exit code and stderr returned. `fmt` parses without evaluating, so the config's three `env()` calls need no Grafana credentials present — this gate never reads a secret. `--network none`, the single read-only file mount, no env passthrough and no docker-socket mount are the deliberate containment of this step (T-15-69): the validation container gets strictly less than the production sidecar. Give the call a generous timeout (about five minutes) so a cold CI runner pulling the image is not mistaken for a parse failure. Do NOT pass `-w` — this gate asserts parse validity, never formatting, and an in-place rewrite would destroy the reviewable diff Task 2 produces.

    Implement `runValidation` with `dockerAvailable`/`runFmt` as injectable seams defaulting to the real implementations (the same dependency-seam convention `ProcessSendJobDeps.sendMail` established in this repo), and `requireBinary` defaulting to whether `ALLOY_VALIDATE_REQUIRE_BINARY` is set to a non-empty value other than `0`. The CLI prints the resolved image reference, whether the binary layer ran or was skipped and why, each violation as `[rule] line:column detail`, and exits 1 when any violation exists.

    Create the three fixtures under `scripts/__fixtures__/alloy-config/`. `hash-comment-header.alloy` must reproduce the committed file's opening shape byte-faithfully enough that the violation lands at 1:1 — copy the first few lines of the real file as they stand today, then a valid `discovery.docker "containers"` block. `hash-trailing-comment.alloy` puts the rejected marker mid-line after a valid argument. `valid-with-slash-comments.alloy` is the false-positive guard: a `//` comment and a block comment whose text both name the rejected character, plus a quoted string value containing it, and zero violations expected.

    Create `scripts/__tests__/validate-alloy-config.test.mjs` following validate-prod-compose.test.mjs's mix — pure helpers asserted directly against the fixtures, plus the CLI exercised as a real subprocess for exit-code behavior. Include one assertion that docker-compose.prod.yml's raw text contains `CONTAINER_CONFIG_PATH`, so the in-container path this gate mounts at cannot drift from the one the service declares. Do NOT yet assert anything about the real docker/alloy/config.alloy being clean — that lock is Task 2's, and adding it here would make this task's own verification unpassable by construction.

    Record the RED evidence in this task's SUMMARY: run `node scripts/validate-alloy-config.mjs` against the repository as it stands right now and capture its output — it must exit 1 and report the illegal token at line 1, column 1 of the real config.alloy, matching the reproduction in .planning/debug/alloy-config-hash-comments.md. This is a one-time observation for the record, deliberately NOT the automated verify below, because Task 2 inverts it.
  </action>
  <verify>
    <automated>npx vitest run --root scripts __tests__/validate-alloy-config.test.mjs</automated>
  </verify>
  <done>Every fixture assertion passes: the header fixture reports the illegal token at 1:1, the trailing-marker fixture reports it at its own column, the legal fixture reports nothing despite naming the character in comments and in a quoted value, `resolveAlloyImageRef` returns the compose file's own pinned reference and rejects a mutable tag, the three docker-availability states behave as specified, and the CLI subprocess with no `docker` on PATH and the require-binary variable set exits non-zero naming `alloy-binary-check-unavailable`. Running the CLI against the current repository exits 1 on the real config — captured in the SUMMARY as the fail-first proof.</done>
</task>

<task type="auto">
  <name>Task 2: Correct config.alloy's comment token and lock it with the real parser (GREEN)</name>
  <files>docker/alloy/config.alloy, scripts/__tests__/validate-alloy-config.test.mjs</files>
  <read_first>docker/alloy/config.alloy (all 137 lines), .planning/debug/alloy-config-hash-comments.md (the counter-test that established the fix is comments-only)</read_first>
  <precondition>The `docker` CLI is installed with a reachable daemon and the image docker-compose.prod.yml pins for the `alloy` service is present locally or pullable — the debug session ran this exact container successfully, and the image is already in the local store. If the daemon is not reachable, halt and report rather than skipping the binary layer.</precondition>
  <action>
    Apply the mechanical correction the debug session verified: on every line of docker/alloy/config.alloy whose first non-whitespace character is the rejected marker, replace that one leading marker with Alloy's own line-comment token, preserving the line's leading whitespace exactly. All 88 comment lines are affected (the header block plus the four inline blocks). Change nothing else — do not reflow, reword, shorten, merge or delete a single comment, do not touch any functional block, and do not run `alloy fmt -w` or any formatter. The diagnosis established that every functional block is already valid Alloy; the intended diff is a comment-token swap and nothing more, and keeping it that way is what makes it reviewable against the diagnosis.

    Then add one short paragraph at the top of the file's existing header block, in the file's own voice and comment style, stating: this configuration language's only comment tokens are the line and block forms Alloy defines; the marker this file previously used is not one of them and is rejected by the lexer at the first byte, which restart-loops the sidecar and silently stops all log delivery (G-15-4); and `npm run verify:alloy-config` now parses this file with the real pinned binary before it can ship. That paragraph will itself name the rejected character in prose — intentionally. It is the live proof that Task 1's scanner is comment-aware rather than a blind character search, and the gate must stay green with it present.

    Finally, add the regression lock to `scripts/__tests__/validate-alloy-config.test.mjs`: an assertion that `scanIllegalCommentTokens` over the real docker/alloy/config.alloy returns an empty array. This is the assertion that could not exist before this task, and the one that fails the moment anyone reintroduces the defect.
  </action>
  <verify>
    <automated>npx vitest run --root scripts __tests__/validate-alloy-config.test.mjs && docker run --rm --network none -v "$PWD/docker/alloy/config.alloy:/etc/alloy/config.alloy:ro" grafana/alloy:v1.18.1 fmt /etc/alloy/config.alloy > /dev/null</automated>
  </verify>
  <done>The pinned Alloy binary parses the committed config with exit 0 and no diagnostics — the same command that reproduced the illegal-character error on the pre-fix file now succeeds. The gate's static scan finds nothing, including in the new header paragraph that names the rejected character in comment text. `git diff docker/alloy/config.alloy` shows only leading-marker substitutions plus the one added paragraph — no functional block altered, no comment lost.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the gate into the required CI check, fail-closed, and file it</name>
  <files>package.json, .github/workflows/ci.yml, scripts/__tests__/validate-alloy-config.test.mjs, SPECIFICATION.md, docs/runbooks/log-shipping-and-backstop-alerts.md</files>
  <read_first>package.json (the `verify:*` / `check:*` script block), .github/workflows/ci.yml (the `static` job, specifically the existing "Production compose invariants" and "Runbook coverage" steps), SPECIFICATION.md §7 (the plan-15-17 Alloy paragraph) and §8 (the grafana/alloy row), docs/runbooks/log-shipping-and-backstop-alerts.md (the "No-logs-received fired" recovery section)</read_first>
  <action>
    Add `"verify:alloy-config": "node scripts/validate-alloy-config.mjs"` to package.json's scripts, adjacent to the existing `verify:prod-compose` entry.

    Add a step to the `static` job in .github/workflows/ci.yml, immediately after "Production compose invariants", named for OPS-10 and this gap, running `npm run verify:alloy-config` with `ALLOY_VALIDATE_REQUIRE_BINARY: "1"` in the step's `env:`. `static` is already a required status check under branch protection, so the gate becomes blocking with no repository-admin action — the same reasoning the Sentry redaction step recorded when it chose this job. Write a comment above the step explaining why the variable is there: the ubuntu runner has a Docker daemon, so the real parser runs here; without the variable a runner that lost Docker would degrade to the static scan alone and the gate would quietly stop doing the thing it exists for. Note in the comment that this step pulls the pinned vendor image on a cold runner.

    Add the wiring lock to `scripts/__tests__/validate-alloy-config.test.mjs`: assert package.json declares the `verify:alloy-config` script pointing at this gate, and assert .github/workflows/ci.yml's text contains both a step invoking that script and the require-binary variable. A future edit that moves the step or drops the variable then fails a test instead of silently reverting the gate to advisory — the vacuous-gate class this repo has hardened against before (the ignores-glob typo that made `eslint .` check nothing and exit 0).

    Update SPECIFICATION.md, in the file's own language, per .claude/CLAUDE.md's filing rule. In §7's plan-15-17 Alloy paragraph, append that this file's syntax is now machine-checked by `scripts/validate-alloy-config.mjs` (`npm run verify:alloy-config`, blocking in CI's `static` job): a static comment-token scan plus a parse by the real pinned binary in a network-less container mounting only this one file read-only, with the image reference read from docker-compose.prod.yml rather than restated — and that this closes the hole 15-17 flagged, where no real Alloy binary ran against the file anywhere in the repository. Extend the §8 grafana/alloy row to note the image is now also used by that gate, not only by the prod sidecar. No new environment variable enters docker/prod.env.example (`ALLOY_VALIDATE_REQUIRE_BINARY` is a CI step variable, never an application or operator secret), so §3 needs no change and the env-coverage gate is unaffected.

    Update docs/runbooks/log-shipping-and-backstop-alerts.md's "No-logs-received fired" recovery section: add the sidecar failing to load its configuration as an explicit first thing to check, with the observable symptom (the container restarting rather than running, and Alloy reporting an illegal character with a line and column at startup), the one-line reproduction against the real image, and the pointer to `npm run verify:alloy-config` as the pre-deploy check. Record that this exact failure produced a total log-delivery outage with a green-looking deploy, and that the dead-man's-switch rule is the alert expected to catch it. Keep it inside the existing section structure — do not restructure the runbook.
  </action>
  <verify>
    <automated>npm run verify:alloy-config && npx vitest run --root scripts __tests__/validate-alloy-config.test.mjs && npm run lint && npm run check:spec-env-coverage</automated>
  </verify>
  <done>`npm run verify:alloy-config` exits 0 and prints the compose-resolved image reference plus a ran-the-binary line. The CI `static` job carries the step with the require-binary variable, and the wiring assertions in the test file pass. SPECIFICATION.md §7/§8 and the log-shipping runbook describe the gate as built; `check:spec-env-coverage` and lint still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → production host | A configuration file committed here is loaded verbatim by a long-running production container; a parse error there is invisible to every check that runs here. |
| CI runner → container runtime | The gate starts a third-party vendor container on the runner and mounts a repository file into it. |
| gate → docker-compose.prod.yml | The image the gate validates against is derived from the compose file; a drift between them makes the gate's verdict meaningless. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-68 | Denial of Service | docker/alloy/config.alloy loaded by the prod `alloy` sidecar | critical | mitigate | The realized gap: an unparseable config restart-loops the sidecar under `restart: unless-stopped`, producing a total log-delivery outage behind a green deploy, with both backstop alert rules reading an empty stream. Task 2 corrects the token; Task 1's gate plus Task 3's blocking CI step make a reintroduction fail before merge, parsed by the same binary version production loads. |
| T-15-69 | Information Disclosure | the validation container started by the gate | medium | mitigate | The gate runs the vendor image with `--network none`, a single read-only bind mount of config.alloy, no env passthrough and no docker-socket mount — strictly less capability than the production sidecar (which does need the socket, T-15-59). `alloy fmt` parses without evaluating, so the config's three `env()` calls resolve nothing and no Grafana credential is ever present in the gate's environment. |
| T-15-70 | Tampering | the image reference the gate validates against | medium | mitigate | Read at run time from docker-compose.prod.yml's `alloy` service via validate-prod-compose.mjs's own resolver, never restated in the gate; a mutable tag on that service is already a hard failure of `verify:prod-compose` (FIRST_PARTY_IMAGE_SERVICES includes `alloy`), and the gate re-asserts non-mutability itself. |
| T-15-71 | Repudiation | the CI `static` job's alloy-config step | high | mitigate | A gate that skips silently reports success while checking nothing. `ALLOY_VALIDATE_REQUIRE_BINARY` on the CI step turns an unreachable Docker daemon into a violation, and a subprocess test plus a workflow-text assertion fail if the variable or the step is ever dropped. |
| T-15-72 | Denial of Service | CI feedback loop | low | accept | The step pulls a roughly 157MB vendor image on a cold runner, adding time to a required check and creating a dependency on registry availability for merges. Accepted: the failure it prevents is a silent total-outage class, and the pull is cached on warm runners. |
| T-15-SC | Tampering | npm/pip/cargo installs | high | accept | This plan performs no package-manager install and adds no dependency — `scripts/validate-alloy-config.mjs` is Node built-ins only, reusing an existing first-party module. The package-legitimacy gate is not in play; if any task is found to need a new package, stop and route it through the phase's blocking legitimacy checkpoint instead. |
</threat_model>

<verification>
Gap G-15-4 is closed when all of the following hold together:

1. `docker run --rm --network none -v "$PWD/docker/alloy/config.alloy:/etc/alloy/config.alloy:ro" grafana/alloy:v1.18.1 fmt /etc/alloy/config.alloy` exits 0 — the same invocation that reproduced `illegal character U+0023 '#'` at 1:1 on the pre-fix file.
2. `npm run verify:alloy-config` exits 0 and reports that the binary layer actually ran, against the image reference resolved from docker-compose.prod.yml.
3. `npx vitest run --root scripts __tests__/validate-alloy-config.test.mjs` passes, including the header fixture that still reproduces the shipped defect at 1:1 and the regression lock over the real file.
4. `.github/workflows/ci.yml`'s `static` job runs the gate with the require-binary variable set, asserted by test rather than by reading the file.
5. `npm run verify:prod-compose`, `npm run check:spec-env-coverage`, `npm run check:runbook-coverage` and `npm run lint` are unaffected.

<human-check>
Operator-side, at the next production deploy: redeploy the prod compose stack with the committed configuration and confirm the `alloy` container reaches and stays in a running state (not restarting), and that log lines continue to arrive in Loki with the `service`/`container`/`level` labels. The operator already confirmed end-to-end shipping during UAT using a corrected configuration and provisioned both alert rules; this only confirms the committed file is that same working configuration.
</human-check>
</verification>

<success_criteria>
- The production Alloy sidecar's configuration parses under the exact pinned image production runs, proven by the real binary rather than by inspection.
- The defect class that shipped is caught before merge by a required CI check, and the check cannot degrade to a silent pass on a runner without Docker.
- The gate validates against the deployed image by construction, and false-positives on neither legal comment text nor quoted values naming the rejected character.
- docker-compose.prod.yml is unchanged; the config diff is comment-token substitutions plus one explanatory paragraph.
- SPECIFICATION.md and the log-shipping runbook describe the gate and the failure signature as built.
</success_criteria>

<output>
Create `.planning/phases/15-observability-alerting-frontend-resilience/15-22-SUMMARY.md` when done.

Record explicitly: the Task 1 RED observation (the gate's exit-1 output against the pre-fix file), the exact count of comment lines converted in Task 2, and the fact that the live Loki/alert-rule half of G-15-4's truth was confirmed by the operator during UAT rather than by an automated check in this repository.
</output>
