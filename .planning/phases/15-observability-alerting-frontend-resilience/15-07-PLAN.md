---
phase: 15-observability-alerting-frontend-resilience
plan: 07
type: execute
wave: 4
depends_on: ["15-05"]
files_modified:
  - apps/web/src/features/campaigns/CampaignsListPage.tsx
  - apps/web/src/features/campaigns/CampaignDetailPage.tsx
  - apps/web/src/features/campaigns/CampaignProgress.tsx
  - apps/web/src/features/campaigns/SendSettingsPage.tsx
  - apps/web/src/features/campaigns/TemplateSenderPickers.tsx
  - apps/web/src/features/flows/list/FlowsListPage.tsx
  - apps/web/src/features/flows/detail/FlowDetailPage.tsx
  - apps/web/src/features/dashboard/WorkspaceDashboard.tsx
  - apps/web/src/features/team/TeamPage.tsx
  - apps/web/src/features/api-keys/ApiKeysSettings.tsx
  - apps/web/src/features/sendgrid-key/SendGridKeySettings.tsx
  - apps/web/src/features/workspace-home/WorkspaceHome.tsx
autonomous: true
requirements: [OPS-17]

must_haves:
  truths:
    - "Every remaining useQuery-backed region on the campaigns, flows, dashboard, team and settings surfaces renders a distinct inline error state with a working Retry when its fetch fails."
    - "Every list on these surfaces renders an explicit empty state when a successful fetch returns zero rows, textually distinct from its error state."
    - "A failed chart or widget query on the dashboard leaves the rest of the dashboard rendered — no single region's failure blanks the page."
    - "No data-load failure on these surfaces is reported solely by a toast."
    - "Every converted region uses the shared QueryErrorState/EmptyState components rather than page-local error markup."
  artifacts:
    - apps/web/src/features/campaigns/CampaignsListPage.tsx
    - apps/web/src/features/flows/list/FlowsListPage.tsx
    - apps/web/src/features/dashboard/WorkspaceDashboard.tsx
  key_links:
    - "This plan consumes the components plan 15-05 built; it introduces no new shared component so the two OPS-17 plans cannot drift into two error idioms."
  prohibitions:
    - statement: "A toast must never be the only record of a data-load failure — toasts stay reserved for action failures (save, delete) per D-11."
      status: kept
      verification: flagged-unverified
    - statement: "No page-local error component may be introduced; the shared components from plan 15-05 are the only error/empty idiom."
      status: kept
      verification: flagged-unverified
    - statement: "No query key, request URL or API contract may change in this plan — it is presentational only."
      status: kept
      verification: flagged-unverified
---

<objective>
Finish OPS-17: honest failure and emptiness across the campaigns, flows, dashboard, team and settings surfaces.

Purpose: plan 15-05 built the shared inline error/empty components and converted the contacts, segments and send-log surfaces. The remaining surfaces carry the same defect — a failed fetch renders as an empty widget indistinguishable from "nothing here yet". The dashboard is the worst case, because several independent widgets share one page and one failure currently takes the page's meaning with it.
Output: every remaining data region on these surfaces branching pending → error → empty → data through the shared components.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-CONTEXT.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-PATTERNS.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the campaigns surface</name>
  <files>apps/web/src/features/campaigns/CampaignsListPage.tsx, apps/web/src/features/campaigns/CampaignDetailPage.tsx, apps/web/src/features/campaigns/CampaignProgress.tsx, apps/web/src/features/campaigns/SendSettingsPage.tsx, apps/web/src/features/campaigns/TemplateSenderPickers.tsx</files>
  <read_first>
    - apps/web/src/components/QueryErrorState.tsx and apps/web/src/components/EmptyState.tsx (built by plan 15-05 — the exact props contract)
    - apps/web/src/features/contacts/ContactsListPage.tsx (as converted by plan 15-05 — the branch order and stale-data-with-banner behaviour to replicate)
    - apps/web/src/features/campaigns/CampaignsListPage.tsx, CampaignDetailPage.tsx, CampaignProgress.tsx, SendSettingsPage.tsx, TemplateSenderPickers.tsx
  </read_first>
  <action>
    Apply the same branch order plan 15-05 established (pending skeleton → `QueryErrorState` wired to that query's own `refetch`/`isFetching` → `EmptyState` on a zero-row success → data) to every `useQuery` region on these five files.
    `CampaignProgress` polls while a campaign is sending: a transient poll failure must not wipe the last known progress. Keep the last successful values rendered and surface the failure as a contained banner above them, matching the stale-data behaviour plan 15-05 established for `ContactsListPage`.
    `TemplateSenderPickers` fetches from the tenant's SendGrid account: when that fetch fails the picker must say the template/sender list could not be loaded and offer Retry — never render as an empty picker, which reads as "this account has no templates" and has previously been a real support question.
    Leave mutation error handling (launch, schedule, save) on toasts where it already is — D-11 keeps toasts for action failures. Do not convert those.
  </action>
  <verify>
    <automated>npx vitest run --root apps/web && npm run build -w apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'QueryErrorState' apps/web/src/features/campaigns/CampaignsListPage.tsx apps/web/src/features/campaigns/CampaignDetailPage.tsx apps/web/src/features/campaigns/CampaignProgress.tsx apps/web/src/features/campaigns/SendSettingsPage.tsx apps/web/src/features/campaigns/TemplateSenderPickers.tsx` returns at least 1 for each file.
    - `grep -c 'EmptyState' apps/web/src/features/campaigns/CampaignsListPage.tsx` returns at least 1.
    - The set of `queryKey` values across these five files is unchanged from HEAD.
    - `npm run build -w apps/web` exits 0 and `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Every campaigns data region distinguishes failure from emptiness, and a polling failure never erases known progress.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the flows and dashboard surfaces</name>
  <files>apps/web/src/features/flows/list/FlowsListPage.tsx, apps/web/src/features/flows/detail/FlowDetailPage.tsx, apps/web/src/features/dashboard/WorkspaceDashboard.tsx</files>
  <read_first>
    - apps/web/src/features/flows/list/FlowsListPage.tsx and apps/web/src/features/flows/detail/FlowDetailPage.tsx
    - apps/web/src/features/dashboard/WorkspaceDashboard.tsx (each widget's own query — note which regions are independent so each gets its own error state)
    - apps/web/src/components/QueryErrorState.tsx and EmptyState.tsx
  </read_first>
  <action>
    Apply the same branch order to the flows list, the flow detail page's own queries, and every independent widget on the workspace dashboard.
    The dashboard is the case D-11 was written for: each widget (trend chart, counters, mini-lists) owns its own inline error state, so one failing widget shows a contained error while the rest of the page continues to render real data. Do not introduce a single page-level error branch that replaces the whole dashboard.
    `FlowDetailPage` loads the flow definition the canvas renders. If that load fails the canvas must not mount over missing data — show the error state in place of the canvas with Retry, so the editor never opens on a partially-loaded flow. Do not touch the canvas's own dirty-state or save handling; plan 15-09 owns that file's unsaved-changes behaviour and must not collide with this change.
  </action>
  <verify>
    <automated>npx vitest run --root apps/web && npm run build -w apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'QueryErrorState' apps/web/src/features/flows/list/FlowsListPage.tsx apps/web/src/features/flows/detail/FlowDetailPage.tsx apps/web/src/features/dashboard/WorkspaceDashboard.tsx` returns at least 1 for each file, and at least 2 for `WorkspaceDashboard.tsx`.
    - `grep -c 'EmptyState' apps/web/src/features/flows/list/FlowsListPage.tsx` returns at least 1.
    - `WorkspaceDashboard.tsx` contains no single early return that replaces the whole page on any one query's error.
    - `npm run build -w apps/web` exits 0 and `npm run lint` exits 0.
  </acceptance_criteria>
  <done>A failing dashboard widget no longer takes the page with it, and the flow canvas never opens on data that failed to load.</done>
</task>

<task type="auto">
  <name>Task 3: Convert the team, settings and workspace-home surfaces</name>
  <files>apps/web/src/features/team/TeamPage.tsx, apps/web/src/features/api-keys/ApiKeysSettings.tsx, apps/web/src/features/sendgrid-key/SendGridKeySettings.tsx, apps/web/src/features/workspace-home/WorkspaceHome.tsx</files>
  <read_first>
    - apps/web/src/features/team/TeamPage.tsx, apps/web/src/features/api-keys/ApiKeysSettings.tsx, apps/web/src/features/sendgrid-key/SendGridKeySettings.tsx, apps/web/src/features/workspace-home/WorkspaceHome.tsx
    - apps/web/src/components/QueryErrorState.tsx and EmptyState.tsx
  </read_first>
  <action>
    Apply the same branch order to every `useQuery` region on these four files.
    `SendGridKeySettings` is the highest-consequence case here: a failed status fetch must never render as "no key configured", because that reads as an instruction to paste a key that is already stored. Its error state must say the key status could not be loaded and offer Retry.
    `ApiKeysSettings`'s list and `TeamPage`'s member/invite lists each get an explicit empty state distinct from their error state.
    Finish by sweeping `apps/web/src/features` for any remaining `useQuery` call whose consumer has no error branch, and record any deliberately-skipped site (with the reason) in the summary rather than leaving it silently unconverted.
  </action>
  <verify>
    <automated>npx vitest run --root apps/web && npm run build -w apps/web && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'QueryErrorState' apps/web/src/features/team/TeamPage.tsx apps/web/src/features/api-keys/ApiKeysSettings.tsx apps/web/src/features/sendgrid-key/SendGridKeySettings.tsx apps/web/src/features/workspace-home/WorkspaceHome.tsx` returns at least 1 for each file.
    - The summary lists every `useQuery` site under `apps/web/src/features` that remains without an error branch, with a reason for each.
    - `npm run build -w apps/web` exits 0 and `npm run lint` exits 0.
  </acceptance_criteria>
  <done>OPS-17's inline half is complete across the app, with any remaining gap named rather than hidden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → browser | Error responses are rendered into operator-facing UI on settings pages that display security-relevant state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-19 | Repudiation | `SendGridKeySettings` rendering a failed status fetch as "no key configured" | high | mitigate | Explicit error branch with distinct copy and Retry; the empty state is reachable only from a successful zero-result response |
| T-15-20 | Information Disclosure | rendering raw API error bodies on settings pages | medium | mitigate | Shared `QueryErrorState` renders a fixed message plus at most a short status-derived detail, never a raw server body |
| T-15-21 | Repudiation | dashboard widget failure presented as zero activity | high | mitigate | Per-widget error regions; no page-level early return |
| T-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs in this plan; all dependency additions are gated by plan 15-01's blocking human legitimacy checkpoint |
</threat_model>

<verification>
- `npx vitest run --root apps/web` exits 0.
- `npm run build -w apps/web` exits 0 and `npm run lint` exits 0.
- Manual: with the API stopped, every converted region shows its own error state and the page shell stays usable.
</verification>

<success_criteria>
OPS-17 is true across the app: failed API calls, empty states and paginated lists each show the user what is actually true.
</success_criteria>

## Artifacts this phase produces

New symbols/paths introduced by THIS plan: none — this plan consumes `QueryErrorState` and `EmptyState` from plan 15-05 and adds no new exported symbol or file.

## Flagged assumptions (edge-probe fallback)

- **OPS-17 (unclassified, unresolved)** — RESOLVED into `must_haves.truths` for the remaining surfaces. The residual edge — `useQuery` sites reached only through code paths this sweep cannot statically enumerate — is handled by requiring the summary to name every unconverted site with a reason, so nothing is silently dropped.

<output>
Create `.planning/phases/15-observability-alerting-frontend-resilience/15-07-SUMMARY.md` when done
</output>
