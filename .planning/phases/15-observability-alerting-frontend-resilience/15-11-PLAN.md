---
phase: 15-observability-alerting-frontend-resilience
plan: 11
type: execute
wave: 7
depends_on: ["15-06", "15-03", "15-10"]
files_modified:
  - apps/web/src/lib/sentry.ts
  - apps/web/src/components/RouteErrorBoundary.tsx
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/lib/__tests__/sentry.test.ts
  - docker/Dockerfile.web
  - .github/workflows/images.yml
  - SPECIFICATION.md
autonomous: true
requirements: [OPS-08, OPS-17]

user_setup:
  - service: sentry
    why: "Frontend project DSN (D-06). Build-time value, not a secret, not routed through MEGA_CRM_ENV_FILE."
    env_vars:
      - name: VITE_SENTRY_DSN
        source: "Sentry (EU) -> mega-crm-web -> Client Keys (DSN); supplied to the web image build as a build argument"

must_haves:
  truths:
    - "A render error inside any feature route is caught by a route-level error boundary that renders a contained error panel while the workspace shell and navigation stay rendered and usable."
    - "The caught render error is reported to the web Sentry project, tagged with the route and the workspace, and scrubbed by the shared beforeSend."
    - "Session Replay is not enabled and browser performance tracing is not enabled in the web SDK initialization."
    - "With no build-time DSN present, the app builds and runs normally and the SDK stays uninitialized."
    - "The error panel offers a recovery path (reload the route / go back) rather than being a dead end."
    - "The frontend DSN is supplied as an image build argument and never read from MEGA_CRM_ENV_FILE."
  artifacts:
    - apps/web/src/lib/sentry.ts
    - apps/web/src/components/RouteErrorBoundary.tsx
    - apps/web/src/lib/__tests__/sentry.test.ts
  key_links:
    - "Sentry's own ErrorBoundary component is used rather than a hand-rolled componentDidCatch class — the catch/report/fallback wiring is atomic and already tested upstream."
    - "The boundary wraps route elements inside the /w/:slug shell so the shell survives; wrapping the shell itself would defeat D-11's contained-failure requirement."
  prohibitions:
    - statement: "Session Replay must stay off (D-08) — it records tenant screens containing contact emails and segment data, and no masking story has been tested."
      status: kept
      verification: flagged-unverified
    - statement: "Browser performance tracing must stay off (D-08)."
      status: kept
      verification: flagged-unverified
    - statement: "No hand-rolled error boundary class may be introduced when Sentry's own ErrorBoundary covers the case."
      status: kept
      verification: flagged-unverified
    - statement: "VITE_SENTRY_DSN must not be added to MEGA_CRM_ENV_FILE or to docker/prod.env.example — it is a build-time, non-secret value baked into the static bundle."
      status: kept
      verification: flagged-unverified
---

<objective>
OPS-08 (frontend half) and the boundary half of OPS-17/D-11: render errors are caught, contained and reported.

Purpose: `apps/web` has no error boundary anywhere today, so any render-time exception blanks the whole app. D-11 wants a layered story — plans 15-05 and 15-07 built the inline half for failed fetches; this plan adds the route-level boundary for unexpected render errors, and the web Sentry project that receives them.
Output: web SDK initialization (errors only), a route error boundary, and a build-time DSN pipeline.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-CONTEXT.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-RESEARCH.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-PATTERNS.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-06-SUMMARY.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-10-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initialize the web Sentry SDK — errors only</name>
  <files>apps/web/src/lib/sentry.ts, apps/web/src/main.tsx, apps/web/src/lib/__tests__/sentry.test.ts</files>
  <read_first>
    - apps/web/src/main.tsx (the render entry point — initialization belongs before the first render)
    - apps/api/src/sentry.ts (plan 15-10's module — mirror its structure and its no-DSN behaviour)
    - packages/redaction/src/sentry-scrub.ts (`sentryBeforeSend`; confirm `apps/web` can import `@mega-crm/redaction` — check its package dependencies and add the workspace dependency if it is absent, without adding any Sentry runtime dependency to the redaction package)
    - apps/web/src/lib/ (the existing lib module conventions: `api.ts`, `authClient.ts`, `queryClient.ts`)
  </read_first>
  <behavior>
    - With a DSN present, initialization configures beforeSend as the shared scrub hook.
    - With no DSN present, initialization is a no-op and does not throw.
    - The initialization options contain no replay integration and no browser tracing integration.
    - The initialization options set a zero tracing sample rate and a zero replay sample rate.
    - Captured events carry the current route path and the current workspace slug as tags when available.
  </behavior>
  <action>
    Create `apps/web/src/lib/sentry.ts` exporting an initialization function that reads the build-time DSN from Vite's env, sets `environment` and `release` from build-time values, wires `beforeSend` to `packages/redaction`'s `sentryBeforeSend`, and enables no integrations beyond the SDK's error defaults.
    Session Replay and browser tracing are both explicitly off (D-08). Do not add the replay integration "disabled" — do not include it at all, and set the corresponding sample rates to zero so a future default flip cannot silently enable it. Record the reason in the module header: replay records tenant screens containing contact emails and segment data, which is exactly the channel OPS-09 exists to close, and no masking story has been tested.
    Tag events with the current route and the current workspace slug. Read those from the router/location at capture time through a global event processor rather than passing them at each capture site.
    Call the initialization in `apps/web/src/main.tsx` before the root render.
    Test the initialization options object directly (export it or the builder that produces it) so the absence of replay/tracing is a source-independent assertion rather than a code review.
  </action>
  <verify>
    <automated>npx vitest run --root apps/web src/lib/__tests__/sentry.test.ts && npm run build -w apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/sentry.ts` exists and references `sentryBeforeSend`.
    - A test case asserts the built initialization options contain no integration whose name matches replay or browser tracing.
    - A test case asserts both the tracing sample rate and the replay sample rates in the options are 0.
    - A test case asserts initialization with an empty DSN does not throw.
    - `npm run build -w apps/web` exits 0.
  </acceptance_criteria>
  <done>The frontend reports exceptions and nothing else, with the PII-bearing SDK features structurally absent rather than merely turned off.</done>
  <reversibility rating="costly">Events sent to a live DSN cannot be retracted. The one-way decision itself — whether to point any SDK at a live Sentry project at all — was taken at plan 15-10's decision checkpoint and covers all three projects; this plan executes under it. Enabling Session Replay later would additionally require a tested masking story, which is why it is absent rather than configured-off.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Route-level error boundary with a contained fallback</name>
  <files>apps/web/src/components/RouteErrorBoundary.tsx, apps/web/src/App.tsx</files>
  <read_first>
    - apps/web/src/App.tsx (as migrated by plan 15-03 — the `createRoutesFromElements` tree, the lazy route elements and the nested `/w/:slug` shell route)
    - apps/web/src/features/app-shell/AppShell.tsx (what must keep rendering when a child route fails)
    - apps/web/src/components/QueryErrorState.tsx (plan 15-05 — the fallback panel should read as its sibling, not a fourth error language)
    - .planning/phases/15-observability-alerting-frontend-resilience/15-RESEARCH.md § Don't Hand-Roll (use `Sentry.ErrorBoundary`, not a custom class)
  </read_first>
  <action>
    Create `apps/web/src/components/RouteErrorBoundary.tsx` wrapping `@sentry/react`'s own `ErrorBoundary` with this app's fallback panel. Do not hand-roll a `componentDidCatch` class — the catch/report/fallback wiring is exactly what the SDK component provides, and a hand-rolled version duplicates logic that is already tested upstream.
    The fallback is a contained panel, not a full-page takeover: it renders inside the route outlet so `AppShell`'s navigation and workspace switcher stay usable. It offers a recovery path — retry the route render and a link back to the workspace home — so the user is never stranded. Russian copy, consistent with plan 15-05's error component.
    Wrap the route elements inside the `/w/:slug` shell with the boundary, plus the standalone auth/onboarding routes. Reset the boundary on location change so navigating away from a broken route clears it. Do NOT wrap the shell route itself with the same boundary — that would take the navigation down with the failing child and defeat D-11's contained-failure requirement.
    Combine cleanly with plan 15-03's Suspense: the boundary must be outside the Suspense boundary so a failed lazy chunk import surfaces in the error panel rather than hanging on the skeleton forever.
  </action>
  <verify>
    <automated>npx vitest run --root apps/web && npm run build -w apps/web && npm run check:web-chunks</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/RouteErrorBoundary.tsx` exists and uses `@sentry/react`'s `ErrorBoundary`.
    - `grep -c 'class .* extends .*Component' apps/web/src/components/RouteErrorBoundary.tsx` returns 0.
    - `apps/web/src/App.tsx` wraps route elements with the boundary and does not wrap the `/w/:slug` shell element itself with it.
    - In `App.tsx`, the boundary encloses the Suspense boundary (not the other way round).
    - `npm run build -w apps/web` exits 0 and `npm run check:web-chunks` exits 0.
  </acceptance_criteria>
  <done>One failing feature route shows a contained, recoverable error panel while the rest of the app keeps working.</done>
</task>

<task type="auto">
  <name>Task 3: Build-time DSN pipeline for the web image</name>
  <files>docker/Dockerfile.web, .github/workflows/images.yml, SPECIFICATION.md</files>
  <read_first>
    - docker/Dockerfile.web (its build stages and any existing build arguments)
    - .github/workflows/images.yml (the matrix that builds api/web/worker images and pushes to GHCR; how per-app build inputs are supplied)
    - scripts/check-spec-env-coverage.mjs (the one-directional gate — this variable must NOT be added to `docker/prod.env.example`, so it must not become a required coverage name)
    - SPECIFICATION.md section 3 (plan 15-10 already recorded the build-time/not-a-secret rule for this variable — this task fulfils it)
  </read_first>
  <action>
    Add a build argument for the frontend DSN to `docker/Dockerfile.web`, exposed to the Vite build step so the value is baked into the static bundle. `apps/web` is served as static assets by Caddy with no runtime environment injection point, so build time is the only place this can be supplied.
    Pass the value from the image workflow for the web app only. A Sentry DSN authorizes sending events to a project, not reading from it — it is not a secret and must not be routed through `MEGA_CRM_ENV_FILE` or `docker/prod.env.example`. A missing value must produce a working build with the SDK uninitialized, never a build failure, so that a fork or a local build without the value still succeeds.
    Update `SPECIFICATION.md` section 2 (the `@sentry/react` dependency is now actually used, not merely declared), section 3 (confirm the build-time entry recorded by plan 15-10) and section 7 (the frontend's error-only posture, the route boundary, and the explicit absence of replay and browser tracing).
  </action>
  <verify>
    <automated>npm run check:spec-env-coverage && docker build -f docker/Dockerfile.web -t mega-crm-web:planverify . && docker image rm mega-crm-web:planverify</automated>
  </verify>
  <acceptance_criteria>
    - `docker/Dockerfile.web` declares a build argument for the frontend DSN and makes it visible to the Vite build step.
    - `grep -c 'VITE_SENTRY_DSN' docker/prod.env.example` returns 0.
    - `.github/workflows/images.yml` supplies the build argument for the web matrix entry only.
    - A build with no value supplied succeeds.
    - `npm run check:spec-env-coverage` exits 0.
    - `SPECIFICATION.md` sections 2, 3 and 7 all reflect the frontend Sentry surface.
  </acceptance_criteria>
  <done>The frontend DSN reaches the bundle through the build, is documented as deliberately not a secret, and its absence never breaks a build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Sentry SaaS (EU) | Frontend error events leave the user's browser directly to a third party |
| CI build → published image | A build argument is baked into a publicly-served static bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-34 | Information Disclosure | Session Replay recording tenant screens (contact emails, segment data) | critical | mitigate | The replay integration is structurally absent from the initialization, not configured off, and the sample rates are asserted zero by test (D-08) |
| T-15-35 | Information Disclosure | React component props/state captured in a render-error event | high | mitigate | `beforeSend` routes the whole event through the shared `scrub()`, the same gate plan 15-06 made blocking |
| T-15-36 | Information Disclosure | a genuine secret accidentally supplied through the same build-argument channel | medium | mitigate | SPECIFICATION section 3 records explicitly that this channel is for the non-secret DSN only and that secrets travel through `MEGA_CRM_ENV_FILE`; the DSN is deliberately kept out of `prod.env.example` |
| T-15-37 | Denial of Service | an unbounded error loop in a route re-mounting and re-reporting | medium | accept | The boundary renders a static fallback rather than re-rendering the failing subtree; SDK-side rate limiting bounds the report volume |
| T-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs in this plan; all dependency additions are gated by plan 15-01's blocking human legitimacy checkpoint |
</threat_model>

<verification>
- `npx vitest run --root apps/web` exits 0.
- `npm run build -w apps/web`, `npm run check:web-chunks`, `npm run check:spec-env-coverage`, `npm run lint` all exit 0.
- Manual: throwing inside a feature route shows the contained panel with the shell still rendered.
</verification>

<success_criteria>
Phase success criterion 2's frontend half is true, and D-11's boundary layer is in place: an exception from the frontend reaches Sentry, and one failing route never blanks the app.
</success_criteria>

## Artifacts this phase produces

New symbols/paths introduced by THIS plan: the initialization export and options builder of the new file `apps/web/src/lib/sentry.ts`; `RouteErrorBoundary` (new file `apps/web/src/components/RouteErrorBoundary.tsx`); new test file `apps/web/src/lib/__tests__/sentry.test.ts`; a new build argument in `docker/Dockerfile.web` and its matching workflow input.

## Flagged assumptions (edge-probe fallback)

- **OPS-08 (concurrency, unresolved)** — resolved for the backend in plan 15-10; the frontend half adds no concurrency surface (one SDK per browser tab, no shared state).
- **OPS-17 (unclassified, unresolved)** — RESOLVED into `must_haves.truths` for the boundary layer: a render error is contained rather than blanking the app, and the fallback offers recovery.

<output>
Create `.planning/phases/15-observability-alerting-frontend-resilience/15-11-SUMMARY.md` when done
</output>
