---
phase: 15-observability-alerting-frontend-resilience
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/components/RouteSuspenseFallback.tsx
  - apps/web/vite.config.ts
  - scripts/check-web-chunks.mjs
  - package.json
autonomous: true
requirements: [OPS-16]

must_haves:
  truths:
    - "A production build of apps/web emits a distinct chunk file containing @xyflow/react and a distinct chunk file containing recharts, and neither is referenced by the entry HTML's initial script tags."
    - "Every feature route element in App.tsx is loaded through React.lazy behind a Suspense fallback; opening a route that has not been visited fetches its chunk on demand."
    - "The application still renders and navigates identically after the switch from BrowserRouter to a data router — the same route paths, the same nested /w/:slug shell, the same RootRedirect behaviour."
    - "npm run check:web-chunks exits non-zero if the canvas or charts vendor chunk stops being a separate file."
    - statement: "A chunk that fails to load (network error during lazy import) surfaces as an error rather than a blank route."
      verification: backstop
  artifacts:
    - apps/web/src/App.tsx
    - apps/web/src/components/RouteSuspenseFallback.tsx
    - apps/web/vite.config.ts
    - scripts/check-web-chunks.mjs
  key_links:
    - "The data-router migration (createBrowserRouter + RouterProvider) is the hard prerequisite for plan 15-09's useBlocker; useBlocker throws under the declarative <BrowserRouter> tree."
    - "vite build manifest is what makes the chunk-boundary assertion checkable in CI rather than eyeballed."
  prohibitions:
    - statement: "The object form of Rollup's manualChunks must not be used — Vite 8's default Rolldown bundler does not support it and will silently no-op or error (RESEARCH.md Pitfall 2)."
      status: kept
      verification: flagged-unverified
    - statement: "No <Route> element's path, nesting or element component may change during the router migration — this is a shape change only."
      status: kept
      verification: flagged-unverified
    - statement: "No per-route eager/lazy judgement calls — D-14 locks a uniform lazy pattern for every feature route."
      status: kept
      verification: flagged-unverified
---

<objective>
Route-level code splitting (OPS-16, D-14) plus the data-router migration every later frontend plan depends on.

Purpose: the canvas editor (`@xyflow/react`) and the dashboard charts (`recharts`) are the two heaviest vendor bundles in `apps/web`, and every route is eagerly imported today. Splitting them is OPS-16's requirement; migrating `App.tsx` from `<BrowserRouter>` to a data router is the prerequisite RESEARCH.md Pitfall 1 identifies for plan 15-09's `useBlocker`, and doing both in one pass touches `App.tsx` once instead of twice.
Output: a data-router `App.tsx` with lazily-loaded feature routes, pinned vendor chunk groups in `vite.config.ts`, and a CI-checkable chunk-boundary assertion.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-CONTEXT.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-RESEARCH.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate App.tsx to a data router with lazily-loaded feature routes</name>
  <files>apps/web/src/App.tsx, apps/web/src/components/RouteSuspenseFallback.tsx</files>
  <read_first>
    - apps/web/src/App.tsx (full file — the 18 eager feature imports, the `RootRedirect` component and the exact 21-route tree including the nested `/w/:slug` shell)
    - apps/web/src/features/app-shell/AppShell.tsx (the shell that must keep rendering around nested routes)
    - apps/web/src/features/contacts/ContactsListPage.tsx (the Card/Skeleton composition idiom the Suspense fallback should match)
    - .planning/phases/15-observability-alerting-frontend-resilience/15-RESEARCH.md § Pattern 2 and § Pitfall 1
  </read_first>
  <action>
    Convert `App.tsx` to the data-router form using the minimal-diff path from RESEARCH.md Pattern 2: build the route tree with `createRoutesFromElements` wrapping the EXACT same `<Route>` JSX that exists today, pass it to `createBrowserRouter`, and render `<RouterProvider router={router} />` in place of `<BrowserRouter><Routes>…</Routes></BrowserRouter>`. Keep `QueryClientProvider` and `<Toaster />` exactly where they are. No route path, no nesting relationship and no element component changes — this is a shape change only. All imports continue to come from the `react-router` package (this repo has no `react-router-dom` dependency).
    Convert every feature/route page import to `React.lazy(() => import(...))`, uniformly, with no per-route exemptions (D-14). `RootRedirect`, `AppShell` and the `queryClient`/`useSession`/`apiGet` imports stay eager — they are shell, not feature code. Wrap the lazily-loaded elements in `<Suspense>` with a route-level fallback.
    Create `apps/web/src/components/RouteSuspenseFallback.tsx` exporting a single fallback component built from the existing Card/Skeleton primitives already used in the feature pages, so a route in flight looks like the page it is becoming rather than a blank screen. Keep it presentational — no data fetching, no router hooks.
  </action>
  <verify>
    <automated>npm run build -w apps/web && npx vitest run --root apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/App.tsx` references `createBrowserRouter`, `createRoutesFromElements` and `RouterProvider`.
    - No JSX element `<BrowserRouter` and no `<Routes` element remains: `grep -cE '<(BrowserRouter|Routes)[ >]' apps/web/src/App.tsx` returns 0 (element form only — a prose mention of the old shape in a comment is not a usage).
    - `grep -c 'React.lazy\|lazy(' apps/web/src/App.tsx` returns at least 18.
    - The set of `path=` values in `apps/web/src/App.tsx` is identical to the set at HEAD (compare with `git show HEAD:apps/web/src/App.tsx`).
    - `apps/web/src/components/RouteSuspenseFallback.tsx` exists and exports a component.
    - `npm run build -w apps/web` exits 0.
  </acceptance_criteria>
  <done>The app runs on a data router with every feature route lazily loaded behind a skeleton fallback, and the route table is provably unchanged.</done>
  <reversibility rating="costly">The declarative-to-data router migration changes the router runtime every later frontend plan builds on (useBlocker, route error elements); reverting means rewriting those too.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Pin the heavy vendor chunks and assert the boundary in CI</name>
  <files>apps/web/vite.config.ts, scripts/check-web-chunks.mjs, package.json</files>
  <read_first>
    - apps/web/vite.config.ts (full file — 24 lines, no `build` block exists today)
    - apps/web/package.json (the `build` script and the installed vite major)
    - package.json (root script naming convention for `check:*` / `verify:*` gates)
    - scripts/validate-prod-compose.mjs (the established shape for a repo check script: plain node, explicit failure messages, non-zero exit)
    - .planning/phases/15-observability-alerting-frontend-resilience/15-RESEARCH.md § Pattern 5 and § Pitfall 2
  </read_first>
  <action>
    Add a `build` block to `apps/web/vite.config.ts` that (a) enables the build manifest so the chunk graph is machine-readable, and (b) declares two vendor chunk groups pinning `@xyflow/react` and `recharts` into their own chunks. Use Rolldown's native `output.advancedChunks.groups` form with `name`/`test` regex entries (RESEARCH.md Pattern 5) — the classic object form of `manualChunks` is unsupported by Vite 8's default Rolldown bundler and either silently no-ops or errors (Pitfall 2). If the installed Vite major turns out to predate Rolldown, fall back to the FUNCTION form of `manualChunks`, never the object form, and record which form was used in the summary.
    Create `scripts/check-web-chunks.mjs`: read the build manifest produced by the previous step, assert that a chunk whose module list includes `@xyflow/react` exists and is distinct from the entry chunk, assert the same for `recharts`, and assert neither appears in the entry chunk's own initial import set. Exit non-zero with an explicit message naming the missing boundary. The script must fail loudly if the manifest file is absent rather than passing vacuously.
    Register it as a root `check:web-chunks` npm script following the existing `check:*` naming, and note in the script header that it requires a completed `npm run build -w apps/web`.
  </action>
  <verify>
    <automated>npm run build -w apps/web && npm run check:web-chunks</automated>
  </verify>
  <acceptance_criteria>
    - The object form is absent: `grep -v '^\s*[/*]' apps/web/vite.config.ts | grep -cE 'manualChunks\s*:\s*\{'` returns 0 (comment lines excluded — the file's explanatory comment about why the object form is unsupported is required and must not invalidate this check). Either the Rolldown `advancedChunks` groups form or a function-form `manualChunks(` is present.
    - `node scripts/check-web-chunks.mjs` exits 0 after a successful build.
    - Deleting the vendor group config and rebuilding makes `node scripts/check-web-chunks.mjs` exit non-zero (demonstrate once, then restore).
    - Running `node scripts/check-web-chunks.mjs` with no build output present exits non-zero.
    - Root `package.json` contains a `check:web-chunks` script.
  </acceptance_criteria>
  <done>The canvas and charts vendor chunks are separate files, and a CI-runnable check fails if that stops being true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN/static host → browser | Lazily-fetched chunks are loaded at runtime from the static asset host |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-08 | Denial of Service | lazy chunk fetch failure after a deploy replaces hashed asset filenames | medium | mitigate | Route-level Suspense plus (plan 15-11) a route error boundary so a failed dynamic import shows a contained, retryable error panel instead of a blank route |
| T-15-09 | Tampering | build-time chunk configuration silently not applied | low | mitigate | `scripts/check-web-chunks.mjs` asserts the boundary against the real build manifest and fails closed when the manifest is missing |
| T-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs in this plan; all dependency additions are gated by plan 15-01's blocking human legitimacy checkpoint |
</threat_model>

<verification>
- `npm run build -w apps/web` exits 0 and `npm run check:web-chunks` exits 0.
- `npx vitest run --root apps/web` exits 0.
- `npm run lint` exits 0.
- Manual: opening `/w/:slug` does not download the canvas chunk; navigating to `/w/:slug/flows/:id` does.
</verification>

<success_criteria>
Phase success criterion 4 is true: the app loads with route-level code splitting and canvas/heavy dashboard chunks arrive only when those routes are opened.
</success_criteria>

## Artifacts this phase produces

New symbols/paths introduced by THIS plan: `RouteSuspenseFallback` (new file `apps/web/src/components/RouteSuspenseFallback.tsx`); the module-level `router` constant in `apps/web/src/App.tsx`; new file `scripts/check-web-chunks.mjs`; new root npm script `check:web-chunks`; new `build` block with vendor chunk groups in `apps/web/vite.config.ts`.

## Flagged assumptions (edge-probe fallback)

- **OPS-16 (unclassified, unresolved)** — RESOLVED into `must_haves.truths`: the observable edge is the chunk boundary itself, asserted against the real build manifest by `check:web-chunks`. The remaining edge — what a user sees when a lazily-imported chunk fails to load — is carried as a `backstop` truth here and is actually closed by plan 15-11's route error boundary.

<output>
Create `.planning/phases/15-observability-alerting-frontend-resilience/15-03-SUMMARY.md` when done
</output>
