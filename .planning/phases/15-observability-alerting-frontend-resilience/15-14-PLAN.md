---
phase: 15-observability-alerting-frontend-resilience
plan: 14
type: execute
wave: 10
depends_on: ["15-13"]
files_modified:
  - apps/api/src/modules/ops/webhook-lag-watchdog.ts
  - apps/api/src/modules/ops/failed-send-share-watchdog.ts
  - apps/api/src/modules/ops/__tests__/webhook-lag-watchdog.test.ts
  - apps/api/src/modules/ops/__tests__/failed-send-share-watchdog.test.ts
  - apps/api/src/server.ts
  - SPECIFICATION.md
autonomous: true
requirements: [OPS-13]

must_haves:
  truths:
    - "When the newest ingested SendGrid webhook event is older than the configured lag and sends are outstanding awaiting evidence, the webhook-lag watchdog evaluates unhealthy."
    - "A quiet period with no outstanding sends awaiting evidence evaluates healthy regardless of how long ago the last webhook arrived."
    - "The failed-send-share watchdog computes failures over a rolling window as a share of terminal outcomes only; rate-limited deferrals and other non-terminal control-flow outcomes are excluded from both numerator and denominator."
    - "A window with too few sends to be statistically meaningful evaluates healthy rather than alerting on a one-of-two failure."
    - "All four OPS-13 watchdogs are started at API boot with independent dedup windows, each in the same process as the existing five watchdogs and a different process from the workers they watch."
    - "Alert bodies for both new watchdogs contain no workspace id, contact value or send id."
    - statement: "The failed-send-share denominator counts only sends whose outcome is terminal at evaluation time; sends still reconciling are excluded from both sides rather than counted as successes."
      verification: backstop
  artifacts:
    - apps/api/src/modules/ops/webhook-lag-watchdog.ts
    - apps/api/src/modules/ops/failed-send-share-watchdog.ts
  key_links:
    - "apps/api/src/server.ts is where all nine watchdogs are wired; its existing five-watchdog block is the pattern the four new ones join."
    - "The failed-send-share denominator depends on the same terminal/non-terminal distinction the processor wrapper's control-flow allowlist encodes — counting a rate-limit deferral as a failure would make routine backpressure look like an outage."
  prohibitions:
    - statement: "Rate-limited deferrals and other non-terminal control-flow outcomes must never be counted as failed sends — doing so turns routine backpressure into a false outage alert."
      status: kept
      verification: flagged-unverified
    - statement: "The webhook-lag watchdog must not duplicate the existing ingestion-health watchdog's alert on the same condition; the two must be shown to answer different questions."
      status: kept
      verification: flagged-unverified
    - statement: "No alert body may contain a workspace id, contact email, phone or send id."
      status: kept
      verification: flagged-unverified
    - statement: "No new migration may be added; ops_alert_state from plan 15-12 is the only storage these use."
      status: kept
      verification: flagged-unverified
---

<objective>
OPS-13, remaining two alerts — webhook lag and share of failed sends — and boot wiring for all four.

Purpose: the two alerts that answer "is delivery evidence still arriving" and "is a disproportionate share of mail failing". Both are shaped by a trap the research names: the failed-send-share denominator must exclude the deliberate, non-terminal control-flow outcomes (rate-limit deferrals) that the send pipeline produces routinely, or normal backpressure reads as an outage.
Output: two more watchdogs on the shared claim primitive, and all four started at API boot alongside the existing five.
</objective>

<execution_context>
@/Users/primeropanther/.claude/gsd-core/workflows/execute-plan.md
@/Users/primeropanther/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-CONTEXT.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-RESEARCH.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-PATTERNS.md
@.planning/phases/15-observability-alerting-frontend-resilience/15-13-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Webhook-lag watchdog</name>
  <files>apps/api/src/modules/ops/webhook-lag-watchdog.ts, apps/api/src/modules/ops/__tests__/webhook-lag-watchdog.test.ts</files>
  <read_first>
    - apps/api/src/modules/ops/ingestion-health-watchdog.ts (full file — READ THIS FIRST and establish exactly what condition it already alerts on, so this new watchdog answers a different question rather than double-alerting on the same incident)
    - apps/api/src/modules/ops/send-reconciler-watchdog.ts (the structural template, again)
    - packages/db/src/schema/send-events.ts and packages/db/src/schema/ingress-journal.ts (where webhook receipt is recorded and which timestamp is server-set rather than provider-supplied)
    - packages/db/src/ops/alert-state.ts
  </read_first>
  <behavior>
    - Newest ingested webhook event older than the lag threshold AND outstanding sends awaiting evidence: unhealthy.
    - Newest ingested webhook event older than the lag threshold with NO outstanding sends awaiting evidence: healthy.
    - Recent webhook events with outstanding sends: healthy.
    - No webhook events ever recorded, with outstanding sends: unhealthy with its own distinct reason.
    - The lag measurement uses a server-set receipt timestamp, never a provider-supplied event timestamp.
    - Alerts once per dedup window under its own alert name.
  </behavior>
  <action>
    Create `webhook-lag-watchdog.ts` in the same structure as plan 15-13's watchdogs. Lag is measured from a SERVER-SET receipt timestamp, never from a provider-supplied `occurred_at`: a provider can send an arbitrarily old or future timestamp, and Phase 13 already quarantines out-of-range values for exactly that reason. Read the schema before choosing the column.
    The healthy-when-quiet rule is the one that decides whether this alert is usable: a system with nothing outstanding to hear about is not lagging, it is idle. Gate the unhealthy verdict on there being outstanding sends awaiting evidence, so a quiet weekend does not page the operator.
    Before finalising, write into the module header what this watchdog asks that `ingestion-health-watchdog.ts` does not, and pick a dedup window that does not collide with it. If the two turn out to answer the same question, say so in the summary and narrow this one rather than shipping two alerts for one incident.
  </action>
  <verify>
    <automated>npx vitest run --root apps/api src/modules/ops/__tests__/webhook-lag-watchdog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `webhook-lag-watchdog.ts` exports its constants, a pure evaluation function, a renderer, an orchestration function and a start function.
    - A test case asserts the quiet-with-no-outstanding-sends case is healthy even with a very old newest event.
    - A test case asserts the never-received-any-event case is unhealthy with its own reason.
    - The lag query reads a server-set receipt timestamp column, not a provider-supplied one.
    - The module header states the distinction from `ingestion-health-watchdog.ts`.
    - A test case asserts the rendered body contains none of a planted workspace id, contact email or send id.
  </acceptance_criteria>
  <done>Delivery-evidence lag alerts when it matters and stays quiet when the system is merely idle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Failed-send-share watchdog</name>
  <files>apps/api/src/modules/ops/failed-send-share-watchdog.ts, apps/api/src/modules/ops/__tests__/failed-send-share-watchdog.test.ts</files>
  <read_first>
    - packages/delivery-core/src/index.ts and the send-status vocabulary it exports (the six-value status set — which are terminal, which are ambiguous, which are in flight)
    - apps/worker/src/queues/send-dispatch.ts (the rate-limited outcome and the other non-terminal results — the values that must NOT be counted as failures)
    - apps/worker/src/processor-wrapper.ts (plan 15-08's control-flow allowlist — the same terminal/non-terminal distinction, encoded once already)
    - apps/api/src/modules/ops/send-reconciler-watchdog.ts (the template)
    - packages/db/src/ops/alert-state.ts
  </read_first>
  <behavior>
    - A window whose terminal outcomes are mostly failures yields unhealthy with the observed share.
    - A window with many rate-limited deferrals and no terminal failures yields healthy — deferrals are in neither the numerator nor the denominator.
    - A window whose sends are still reconciling yields healthy — non-terminal outcomes are excluded from both sides rather than counted as successes.
    - A window with fewer terminal outcomes than the minimum sample size yields healthy regardless of the share.
    - Exactly at the share threshold is healthy; strictly above is unhealthy.
    - No sends at all in the window yields healthy.
  </behavior>
  <action>
    Create `failed-send-share-watchdog.ts` in the same structure. The denominator is terminal outcomes only. Derive the terminal/non-terminal split from the exported send-status vocabulary rather than hard-coding status strings, so a future status addition cannot silently land on the wrong side of the ratio.
    Add a minimum-sample-size constant with a rationale comment: a share alert on two sends is noise, and the smallest tenant will otherwise trip this alert constantly. Below the minimum, the verdict is healthy.
    Use the same strictly-greater-than boundary convention as the other watchdogs and pin it with an explicit test.
    Thresholds and the rolling window length are Claude's discretion (CONTEXT.md) — write the rationale in each constant's comment and state plainly that they are first estimates for tuning from real operation.
  </action>
  <verify>
    <automated>npx vitest run --root apps/api src/modules/ops/__tests__/failed-send-share-watchdog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The terminal/non-terminal split is derived from the exported send-status vocabulary, not from inline string literals.
    - A test case asserts a window dominated by rate-limited deferrals is healthy.
    - A test case asserts a window of still-reconciling sends is healthy.
    - A test case asserts the minimum-sample-size rule.
    - A test case asserts the at-threshold-healthy / above-threshold-unhealthy boundary.
    - A test case asserts the rendered body contains none of a planted workspace id, contact email or send id.
    - `npx vitest run --root apps/api` exits 0.
  </acceptance_criteria>
  <done>The failure-share alert measures real failure and not routine backpressure.</done>
</task>

<task type="auto">
  <name>Task 3: Wire all four watchdogs at boot</name>
  <files>apps/api/src/server.ts, SPECIFICATION.md</files>
  <read_first>
    - apps/api/src/server.ts (the existing block that starts the five watchdogs, each with its own dispatch function, its own comment explaining why its dedup window does not mask its neighbours', and its own armed log line)
    - apps/api/src/modules/ops/queue-depth-watchdog.ts and oldest-job-age-watchdog.ts (plan 15-13)
    - apps/api/src/modules/ops/webhook-lag-watchdog.ts and failed-send-share-watchdog.ts (Tasks 1 and 2)
    - apps/api/src/env.ts (`OPERATOR_ALERT_EMAIL` is already required at boot — the four new alerts reuse it, adding no new configuration)
  </read_first>
  <action>
    Start the four new watchdogs in `apps/api/src/server.ts`, following the existing block's conventions exactly: each gets its own real dispatch function through the platform mail path, its own comment stating why its dedup window and health state cannot mask a neighbour's, and its own armed log line. Reuse `OPERATOR_ALERT_EMAIL` (D-04: email only) — add no new configuration variable.
    Do not disturb the existing five watchdogs' registration, their intervals or their ordering.
    Update `SPECIFICATION.md` section 5 with all four ticks (the two from plan 15-13 are already listed; add these two) — names, intervals, thresholds, what each queries and the shared `ops_alert_state` dedup table — and section 7 with the alerting topology: four in-app business alerts by email, with the hosted-provider dead-man's switch arriving in plan 15-17.
  </action>
  <verify>
    <automated>npx vitest run --root apps/api && npm run lint && npm run check:spec-env-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Watchdog({' apps/api/src/server.ts` returns at least 9.
    - Each of the four new start calls has an adjacent comment stating its dedup independence, matching the existing five.
    - `git diff apps/api/src/server.ts` shows no change to the existing five watchdogs' options.
    - No new environment variable is introduced (`git diff docker/prod.env.example` is empty for this plan).
    - `SPECIFICATION.md` section 5 lists all four new ticks and section 7 describes the alerting topology.
    - `npx vitest run --root apps/api` exits 0.
  </acceptance_criteria>
  <done>All four OPS-13 alerts run on every API boot, alongside the five that already did.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| platform tick → operator email | Alert bodies leave the system to an external mailbox |
| provider-supplied timestamps → lag computation | A provider can supply an arbitrary event timestamp |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-46 | Tampering | provider-supplied `occurred_at` used to compute lag | medium | mitigate | Lag measured from a server-set receipt timestamp only; the provider-supplied value is never the lag input |
| T-15-47 | Denial of Service | false outage alerts from counting rate-limit deferrals as failures | high | mitigate | Terminal/non-terminal split derived from the exported status vocabulary; deferral and reconciling cases asserted healthy by dedicated tests |
| T-15-48 | Denial of Service | share alerts on tiny samples paging on every quiet tenant | medium | mitigate | Minimum-sample-size constant with a rationale comment and its own test case |
| T-15-49 | Information Disclosure | tenant data in an alert email body | high | mitigate | Renderers emit counters, shares, ages and reason names only; asserted by planted-value absence tests |
| T-15-50 | Denial of Service | two watchdogs alerting on one incident | medium | mitigate | The webhook-lag module must state its distinction from the existing ingestion-health watchdog and choose a non-colliding window, or be narrowed |
| T-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs in this plan; all dependency additions are gated by plan 15-01's blocking human legitimacy checkpoint |
</threat_model>

<verification>
- `npx vitest run --root apps/api` exits 0.
- `npm run lint` and `npm run check:spec-env-coverage` exit 0.
- Nine watchdogs are armed at boot, each logging its own armed line.
</verification>

<success_criteria>
Phase success criterion 3's alert half is true: alerts fire on queue depth, oldest job age, webhook lag and share of failed sends.
</success_criteria>

## Artifacts this phase produces

New symbols/paths introduced by THIS plan: the threshold constants, evaluation functions, renderers, orchestration functions and start functions in the new files `apps/api/src/modules/ops/webhook-lag-watchdog.ts` and `apps/api/src/modules/ops/failed-send-share-watchdog.ts`; the minimum-sample-size constant; new test files under `apps/api/src/modules/ops/__tests__/`; four new start calls and four new armed log lines in `apps/api/src/server.ts`.

## Flagged assumptions (edge-probe fallback)

- **OPS-13 (unclassified, unresolved)** — RESOLVED into `must_haves.truths` for these two alerts. Threshold and window VALUES remain a FLAGGED ASSUMPTION for all four alerts: they are planner-discretion first estimates (CONTEXT.md leaves them to discretion) and have not been validated against production load; each constant's comment must say so, and the runbooks in plan 15-18 must tell the operator how to tune them.

<output>
Create `.planning/phases/15-observability-alerting-frontend-resilience/15-14-SUMMARY.md` when done
</output>
